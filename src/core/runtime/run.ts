// Bridge: exposes the workflow entry point to the hooks layer.
export { runWorkflow } from '../../engine/orchestrator/index.js';
export type { RunWorkflowOptions } from '../../engine/orchestrator/index.js';
