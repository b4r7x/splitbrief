{
  "name": "add-endpoint-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --experimental-strip-types --loader ./scripts/ts-js-loader.mjs --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  }
}
