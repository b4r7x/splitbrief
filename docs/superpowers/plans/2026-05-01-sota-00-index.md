# SOTA Review Implementation Index

> Master list of all plan files from the 2026-05-01 SOTA audit. Each file is an independent session for an opencode agent.

## Execution Order

**Dependency graph:**
```
SOTA-02 (layer inversion) → then SOTA-01 Tasks 2-3 (routing-fields imports workflow-events)
SOTA-03, SOTA-04, SOTA-05, SOTA-06 — fully independent of everything
```

**Recommended sequence:**
1. Run SOTA-02 FIRST (moves a file, updates imports across many files)
2. Then run SOTA-01 + SOTA-03 + SOTA-04 + SOTA-05 + SOTA-06 in parallel

**IMPORTANT:** SOTA-01 and SOTA-02 both modify `src/engine/orchestrator/task/step.ts`. Never run them simultaneously.

### Deferred (not in these plans — needs separate planning)
- Truncation unification (M7+M11): domain-specific markers make this non-trivial
- `runWorkflow` closure mutation (M9): risk of subtle bugs, needs careful read first

## Plan Files

| File | Scope | Tasks | Est. Time |
|------|-------|-------|-----------|
| `sota-01-bugs-routing.md` | Fix worktreeName bug + extract routing fields helper | 3 tasks | 10 min |
| `sota-02-layer-inversion.md` | Move workflow-events.ts from orchestrator to events layer | 3 tasks | 10 min |
| `sota-03-type-safety.md` | OTel casts, dead re-export, testReset, advisory hook | 4 tasks | 10 min |
| `sota-04-dry-cleanup.md` | Merge slug, extract deepEqual, path consolidation, selector fix | 4 tasks | 15 min |
| `sota-05-test-cleanup.md` | Delete trivial tests, unify fuzzy matching, fold runner-config test | 3 tasks | 10 min |
| `sota-06-faux-provider.md` | Create faux planner/implementer test infrastructure | 4 tasks | 15 min |

## What Was Audited (source of these plans)

9 parallel Opus agents analyzed:
1. pi-mono architecture (benchmark project)
2. tiny-spec engine layer
3. tiny-spec React/TUI layer
4. tiny-spec testing strategy
5. pi-mono vs tiny-spec harness comparison
6. tiny-spec core/utils/stores infrastructure
7. SOTA UX gap analysis (first-run, feedback loop, cost visibility, etc.)
8. pi-mono killer features (session tree, compaction, extensions)
9. Ideal testing architecture design

## What's NOT in these plans (future work)

These are larger efforts that need their own dedicated planning sessions:

### UX SOTA (separate planning needed)
- Zero-friction entry: `diptych "feature"` without subcommand
- Hero savings stat post-run: "$0.12 vs $0.95 — 87% saved"
- Live heartbeat during planner waits (token counter, phase hint)
- Smart `diptych continue` replacing attach/resume/detach
- Contextual keybindings in plan editor footer
- Cost-gated plan approval with savings preview
- Streaming partial output from API implementers

### Architecture SOTA (separate planning needed)
- Append-only tree session model (branching on recovery)
- Branch summarization for recovery context
- Structured compaction for long plans
- Agent/profile cycling keybinding
- `@file` syntax for planner context
- Tree navigation with active-path markers
- Unified `/` command autocomplete

### Testing SOTA (beyond faux provider)
- Rewrite CLI command tests with DI pattern (no vi.mock)
- Add config validation error message tests
- Add CLI exit code contract tests
- Add planner timeout + partial recovery test
- Add error event → UI propagation test
- Level 2 faux: HTTP-level SSE server for api.ts tests

## Verification

After ALL plan files are executed:

```bash
npm run test-ci
```

Must pass with zero failures.
