# Implementation Plan: UX Overhaul

**Branch**: `009-ux-overhaul` | **Date**: 2026-03-28 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/009-ux-overhaul/spec.md`

## Summary

Transform diptych from a command-line-argument-first tool into an interactive TUI application with a home screen, 4 permission modes (supervised/normal/auto/plan-only), cascading spec/plan regeneration with back navigation, TLDR changeset cards, dialog-style box-drawing event cards, task-level control in supervised mode, and comprehensive keyboard shortcuts. All changes are additive — existing CLI commands remain backwards compatible.

## Technical Context

**Language/Version**: TypeScript 5.9+, ESM only (`"type": "module"`)
**Primary Dependencies**: Ink 5.x (React for CLI), @inkjs/ui, commander ^14, openai ^6, simple-git, yaml
**Storage**: JSON files (.diptych/state.json, events.jsonl), Markdown files (spec.md, plan.md, tasks.md), YAML config (.diptych/config.yaml)
**Testing**: Node.js built-in test runner (`node:test` + `node:assert/strict`), executed via `tsx --test`
**Target Platform**: macOS (primary), Linux (secondary)
**Project Type**: CLI tool (npm package)
**Performance Goals**: TUI renders at Ink's 30fps cap; mode switching takes effect within 1 action
**Constraints**: Minimum terminal 80x24; no external TUI libraries beyond Ink/inkjs-ui; zero emoji unicode in output
**Scale/Scope**: ~30 source files, ~400 tests, 7 user stories, 44 functional requirements

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Cost-Optimal Orchestration | PASS | TLDR generation adds ~200 tokens per regeneration — negligible. No new Opus calls for implementation. Mode switching is client-side only. |
| II. Spec-Driven Development | PASS | This feature follows the full speckit workflow. Task prompts unchanged. |
| III. Local-First Implementation | PASS | No changes to implementation phase. New features are TUI-only. |
| IV. Functional Purity | PASS | All new code follows pure functions + module-scoped state. No classes. ESM with .js extensions. |
| V. Validate Before Commit | PASS | Validation pipeline unchanged. Supervised mode adds user review ON TOP of automated validation, not instead of it. |
| VI. Identity & Anti-Goals | PASS | This is exactly what Principle VI calls for: "Beautiful visualization of the orchestration is part of the product identity." Dialog-style cards, flow arrows, and mode control make the planner/implementer collaboration visible. |

No violations. No complexity tracking entries needed.

## Project Structure

### Documentation (this feature)

```text
specs/009-ux-overhaul/
├── spec.md              # Feature specification (7 stories, 44 FRs)
├── plan.md              # This file
├── research.md          # 6 research sections
├── data-model.md        # Entity definitions, state transitions, migration
├── quickstart.md        # End-user guide
├── contracts/
│   └── cli-commands.md  # CLI, slash command, and keyboard contracts
└── tasks.md             # (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/
├── cli.ts                    # MODIFY: Add default action (home screen), --mode flag
├── app.tsx                   # MODIFY: Add screen router (home/workflow/summary), overlay state, mode state
├── types.ts                  # MODIFY: Add PermissionMode, Phase 'review-gate', new StateActions, new TuiEvents
├── config.ts                 # MODIFY: Add mode to Config.workflow, validation
├── state.ts                  # MODIFY: Add transitions, stateVersion 3, migration
├── tui/
│   ├── home-screen.tsx       # NEW: Branding, context info, text input, slash commands
│   ├── mode-picker.tsx       # NEW: Tab overlay with Select for 4 modes
│   ├── help-overlay.tsx      # NEW: Context-sensitive shortcut list
│   ├── slash-input.tsx       # NEW: "/" activated command input with filtering
│   ├── review-gate.tsx       # NEW: Pre-implementation task summary with approval
│   ├── task-preview.tsx      # NEW: Supervised mode pre-task card
│   ├── task-result.tsx       # NEW: Supervised mode post-task card with diff
│   ├── dialog-card.tsx       # NEW: Box-drawing card wrapper (replaces inline borders)
│   ├── flow-arrow.tsx        # NEW: Directional arrows between cards (▼/▲ with labels)
│   ├── changes-card.tsx      # NEW: TLDR changeset display
│   ├── layout.tsx            # MODIFY: Add overlay rendering, expanded keybindings
│   ├── header.tsx            # MODIFY: Minor — no functional changes needed
│   ├── event-card.tsx        # MODIFY: Wrap in dialog-card, use ASCII indicators, embed validation
│   ├── conversation-flow.tsx # MODIFY: Handle new event types, flow arrows between sections
│   ├── cost-footer.tsx       # MODIFY: Add mode indicator [S]/[N]/[A]/[P], context-sensitive hints
│   ├── prompt.tsx            # MODIFY: Add [b] back key, restyle with box-drawing
│   ├── pipeline-bar.tsx      # MODIFY: Add review-gate phase mapping
│   ├── picker.tsx            # KEEP: Unchanged (used in init command)
│   ├── summary.tsx           # KEEP: Unchanged
│   ├── question-prompt.tsx   # KEEP: Unchanged
│   ├── user-input.tsx        # KEEP: Unchanged
│   ├── diff-view.tsx         # KEEP: Unchanged
│   └── task-summary.tsx      # MODIFY: Use ASCII indicators (* instead of ✓)
├── orchestrator/
│   ├── orchestrator.ts       # MODIFY: Navigable review loop, review gate, supervised task hooks
│   ├── tldr-parser.ts        # NEW: Parse <!-- TLDR:{...} --> markers (follows question-parser pattern)
│   └── ...                   # KEEP: All other orchestrator files unchanged
├── spec/
│   └── templates.ts          # MODIFY: Add TLDR instruction to buildRegeneratePrompt
└── utils/                    # KEEP: All utility files unchanged

tests/
├── state.test.ts             # MODIFY: Test new transitions, stateVersion migration
├── tldr-parser.test.ts       # NEW: TLDR marker parsing tests
├── home-screen.test.ts       # NEW: Home screen rendering
├── mode-picker.test.ts       # NEW: Mode picker overlay
├── review-gate.test.ts       # NEW: Review gate component
├── dialog-card.test.ts       # NEW: Box-drawing card wrapper
├── changes-card.test.ts      # NEW: Changeset card rendering
├── flow-arrow.test.ts        # NEW: Arrow component
├── event-card.test.ts        # MODIFY: Updated for ASCII indicators
├── cost-footer.test.ts       # MODIFY: Mode indicator tests
├── pipeline-bar.test.ts      # MODIFY: review-gate phase
├── conversation-flow.test.ts # MODIFY: New event types
├── config.test.ts            # MODIFY: Mode config validation
├── orchestrator.test.ts      # MODIFY: New callback signatures
└── prompt.test.ts            # NEW: Back key, restyle
```

**Structure Decision**: Single project layout. All new TUI components go in `src/tui/`. One new parser in `src/orchestrator/`. No new directories — follows existing convention.

## Complexity Tracking

No violations — all changes align with constitution principles.
