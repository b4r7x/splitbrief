# CLI Interface Contract

**Status**: Unchanged — this restructure does not modify the CLI interface.

All 5 commands (`start`, `spec`, `init`, `status`, `resume`) retain their current signatures, flags, and behavior. Only the visual rendering of the TUI output changes.

See `specs/002-cost-optimized-orchestrator/contracts/cli-commands.md` for the full CLI contract.
