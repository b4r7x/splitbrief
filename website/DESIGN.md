# SPLITBRIEF website — design sheet

The single source of truth for `website/`. Every implementer reads this whole file before touching a line. The visual target is `.nuke/2026-09-06-225118-spec-website/reference.png` (an "orch" comp, 1024×1536); this sheet translates it to SPLITBRIEF and to a 1440-wide frame. Where the sheet and the reference disagree, the sheet wins (copy is product-true; geometry follows the reference).

## 0. Brief (nuke-creative)

- **Audience:** developers who already run Claude Code / Codex / OpenCode and are skeptical of "AI startup" pages. They judge in 3 seconds and read the small print.
- **Feel:** precise · quiet · slightly haunted. Editorial restraint, terminal-native detail.
- **Reference point:** late-issue print typography (Didone display + tiny mono annotations) laid over a dark terminal — the reference comp, not a "cyberpunk" page.
- **Constraints:** one static page, TypeScript, Vite build to `dist/`, no UI framework. Runtime dependencies only for motion and only when they earn it (GSAP core + a plugin is allowed for the §7 concept when native CSS/WAAPI cannot express a timeline cleanly; nothing else). Google Fonts CDN. WCAG AA for all body/label text (labels at ≥ 4.5:1 on the canvas; decorative particles exempt). `prefers-reduced-motion` honoured everywhere. No image assets — the ghosts are rendered from characters.
- **Direction (chosen):** `editorial + technical`. Type pairing: **Bodoni Moda** (display — a true Didone with an optical-size axis; chosen over Instrument Serif on 2026-09-07 after an A/B against the reference: 23:1 stroke contrast vs 2.6:1, letter width 8.7× cap vs 6.6×, fits the 5-column measure without overflow) + **JetBrains Mono** (labels, body) + **Space Mono** (the wide lower manifesto face). Palette: canvas near-black tinted cold, off-white ink, **two accents only — blue (planner) and green (implementer)**. Layout stance: an asymmetric hero — 5/12 type column, 7/12 diagram, headline larger than anything else, small caps annotations pinned to the corners like a technical drawing. Signature element: **three ASCII ghost daemons routed through one `tasks.md` card** — living character fields, not an illustration.
- **Runner-up (recorded):** `brutalist` — raw mono everywhere, no serif. Rejected: loses the print tension that makes the reference memorable.
- **Anti-default gate:** no Inter/system-ui; no blue-purple gradient; no centered hero; no three-card grid; no glassmorphism; no emoji; no gradient blobs; no bento; one motion concept (§7); reduced motion respected.

## 1. Tokens

All in `src/styles/tokens.css` as custom properties on `:root`. No other file declares a raw colour, font, or spacing value.

### Colour

| token | value | use |
|---|---|---|
| `--bg` | `#0b0c0e` | canvas (never `#000`; cold tint) |
| `--bg-vignette` | `radial-gradient(120% 90% at 60% 20%, #101216 0%, #0b0c0e 55%, #08090a 100%)` | body background — the one atmosphere element |
| `--ink` | `#ece9e2` | headline, primary text |
| `--ink-2` | `#b9b6ae` | body copy (≥ 7:1 on bg) |
| `--ink-3` | `#84827c` | labels, eyebrows, comments (4.6:1 against the vignette's lightest stop `#101216` — the AA floor for small text; `page.e2e.ts` measures against that stop, not `--bg`) |
| `--ink-4` | `#4a4946` | ghost headline line ("ARE GOOD."), decorative only |
| `--hair` | `rgb(255 255 255 / .12)` | rules, box borders |
| `--hair-strong` | `rgb(255 255 255 / .22)` | CTA box border, focus ring base |
| `--blue` | `#6e97ea` | planner accent (labels, ghost, route) |
| `--blue-dim` | `#3a5aa0` | planner ghost low-density cells |
| `--green` | `#58c48f` | implementer accent |
| `--green-dim` | `#2f7a58` | implementer ghost low-density cells |
| `--white-spark` | `#f4f2ec` | 12% of ghost cells and the route packet |
| `--focus` | `#6e97ea` | focus ring (2px solid, offset 3px) |
| `--selection` | `rgb(110 151 234 / .35)` | `::selection` |

Reviewer ghost: mixed cells — 55% blue-dim, 35% green-dim, 10% spark — it belongs to neither lab.

### Type

| token | value |
|---|---|
| `--font-display` | `"Bodoni Moda", "Bodoni Moda Fallback", Georgia, "Times New Roman", serif` — `font-optical-sizing: none; font-variation-settings: "opsz" 20` — chosen by measuring the O's top hairline of `TWO MODELS` at 1440 / DPR 1 (77.76px, cap 58px) as % of cap: opsz 48 → 0.84%, 60 → 0.73%, 72 → 0.64% (all under the 1.5–3% window), 36 → 0.87%, 24 → 1.44%, **20 → 1.66% (0.96px; 1.71% at DPR 2)**, 16 → 1.96%; the reference itself measures 1.23% with width/cap 8.85 — opsz 20 gives 8.88 |
| `--font-mono` | `"JetBrains Mono", "JetBrains Mono Fallback", ui-monospace, "SF Mono", Menlo, monospace` |
| `--font-wide` | `"Space Mono", "Space Mono Fallback", ui-monospace, monospace` |
| `--text-display` | `clamp(3.25rem, 5.4vw, 4.9rem)` — 52→78.4px (77.76px at 1440, cap ≈ 58px; the reference implies 65); line-height `1`; letter-spacing `0`; weight 400; `TWO MODELS` must fit the 5-column measure (533px at 1440) with no `nowrap` masking — measured 519.9px at opsz 20; the 4.9rem ceiling keeps it inside the column above 1450px (at 84px it measures 549px) and 5.4vw keeps ≥ 9px of margin down to the 1100px tier edge; below 768 the token is `clamp(2.75rem, 13vw, 3.25rem)` (one media block in tokens.css) — `TWO MODELS` is 6.686× the font size, so the 3.25rem floor (347.7 px) fits the 350 px column at 390 but wraps in the 320 px column at 360; 13vw renders 50.7 px / 339 px at 390 and 46.8 px / 313 px at 360 (measured 2026-09-08, Phase 6) |
| `--text-wide` | `clamp(1.375rem, 2.1vw, 1.9rem)` — the "// YOU DESCRIBE" block; line-height `1.25`; letter-spacing `.04em` |
| `--text-body` | `.9375rem` (15px); line-height `1.6` — hero paragraph, manifesto paragraph |
| `--text-label` | `.75rem` (12px); line-height `1.45`; letter-spacing `.08em`; uppercase — every annotation, nav link, step, works-with, list |
| `--text-micro` | `.6875rem` (11px) — diagram side labels, brief card body, footer |
| `--text-cta` | `.875rem` (14px) — the install line |

Fonts load from Google Fonts: `Bodoni+Moda:opsz,wght@6..96,400`, `JetBrains+Mono:wght@400;500`, `Space+Mono:wght@400` with `display=swap`, `<link rel="preconnect">` to `fonts.googleapis.com` and `fonts.gstatic.com` (`crossorigin`). Each family gets a size-adjusted local fallback `@font-face` (`size-adjust`, `ascent-override`, `descent-override`) so the swap does not shift layout; the metrics are calibrated by measuring the real face against the fallback on the strings the page actually sets (the display face is calibrated on the uppercase h1 against Georgia; re-measured after every face change and recorded in base.css). Headings `text-wrap: balance`; paragraphs `text-wrap: pretty`; numeric columns `font-variant-numeric: tabular-nums`.

### Space & grid

- Scale: `--s-1: 4px · --s-2: 8px · --s-3: 12px · --s-4: 16px · --s-6: 24px · --s-8: 32px · --s-12: 48px · --s-16: 64px · --s-24: 96px · --s-32: 128px`. No other values.
- Page gutter `--gutter: clamp(20px, 4.4vw, 64px)`. Content max-width `1312px` (1440 − 2×64), centred.
- Grid: 12 columns, `column-gap: var(--s-6)`. Hero left = cols 1–5, hero diagram = cols 6–12. Routes table spans 1–9, its claim sits in 11–12. Manifesto: heading 1–4, paragraph 5–9, list 11–12. Footer: 1–3 / 5–8 / 11–12. One right rail: every claim below the hero starts on the hero claim's column (11, §4), so the four right-column x's are equal at 1440 (measured 2026-09-08, fix cycle 1: at col 10 they stepped 111 px left of the hero claim).
- Section gap `--s-24` (96px); the manifesto is the breath: `--s-32` above and below. The hero→routes seam is the one seam with a rule in it and runs tighter: hero `padding-bottom --s-12` + hairline + routes `padding-top --s-16` = 113 px, so the rule sits 48 px under the works-with dash instead of floating in 161 px of bare canvas (64 + 97 measured 2026-09-08, fix cycle 1; the reference, scaled, is 42 / 67).
- Rules: 1px `--hair`. The dash ornament `—` (an 18px×1px `--ink-3` line, `.dash`) closes every annotation block; it is the running motif together with the `°` mark and the `[ ]` brackets.

### Surfaces

One surface system: **hairline borders, no shadows, no radius** (`border-radius: 0` everywhere). The only boxes on the page are the CTA and the `tasks.md` card; both are 1px `--hair-strong` on transparent, with an inset 1px top highlight `inset 0 1px 0 rgb(255 255 255 / .05)`. Grain: an SVG `feTurbulence` overlay on `body::after`, opacity `.06` (the nuke-design cap — at `.045` it measured invisible on this canvas), `pointer-events: none`, `mix-blend-mode: overlay`.

**v2 (2026-09-09) — a second surface with its own job.** The lower page's two panels (§16.1, §16.2) are *documents*, not controls: fill `--panel`, 1px `--hair` (one step under the CTA's `--hair-strong` — the fill carries the rest), the same `--highlight`, no radius, no shadow. The CTA and the hero card keep the control surface. Two surfaces on the page, each named once: a control is bordered on transparent, a document is filled and faintly edged. Nothing else gets a box.

### v2 additions (2026-09-09, the lower page — §16)

| token | value | use |
|---|---|---|
| `--highlight` | `inset 0 1px 0 rgb(255 255 255 / .05)` | the inset top highlight the document surface (§16) and the control surface (above) share; until fix cycle 1 the comp defined it inline and the sheet never named it |
| `--panel` | `#0e1114` | the document surface's fill — the editor and the terminal (§16.1–16.2). One step above `--bg` (the reference measures canvas (8,11,13) → panel (12,17,20)); never used as a text colour |
| `--red` | `#cf6a52` | the failure tone, in the run transcript only — `✗`, `error`, `FAIL` (§16.2); 5.0:1 on `--bg`. A status colour, not a third accent: one hot spot, one meaning; the reference's `▶` markers are warm too and are *not* red here (§16.2 records why) |
| `--text-note` | `.8125rem` (13px), line-height `1.6` (20.8px); ≥ 1600: `.875rem` (14px, 22.4px — the nearest whole pixel to the content-scaled 13.6) | the lower page's statements, caps lines, the 04 lead and the callout values — the reference's measure is 41 characters in 249 ref px (320 px at 1440), which is 13px, not the lede's 15; set as `max-width: calc(41ch + 1px)` (§16 shared grammar says why the pixel) |
| `--text-title` | `1.5rem` (24px) at every desktop tier — the reference scaled into the 1392 box is 24.4px, so the title has no FHD step; < 768 `1.25rem`. Line-height 1.25, weight 500, tracking `.02em`, uppercase, `--ink` | `BEFORE ANY CODE` / `AFTER EVERY TASK` / `FOR THE WHOLE RUN` |
| `--text-serif-sub` | `1.75rem` (28px); ≥ 1600 `1.8125rem` (29px — the reference's 21.8 ref px × 1.3423; `KEEP THE EVIDENCE.` measures 321 px against the 330 px column and 310 px at 28px against 310); < 768 `1.5rem`. Line-height 1.08, `--font-display`, opsz 20, `--ink-3`, `white-space: nowrap` per line | the serif triplet under each title |
| `--text-breath` | `3rem` (48px); ≥ 1600 `3.5rem`; < 768 `2.125rem` (34px — `SPLITBRIEF HOLDS` must hold one 350 px line). Line-height 1.05, `--font-display`, opsz 20 | the 04 statement (§16.3) |
| `--text-art` | `.6875rem` (11px); ≥ 1600 `.75rem` (12px); < 768 `.6875rem` | panel rows and the tree — the reference's advance is 6.9 px per character at 1440, between 11px (6.6) and 12px (7.2), and 11 is the size that keeps its 13px pitch; no uppercase, tracking 0 |
| `--line-art` | `13px`; ≥ 1600 `14px`; < 768 `15px` | panel row pitch — the reference's 10.1 ref px (12.98 at 1440; 13.57 in the 1392 box, rounded to the whole pixel because a fractional pitch paints the gutter numbers on alternating half pixels); the phone's wrapped rows take 15 |
| `--bar-h` | `30px`; ≥ 1600 `32px`; < 768 `28px` | a panel's title bar, its bottom hairline included — the reference's 24 ref px |
| `--rail` | `80px` | the aura rail's distance from the content box's right edge (§16.5) |
| `--head-w` | `310px`; ≥ 1600 `330px` | the 02/03 row's head column — the same width as cols 1–3 of the twelve-column grid |
| `--panel-w` | `494px`; ≥ 1600 `517px` | the 02/03 row's panel — the reference's 385 ref px × 1.2834, and × 1.3423 in the 1392 box; the notes take the rest of the row (§16 shared grammar) |
| `--content-max` | `1392px` **at ≥ 1600 only** (1312 below) | the FHD tier's content width (§11) |

The three tier blocks (`≥ 1600`, `< 768`) live in `tokens.css` next to the existing `--text-display` phone block; no other file redeclares a token.

## 2. Page skeleton (top → bottom)

v2 (2026-09-09). Every class below is a hook the implementer uses verbatim; §16 specifies each.

```
<header class="nav">                    wordmark · tagline · links · 9-dot mark                                   §3
<main>
  <section class="hero" id="top">       ol.steps (three anchors, §3) · claim · h1 · lede · cta · works | diagram  §4
  <div class="lower">                   position: relative — the wrapper that carries the rail                   §16.5
    <span class="rail">                   the dashed trace, full height of .lower, with one .spark inside          §16.5–16.6
    <section class="s02" id="briefs">     div.aura[aria-hidden] (canvas.dots ×2 · span.stop · span.frag ×n)         §16.5
                                          div.grid › div.head (h2.title › span.eyebrow[aria-hidden] + span.line · p.sub · p.tick-list)
                                                   › figure.panel.editor (figcaption.bar · ol.lines › li[style=--row] › span.n + span.t)
                                                   › div.notes (p · p.caps · p · p.caps · span.marg[aria-hidden] ×2 · p.marg-list)   §16.1
    <section class="s03" id="validation"> div.aura (dots · stop · frags) · div.grid › div.head · figure.panel.terminal (ol.lines[style=--beat:0]) · div.notes (p · p · ol.steps · marg ×3)   §16.2
    <section class="s04" id="control">    div.aura (dots · stop · frags) · div.grid › div.head · p.breath · p.lead · div.callouts (p.callout ×3) · p.creed · pre.tree (span.row ×9) · marg ×3   §16.3
  </div>
  <hr class="rule rule--bleed">         full-bleed, the footer's seam                                             §16.4
</main>
<footer class="foot">                   p.brand (a.wordmark · span.claim) · p.right (span.links · span.seats)     §16.4
```

The hero → 02 seam is `.s02`'s own full-bleed `border-top`; the v1 `hr.rule` after the hero is gone, and so are `section.routes`, `section.brief-sheet`, `section.manifesto`, `section.ladder`, `section.record` and the short rule (§8, §9, §15). Sections 02–04 are full-width elements (`position: relative`) so their aura layers bleed to the viewport; the content sits in their `div.grid` (the §1 twelve columns, `width: min(var(--content-max), 100% - 2 * var(--gutter))`, `z-index: 1` over the aura).

Semantic HTML: `header/nav/main/section/footer`, `h1` is the hero statement, each lower section's `h2` is its title (`Before any code` / `After every task` / `For the whole run`) with the eyebrow number inside it as an `aria-hidden` span, so the heading's name is never a number (§16; v2, fix cycle 1), the routes are a real `<table>` with `<caption>` (visually hidden) and `scope="col"` headers, the brief card is a `<figure>` with `<figcaption>`, the ghosts are `<canvas role="img" aria-label="…">`, each followed by a `<pre class="ghost-fallback" aria-hidden="true">` holding the same character art (frame 0, printed by `tools/render-static.ts`). `<html class="no-js">`; a one-line inline `<script>` in `<head>` (`document.documentElement.classList.replace('no-js','js')`) swaps the class before the body parses — module scripts defer, so doing it in `main.ts` would flash the `<pre>` fallbacks; `.no-js canvas` is hidden and `.js .ghost-fallback` is hidden, so exactly one of the pair renders. Reduced motion keeps the canvas (frame 0) — the `<pre>` is the no-JS path only.

## 3. Nav (`header.nav`)

Height 96px (padding-block `--s-8`). Four items on one row, baseline-aligned:

1. **Wordmark** `splitbrief°` — `--font-mono`, 22px, weight 500, `--ink`; the `°` is a `<sup>` in `--ink-3` at 60% size, `top: -.6em`. Links to `#top`.
2. **Tagline** (cols 3–4, starts at ~col 3): three lines, `--text-label`, `--ink-3`: `ONE PLANS.` / `ONE EXECUTES.` / `ONE CONTRACT.`
3. **Links** (right-aligned, before the mark): `[ docs ]` → `https://github.com/b4r7x/splitbrief/tree/main/docs`, `[ github ]` → `https://github.com/b4r7x/splitbrief`. `--text-label`, `--ink-2`; brackets are part of the text; hover: brackets turn `--blue`, text `--ink`; `text-decoration: none`; visible focus ring.
4. **9-dot mark** — a 3×3 grid of 3px dots, 20×20 box, `--ink-2`; pure ornament (`aria-hidden`, a `<span>`, not a button). Sits at the right gutter.

**v2 (2026-09-09) — the steps are anchors.** The hero's `ol.steps` (§4, the three rows under the nav) reads `01 │ HERO` · `02 │ BRIEFS` · `03 │ VALIDATION`, per the reference; each label is an `<a>` — `#top` (the hero section's `id`), `#briefs` (`section.s02`), `#validation` (`section.s03`). Same grammar as before (number `--ink-3` in a 32 px cell, the continuous `--hair` rule, label `--ink-2`, 18 px pitch); hover the label turns `--ink`, focus shows the ring, `scroll-behavior: smooth` on `html` under `prefers-reduced-motion: no-preference` only. **04 is not listed** (decision recorded): a fourth row moves the works-with dash from y 1 072 to 1 090 on a 1 920×1 080 fold (§4's y-table: 96 nav + 976 column + 18), 10 px under the fold ruling 5 protects. Section 04 is the coda the reader scrolls into; the footer's `PLANS / EXECUTES / REVIEWS` is text, not links, as in the reference.

**v2 build, 2026-09-09: the steps anchors take a 24 px hit box, not 44** (D-21). `padding-block: calc((24px - 1lh) / 2); margin-block: calc((1lh - 24px) / 2)` on the anchor, so the 18 px pitch §4's geometry and ruling 2 protect is unchanged and the target still clears WCAG 2.5.8's 24 px minimum. 44 px would have moved every row. The e2e assertion reads `offsetHeight >= 24` AND the rect `>= 23.99`: the rect alone reads `23.99999237060547` on roughly 1 load in 30, a float32 artefact of subtracting two independently rounded edge coordinates, not a short box (30-sample measurement, phase-p1b).

## 4. Hero (`section.hero`)

Grid 12 columns; `min-height: calc(100vh - 96px)`; `padding-block: var(--s-6) var(--s-12)` (the bottom pad is the hero→routes seam, §1).

### Left column (cols 1–5) — vertical order, five rows, with the y-positions the reference implies at 1440 (nav bottom = 0)

| y | element | spec |
|---|---|---|
| 24 | **Steps** | three rows: `01 │ HERO`, `02 │ BRIEFS`, `03 │ VALIDATION` (v2, 2026-09-09 — anchors, §3; `PLAN` / `EXECUTE` / `REVIEW` until then). Number `--ink-3`, one continuous 1px `--hair` vertical rule spanning the three rows between number and label (`border-left` on the label cell, `padding-left: var(--s-3)`, number column 32px wide), label `--ink-2`. `--text-label`. Row pitch 18px. |
| 189 | **Headline** `h1` | margin-top `--s-24`; four lines, `--text-display`, `--font-display`: `TWO MODELS` / `ARE GOOD.` (this line `--ink-4`) / *stanza gap `--s-24`* / `A SYSTEM` / `IS BETTER.` Each line a `<span class="line">` (display block) so the muted line and the stanza gap are styleable; the h1 text reads "Two models are good. A system is better." to assistive tech (use `aria-label` on the h1 to avoid the visual line breaks being read as pauses). No uppercase transform — the source text is uppercase. |
| 624 | **Lede** `p` | four lines, `--text-body`, `--ink-2`, `max-width: 34rem`: `**splitbrief** runs two coding tools against one job — three when another lab reviews. The stronger one plans and reviews, the cheaper one executes — and splitbrief holds the contract between them.` — first word `<strong>` in `--ink`, weight 500. Closed by `.dash`. The `<br>` sits after `job —` (both dashes are glued to the word before them with `&nbsp;`, so no line opens on a dash), and the rag at 1440 is 51 / 58 / 56 / 26 characters — `text-wrap: pretty` carries `the` down to the last line. The `<br>` is hidden below 1250: there the five-column measure (44 characters at 1100) no longer holds the 51-character first clause and the forced break left `one job —` alone on a line; without it the lede sets in five lines from 1100 to 1249 and four from 768 up, no line shorter than two words (owner's copy, 2026-09-09 — three lines, `…against one job.`, until then). |
| 729 | **CTA** | a `<button class="cta" type="button">` `max-content` wide (≥ 264px) × 44px, both label states stacked in one grid cell so the width never changes on click: `$ npm install -g splitbrief` in `--text-cta`, `--ink`, the `$` in `--ink-3`; right-aligned 14px "two overlapping squares" copy glyph drawn with two 1px-bordered `<span>`s (no icon font, no emoji). Click → `navigator.clipboard.writeText('npm install -g splitbrief')`, the label swaps to `copied to clipboard` for 1.6s (live region `aria-live="polite"`), border flashes `--blue`. Hover: border `--ink-3`. Focus: ring. It is the most built thing in the column by design (nuke-design rule 6). |
| 821 | **Works with** | eyebrow `WORKS WITH` (`--ink-3`) then six lines `--text-label` `--ink-2`: `CLAUDE CODE` / `CODEX` / `OPENCODE` / `CURSOR` / `COPILOT · KILO CODE` / `OLLAMA · LM STUDIO`. Closed by `.dash`. |

### Top-right claim (cols 11–12, top of the hero, aligned with the steps row)

Three lines `--text-label`, right column but left-aligned text: `DIFFERENT LABS.` / `FEWER BLIND SPOTS.` / `REAL EVIDENCE.` — first two `--ink-2`, third `--ink`. Closed by `.dash`. A lone `/` glyph (`--ink-3`, 14px) sits 40px above-left of it as a drafting mark (matches the reference).

### Right column — the diagram (`.diagram`, cols 6–12)

A `position: relative` box **760 × 860px** at 1440 (`aspect-ratio: 760 / 860; width: 100%`), `overflow: hidden`. Inside it a fixed **760×860 stage** (`.stage`) carries every child at absolute `(x%, y%)` coordinates; the stage is scaled with `transform: scale(boxWidth / 760)` (`transform-origin: 0 0`) so the character grids never reflow (§6 lock rule). Coordinates below are `(x%, y%)` of the stage; anchor = element **centre** unless the row says top-left or top-anchored. Derived rims (for routes) are computed from centre ± half-size and quoted so the critic can measure them. The top-right hero claim (cols 11–12, which start at stage x ≈ 73%) overlays the stage's top-right corner; no stage element is placed at `x > 73%` with `y < 12%`.

| element | anchor | notes |
|---|---|---|
| **planner ghost** (canvas 210×275 = 30×25 cells) | centre (14%, 40%) → spans x 0.2–27.8%, y 24–56% | blue |
| **implementer ghost** (canvas 182×242 = 26×22 cells) | centre (67%, 39%) → spans x 55–79%, y 25–53% | green |
| **reviewer ghost** (canvas 154×198 = 22×18 cells) | centre (68%, 72%) → spans x 58–78%, y 60.5–83.5% | mixed, lower density (§6) |
| **brief card** (`figure`, 108×104) | centre (39%, 46%) → spans x 31.9–46.1%, y 40–52% (the reference's 84-wide card cannot hold the six §5 lines at 11px without clipping — measured 2026-09-07, Batch 3.B; the routes are drawn to the card's centre and occluded by it, so the edges they meet are the card's real edges) | `tasks.md` — §5 |
| **route A: planner → card** | horizontal from the planner's right rim (27.8%, 46%) to the card's left edge (31.9%, 46%) | `--ink-3`, `1px dashed` (4px on / 4px off) |
| **route B: card → implementer** | horizontal from the card's right edge (46.1%, 46%) to the implementer's left rim (55%, 46%) — it enters the ghost's lower third | same stroke, straight |
| **route C: card → reviewer** | from the card's bottom centre (39%, 52%) down to (39%, 60%), right to (49%, 60%), down to (49%, 72%), right to the reviewer's left rim (57.9%, 72%) | same stroke; corners are hard 90°; no curves anywhere |
| **tick + label: planner** | tick 1px × 28px `--ink-3`, top-anchored at (14%, 18%) (ends 21.3%, the crown starts at 24%); label right of the tick, `--text-label` `--ink`: `CLAUDE` / `+ FABLE` | |
| **tick + label: implementer** | tick top-anchored at (67%, 18%); label `OPENCODE` / `+ DEEPSEEK` | |
| **tick + label: reviewer** | tick top-anchored at (76%, 55%) (below the implementer's bottom at 53%, above-right of the reviewer's crown); label `CODEX` / `+ GPT-5.6°` | the `°` marks the seat that signs off |
| **seat label: planner** | top-left at (6%, 60%) — below-left of the ghost (bottom 56%) | `PLAN` (`--ink`) then `ANALYZE` / `DECOMPOSE` / `ROUTE` (`--ink-3`), `--text-micro`; a 1px×12px tick above it; closed by `.dash` |
| **seat label: implementer** | top-left at (84%, 36%) — right of the ghost (right rim 79%) | `IMPLEMENT` / `GENERATE` / `ITERATE` / `TEST` |
| **seat label: reviewer** | top-left at (82%, 76%) — right of the ghost (right rim 78%) | `REVIEW` / `READ` / `EVALUATE` / `CONFIRM` |
| **`+` crosshair** | centre (30%, 70%) — moved from (26%, 63%) on 2026-09-07 (fix cycle 1), where it sat on the PLAN label's right edge | `--ink-3`, 14px — drafting mark |
| **rain** | two seeded glyph columns, one per lab seat, centred on the tick's x (planner 14%, implementer 67%), 6 cells (42px) wide, from y 6% down to y 17.5% — 9 rows of 11px; the column ends 0.5% above the tick top, so the tick reads as the funnel that carries the rain into the crown | `.`/`:` glyphs, `--ink-4`, `--text-micro`, placed by `features/diagram/rain.ts` (`rainPoints`, `lib/noise` seeded): a cell lights when its hash falls under a share that tapers from 36% on the top row to 20% on the bottom row, so at most 30% of a column's 54 cells are lit (planner 10, implementer 15; the bottom share was 12% until 2026-09-07, Phase 4 — the Phase 3 critic measured the lowest rows as empty, so the funnel into the tick was not perceptible on a still frame). Replaced the 2px dotted gradient line on 2026-09-07 (fix cycle 1): a single dotted line read as a leader from the claim's dash down to OPENCODE, not as weather. Placed by JS like the scatter, so the no-js page has no rain. → part of the motion concept (§7); the column never touches the tick or its label. The compact stage (§11) has no rain: its ticks sit on the rail beside the ghosts, so there is no funnel for it to fall into |
| **scatter** | ~60 `.`/`:`/`·` glyphs in a loose diagonal band from (0%, 6%) to (24%, 26%), a light trail of ≤ 30 `.`/`:` glyphs across the lower-left quarter (x 0–39%, y 60–100%; more than 4× sparser than the band, clear of the PLAN label, the `+` and every other keep-clear box), a second light trail of ≤ 12 `.`/`:` glyphs under the reviewer (x 55–100%, y 85–100%), plus a sparse halo (≤ 40 glyphs) within 30px of each ghost's rim | `--ink-4`, generated by `features/diagram/scatter.ts` with a fixed seed so renders are deterministic; anything outside the stage is clipped by the box. The lower-left trail was added 2026-09-07 (fix cycle 1): the quarter under the planner was a void that pointed at nothing; the lower-right one on 2026-09-08 (Phase 6, from the Phase 5 critic): the corner under the reviewer was the last void. On the compact stage (§11) the band and both trails give way to one trail of ≤ 12 glyphs beside the card (x 60–100%, y 28–48%); the halos follow the ghosts |

The three routes are drawn once, as one inline `<svg class="route-lines">` per tier (`viewBox` = the stage, `inset: 0`) holding three `<path class="route route--a|b|c">` — `stroke-dasharray: 4 4`, `--ink-3`, `shape-rendering: crispEdges`, hard corners. The path's `d` is the single source for the drawn line **and** for the packets' `offset-path`: `packet.ts` parses it, extends it 8 px into the rims, and derives the cue times from its length (§7.3). The endpoints in each `d` are hand-derived from the ghost centres and sizes in the table above (the compact `d`s from §11's spine): moving a ghost means editing the `d` — nothing recomputes it. Until 2026-09-08 (Phase 6) the routes were CSS gradient boxes and `packet.ts` carried its own copy of route C's corners; the compact tier (§11) needed a second polyline set, so the drawing became the truth and the copy went.

The ghosts are the top-3 elements by rendered size on the page after the headline (nuke-design rule 2); nothing in the diagram may be brighter than a ghost's spark cells except the route packet while it travels.

## 5. The brief card (`figure.brief`)

`max-content` × 104px (108px wide at 11px — the reference's 84 clips `T3 jwt.test.ts`; `min-width: 84px`), 1px `--hair-strong`, inset top highlight, padding `--s-2`, background `--bg` (it must occlude the route line behind it). Content, `--text-micro`, `--font-mono`:

```
tasks.md            ← --ink, weight 500, 12px, as <figcaption>
TASK BRIEF          ← --ink-3, letter-spaced eyebrow
──────              ← 1px --hair, full width, margin-block --s-2
T1 jwt.ts       ✓   ← --ink-2, the ✓ in --green
T2 guard.ts     ▸   ← --ink, the ▸ in --blue (the task in flight)
T3 jwt.test.ts      ← --ink-3
```

The glyphs `✓ ▸` come from JetBrains Mono (it has them); no icon font. Card width stays fixed; overflow hidden; text never wraps.

## 6. The ghosts — `src/features/diagram/{seats,silhouette,density,atlas,ghost}.ts`

**Concept:** each seat is a daemon — a Pac-Man-lineage ghost silhouette rendered as a field of terminal characters whose density breathes. Not a mascot: no mouth, no smile, the eyes are voids.

**Cell grid (locked):** cell `7 × 11` CSS px, glyphs drawn at `11px "JetBrains Mono"`. Grids: planner `30 × 25`, implementer `26 × 22`, reviewer `22 × 18` (aspect ≈ 1 : 1.3 like the reference). The canvas is sized `cols*7 × rows*11` CSS px and backed at `devicePixelRatio` (cap 2). It is never resized by CSS; the diagram box scales it with `transform` (wordmark/ASCII lock rule).

**Silhouette** `inside(u, v)` on normalised coords `u ∈ [0,1]` across, `v ∈ [0,1]` down:
- crown: for `v < 0.42`, inside a semi-ellipse centred `(0.5, 0.42)` with radii `(0.5, 0.42)`
- body: `0.42 ≤ v < 0.86` full width
- skirt: `0.86 ≤ v ≤ 1`: four scallops — inside when `v ≤ 0.86 + 0.14 * |sin(u * 4π)|`
- eyes: two ellipses centred `(0.34, 0.40)` and `(0.66, 0.40)`, radii `(0.11, 0.13)` → **void** (density 0). One pupil cell `@` per eye on the pupil row `⌊0.40 · rows⌋ + gaze.r`: take the void's cell span on that row, `mid = (first + last) / 2`, pupil column `⌊mid + gaze.c / 2⌋` — half a cell **toward the brief card** (planner gaze `+1` → cols 10/20; implementer `−1` → 8/16; reviewer `−1`, and one row up → 6/13). Measured 2026-09-07 (fix cycle 1): the earlier "eye centre + one cell" put the pupils against the socket wall and the two eyes disagreed by a cell; the span midpoint keeps a free cell on both sides of every pupil.

**Density** `d(c, r, t) ∈ [0, 0.875]` for body cells (only the pupils return 1):
- base `d0 = 0.42 + 0.42 * (1 − dist⁴)` with `dist = clamp(hypot((u − 0.5) / 0.5, (v − 0.6) / 0.85), 0, 1)` — a plateau whose peak is the belly (`v = 0.6`), flat across the middle of the body and dropping only at the rim. The vertical radius was 0.7 until 2026-09-07 (Phase 4, Phase 3 critic's craft item): the crown apex sat at `dist ≈ 0.83`, base 0.64, and after the ×0.45 edge falloff the top two crown rows rendered as `.`/`:`; at 0.85 the apex is `dist ≈ 0.68`, base 0.75, and the crown rows render `:`/`-`/`=` like the reference's. Measured 2026-09-07 (fix cycle 1): the dome `0.55 + 0.45 (1 − dist²)` centred at `v = 0.5` put the brightest row two cells under the eyes (a grin), and once the eye ring was gone it made the column between the eyes one and a half glyphs brighter than the cheeks (a nose); the quartic keeps bridge and cheeks within half a glyph and the crown as dense as the reference's
- weave: odd rows `× 0.85`, odd columns `× 0.88` — louvres, not stripes: glyphs alternate within a row (`oOoO` / `:.:.`) as well as between rows; at `t = 0` at least 40% of adjacent body-cell pairs differ in glyph (tested; measured 63–68%). The earlier row-only `× 0.72` read as horizontal banding
- edge falloff: cells whose 8-neighbourhood is not fully inside the **silhouette** `× 0.45`. The eyes do not count as edges — counting them dimmed a ring around each void, which framed a bright patch under the eye gap (the mouth of fix cycle 1) and a bright bridge between the eyes
- breath: `× (0.82 + 0.18 * sin(2π t / P + phase))`, `P` per seat: planner 6.5s, implementer 2.8s, reviewer 9s; `phase`: planner 0, implementer 2.1, reviewer 1.0. Frame 0 is what every screenshot and every reduced-motion visitor sees, so no seat may open at the trough: the reviewer's earlier 4.2 opened at `× 0.66` and read as dirt beside the others (lit-pixel share 0.15 vs 0.22/0.26, measured 2026-09-07); at 1.0 it opens mid-breath (`× 0.97`; share 0.27 vs 0.23/0.28)
- noise: `+ 0.18 * n(c / 4, r / 4, ⌊t*F⌋ / 4)` with `n` a seeded value-noise (deterministic per seat; **no `Math.random` at render time**) and `NOISE_SPAN = 4` — four cells spatially and four ticks temporally, so the grain is coherent blotches that drift rather than per-cell static; `F` = planner 6 Hz, implementer 12 Hz, reviewer 4 Hz
- clamp: body cells are capped at `0.875` = glyph index 7 (`O`); `@` (index 8) is reserved for the pupils, so no body row can ever grin
- halo: cells **outside** the silhouette whose 8-neighbourhood touches it (the one-cell ring) get `d = 0.05 + 0.1 * n` — a `.` only where `n ≥ 0.75`, about a quarter of the ring (the scattered `.` around the reference ghosts). Until 2026-09-07 (Phase 4, Phase 3 critic's craft item) the rule was `0.08 + 0.1 * n` over a two-cell ring, which lit ~55% of both rings and read as fog rather than stray cells
- reviewer: no global gain (it is the quiet seat, and its all-dim palette is what keeps it quiet; rendered on 2026-09-07 during Batch 3.B, `× 0.7` and `× 0.85` both read as dirt — at frame 0 its breath is already at the trough, `× 0.66` — so the multiplier was removed and the palette alone carries the restraint)

**Glyph ramp** (density → char): `' ', '.', ':', '-', '=', 'o', '0', 'O', '@'` — index `⌊d * 8⌋` clamped. **Colour ramp:** alpha belongs to the glyph index and is baked into the atlas once per `(glyph, tint)` tile — `d < .3` → alpha `.55`; `.3–.7` → `.6 + d*.4`; `> .7` → alpha 1. The tint is chosen per cell by `tintFor(seat, d, hash)`: `d < .3` → `*-dim`, otherwise the accent; every cell whose seeded hash `< 0.12` draws in `--white-spark` instead (the sparkle). Reviewer: hash chooses blue-dim / green-dim / spark at 55/35/10.

**Glitch:** every 3–7s (seeded schedule), one random body row shifts by +1 col for 90ms (implementer) or 160ms (planner); the reviewer never glitches.

**Rendering:** one **glyph atlas** per ghost — an offscreen canvas holding each `(glyph, colour)` pair pre-rendered once; the frame loop is `clearRect` + `drawImage` per non-empty cell. Frame loop via `requestAnimationFrame`, but cells update on a fixed tick (planner/implementer 12 fps, reviewer 8 fps); the rAF only redraws when the tick changed. The loop **pauses** when `document.hidden`, when the canvas leaves the viewport (`IntersectionObserver`), and never starts when `matchMedia('(prefers-reduced-motion: reduce)')` matches — then it draws frame `t = 0` once. The loop is `diagram/ticker.ts` (`createTicker(canvas, rate, onTick)`), shared with the rain drift (§7.2). `ghost.ts` exports `createGhost({ canvas, seat, pose? })` → `{ renderFrame(t, pose?), start(), stop() }` (grid, seed, and timing come from the `Seat` record in `seats.ts`; `pose` is read every tick and hands `density()` the §7 reaction — gaze, lift, gain, jolt); `silhouette.ts`, `density.ts`, and `atlas.ts` hold the pure functions and are unit-tested.

**Accessibility:** each canvas carries `role="img"` and `aria-label` — `"Planner daemon: Claude Code with Fable"`, `"Implementer daemon: OpenCode with DeepSeek"`, `"Reviewer daemon: Codex with GPT-5.6"`.

## 7. Motion — one concept: *the system is alive and routing*

Everything that moves belongs to this one idea; nothing else animates. Phase 4 (2026-09-07) added the responses recorded under each item — every one of them is something the packet causes — and the list below is the whole of what the code does; a task-row flip on the card (`T2 ✓`, `T3 ▸`) was built and removed: it marked the task done before the implementer had received it and snapped back at every loop seam.

1. **Breath** — the ghosts' density field (§6). Calm planner, quick implementer, slow reviewer. The loops run on `diagram/ticker.ts` (`createTicker(canvas, rate, onTick)`: planner and implementer 12 fps, reviewer 8 fps; paused while the tab is hidden or the canvas is off-screen). Each ghost also **reacts to the packet** through the `Pose` it hands `density()` every tick (`pose.ts`, pure, from the timeline's clock): *gaze* — the pupils point at the packet: `c` = −1 / 0 / +1 by whether it is left of, within, or right of the middle half of the ghost's box, `r` = −1 / 0 / +1 against a band 40% of the box height either side of the eye row, and the seat's own gaze (§6) returns whenever nothing is in flight — so all three sit level while the brief runs planner → card → implementer, the planner and the implementer drop their gaze one row while it goes down to review (2.7–8.8 s), the reviewer meets it level on the last leg, and everyone looks back toward the card at rest (the ±25% band measured 2026-09-07 kept every ghost looking down for 84% of the loop, because both routes run below the eyes); *lift* — the rim cells (edge falloff ×0.45) rise to ×0.9 over the 0.6 s before the packet touches the seat and settle back over 0.3 s (planner, bracing before it emits at t = 0), 0.5 s (implementer) or 0.6 s (reviewer); *gain* — the reviewer alone multiplies its body ×1.15 for 0.4 s when the packet lands (the confirm); *jolt* — the implementer alone tears when the packet lands: its seeded glitch row shifts one cell for 250 ms (`pose.ts` `JOLT`), three consecutive 12 fps ticks, while the body stays put — a glitch, not a stumble. The seat's own 90 ms glitch length was one 83 ms tick and invisible in every capture (fix cycle 1); measured on real-time frames 2026-09-07 the torn row holds at 2.73, 2.82 and 2.90 s and returns at 2.98 s. The per-seat moments come from `timeline.ts` `schedule()` (planner 0 s, implementer 2.69 s, reviewer 8.71 s at 760×860 — §7.3).
2. **Rain** — the seeded glyph columns above planner and implementer (§4) drift downward at 14 px/s: `rain.ts` `rainPhase(t)` turns the elapsed time into a whole-pixel translate of the column (0–10 px) and a *generation* every 11 px; each generation moves every surviving glyph one row down (its lit test is re-evaluated at the new row's share, so the fall thins toward the tick and the taper survives the motion) and re-seeds the top row. Driven from `diagram/mount.ts` by the same ticker at 14 Hz. The rain is glyphs, not a gradient, so it is no longer a `background-position` shift.
3. **Packet** — two `--white-spark` `■` elements (7×7px), `.packet--impl` and `.packet--rev`, on one shared timeline whose length is derived from the drawn routes (`timeline.ts` `schedule()`: every visible leg at `SPEED` 55 px/s, the card crossing `DWELL` 0.6 s, then `REST` 1.3 s after the reviewer's landing — 10.0 s at 760×860, 5.7 s on the compact stage, where a `HANDOFF` of one `DWELL` sits between the implementer's landing and the reviewer leg; one WAAPI animation per element, infinite, `fill: both`). **One speed, 55 px/s, on every visible leg**: the legs are timed to their measured stage lengths — route A 39 px, route B 76 px, route C 331 px (the fix-cycle-1 critic measured 33 / 47 / 83 px/s, so one object read as two). t 0–0.7 s `.packet--impl` fades in over 120 ms and travels route A from 8 px inside the planner's rim (it emerges between the rim glyphs) to the card's left edge · 0.7–1.3 s it crosses the card **behind** it (`timeline.ts` `DWELL`; the card occludes, §5 — the one leg that is not 55 px/s, and it is never seen) while the card's outline takes the packet's colour — `border-color` `--hair-strong` → `--white-spark` over 100 ms, held to 1.3 s, back by 1.4 s; the `T2 … ▸` row already rests at `--ink` (§5) and does not change · 1.3–2.7 s it travels route B to 8 px inside the implementer's rim, then fades out over 120 ms while the implementer's tick flares `--ink-3` → `--white-spark` (50 ms up, held 0.4 s, back over 100 ms) and its seat list `GENERATE / ITERATE / TEST` lifts `--ink-3` → `--ink-2` on the same envelope · at 2.7 s `.packet--rev` appears 8 px above the card's bottom centre and travels route C (6.0 s, to 8.7 s), then fades out while the reviewer's tick and seat list answer the same way — on the compact stage, where route C leaves the implementer's skirt, `.packet--rev` waits `timeline.ts` `HANDOFF` after the landing (one `DWELL`, 0.6 s; 0 on the wide stage, where the card already separates the two packets): the implementer packet fades at the crown, the tick flares and the jolt runs with nothing lit, then the reviewer packet emerges at the skirt — paused frames 2026-09-08 (fix cycle 1): at the landing only the implementer's is lit, at +0.3 s both are dark while the tick is `--white-spark`, at +0.9 s only the reviewer's is lit, 16 px down route C; until then `rev.from` was the landing itself and the compact stage moved the packet 226 px from crown to skirt in one frame · 8.7–10.0 s rest (both hidden): the reviewer's flash ends at 9.25 s, the planner braces from 9.4 s and the next brief leaves at 10.0 s — one beat of stillness, measured on paused real-time frames 2026-09-08 (Phase 6, the Phase 4 critic's item; until then the loop was 9 s and re-emitted 0.3 s after the landing) — every reaction is a cue-relative loop started with a WAAPI `delay` of its cue, so nothing is folded at the seam. Measured 2026-09-07 (fix cycle 1) from `offsetDistance` at paused times: A 55.8 px/s (8.4 → 36.3 px over 0.15–0.65 s), B 54.2 px/s (149.6 → 220.1 px over 1.35–2.65 s), C 55.2 px/s (2.8 → 328.3 px over 2.75–8.65 s). The tick labels themselves stay `--ink`: a flash to `--white-spark` measured invisible on 2026-09-07 (a 3% step), so the tick carries it. Each packet tows a **wake**: three `--white-spark` squares of 5, 4 and 3 px at opacity .45, .30 and .15 riding the same path and keyframes 0.12, 0.24 and 0.36 s behind it (a WAAPI `delay`), so the tail is the same 20 px on every leg (measured 20.1 / 19.6 / 19.9 px on A / B / C) and closes up where the packet stops. Implemented in `features/diagram/packet.ts` as `offset-path: path(...)` with `offset-distance` keyframes — `route-geometry.ts` parses each route's SVG `d` (§4) into its polyline in stage px and extends it 8 px past both ends — so packets and wakes stay glued to the lines under `transform: scale()` and on either stage. The timeline starts at boot, so on first load the planner's leg overlaps the diagram's entrance: the system is already running when it appears.
4. **Entrance** — on load only: `nav` (0) · steps, the `/` mark and the claim (1) · `TWO MODELS` (2) · `ARE GOOD.` (3) · `A SYSTEM` and the diagram (4) · `IS BETTER.` (5) · lede (6) · CTA (7) · works-with (8) fade from `opacity 0; translate 0 8px` to rest, each starting at its index × 60 ms, 480 ms, `ease-out`, `both` — so the diagram fades in as one unit after 240 ms. The fragments layer fades in over 480 ms once placed. No scroll-triggered animation anywhere; the routes/manifesto sections are static.
5. **Background fragments** — `features/fragments/mount.ts` places the 20 mono strings of the pool below, each once, across the viewport (`position: absolute; inset: 0` layer behind content — sized by the initial containing block, so it is the first viewport and scrolls away with the hero — `z-index: -1`, `--ink-4`, 11px, opacity `.18–.35`), each hovering: it drifts upward 64px over 14–28s (120 px, then 96 px until 2026-09-08, see the seed note), fading in over the first second and out over the last 2s, and restarts at its origin (no wrapping, no infinite scroll); each starts mid-cycle at its own seeded phase (a negative `delay`), so nothing moves in unison. Pool: `brief -> implementer`, `typecheck · lint · test`, `retry(3) -> escalate`, `const brief = compile(spec)`, `while (red) retry()`, `promote(diff)`, `evidence.jsonl`, `real software`, `lower spend`, `fewer blind spots`, `one file per brief`, `0x2f 0x62 0x72`, `[ 3 / 7 ]`, `∴`, `//`, `→`, `T1 ✓`, `T2 ▸`, `hash ok`, `worktree` (20 strings). Origins, drift durations, opacities and phases are drawn from `lib/noise` `hash` with seed 8088 (no `Math.random` anywhere in `src/`; seed 74 carried Phases 4–5, seed 912 Phase 6 to T-014 and seed 7958 T-015 to the final fix — on 2026-09-08, Phase 6, the diagram's scatter and rain glyphs joined the keep-clear list and type earned a 24 px horizontal clearance, after which no seed of 1000 placed all 20 with a 120 px rise; at 96 px, one `--s-24`, seed 181 placed 20 with none on a text line. Fix cycle 1 the same day added the two gutter strips and the label blocks below, after which no seed of 2000 placed more than 17 at 96 px; at 64 px, one `--s-16`, 209 of 1500 seeds place 20, and seed 912 was the one with no fragment on a label's lines within 100 px and the widest spread. T-015 (2026-09-08) added the quiet strip under the nav and the two rain column boxes below, after which seed 912 placed 18; 20 of 5 000 seeds place 20, six of them keep every label's lines clear within 100 px at the origin, and seed 7958 is the one of those with the widest spread (82 px between the nearest two origins) — its one travel-band crossing is `typecheck · lint · test` reaching the `+ FABLE` line 64 px to its right in the fading last second of its rise; no seed of 20 000 has none. The final fix (2026-09-09) set the lede four lines deep, and that one line of free height was the margin: no seed of 30 000 places all 20 at 1440×900 any more — 23 place 19 — so seed 8088 is the 19-seed with no origin within 100 px of a label's lines and the widest spread (70 px); `worktree` is the string it leaves out. The seed search measures the page at `fonts.ready`, as `main.ts` does — the rain has drifted a few px by any later moment and the placement differs), so the layout is identical on every render and the screenshots are reproducible. Each string's first 1000 candidates are drawn from its own quarter of the free height (string *i* → stripe *i* mod 4) and no origin lands within 60 px of another, so the strip under the nav no longer collects half the pool. Fragments never overlap the type or the diagram's marks: the placer rejects any origin whose box **plus its full 64px travel band** (6 px margin; type boxes are also grown 18 px each side, so a fragment's ink stays `--s-6` from any text line — the Phase 6 fold had `typecheck · lint · test` reading as a fourth word of `+ DEEPSEEK`) intersects a keep-clear box — the headline, lede and CTA the concept demands, **the whole header band and a `--s-6` quiet strip under it** (`header.nav`'s box stretched to the full viewport width and grown 24 px down, so the gutters beside the wordmark and the links are silent too and no fragment's ink rises within one line of the tagline — the Phase 6 critic's carry, applied in T-015), the steps' cells, the `/` mark, the claims, works-with, the three canvases, the tick labels, the seat labels, the card, the three routes, the `+`, every scatter glyph (each `span` is a mark, since Phase 6) and **the two rain columns as boxes** — 42 px wide on each lab tick's x, from y 6 % of the stage to the tick's top (§4); the rain drifts, so the glyphs lit at boot were the wrong shape to keep clear (T-015), **the two gutter strips** (`.hero`'s left offset wide, full height — until fix cycle 1 nine of the 20 sat outside the 64 px gutters and single glyphs at x = 7 and x = 1390 read as clipped marks on the screen edge) and **each tool label and seat list grown by its own width to either side and one line height above and below** (a gap narrower than the block reads as a third column of it — the fold had `typecheck · lint · test` 42 px right of `+ DEEPSEEK` reading as its tail, and after the first fix `while (red) retry()` 24 px beside it on its own line; the 24 px text clearance alone never blocks a same-line neighbour), and, since Phase 5, the routes eyebrow and rows, the manifesto heading, paragraph and list, and the three footer columns (the layer is the first viewport and the boxes are measured at scroll 0, so the lower sections take part only on a viewport tall enough to show them at load; the atmosphere lives in the hero — below the fold the page is silent. Until 2026-09-08 the layer was `position: fixed` and at max scroll 11 of the 20 rode across the routes rows, the routes claim and the footer tick; `absolute` keeps them with the hero without a scroll-linked animation, which this section rules out) — or a band already taken by another fragment, so no two fragments ever cross. `main.ts` measures the boxes once after fonts load and again on `resize` (one queued `requestAnimationFrame`, so a drag re-places once per frame), **at rest**: every finite animation (the entrance) is sampled at its end and restored inside the same task, so the 8 px entrance offset never shifts a placement (it moved the count between 27 and 28 until 2026-09-07). Origins sit at least one travel (64 px) below the top edge and above the fold, so every band — origin to the end of its rise — stays on screen for the whole cycle (fix cycle 1: 8 of 28 were parked at y ≈ 1–13 and rose off-screen before their fade-in completed, and 14 bands crossed the nav); the search draws up to 2000 seeded candidates per string — at 1440×900 19 of the 20 place, and **the header band is silent**. The narrower tiers keep whatever fits between the type and the marks: 20 at 1024×900 and 12 at 768×900 (measured 2026-09-09, seed 8088 — 15 / 8 under seed 7958; none outside the gutters at any width). Below 768 `mount.ts` places nothing (`PHONE`): the phone hero is all type and diagram, and the two or three strings that fitted there sat in the claim → headline gap and read as a fourth claim line — `brief → implementer`, `typecheck · lint · test` (the final critic, 2026-09-09) — so the atmosphere is a desktop thing. Opacity never exceeds `.35`.

`prefers-reduced-motion: reduce` → 1 renders frame 0 only (the seat's own gaze, no lift), 2/3/5 are off (`animation: none`; both packets and their wakes `display: none`; `main.ts` calls `stop()` on the diagram and the fragments, which cancels every WAAPI animation — the card outline, ticks and seat lists sit at rest — and leaves the fragments static at their origins), 4 is replaced by instant visibility. `@media (prefers-reduced-motion)` lives in `src/styles/motion.css` alongside the keyframes; `lib/reduced-motion.ts` owns the single `matchMedia` and its `change` subscription, and `main.ts` starts or stops both features live when it fires.

**v2 build, 2026-09-09: the hero's class names are now shared with the lower page, so two sheets scope them** (D-14). `hero.css` scopes `.steps` and `.line` under `.hero`, and `motion.css` scopes its three `.steps` selectors the same way (its `.line` rules already read `.hero h1 .line`): §16.2's `ol.steps` and §16.3's breath lines reuse both class names with their own grammar, and without the scoping they inherited the hero's. Briefs T-106 and T-121.

**v2 build, 2026-09-09: the hero places 19 fragments at 1440×900** (D-13, item 5 above). The floor asserted in `motion.e2e.ts` is `>= 18`, not an exact 19: the steps' anchors changed the labels' rendered widths, which enter the placer's keep-clear, so an exact count would be pinned to a label's pixel width. 19 is the measured count on the shipped build and it survived every later change, including the placer's move to `features/fragments/place.ts` and the text-clearance growth of the P5 fix batch, which briefly took it to 15 before the growth was made per-layer (§16.5).

## 8. Routes section (`section.routes`)

REMOVED in v2 (2026-09-09) — see §16.

## 9. Manifesto (`section.manifesto`) — the breath

REMOVED in v2 (2026-09-09) — see §16.

## 10. Footer (`footer.foot`)

v2 (2026-09-09), rewritten to the reference footer; the v1 three-column footer with the centred tick and the long repo link is gone. One row, two ends, on the §1 content width (`width: min(var(--content-max), 100% - 2 * var(--gutter))`, `margin-inline: auto`), preceded by the full-bleed `hr.rule.rule--bleed` (`width: 100%`, 1px `--hair`). `display: flex; justify-content: space-between; align-items: baseline; column-gap: var(--s-6); padding-block: var(--s-8)` — 32 + 17 + 32 = 81 px at every desktop width (87 measured with the wordmark's 22 px line; the reference footer is cropped at 40 ref px and gets the nav's row minus one step, not the nav's 96). `--text-label`, line-height 1.45, tracking `.08em`, uppercase, `--ink-2`.

- **left** `p.brand` (`display: flex; align-items: baseline; gap: var(--s-8)`): `a.wordmark` `splitbrief°` — the nav's wordmark verbatim (§3: `--font-mono`, 22px, 500, `--ink`, the `°` a `<sup>` in `--ink-3` at 60 %, `top: -.6em`; `href="#top"`), then `span.claim` `BUILDS BETTER SOFTWARE.` (`--ink-2`).
- **right** `p.right` (`display: flex; align-items: baseline; gap: var(--s-8)`): `span.links` — `[ docs ]` → `https://github.com/b4r7x/splitbrief/tree/main/docs`, `[ github ]` → `https://github.com/b4r7x/splitbrief`, gap `--s-6`, the nav links' markup and states (`span.bracket` turns `--blue` on hover, text `--ink`, 44 px hit box, ring) — then `span.seats` `PLANS / EXECUTES / REVIEWS` in `--ink-3` with each `/` a `span.sep` in `--ink-4`; text, not links (the reference's; §3).

Geometry at 1440: rule at the page's y 2 904, footer 2 905 → 2 992 (§16.8); wordmark x 64 (140 wide), claim x 236, `[ docs ]` x 961, `[ github ]` x 1 050, `PLANS` x 1 164 → the last glyph at 1 376 (the content edge). At 1920 (`--content-max` 1392): x 264 / 436 / 1 241 / 1 330 / 1 444 → 1 656. < 768: `flex-direction: column; align-items: flex-start; gap: var(--s-4); padding-block: var(--s-6) var(--s-8)`, `.brand { flex-wrap: wrap; row-gap: var(--s-2) }` so the claim wraps whole under the wordmark (fix cycle 1: as a rigid row it broke `BUILDS BETTER / SOFTWARE.`), `.right` also a column with `gap: var(--s-2)` — brand, then the two links on one row, then the seats; the page ends on `PLANS / EXECUTES / REVIEWS`. §16.4 repeats this footer in full for the implementer, who reads §16 alone.

## 11. Responsive tiers

| width | layout |
|---|---|
| ≥ 1100 | as above; the diagram box scales fluidly between 584px and 760px wide via `width: 100%` on the 7-column span; ghosts keep their pixel size (they are placed by %) — at 584px the scale factor is ~0.77, applied with `transform: scale()` on an inner 760×860 stage |
| 768–1099 | hero stacks: left column full width (headline 55.3 px at 1024), the diagram after the CTA at `max-width: 760px` with `margin-top --s-6`, and works-with last (`order: 1`; until 2026-09-09 the diagram followed works-with with `--s-12` above it, so the first 900 px held no part of the object — the final critic's item): the diagram's top edge sits at y 823 at 1024 and 810 at 768, so the fold cuts the object's scatter and rain, but the planner's crown stays under it — canvas top 1 030 at 1024 and 1 000 at 768; the ≈ 130 px that would bring it inside 900 can only come from the headline block (the `--s-24` above the h1 and the `--s-24` stanza gap), the owner's call — `--fit` is `min(1, box / stage)`, so the cell grid is never upsampled (until 2026-09-08 the box was the full content width and the stage scaled ×1.23 at 1024); nav: the tagline moves to cols 4–6 with `white-space: nowrap` (at cols 3–4 it wrapped into the wordmark from 1000 down) and the links to cols 7–11; routes table keeps all columns (arrow gutters `--s-8`, 689 px at 768) with the claim **under the table** at cols 10–12, `margin-top --s-8` (on the eyebrow row it pushed the table 73 px down — Phase 5 critic); the hero claim sits at cols 10–12 on the steps row, so the hero claim, the routes claim, the manifesto list and the footer's right column share one rail — x = 763.5 at 1024 and 577.1 at 768, measured 2026-09-08 (fix cycle 1; at cols 7–12 the two claims floated at x 524, aligned to nothing); manifesto becomes 2 rows (heading full width, then paragraph + list side by side, `row-gap: var(--s-12)`); the manifesto list and the footer's right column keep cols 10–12 at this tier (on the 1440 rail, 11–12, `HIGHER QUALITY` and `BETTER OUTPUT.` wrap from 1000 down — measured 2026-09-08); footer 3 columns, the centre one cols 4–9 so the repo link never breaks |
| < 768 (390 target) | nav: a flex row of wordmark + links only (`justify-content: space-between`, `column-gap --s-4`, links `gap --s-3`, `white-space: nowrap`); the tagline and the 9-dot mark are `display: none` (both decorative; at 360 the wordmark's 140 px and the links' 159 px fill the 320 px row to the pixel). Headline `--text-display` becomes `clamp(2.75rem, 13vw, 3.25rem)` (§1), so `TWO MODELS` never wraps: 339 px at 390, 313 px at 360. **Diagram compact tier** (`@container hero (max-width: 700px)` — `.hero` is the `hero` inline-size container and 700 px is its width at a 768 viewport): the 760×860 stage is replaced by a vertical **350×980** stage (the box `max-width: 350px; aspect-ratio: 350 / 980`), scaled by `min(1, box / 350)` — 1.0 at 390, 0.914 at 360. One spine at x = 38% (133 px) carries, top to bottom, the planner (centre 15% → y 9.5–284.5), the card (39% → 330.2–434.2), the implementer (61.5% → 481.7–723.7) and the reviewer (89% → 773.2–971.2), 46–49 px apart; the routes are the straight vertical dashed segments between them — A planner rim → card top, B card bottom → implementer crown, **C implementer skirt → reviewer crown** (the stack reads plan → brief → execute → review; a card → reviewer bypass around the implementer would have needed a 585 px detour). A rail at x = 71.4% (250 px) holds every annotation: each seat's tick (1×28) and tool label 30 px under the ghost's top (planner 4%, implementer 52.2%, reviewer 82%) and its seat list bottom-aligned 10 px above the ghost's bottom (17.7% / 62.5% / 87.8%); the `+` sits beside the card at (80%, 40%). The first draft of this row put tool labels right and seat labels left of centred ghosts: at 350 px a 210 px planner leaves 70 px a side, and `+ DEEPSEEK` (82 px) and `DECOMPOSE` (67 px) do not fit — hence the spine and rail (2026-09-08, Phase 6). Same canvases, same 7×11 cell grid before the stage's uniform fit-scale; no rain and one trail beside the card (§4); the packets ride the same drawn routes at 55 px/s, so the loop is 5.7 s here — the reviewer leg waits one `DWELL` at the implementer (§7.3); the no-JS `<pre>` art sits on the same spine. The claim keeps cols 7–12 (163 px at 390, 148 px at 360 — `FEWER BLIND SPOTS.` is 147 px), three lines. The lede's `<br>` is hidden (below 1250, §4) so `text-wrap: pretty` sets the paragraph (the forced break left a 16-character line, `against one job.`, in an eight-line block). Works-with becomes a 2×3 grid (`grid-auto-flow: column`, gap `--s-8`) with the dash on the block. Routes table: index + plan / implement / review stacked as three lines per row with the goal comment as the first line (`// feature`), arrows become `↓` on their own line — each `tr` is a two-column grid (index · text, rows `--s-6` apart, 18 px lines) with the comment spanning the first line; Chrome keeps the table / row / cell / columnheader roles under the display change (verified through the CDP accessibility tree, 2026-09-07). Manifesto single column. Footer single column, centred tick, the link last (`order: 1`). No horizontal scroll at 390px or 360px. |

**v2 (2026-09-09) — the FHD tier and the lower page's tiers.** Ruling 5 makes 1 920×1 080 a first-class fold. Decision: **a capped container, not a fluid grid** — at `≥ 1600` `--content-max` becomes `1392px` (gutters of 264 px at 1920, the aura full-bleed across them), the lower page's type steps up one notch through the §1 v2 tokens, and the hero changes nothing but its width: the diagram box grows to 802 px and holds the 760 px stage at scale 1 (`--fit = min(1, box / stage)`) with 42 px of slack on the right, the headline stays at its 4.9 rem ceiling, and the hero's last ink — the works-with dash — sits at y 1 072 at 1 920×1 080 (measured from §4's y-table), inside the fold; the diagram's 860 px stage ends at y 980. Rejected: a fluid grid to 1920 (content 1 792 — the diagram box would be 1 042 px wide, so either the ghosts sit in its left 760 px with the claim column 280 px away, or a scaled stage is 1 179 px tall and leaves the fold), and a hybrid grid to 1 680 (the same two failures at 905 px). At 1 440×900 the hero is byte-identical to v1, so the diagram is cut exactly as today. The lower page's own breakpoints are its measure, not the hero's: the reference's 02/03 row is fixed — head 310 · panel 494 · notes ≥ 321 (the 41-character measure) plus two 24 px gaps = 1 173 px — and the content box holds it from 1 300 up, so **1360** is where the lower page turns three-column (59 px of slack there); every width and what moves where is in §16.7:

| width | hero | lower page (§16.7) |
|---|---|---|
| ≥ 1600 (1920) | as ≥ 1100, content 1392 | the reference row (head 330 · panel 517 · notes 497), rail, stops and marginalia on, FHD type tokens |
| 1360–1599 (1440) | as ≥ 1100 | the reference row (head 310 · panel 494 · notes 460), rail, stops and marginalia on |
| 1100–1359 | as ≥ 1100 | two rows: the head band (cols 1–6) over panel (1–7) + notes (8–12); rail, marginalia and rail-side dot fields off |
| 1024–1099 | 768–1099 tier | same two rows |
| 768–1023 | 768–1099 tier | head band · panel full width · notes (cols 1–8); 04's breath full width |
| < 768 (390) | compact stage | single column, source order; panels wrap their rows with a hanging indent; marginalia, stops and the rail off; no aura below 768 |

Container queries (`@container hero (max-width: …)`) drive the diagram tier; media queries drive the page grid. The diagram's JS reads the tier from the visible `.route-lines` SVG (`checkVisibility()`) and rebuilds its scene — boxes, packets, scatter, rain — when the stage's layout width changes (a rotation across 768); the ghosts' canvases stay. Touch targets ≥ 44px for the CTA and links (padding, not font size).

## 12. Craft floor (checked on screenshots, every phase)

- headline-to-body ≥ 4:1 (124/15 ✓); nav ink ≈ 55% (`--ink-3`) so it recedes
- exactly two accents, each ≤ 3 element types (blue: planner ghost, planner labels/ticks, focus/copy flash · green: implementer ghost, implementer labels, the `✓`)
- one atmosphere (vignette + grain), nowhere else
- no orphans under any headline line; no mid-URL breaks in the footer link (`overflow-wrap: anywhere` only there)
- every interactive element: visible 2px focus ring, hover state, `cursor: pointer`; the nav links, CTA and footer link measure 44 px tall at every width (2026-09-08)
- `::selection` styled; `<html lang="en">`; `<meta name="theme-color" content="#0b0c0e">`; `<title>splitbrief — one plans, one executes, one contract</title>`; `<meta name="description">` = the lede
- Lighthouse a11y ≥ 95, performance ≥ 95 (no assets besides fonts; built JS ≤ 90 KB gzipped including any motion library, ≤ 12 KB gzipped of our own code) — 2026-09-08, desktop, against `vite preview`: performance 99, accessibility 100, best practices 100, SEO 91 (the one SEO miss is the preview server's missing robots.txt)

## 13. Stack and file map (sota-structure)

**Stack:** Vite (vanilla TypeScript template, `base: './'` so `dist/` deploys under any path — it is served over HTTP, never opened from `file://`, which Vite's module scripts do not support; build → `dist/`) · optional `gsap` as the only runtime dependency, imported per module (`import { gsap } from 'gsap'`), tree-shaken, used only inside `src/features/**` · TypeScript strict (`moduleResolution: "bundler"`, extensionless imports — the Vite convention; this package is independent of the CLI's NodeNext config) · Vitest for unit tests, colocated · `@playwright/test` with `channel: 'chrome'` (the installed Google Chrome, no browser download) for e2e, screenshots, and motion assertions, running against `vite preview` · Biome for lint + format. No UI framework; `dependencies` holds at most `gsap`.

```
website/
  package.json               private; scripts: dev · build · preview · typecheck · lint · format · test · e2e · shots · render-static
  package-lock.json          the lockfile `npm ci` installs from
  tsconfig.json              strict, bundler resolution, noUncheckedIndexedAccess, exactOptionalPropertyTypes, types: ["vite/client", "node"] (the e2e files read process.env and vite references the node types)
  vite.config.ts             base './' (host-agnostic asset paths), build.target 'es2022', preview port 4173
  vitest.config.ts           include src/**/*.test.ts, environment 'node' (pure functions only — DOM code is e2e-tested)
  playwright.config.ts       channel 'chrome', webServer 'npm run build && npm run preview', baseURL http://localhost:4173, testDir tests/e2e, testMatch **/*.e2e.ts
  biome.json                 same style as the repo root (2 spaces, 100 cols, single quotes, kebab-case filenames), includes src/ tests/ tools/ and the config files
  index.html                 the page — Vite entry; semantic skeleton, all copy, canvases, <pre class="ghost-fallback"> siblings shown only under html.no-js, the two route-lines SVGs (one per stage) whose path d is the routes' single source
  DESIGN.md                  this sheet
  README.md                  ≤ 10 lines: install, dev, build, preview, typecheck, lint, format, test, e2e, shots, render-static, design-sheet and handoff pointers
  HANDOFF.md                 the maintainer handoff: read order, decisions, process rules, state at finish, how to run, where things are
  handoff/                   the build's evidence — spec.md, requirements.md, plan.md, prompt.md, exec-plan.md, exec-progress.md (the phase ledger), exec.md (per-phase evidence), extension-research.md, reference.png (the visual target) and six milestone captures (p2fix-fold, p6-390, p8e-1440, p8e-fold, p8e-390); every other capture lives in the gitignored run dir `.nuke/2026-09-06-225118-spec-website/shots/`
  .gitignore                 test-results/ (Playwright's outputDir); dist/ and node_modules/ are already covered by the root ignore
  src/
    main.ts                  boot: mountCopyButton · mountDiagram · mountFragments (the no-js → js swap is the inline head script, §2); measures the fragments' keep-clear boxes at rest (§7.5) — text, marks, the header band and its quiet strip, the two gutter strips, the label/seat blocks and the two rain column boxes — and starts/stops both motion features from lib/reduced-motion
    styles/                  the shared stylesheet tier (a technical-type dir is sanctioned in the shared tier of a tiny app)
      main.css               @layer tokens, base, layout, sections, motion; then @import url(...) layer(...) for every stylesheet below, in order
      tokens.css             §1 custom properties only
      base.css               @font-face size-adjusted fallbacks, reset, body (background-color + background-image), grain, ::selection, :focus-visible, .dash, .rule, .visually-hidden, .no-js/.js switches
      nav.css                §3
      hero.css               §4 left column, claim, works-with; the hero grid (the CTA box lives in cta.css)
      cta.css                  §4 CTA: the box, the two-state label grid (idle + notice stacked in one cell), the copy glyph, the copied flash
      diagram.css            §4 right column, stage + fit-scale, routes, ticks, labels, rain, scatter
      brief.css              §5 card (split out of diagram.css on 2026-09-07 — the stage sheet alone sits near the 200-line cap)
      diagram-compact.css    §11 compact tier — stage, spine, rail (the container query); the card's compact position lives in brief.css
      routes.css             §8
      manifesto.css          §9
      footer.css             §10
      brief-sheet.css        §15.1 — the sheet box and its 707 / 689 px crops, the two marginalia statements, the tiers
      ladder.css             §15.2 — the run block, its bar and rows (the callout rail, the phone tails), the rungs (steps grammar), the tiers
      record.css             §15.3 — the tree and its notes (stacked below 768), the two $ lines on the rail, the tiers
      motion.css             §7 keyframes + the prefers-reduced-motion block
    lib/
      noise.ts               hash(seed, x, y, z) and valueNoise(seed, x, y, z) — pure; consumers: diagram/density.ts, diagram/scatter.ts, diagram/rain.ts, fragments/mount.ts (placement seed)
      noise.test.ts
      reduced-motion.ts      prefersReducedMotion(): boolean + onReducedMotionChange(cb) over one matchMedia; consumers: diagram/ghost.ts, main.ts (which starts/stops the diagram and the fragments)
    features/
      copy-button.ts         mountCopyButton(button, clipboard): clipboard write, 1.6 s "copied to clipboard" swap, border flash (the hero CTA; two files, so flat)
      copy-button.test.ts    the label/timer logic with vi.useFakeTimers() and a fake clipboard
      diagram/
        seats.ts             the per-seat config table (grid, period, frequency, phase, glitch, palette) — data only
        silhouette.ts        inside(u, v), eye(u, v), pupilCell(seat, side, gaze?) — pure
        silhouette.test.ts
        density.ts           Pose type; density(seat, c, r, t, pose?) with breath, weave, edge falloff (lifted by pose), noise, clamp, halo; glitchRow(seat, t, jolt?) — pure
        density.test.ts
        atlas.ts             GLYPHS ramp, glyphFor(d), tintFor(seat, d, hash), buildAtlas(seat, dpr) → offscreen canvas it creates itself (alpha per glyph index baked into the tiles)
        atlas.test.ts        ramp bounds + tint bands (pure parts)
        ghost.ts             createGhost({ canvas, seat, pose? }) → { renderFrame(t, pose?), start(), stop() }: fixed-tick loop on ticker.ts, reduced-motion frame 0, the pose read every tick
        scatter.ts           seeded band + trails + halo glyph placement per tier (Field.tier: wide · compact) → DocumentFragment
        scatter.test.ts      determinism + exclusion of ghost interiors and keep-clear boxes + region membership on both stages
        rain.ts              rainPoints(tickXs): the two seeded rain columns (§4) — reuses scatter's point type and fragment
        rain.test.ts         per-column lit cap, taper, glyph set, determinism
        route-geometry.ts    Point, Polyline, length(points), pointAt(points, distance), parsePath(d) ← the SVG route's d, extend(points, by), offsetPath(points) → the CSS path() string — pure
        route-geometry.test.ts
        timeline.ts          SPEED 55 px/s · DWELL 0.6 s · REST 1.3 s · HANDOFF per tier (wide 0, compact one DWELL), and schedule(distances, tier) → Timeline { period, handoff, card, cues (per seat: at · settle · jolt · gain), loop (the infinite WAAPI options), frame(t, style) } — the loop's length derived from the drawn routes, pure
        timeline.test.ts     the 760×860 distances give 0.71 / 1.31 / 2.69 / 8.71 / 10.0 s; the compact stage holds the brief in the implementer for a dwell before the reviewer leg (2.59 / 4.38 / 5.68 s)
        pose.ts              poseAt({ cue, t, period, box, seen }) → Pose (gaze · lift · gain · jolt): a seat's reaction to the packet at time t — pure
        pose.test.ts
        response.ts          reactions(stage, timeline): the keyframes the diagram answers the packet with — the card's outline and the implementer/reviewer tick and seat flashes — each cue-relative and started with a `delay` of its cue, so the reviewer's flash crosses the loop seam
        packet.ts            mountPackets(stage, routes, ghosts): parses the visible route-lines SVG, schedules the timeline from its lengths, rides the two packets and their wakes on offset-path over those polylines, the clock, pose(name) for the ghosts, start/stop
        ticker.ts            createTicker(target, rate, onTick): fixed-tick rAF loop with IntersectionObserver + visibilitychange pause — shared by ghost.ts (cells) and mount.ts (rain); both consumers are this feature, so it stays here rather than in lib/
        find.ts              find(root, selector): querySelector-or-throw, shared by mount, packet and response
        mount.ts             mountDiagram(root): wires seats → canvases and builds the scene — the visible route-lines tier, packets, scatter, rain drift — rebuilding it when the stage's layout width changes; --fit = min(1, box / stage); the only DOM entry for the feature
      fragments/
        pool.ts              the §7.5 string pool — data only
        mount.ts             mountFragments(layer, keepClear): placeFragments(viewport, { text, marks }) with the travel-band test (seeded via lib/noise, 2000 candidates per string in 4 height stripes, 60 px between origins, text boxes grown to a 24 px clearance, every travel band on screen), the hover animation, re-placement on resize (one queued rAF)
        mount.test.ts        all 20 placed on screen, clear of the header band, every exclusion, every text line and each other (the headline is a DOMRect-like object whose sides are prototype getters, so a `{ ...rect }` spread in the placer fails it); same seed + same viewport → identical placements
  tests/
    e2e/
      page.e2e.ts            one h1 · lang · title/description · every canvas labelled · every a[href] · five tbody rows · document.fonts.check for the three families · zero console errors · scrollWidth === clientWidth at 390 and 360 · at 390: the stage is 350×980 with the compact route-lines shown and the three canvases on one x, and the nav links, CTA and footer link are ≥ 44 px tall with a 2px solid :focus-visible ring after Tab · --ink-3 vs #101216 ≥ 4.5 (computed from the live tokens) · gzipped JS: own ≤ 12 KB, own + vendor ≤ 90 KB
      motion.e2e.ts          `test.describe` with `test.use({ reducedMotion: 'reduce' })`: two diagram captures 4 s apart are pixel-identical; default: they differ; CSS layer seeked via the Web Animations API — `page.evaluate(t => document.getAnimations().forEach(a => { a.pause(); a.currentTime = t; }), 2000)` — then the .packet--impl box intersects route B's path box, at 1440 and, on the vertical route, at 390; ghost loops advanced with page.clock (installed and paused one step ahead before goto, so every tick happens inside runFor); 19 fragments at 1440×900 (seed 8088, §7.5), and none intersects the header band or the h1/lede/CTA boxes when sampled at 0, 10, 20, 30 s of animation time; at 390 the hero carries no fragments
      extension.e2e.ts       §15: the sheet (the card's header, cropped at 707 / 689 on a heading, two statements), the transcript (13 rows, ✗ → retry → ✓ in DOM order, the phone tails), the record (tree with six notes, two $ lines, the measurement line), zero animations in all three, the three claims on the routes claim's x
      shots.e2e.ts           writes SHOT_DIR/<SHOT_TAG>-1440.png (full page), -fold.png (1440×900), -390.png (full page, 390×844); SHOT_TIME_MS seeks every CSS animation via getAnimations() and advances page.clock for the ghosts before capture (default 4000); the clock is installed and paused before goto and finished entrances drop to `animation: none`, so two captures of one build are byte-identical
  tools/
    render-static.ts         prints frame-0 character art per seat (imports the pure diagram modules); run once via `npm run render-static`, output pasted into the three <pre class="ghost-fallback">
```

**v2 delta (2026-09-09) — what the tree above loses and gains.** The tree is kept as the v1 record; the lines below override it.

```
REMOVED
  src/styles/routes.css · manifesto.css · ladder.css · record.css · brief-sheet.css   (§8, §9, §15)
  tests/e2e/extension.e2e.ts                                                          (§15's assertions)
  the routes/manifesto/ladder/record/brief-sheet markup in index.html and the v1 footer
ADDED  (one stylesheet per section, one feature for the aura; every file ≤ 200 lines)
  src/styles/lower.css        the .lower wrapper, .grid, the header grammar (.eyebrow .title .sub .tick-list), .notes, .marg, .marg-list, .steps — shared by 02/03/04   §16
  src/styles/panel.css        the document surface: .panel, .bar, .lines, the editor and terminal roles                                                              §16.1–16.2
  src/styles/s02.css          section 02's placement and tiers                                                                                                        §16.1
  src/styles/s03.css          section 03's placement and tiers                                                                                                        §16.2
  src/styles/s04.css          section 04: .breath .lead .callouts .callout .creed .tree, placement and tiers                                                          §16.3
  src/styles/aura.css         .aura, .dots, .frag, .stop, .rail, .spark (+ the @supports and reduced-motion guards)                                                   §16.5–16.6
  src/styles/reveal.css       the pre-reveal states and the .is-in animations for the lower page — enter · tick · draw · flare (motion.css keeps the hero's)          §16.6
  src/features/aura/pool.ts   the per-section fragment pools, the nine traces included (data only)                                                                    §16.5
  src/features/aura/dots.ts   dotField(canvas, spec) — the seeded cell field, pure draw; dots.test.ts for determinism and the lit-share caps                           §16.5
  src/features/aura/layer.ts  mountAura(section, pool, keepClear): fragments (through fragments/mount's placer with a pool argument) + dot fields + shimmer ticker     §16.5
  src/features/aura/reveal.ts mountReveal(sections): one IntersectionObserver, adds .is-in once, unobserves                                                            §16.6
  tests/e2e/lower.e2e.ts      §16.9's assertions
CHANGED
  src/features/fragments/mount.ts   placeFragments(viewport, keepClear, pool = POOL) and mountFragments(layer, keepClear, pool?) — the hero keeps POOL and seed 8088
  src/main.ts                       mounts the three aura layers and the reveal observer after the hero's features; the keep-clear measure is per section (§16.5)
  src/styles/main.css               imports lower · panel · s02 · s03 · s04 · aura in layer(sections), reveal in layer(motion), after the hero's sheets
  src/styles/tokens.css             the §1 v2 tokens and the two tier blocks
  index.html                        the §2 v2 skeleton; the exemption from the 200-line cap now covers the two panels' 57 rows (each with its inline --row), the tree's nine span.row lines and the ghost art — its remaining markup stays ≤ 220 by the T-011 awk count
  tests/e2e/shots.e2e.ts            the 1920 captures (§14) and the scroll-through in settle()
JS budget   own code ≤ 16 KB gzipped (was 12 — the aura feature is ≈ 3–4 KB), total ≤ 90 KB; no library
```

Rules: kebab-case; basename = primary export and never repeats a path segment (the folder carries the feature name — `diagram/mount.ts`, never `diagram/diagram.ts`); a unit earns a folder at 3+ files, otherwise flat siblings; no `utils`/`helpers`/`common`; no `index.ts` barrels; tests colocated; ≤ 200 lines per file (split per responsibility as above, never by line count alone; `index.html` is exempt for the 65 generated lines of `<pre class="ghost-fallback">` art and for the §15 artefacts' bodies — the sheet `<pre>`, the transcript `<ol>`, the tree `<pre>`: data typed into the page, ≈ 70 lines — and its remaining markup, the awk count in T-011, stays under **220**: the three §15 skeletons add ≈ 54 lines of real markup to today's 143, so 200 would leave no room for the seat spans and the marginalia); features never import each other (`copy-button`, `diagram`, `fragments` import only from `lib/` and their own folder; `main.ts` composes); stylesheets live in the shared `src/styles/` tier, one per section. `dist/` and `node_modules/` are gitignored by the repo root already (`dist/`, `node_modules/` patterns are unanchored).

**v2 build, 2026-09-09 — what the delta above says and what was actually built.** The tree and the delta stand as the design record; these lines are the built state, one per deviation.

- **One class, `.fragment`, for every hovering string** (D-4) — the hero's existing name. §16's `.frag`, `.frag--gutter` and `.frag--glyph` were NOT created: the placer decides positions per tier on every resize, so CSS needs no tier classes. Every §16.5 and §16.9 reference to `.frag` reads `.fragment` in the code and in the tests.
- **The pools and layer specs live in `src/features/fragments/pool.ts`** (D-5), not `src/features/aura/pool.ts`. A pool is placer data, and `aura/` must not import `fragments/`. `POOL` keeps its name; there is no `POOL_HERO`.
- **`ticker.ts` was promoted to `src/lib/ticker.ts`** (D-6) on its second consumer, the dot shimmer, with the two import edits in `diagram/ghost.ts` and `diagram/mount.ts` approved out of bounds in the same brief.
- **`src/features/aura/layer.ts` was NOT created** (D-7). The pure placer is `src/features/fragments/place.ts` (`mount.ts` keeps the DOM half; `mount.test.ts` became `place.test.ts`, which pins all 20 hero origins with `toEqual`), `sectionKeepClear` is `src/features/aura/keep-clear.ts`, and `src/main.ts` composes the three section layers and the hero from the one placer.
- **`.rule--bleed` lives in `footer.css`** (D-15) — it is the footer's seam — and `footer.css` landed in P1 with the shell, not P4.
- **The lower page's e2e is eight files, not one `lower.e2e.ts`** (D-19): `lower`, `s02`, `s03`, `s04`, `aura`, `scroll`, `tiers`, plus `frames` for the capture harness. §16.9's ten items do not fit 200 lines in one file. With v1's `page`, `motion` and `shots` the directory holds eleven.
- **Two stylesheets were split at their media queries to stay inside the 200-line cap**, both by ruling and both on the same precedent: `lower.css` + `lower-tiers.css` (P1a) and `s04.css` + `s04-tiers.css` (the sections run; the After contract measured 233 lines through Biome as one file, 133 + 100 as two). `main.css` therefore imports 19 sheets, not 17.
- **`main.css`'s built order** is tokens · base · nav · hero · cta · diagram · brief · diagram-compact · lower · lower-tiers · panel · s02 · s03 · s04 · s04-tiers · aura · footer · motion · reveal. The order §13 requires is unchanged; only the two splits are new.
- **The capture set is ten PNGs and two JSON per tag** (D-11) — see §14.
- **Per-file line caps raised against a Biome measurement, never to fit code that was already written**: `reveal.css` 120 → 130 (measured 129), `aura.css` 120 → 126 (measured 124), `tests/e2e/aura.e2e.ts` 200 → 220 (measured 215), `src/features/fragments/pool.ts` 125 → 130 (measured 127). Biome sets CSS one declaration per line and one grouped selector per line, so a cap estimated before the formatter is systematically low. A cap that cannot be met does not fail — it deforms the code until it is met, which is how one earlier brief came back with its pools aliased to single letters.

## 14. Verification protocol (every phase)

1. `cd website && SHOT_DIR=<run_dir>/shots SHOT_TAG=p<N> npm run shots` → **five** PNGs since v2 (2026-09-09): `<tag>-1440.png` (full page, 1440×900 viewport), `<tag>-fold.png` (1440×900), `<tag>-390.png` (full page, 390×844), **`<tag>-1920.png`** (full page, 1920×1080 viewport) and **`<tag>-1920-fold.png`** (1920×1080, scroll 0). The two new tests call `page.setViewportSize({ width: 1920, height: 1080 })` before `settle()` — the Playwright project stays at 1440×900, exactly as the 390 test already overrides it — and need no new env: `SHOT_DIR`, `SHOT_TAG` and `SHOT_TIME_MS` are the whole interface. `settle()` gains one step before the clock runs: it scrolls the document to the bottom in `innerHeight` steps with a `requestAnimationFrame` between them and back to 0, so every one-shot reveal (§16.6) has fired and the full-page captures show the lower page at its final frame; the reduced-motion project needs no scroll (nothing is hidden there). Playwright builds + previews first; `SHOT_TIME_MS` seeks CSS animations through `getAnimations()` and the ghost loops through `page.clock`. Per-section fold proofs for ruling 5 (`<tag>-1920-s02/-s03/-s04.png`, a 1920×1080 viewport scrolled to each section's top) are made by the critic with the same harness when a phase touches that section (§16.9).
2. Open each PNG and `reference.png` side by side. Write the defect list **before** touching code: geometry drift (positions vs §4 table), type scale, ink levels, alignment, anything accidental. Name the three worst things first.
3. Fix, re-shoot, re-critique. A phase is done at zero open items on its own surface — not "renders".
4. Gates: `npm run typecheck && npm run lint && npm test && npm run e2e` in `website/` — all green, outputs pasted verbatim.
5. Hand back with: the screenshot paths, the final critique, `craft pass: …` line, and the tell-walk (nuke-design §Verification) result with a 1–10 score anchored to the nuke-design sentences.

**v2 build, 2026-09-09: the capture set is ten PNGs and two JSON per tag** (D-11), not the five named in item 1. `SHOT_DIR=<dir> SHOT_TAG=<tag> npm run shots` writes `<tag>-1440.png`, `-fold.png`, `-390.png`, `-1920.png`, `-1920-fold.png`, `-1024.png`, `-768.png`, `-1920-s02.png`, `-1920-s03.png`, `-1920-s04.png` (a section that does not exist yet skips its fold) and `<tag>-hero-1440.json`, `<tag>-hero-1920.json` — the hero's boxes as a stable ordered list, which is what the hero-stability check diffs. `SHOT_DIR`, `SHOT_TAG` and `SHOT_TIME_MS` remain the whole interface. The per-section folds are mechanical, not hand-made by the critic. The spark and transcript frames come from a second command, `SHOT_DIR=<dir> SHOT_TAG=<tag> npx playwright test tests/e2e/frames.e2e.ts` → `<tag>-spark-{25,50,75,100}-{1440,1920}.png` and `<tag>-s03-{500,1200,2400}.png`. Rejected: the critic hand-making the section folds (not mechanical) and pixel-diffing the hero against `current-1440.png` (the steps' label widths re-place the seeded fragments, so bytes cannot match; boxes can).

**v2 build, 2026-09-10 — WARNING: the capture tests assert nothing, so no gate can catch a wrong capture.** `tests/e2e/shots.e2e.ts` and `tests/e2e/frames.e2e.ts` exist to WRITE FILES. Both pass whatever they capture. Eleven passing capture tests are not eleven correct captures, and this build shipped a wrong one for two phases: the transcript frames scrolled to `.s03`'s `offsetTop` — 557, measured from the positioned `.lower`, against a document top of 1697 — with a smooth scroll under a paused clock that left `scrollY` at 80, so every `<tag>-s03-*.png` in the P5 and P6 sets showed the hero instead of the terminal. Nothing in the gates could ever have found it. **Whoever owns a phase opens every PNG that phase produces, at least once.** That is what §16.9's tell-walk and the repo's look-and-logic rule are for, and it is the only instrument that covers this.

**v2 build, 2026-09-10 — the second way a check looks green and is not: a hung suite.** The gate for this build's last batch printed sixty test lines, forty-eight of them with a `✓`, recorded `Tests 55 passed (55)` for the unit suite, and then never terminated — an idle worker at 0 % CPU and thirty-two orphaned Chrome processes, no summary line. Read as "everything that reported, passed", it looks like a pass. It was not: a genuine `fragments keep clear` failure was among the twelve results that never printed, and only killing the run and re-running it per file surfaced it. **A suite that does not print its summary has not passed.** Sample the worker (`sample <pid>`) to tell a stall from a spin, then bisect by test file under an alarm; never treat partial output as a result.

**v2 build, 2026-09-09: the hero-stability check is two-fold** (D-12) — `diff` of `<tag>-hero-1440.json` against `p0`'s (from P1 on) and a byte-identical crop of the hero region against `p1`'s (from P2 on; PIL `ImageChops.difference(...).getbbox()` is `None`; 1440 top 1116 px, 1920 top 1140 px). At 1920 the hero JSON legitimately differs from `p0` and it is not a regression: the content box moves x 304 → 264 and w 1312 → 1392 under the `min-width: 1600px` tier, which `p0` predates. HERO-JSON is defined at 1440, where every diff through the build was empty.

## 15. Extension — the brief, the ladder, the record (T-010, 2026-09-08; revised after the Phase 7 reviews)

SUPERSEDED by §16 on 2026-09-09; the sections it described were removed.

## 16. The v2 lower page (2026-09-09) — 02 · 03 · 04 · footer · aura · scroll

Designed from the owner's `reference-v2.png` (authoritative for 02, 03 and the footer) and the prompt's rulings (run dir `.nuke/2026-09-09-114149-creative-website-v2/`, `design-notes.md` there holds the measurements, the directions and the critique log; `comps/lower-page.html` renders every section below at rest, `comps/lower-geometry.json` holds every box quoted here, and `comps/visual-diff-comp.md` is the ruling-8 table of the comp against the reference — fix cycle 1, 2026-09-09, after the stage-1 critique). Every string is verbatim and collected once more in §16.10; every class is the §2 v2 skeleton's; every token is §1's. **Coordinates:** x is the viewport x at 1440 (content box 64 → 1376; 04's twelve columns: column n starts at `64 + (n − 1) × 111.33`, columns 87.33 wide, gaps 24) and, after a slash, at 1920 (content 264 → 1656, column n starts at `264 + (n − 1) × 118`, columns 94 wide); 02 and 03 are not on the twelve columns — their row is head `--head-w` · panel `--panel-w` · notes (the shared grammar below): x 64 / 398 / 916 at 1440 and 264 / 618 / 1159 at 1920; y is measured from the section's top border. Rounded to the pixel; ± 1 is the tolerance for the comp's own numbers, and against the reference §16.9's ±3 % applies.

**Shared grammar (02, 03, 04).** Each section is a full-width `section` (`position: relative`) holding `div.aura` (§16.5) and `div.grid` (`position: relative; z-index: 1`, `width: min(var(--content-max), 100% - 2 * var(--gutter))`, `margin-inline: auto`, `column-gap: var(--s-6)`). 04's grid is the §1 twelve columns; **02's and 03's grid is the reference's three-column row**: `grid-template-columns: var(--head-w) var(--panel-w) minmax(0, 1fr)` — head 310 · panel 494 · notes 460 at 1440 (x 64 / 398 / 916), head 330 · panel 517 · notes 497 at 1920 (x 264 / 618 / 1159); below 1360 the tier rules (§16.7) put the same three cells back on twelve columns. 02 and 03 carry `border-top: 1px solid var(--hair)` (full-bleed — the reference's seams) and `padding-block: var(--s-6)` (24 — the reference's 02 panel sits 33 px under its seam and its 03 panel 24 px; one grammar, 25 px, fits both within tolerance); 04 has no rule and `padding-block: var(--s-32) var(--s-24)`. The **head** (`div.head`, `grid-column: 1` — `1 / 4` in 04 —, `padding-top: var(--s-2)`) is one `h2.title` (`--text-title`, line-height 1.25, weight 500, tracking `.02em`, uppercase, `--ink`) holding two spans: `span.eyebrow.dash` with `aria-hidden="true"` (`display: block; margin-bottom: var(--s-4)`, `--text-label`, weight 400, line-height 1.45, tracking `.08em`, `--ink-3`, tabular numerals: `02` / `03` / `04`; its `.dash` is §1's 18 × 1 `--ink-3` line at `margin-top --s-4`) and `span.line` with the title text (`display: block`; typed `Before any code`, so the heading's accessible name is the title, never the number), then `p.sub.dash` (`--text-serif-sub`, `margin-top --s-2`, each line a `span.line` at `display: block; white-space: nowrap`), and in 02 a `p.tick-list` (`margin-top --s-12`; a `::before` tick 1 × 12 `--ink-3` with `margin-bottom --s-1`, then `--text-micro` uppercase lines at 16 px pitch, tracking `.08em`, `--ink-3`). The **panel** (`figure.panel`, `grid-column: 2`, `align-self: start`): the document surface (§1 v2) — `background: var(--panel); border: 1px solid var(--hair); box-shadow: var(--highlight)`, `--text-art` at `--line-art` (11 / 13 at 1440, 12 / 14 at 1920, 11 / 15 below 768), `--ink-2`, tracking 0, no uppercase. Its `figcaption.bar`: `display: flex; align-items: center; justify-content: space-between; height: var(--bar-h); padding-inline: var(--s-3); border-bottom: 1px solid var(--hair)` (the height includes that border: 30 / 32 / 28); left `span.name` (weight 500, `--ink-2`, `--text-art`, a `::before` disc 7 × 7, `border-radius: 50%` — the one rounded thing on the page, a status light — `--ink-2` for the editor, `--green` for the terminal, gap `--s-2`); right `span.status` (two spans, `--text-micro`, `--ink-3`, gap `--s-4`, tabular). Its `ol.lines` (`position: relative; padding-block: var(--s-1)` — 4 px, the reference's 3 ref px): each `li` carries its index inline, `style="--row: n"` (1–36 / 1–21; §16.6 reads it), and is `display: grid; grid-template-columns: calc(2ch + var(--s-2)) 1fr` — `span.n` the line number, right-aligned, `padding-inline --s-1`, `--ink-4`, tabular; `span.t` the text, `padding-left --s-1; padding-right --s-3; white-space: pre-wrap; overflow-wrap: anywhere` (word wrap on, as an editor's; at 1440 and 1920 no row wraps — asserted). The gutter rule is one element, `ol.lines::before` (`position: absolute; top: 0; bottom: 0; left: calc(2ch + var(--s-2)); width: 1px; background: var(--hair)`), so it runs through the padding rows and can be drawn in §16.6; `.n` has no border. Text starts 26 px inside the panel's left border at 1440 (1 + 13.2 + 8 + 4; the reference's 23) and 27 at 1920; the text column is 456 px (69 characters at 11 px) at 1440 and 478 px (66 at 12 px) at 1920, so the 65-character rows hold with 4 / 1 characters to spare. The **notes** (`div.notes`, `grid-column: 3`, `position: relative`, `padding-top --s-2`, `--text-note`, line-height 1.6, `--ink-2`): paragraphs at `max-width: calc(41ch + 1px)` — 320.8 px at 1440, 345.4 at 1920; the reference's measure is 41 characters (249 ref px = 320 px), and a 41-character line measures exactly 41ch, which Chrome's subpixel line breaker refuses, so the extra pixel is what makes the rag the reference's — `p + p { margin-top: var(--s-6) }`, `b` = `--ink` (weight inherited); `p.caps` (`margin-top --s-6`, `--text-note` size, weight 500, line-height 18 px, tracking `.08em`, uppercase, `--ink`; `.caps + p { margin-top: var(--s-8) }`); in 03 alone `.s03 .notes p + p { margin-top: var(--s-2) }` and `.s03 .notes .steps { margin-top: var(--s-2) }` — the reference stacks that column with a third of a line between blocks. **Marginalia** in the notes cell, all `--text-micro`, line-height 16 px, `white-space: nowrap`, `position: absolute`: `span.marg` (`aria-hidden="true"` — decorative, `--ink-4` at 2.2:1 on the canvas as the reference sets them dimmer still; `text-align: right`, tracking 0; `right: 0` for the `//` notes, so they end at the content edge as the reference's do; `.marg-b`, the two- and three-line whispers, `right: var(--s-6)` — 24 px inside the edge, where the reference's blocks end; `.marg--caps` adds `text-transform: uppercase; letter-spacing: .08em` for 03's whisper, which the reference sets in caps) and `p.marg-list` (`left: calc(100% - var(--rail) + var(--s-2))` — 8 px right of the rail — uppercase tracked `.08em` at 16 px pitch, `--ink-3`, a `::before` tick 1 × 12 `--ink-3` at `left: calc(-1 * var(--s-2) - 1px); top: -16px` — it sits *on* the rail — and an `::after` 18 × 1 `--ink-3` dash at `margin-top --s-4`). A whisper right-aligned to the content edge crosses the rail's x (the reference's whispers cross its list column the same way); the rail is `--ink-4` dashes under `--ink-4` text, so `.marg` carries a glyph-hugging knock-out — `text-shadow: 1px 0 var(--bg), -1px 0 var(--bg), 0 1px var(--bg), 0 -1px var(--bg)` (four 1 px offsets, no blur) — that blanks the dashes under the letters only; a `--bg` box behind the whisper was rejected because it shows against the vignette, and a 1 px halo does not (fix cycle 1b). Their `top` values are pinned per tier in the tables below (1440 / 1920) and derived from their anchors, so a copy change re-derives them. The **rail** x is the content box's right edge − `--rail`: **1296 / 1576** (measured 1295 / 1575 — the 1 px line's left edge); the marg-lists start at 1304 / 1584 and end ≥ 19 px inside the content edge (`EVIDENCE`, the longest, is 53 px). Each section's aura also holds one **rail stop**, `span.stop` (§16.5 item 5), at its artefact's top.

### 16.1 Section 02 — `section.s02#briefs` — BEFORE ANY CODE — dense

Height **557 / 595** (border 1 + padding 24 + panel 508 / 546 + padding 24; the reference is 437 ref px = 561 at 1440). The panel is the section's height; the notes end 71 px above its bottom at 1440 (their last dash at 462, the panel's bottom at 533) and 93 px above at 1920 (478 vs 571).

| element | x · y · w · h at 1440 | at 1920 | spec |
|---|---|---|---|
| `span.eyebrow.dash` (in the `h2`) | 64 · 33 · 310 · 17 (+ dash at 66) | 264 · 33 · 330 | `02` |
| `h2.title > span.line` | 64 · 83 · 310 · 30 (text 223 wide) | 264 · 83 · 330 · 30 | `BEFORE ANY CODE` (typed `Before any code`, `text-transform: uppercase`); 24 px at both tiers |
| `p.sub.dash` | 64 · 121 · 310 · 91 (+ dash) | 264 · 121 · 330 · 94 | `ONE FILE.` / `ONE BRIEF.` / `NOTHING ELSE.` — `--ink-3`; 28 / 29 px |
| `p.tick-list` | 64 · 277 · 310 · 80 (tick 12 + 4 + 4 × 16) | 264 · 280 | `BRIEFS` / `CONTRACTS` / `CONTEXT` / `CLARITY` — four `span.line`s separated by `<br>` (the phone tier turns the `br` into ` · `) |
| `figure.panel.editor` | 398 · 25 · 494 · 508 | 618 · 25 · 517 · 546 | 1 + bar 30 / 32 + 4 + 36 × 13 / 14 + 4 + 1 |
| `div.notes` | 916 · 25 · 460 · 508 | 1159 · 25 · 497 · 546 | |
| statement 1 `p.dash` | 916 · 33 · 321 · 121 (5 lines + dash) | 1159 · 33 · 345 · 129 | text below; `planner`, `implementer`, `only`, `brief` in `<b>` |
| `p.caps.dash` 1 | 916 · 178 · 321 · 71 (3 × 18 + dash) | 1159 · 186 | `ONE FILE.` / `ONE BRIEF.` / `NOTHING ELSE.` — `display: none` below 768 (Correction X, 2026-09-10): at desktop this and the head's serif statement sit in two columns of one spread and read as an echo, which is the reference's own pairing; at 390 the three columns collapse into one, the two land ~700 px apart in it, and the echo reads as a duplicated paragraph. The serif statement is the one that stays. |
| statement 2 `p` | 916 · 281 · 321 · 104 (5 lines) | 1159 · 289 · 345 · 112 | `splitbrief` in `<b>` |
| `p.caps.dash` 2 | 916 · 409 · 321 · 53 | 1159 · 425 | `CLEAR INPUTS.` / `SAFER OUTPUTS.` |
| `span.marg.marg-a` | right edge 1376, `top: 96px` (notes-relative; = statement 1's fifth line box, 8 + 4 × 20.8, + 4 — the reference sets it level with that line) → y 121 | to 1656, `top: 104px` → y 129 | `// contracts over context` |
| `p.marg-list` | 1304 · `top: 169px` (= caps 1 top + 16; its tick at 153 lands on `ONE FILE.`'s top line) → y 194 · 52 · 97 | 1584 · `top: 177px` → y 202 | `SPEC` / `PLAN` / `BRIEFS` / `EXECUTE` / `REVIEW` |
| `span.marg.marg-b` | right edge 1352, `top: 422px` (= caps 2's text bottom + 2, beside its dash row) → y 447 | to 1632, `top: 438px` → y 463 | `less context` / `more progress.` (two lines, `<br>`) |
| `span.stop` (in `.aura`) | 1304 · `top: 17px` (16 px box centred on the panel's top edge, 25) | 1584 · 17 | `§02` — the tick crosses the rail at y 25 |

**Editor panel.** Bar: `tasks.md` · `Ln 1, Col 1` · `markdown`. Rows 1–36, numbered `1`–`36` (no leading zero — an editor's gutter), each `li` with `style="--row: n"`; roles: `.h` headings `--blue` weight 500; `.k` keys `--ink-3` (the value after the key stays `--ink-2`); `.f` fence lines `--ink-3` (`--ink-4` reads as a smudge at 2:1); `.m` list markers (`-`, `1.`, `2.`, `3.`) `--ink-3`; everything else `--ink-2`, including the signature line 15. Blank rows are `<span class="t"></span>` with their number. Rows 33 (65 characters) and 15 (61) are the longest; both hold on one line at both tiers (measured: no `.t` wraps at 1440 or 1920). Row 1 sits at y 60 and row 36 at 515 (1920: 62 / 552) — the pitch the reference sets (10.11 ref px = 12.98 at 1440).

```
 1  .h  # TASK BRIEF
 2
 3  .k  id: T002                         (key `id:` .k, value --ink-2 — every frontmatter row the same)
 4  .k  title: Add the auth guard
 5  .k  action: create
 6  .k  file: src/auth/guard.ts
 7  .k  depends_on: [T001]
 8
 9  .h  ## Description
10      Guard a route with the JWT helper from T001. Reject a missing
11      or expired token before the handler runs.
12
13  .h  ## Signature
14  .f  ```typescript
15      export function requireAuth(req: Request): Claims | AuthError
16  .f  ```
17
18  .h  ## Implementation Steps
19  .m  1. Read the bearer token from the Authorization header
20  .m  2. Verify it with verifyToken from src/auth/jwt.ts
21  .m  3. Return the claims, or an AuthError naming the reason
22
23  .h  ## Tests
24  .m  - requireAuth returns the claims for a valid token
25  .m  - requireAuth rejects a missing token
26  .m  - requireAuth rejects an expired token
27
28  .h  ## Scope
29  .m  - In bounds: src/auth/guard.ts
30  .m  - Out of bounds: src/auth/jwt.ts — do not change the helper
31
32  .h  ## Escalation
33  .m  - Stop and ask if verifyToken's signature differs from this brief
34
35  .h  ## Evidence
36  .m  - Include test output and typecheck logs in the task result
```

(`.m` marks the marker only — `1.`, `2.`, `3.`, `-` — the rest of the row is `--ink-2`.) The reference's `typeercipt` and `bounds::` are typos, corrected (recorded deviations, product-true).

**Notes copy.** Statement 1: `The planner writes one brief per file. The implementer sees only its own brief — not the spec, not the plan, not the other tasks — and runs it in fresh context, with no memory of the last one.` (the reference's rag, reproduced at 41ch + 1px: `The planner writes one brief per file.` / `The implementer sees only its own brief —` / `not the spec, not the plan, not the other` / `tasks — and runs it in fresh context,` / `with no memory of the last one.`). Statement 2 (rewritten 2026-09-09 by ruling — the reference's `validates every task upfront — typecheck, lint, test` contradicted 03, where the code is validated after every task; a brief has no code to typecheck): `A brief that fails its checks never reaches an implementer. splitbrief validates every brief upfront — scope, file, dependencies, signature — and stops at the first domain or contract error.` (rag: `A brief that fails its checks never` / `reaches an implementer. splitbrief` / `validates every brief upfront — scope,` / `file, dependencies, signature — and stops` / `at the first domain or contract error.` — five lines, as the reference's). The `—` dashes are glued to the word before them with `&nbsp;` so no line opens on a dash.

**390 (350 px column).** Source order, single column: head (eyebrow 33, title 83 at 20px, sub 116 at 24px, tick-list 235 as one line `BRIEFS · CONTRACTS · CONTEXT · CLARITY` — `.tick-list br { display: none }`, `.tick-list br + .line::before { content: " · "; color: var(--ink-4) }`), panel at 291 (`margin-top --s-6`; 11px / 15px rows; bar 28 with `padding-inline --s-2` and `markdown` hidden — `.bar .status span:last-child { display: none }`; wrapped rows hang: `.lines .t { text-indent: -2ch; padding-left: calc(var(--s-1) + 2ch) }`, so a continuation line starts under the first line's third character; eight rows wrap once and the panel is 713 tall), notes at 1 036 (`margin-top --s-8`, `padding-top: 0`, paragraphs at `max-width: none`, 334 tall — the first `p.caps` block is `display: none` below 768, Correction X). Marginalia, the stop and the rail: `display: none` below 1360 (§16.7). Section 1 418 tall (1 513 before Correction X; the notes' top stays at 1 036 because the hidden block sits inside them).

### 16.2 Section 03 — `section.s03#validation` — AFTER EVERY TASK — dense, the proof

Height **388 / 404** (1 + 24 + notes 339 / 355 + 24). The notes run 26 px past the panel at 1440 (their dash at 364 vs the panel's bottom 338) and 19 px at 1920 (380 vs 361); the notes decide the height. The reference's 03 column is set in type 7 % smaller than its 02 column (5.66 vs 6.07 ref px per character in the same 249 px measure), which is how its statement 2 holds 44 characters a line; one `--text-note` sets it in six lines, so the steps sit one line (21 px) lower than the reference's — recorded, and the only 03 row outside ruling 8's tolerance.

| element | 1440 | 1920 | spec |
|---|---|---|---|
| `span.eyebrow.dash` · `h2.title > span.line` · `p.sub.dash` | 64 · 33 / 83 / 121 (4 lines, 138 incl. dash) | 264 · 33 / 83 / 121 (142) | `03` · `AFTER EVERY TASK` · `VALIDATE.` / `RETRY.` / `ESCALATE.` / `KEEP THE EVIDENCE.` — the last line is 310 px at 28px and 321 at 29px: `nowrap`, and it must never wrap (§16.9) |
| `figure.panel.terminal` | 398 · 25 · 494 · 313 | 618 · 25 · 517 · 336 | 1 + bar + 4 + 21 rows + 4 + 1 |
| statement 1 `p` | 916 · 33 · 321 · 83 (4 lines) | 1159 · 33 · 345 · 90 | `splitbrief` in `<b>` |
| statement 2 `p` | 916 · 124 · 321 · 125 (`margin-top: var(--s-2)`; six lines) | 1159 · 131 · 345 · 134 | |
| `ol.steps.dash` | 916 · 257 · 460 · 107 (`margin-top: var(--s-2)`; 5 × 18 + dash) | 1159 · 273 | the hero's steps grammar (§4), inlined: each `li` is `display: flex`; the number is a `span` of `width: var(--s-8)` (32 px) in `--ink-3`; the label `span` carries `border-left: 1px solid var(--hair); padding-left: var(--s-3)` — one continuous rule because every row draws it — in `--ink-2`; `--text-label` uppercase tracked `.08em`, line-height 18 px, tabular numerals, `nowrap` |
| `span.marg.marg-a` | to 1376, `top: 73px` (= statement 1's fourth line box, 8 + 3 × 20.8, + 2.4 — the reference hangs it between its third and fourth lines; the fourth is 31 characters, so the note has 86 px of air) → y 98 | to 1656, `top: 78px` → y 103 | `// not its own judge` |
| `p.marg-list` | 1304 · `top: 115px` (= statement 2 top + 16) → y 140 | 1584 · `top: 122px` → y 147 | `VALIDATE` / `RETRY` / `ESCALATE` / `EVIDENCE` / `IMPROVE` |
| `span.marg.marg-b.marg--caps` | to 1352, `top: 268px` (= steps top + 36, rows 3–5) → y 293 · 105 · 48 | to 1632, `top: 284px` → y 309 | `A SMALLER LOOP` / `A HIGHER BAR` / `REAL PROGRESS` (three lines, caps and tracked as the reference sets them; the round-1 comp's lowercase was a deviation and is gone) |
| `span.stop` | 1304 · `top: 17px` | 1584 · 17 | `T002` |

**Terminal panel.** Bar: `splitbrief run` (green light) · `task: T002` · `14:28:16`. Rows `01`–`21` (two digits — a log's numbering), each `li` with `style="--row: n"` and, on rows 13–21, `--beat: 1` (`ol.lines` carries `style="--beat: 0"` as the default, so rows 1–12 compute). Roles: `.mk` markers `◆ ▶ ○ │` in `--ink-3`; `.strong` `--ink` weight 500 (`T002 Add the auth guard`, `Task complete`); `.dim` `--ink-3` (paths, tails, `T002 (47s)`, `src/auth/guard.ts (create)`); `.ok` `--green` (`✓` × 5 and the `●` of row 20); `.pass` `--green` 500 (`PASS`); `.fail` `--red` (`✗`); `.err` `--red` 500 (`error`, `FAIL`); everything else `--ink-2`. The `[OpenCode CLI · DeepSeek]` tails are `span.tail.dim`, and below 768 `.tail { display: none }`. **The `▶` markers are `--ink-3`, not the reference's orange** (recorded): three warm `▶` plus `✗ error FAIL` would make six hot spots in one panel; the failure triplet is the panel's single hot spot and the greens its answer. Row 21's path is the product's session directory (recorded: `HOW-IT-WORKS.md` documents `.splitbrief/sessions/<id>/`; the reference's `runs/2025-03-08/T002/` is not a path the product writes): 65 characters, one line at both tiers. Two spaces before `│` on rows 06, 07, 15 and 21 (typed, `pre-wrap` keeps them); three spaces between a label and its detail on rows 03, 05, 14, 20; two between `✓ typecheck` and `✓ lint`.

```
01  ◆ Spec → Plan → Briefs → Build → Verify
02
03  ▶ T002 Add the auth guard   src/auth/guard.ts (create)
04
05  ▶ Implementer activity  1/2   [OpenCode CLI · DeepSeek]
06    │ Read   src/auth/jwt.ts
07    │ Write  src/auth/guard.ts
08
09  ✓ typecheck   ✓ lint   ✗ test
10  error   test/auth/guard.test.ts
11  FAIL    expected AuthError for expired token
12
13  ○ retry 2/3 — fixing implementation
14  ▶ Implementer activity  2/2   [OpenCode CLI · DeepSeek]
15    │ Update src/auth/guard.ts
16
17  ✓ typecheck   ✓ lint   ✓ test
18  PASS    3/3 tests passing (412ms)
19
20  ● Task complete   T002 (47s)
21    │ Evidence saved to .splitbrief/sessions/2026-03-08-auth-guard/
```

Row 01's `Spec → Plan → Briefs → Build → Verify` and the rail's `SPEC / PLAN / BRIEFS / EXECUTE / REVIEW` (02) are both the reference's strings and both stay (copy exact); the transcript speaks the CLI's phase names, the rail the page's.

**Notes copy.** Statement 1: `Correctness is not the implementer's to judge. After every task splitbrief runs typecheck, lint and test in your project and stops at the first failure.` (rag: `Correctness is not the implementer's to` / `judge. After every task splitbrief runs` / `typecheck, lint and test in your project` / `and stops at the first failure.`). Statement 2: `A failed task retries with the error attached, up to three times. Then it escalates: a mid‑tier model if configured, a hint from the planner, or a human review. Every run stores logs, results and evidence.` (`mid‑tier` with U+2011; rag: `A failed task retries with the error` / `attached, up to three times. Then it` / `escalates: a mid‑tier model if` / `configured, a hint from the planner, or a` / `human review. Every run stores logs,` / `results and evidence.`). Steps: `01 HIGHER QUALITY` · `02 LOWER SPEND` · `03 REAL EVIDENCE` · `04 YOUR TOOLS` · `05 YOUR RULES`.

**390.** Head (title 83, sub 116 — four lines at 24px, `KEEP THE EVIDENCE.` is 254 px), panel at 261 (383 tall; rows 03 and 21 wrap once with the hanging indent; tails hidden), notes at 676 (statements full width, `p + p` back to `--s-6` and the steps' `margin-top` to `--s-8` below 768 — the phone column has no reference to stack against; steps at 919, 107 tall). Section 1 074 tall.

### 16.3 Section 04 — `section.s04#control` — FOR THE WHOLE RUN — the breath

The colophon (direction recorded in `design-notes.md` §3): the page closes as it opened — a Didone statement with tiny mono annotations pinned around it — and the user's controls are typed in the config's own vocabulary. No rule above it (the 03 skyline and the rail cross the seam, §16.8); `padding-block: 128px 96px`. Height **819 / 858**. Three rows, two columns; the grid's rows are explicit. **One concept, one home (2026-09-09, fix cycle 1):** `TOOLS / MODELS / RULES` appear once as built objects — the three callout heads. The serif sub no longer repeats them (it states the outcome), the rail list names the controls' vocabulary instead, and the triple survives only as the stanza-gap whisper (`--ink-4`, decorative).

| element | 1440 | 1920 | spec |
|---|---|---|---|
| `div.head` (row 1, cols 1–3) | eyebrow 64 · 136 · dash; title 64 · 186 · 310 · 30; sub 64 · 224 · 91 + dash | title 264 · 186 · 30; sub 264 · 224 | `04` · `FOR THE WHOLE RUN` · `NOTHING RUNS` / `OUTSIDE` / `WHAT YOU SET.` — the head's own `padding-top --s-2` puts the eyebrow at 136 (128 + 8); each serif line holds inside the 310 px column at 28px and the 330 px column at 29px (`nowrap`) |
| `p.breath` (row 1, `grid-column: 6 / 13`, `align-self: start`) | 621 · 128 · 755 · 226 | 854 · 128 · 802 · 259 | `--font-display`, `--text-breath`, line-height 1.05, opsz 20; four `span.line`s: `YOU DESCRIBE` (`.muted` → `--ink-3`) / `THE GOAL.` (`--ink`) / `SPLITBRIEF HOLDS` (`.muted.line--stanza`, `margin-top --s-6`) / `THE REST.` (`--ink`). The verbs recede, the nouns stay — the hero's `ARE GOOD.` device turned around. `SPLITBRIEF HOLDS` is 476 px at 48px, 555 at 56px; the left edge is the hero's diagram column (col 6) |
| `p.lead.dash` (row 2, cols 1–4, `margin-top --s-12`) | 64 · 402 · 321 · 145 (6 lines + dash) | 264 · 435 · 345 · 151 | `--text-note`, `max-width: calc(41ch + 1px)`, `text-wrap: pretty` (it carries `it` down so the last line is `it stops.`, not an orphan); `splitbrief` in `<b>` |
| `div.callouts` (row 2, `grid-column: 6 / 13`, `display: grid; grid-template-columns: subgrid; align-items: start; margin-top --s-12`) | 621 · 402 · 755 · 145 | 854 · 435 · 802 · 151 | three `p.callout.dash`: `.callout--tools` cols 1–2 of the subgrid (621 · 402 · 199 · 109), `.callout--models` cols 3–4 (843 · 402 · 199 · 127), `.callout--rules` cols 5–7 (1066 · 402 · 310 · 145). **No stagger** — three ticks on one line; the unequal lengths (3 / 4 / 5 rows) carry the asymmetry |
| `p.callout` | | | the middle register (fix cycle 1): the block is `--text-note`, line-height 18 px, `--ink-2`, tracking 0; a `::before` tick 1 × 12 `--ink-3` (`margin-bottom --s-1`); `b` the head — `display: block`, `--text-label`, tracking `.08em`, `--ink`, line-height 18, `margin-bottom --s-1`; `span.kv` a `display: grid; grid-template-columns: 10ch 1fr; column-gap: var(--s-2); align-items: baseline` key/value grid whose keys (odd cells) are `--text-micro` `--ink-3` and whose values stay `--text-note` `--ink-2`; MODELS is four plain lines with `<br>` at the value size; closed by `.dash`. Heights 109 / 127 / 145 = 12 + 4 + 18 + 4 + rows × 18 + 17 |
| `p.creed.dash` (row 3, cols 1–4, `align-self: end`, `margin-top --s-8`) | 64 · 670 · 36 + dash (its last row is the tree's, 722) | 264 · 710 | `--text-note` size, weight 500, line-height 18, tracking `.08em`, uppercase, `--ink`: `CONTRACTS OVER CONTEXT.` / `SMALLER LOOPS. BETTER OUTPUT.` — bottom-aligned to the tree so the ground field (§16.5) sits between the lead's dash (545) and the creed (670): the lower-left is a paragraph, a relief of dots, then the creed standing on it |
| `pre.tree` (row 3, `grid-column: 6 / 11`, `margin-top --s-8`) | 621 · 578 · 533 · 144 (9 × 16) | 854 · 618 · 566 · 144 | `--text-art` at `line-height: var(--s-4)` (16 px at every tier — a tree is read, not scanned, so it takes the notes' air, not the panels' pitch), `white-space: pre`, `--ink-2`; every line is a `span.row` (`display: inline-block; vertical-align: top`, `style="--row: n"`, 1–9) with the source newline after it, so the rows reveal (§16.6) and the phone tier can drop a note under its file without a blank row; `span.root` (row 1) `--ink`; `span.note` (`// …`) `--ink-3`; the `//` starts at character 23 on every annotated line (`├── ` 4 + the file name padded to 18), so the notes form a column |
| `span.marg.marg-a` (grid-relative) | to 1376, `top: calc(2.1 * var(--text-breath) + 6px)` = 107 → y 235 · 251 wide | to 1656, 124 → y 252 | `// your tools, your models, your rules` — in the breath's stanza gap, right of `THE GOAL.` (which ends at 871) |
| `p.marg-list` (grid-relative) | 1304, `top: calc(2.1 * var(--text-breath) + var(--s-6) + 16px)` = 141 → y 269 (its tick level with `SPLITBRIEF HOLDS`'s top) | 1584, 158 → y 286 | `LIMITS` / `BUDGET` / `RETRIES` / `REVIEW` / `EVIDENCE` — the controls' vocabulary (`max_budget`, `max_retries`, the review seat, the ledger); none of the callout heads |
| `span.marg.marg-b` (grid-relative) | to 1352, `bottom: calc(2 * var(--s-4))` = 32 → y 658 (the tree's rows 6–7) | to 1632, → y 698 | `smaller loops` / `higher confidence.` — the prompt's alternative closing line, kept as the rail's last whisper |
| `span.stop` (in `.aura`) | 1304 · `top: 571px` (the tree's top edge 578 − 8 + 1) · 66 wide | 1584 · `top: 610px` | `auth-guard` — the session's slug, at the record's root line |

**Copy.** Lead: `You choose the tools, the models and the limits. splitbrief holds them for the whole run: every brief, every retry and every review runs inside the rules you set, and what it did is on disk when it stops.` Callouts (keys · values; `·` separators are typed):

```
TOOLS                    MODELS                        RULES
planner      claude‑code fable · deepseek              max_retries   3
implementer  opencode    gpt-5.6 · qwen3               max_budget    5.00 usd
reviewer     codex°      any lab in any seat           pause_at      85 %
                         swap one mid-run              escalation    qwen3 → planner
                                                       snapshots     pre_task · post_task
```

TOOLS and RULES are `span.kv` grids; MODELS is four plain lines with `<br>` (the round-1 line `fable · deepseek · gpt-5.6` was 203 px at the value size against a 199 px cell; split at the seat boundary it names the escalation model too, which RULES already cites). The keys are the ones `splitbrief init` writes (`max_retries`, `max_budget`, `escalation`, `snapshots` — snake_case; `pause_at` renders `budgetPauseThreshold`'s 0.85); the `°` on `codex°` is §4's sign-off mark. Tree:

```
.splitbrief/sessions/2026-03-08-auth-guard/
├── state.json        // resume from here
├── session.jsonl     // every event, append-only
├── spec.md
├── plan.md
├── tasks.md          // every brief, one per file
├── evidence.json     // what was proved, what was observed
├── review.md         // the reviewer's read
└── snapshots/
```

The root line is row 21 of the terminal's path — 03's last line is 04's first object (the seam). Files and notes follow `README.md`'s tree and `HOW-IT-WORKS.md`'s session directory; `evidence.json` is one ledger per session, so the transcript's `Evidence saved to …/` is true after every task.

**390.** Single column in this order (`order` in the phone tier): head (eyebrow 104 — the phone padding is 96 —, title 154, sub 187), breath at 314 (34px, four lines, 167 tall — `SPLITBRIEF HOLDS` is 346 px), lead at 513 (`max-width: none`), creed at 658 (`margin-top --s-6`, `align-self: start` again), callouts stacked at 743 (`row-gap --s-6`, `margin-top --s-8`; the `kv` grid keeps `10ch 1fr`), tree at 1 204 (`--text-micro` at 16 px, `margin-top --s-8`; `.tree .note { display: block; margin-left: 6ch }` inside its `span.row` — the note drops under its file and, because the row is inline-block and the newline follows the row, no blank line follows it: 14 lines, 224 px; `overflow-x: auto` as the last resort, never needed at 350). Section 1 492 tall; the 04 marginalia and stop are hidden below 1360 like the others.

### 16.4 Footer — `footer.foot`

The reference's footer, complete here (§10 says the same; this is the copy an implementer reads). `main` ends with `hr.rule.rule--bleed` (`width: 100%; margin: 0; border: 0; border-top: 1px solid var(--hair)` — the full-bleed seam; the rail's spark parks at the rail's end, whose bottom is this rule, at max scroll, §16.6). Then `footer.foot`: one row, two ends, on the content width (`width: min(var(--content-max), 100% - 2 * var(--gutter)); margin-inline: auto`), `display: flex; justify-content: space-between; align-items: baseline; column-gap: var(--s-6); padding-block: var(--s-8)` — 87 px at every desktop width (32 + the baseline-aligned 22 px wordmark line + 32, measured 87.4); `--text-label`, line-height 1.45, tracking `.08em`, uppercase, `--ink-2`.

- **left** `p.brand` (`display: flex; align-items: baseline; gap: var(--s-8)`): `a.wordmark` `splitbrief°` — the nav's wordmark verbatim (§3: `--font-mono`, 22px, weight 500, `--ink`, `text-transform: none`, line-height 1; the `°` a `<sup aria-hidden="true">` in `--ink-3` at 60 %, `position: relative; top: -.6em; line-height: 0`; `href="#top"`), then `span.claim` `BUILDS BETTER SOFTWARE.` (`--ink-2`).
- **right** `p.right` (`display: flex; align-items: baseline; gap: var(--s-8)`): `span.links` (`display: flex; gap: var(--s-6)`) — `[ docs ]` → `https://github.com/b4r7x/splitbrief/tree/main/docs`, `[ github ]` → `https://github.com/b4r7x/splitbrief`, each an `a` with `display: inline-block; text-transform: none` whose brackets are `span.bracket` (the nav links' markup and states: hover text `--ink` and brackets `--blue`, a 44 px hit box, the focus ring) — then `span.seats` `PLANS / EXECUTES / REVIEWS` in `--ink-3` with each `/` a `span.sep` in `--ink-4`; text, not links (the reference's; §3).

Geometry (footer-relative y; the row is 87 tall, the 12 px line at y 38): 1440 — wordmark x 64 (140 wide), claim x 236, `[ docs ]` x 961, `[ github ]` x 1 050, `PLANS` x 1 164 → the last glyph at 1 376 (the content edge); 1920 (`--content-max` 1392) — 264 / 436 / 1 241 / 1 330 / 1 444 → 1 656. Against the reference (ruling 8) the wordmark, claim and links are wider than the 6.9 px type the reference sets its footer in — `--text-label` is the page's floor — and the seats' right edge lands on the content edge to the pixel; recorded in `visual-diff-comp.md`. **≤ 899 (Correction V, 2026-09-10 — it read `< 768`, one pixel below the 768 tier, so at 768 all four items shared one row and `[ docs ]` and `[ github ]` each broke between their label and their own closing bracket):** `.foot { flex-direction: column; align-items: flex-start; gap: var(--s-4); padding-block: var(--s-6) var(--s-8) }`, `.foot .brand { flex-wrap: wrap; row-gap: var(--s-2) }` — at 390 the 140 px wordmark and the 188 px claim do not share a 350 px row, so the claim wraps whole under the wordmark, never mid-phrase; from 768 to 899 they share the brand row and the claim still holds one line —, `.foot .right { flex-direction: column; align-items: flex-start; gap: var(--s-2) }` — brand (wordmark, then the claim), then the two links on one row, then the seats; 138 px tall at 768 and 899, 162 at 390; the page ends on `PLANS / EXECUTES / REVIEWS`. Measured at 390 / 768 / 899 / 900 / 1024 / 1440: `[ docs ]` and `[ github ]` are one box on one line at every width, `BUILDS BETTER SOFTWARE.` and `PLANS / EXECUTES / REVIEWS` one line each, and `scrollWidth === clientWidth`.

### 16.5 Aura system v2 — one system across the lower page

The hero keeps §7.5 exactly (its layer, pool, seed 8088 and keep-clear). Below the hero the aura is **one system in five materials**, all behind the grid (`z-index: 0` under the grid's 1), all `aria-hidden` (the section's `div.aura` carries it), none inside a panel, none within 24 px of a text line unless a row below says otherwise:

1. **The rail** (`span.rail` in `.lower`, CSS only): `position: absolute; top: 0; bottom: 0; width: 1px; right: calc((100% − min(var(--content-max), 100% − 2 * var(--gutter))) / 2 + var(--rail))`, `background: repeating-linear-gradient(to bottom, var(--ink-4) 0 4px, transparent 4px 8px)` — the hero's routes' 4/4 dash in the hero's quietest ink, from the hero → 02 seam to the footer rule, through both hairlines and the 03 → 04 gap; `container-type: size; view-timeline: --rail block` (§16.6's spark rides it). Each section's `p.marg-list` tick is a solid 1 × 12 `--ink-3` stop on it and each section's `span.stop` tick crosses it; nothing else touches it — every fragment and dot field keeps 8 px clear of its x (the whispers cross it by the reference's placement, §16 shared grammar). It starts under the hero's reviewer seat label (`REVIEW / READ / EVALUATE / CONFIRM`, stage x 82 % → page x ≈ 1 244–1 314 at 1440): the brief the reviewer confirmed goes down the page.
2. **Dot fields** (`canvas.dots`, drawn once by `features/aura/dots.ts`): the ghosts' own cell grid — 7 × 11 CSS px cells, glyphs `.` (share < .3 of the cell's hash) or `:`, `11px "JetBrains Mono"`, `--ink-4` at alpha `.45 + .55 × hash(seed, c, r, 1)`; a cell is lit when `hash(seed, c, r, 0) < share(u, v)` (`lib/noise` `hash`, no `Math.random`; `u, v` the cell centre in 0–1), and the glyph is `:` when `hash(seed, c, r, 2) < .3`. Profiles: `skyline` `share = .61 × (.25 + .75 n(u)) × v^1.6` — a low relief rising to the bottom; `ground` `.43 × (.4 + .6 n(u)) × v^1.2`; `column` `.22 × (.3 + .7 (1 − |u − .5| × 2)) × (.6 + .4 sin(7v + seed))`; `n(u) = .5 + .5 sin(9.4u + seed) cos(4.1u − .7 seed)` (or `valueNoise` at the same span — the shape, not the formula, is the spec: a profile that varies along x and thins toward the top). Canvases are sized `cols × 7` by `rows × 11` CSS px, backed at DPR ≤ 2, never resized by CSS. Three placement classes: `.dots--skyline` at `left: C` (the content box's left edge: `calc((100% − min(var(--content-max), 100% − 2 * var(--gutter))) / 2)`), `.dots--desk` at `left: R + 8` (the rail x + `--s-2`: `calc((100% + min(…)) / 2 − var(--rail) + var(--s-2))`), `.dots--gutter` at `left: E + 24` (the content box's right edge + `--s-6`: `calc((100% + min(…)) / 2 + var(--s-6))`). Fields per section (positions relative to the section):
   - 02 `skyline` seed 2, 44 × 7 (308 × 77): `.dots--skyline`, `bottom: 24px` (y 456–533) — the reference's dotted skyline under the head; the second fragment row (424–440) sits 16 px above it, the 6 px fragment margin included.
   - 02 `column` seed 3, 10 × 6 (70 × 66): `.dots--desk`, `top: 356px` — 48 px under the `SPEC…` list's dash (308), 25 px above `less context` (447); it was 22 px under the list and read as the list dissolving.
   - 03 `skyline` seed 5, 36 × 7 (252 × 77): `.dots--skyline`, `bottom: −44px` (y 355 → 432) — it straddles the 03 → 04 seam (`.s03 .aura { overflow: visible }`; every other aura clips); 24 px under the head column's second fragment row (315–331).
   - 03 `column` seed 6, 10 × 10 (70 × 110): `.dots--gutter`, `top: 140px`, **≥ 1600 only** — beside the `VALIDATE…` list, outside the content box. At 1360–1599 03 has one field: between its stop (18), `// not its own judge` (98), the list (140–254) and `A SMALLER LOOP` (293–341) the rail's right side has no 48 px band left, and the reference's own 03 puts two `:` beside `ESCALATE`, nothing more.
   - 04 `ground` seed 7, 60 × 6 (420 × 66): `.dots--skyline`, `bottom: 181px` (y 572–638 at 1440; 25 px under the lead's dash, 32 above the creed) — the ground the creed stands on, between the paragraph and the caps (fix cycle 1: it sat under the creed and pointed at nothing).
   - 04 `column` seed 8, 10 × 8 (70 × 88): `.dots--desk`, `top: 431px` — 48 px under the `LIMITS…` list's dash (383), 52 px above the `auth-guard` stop (571).
   Below 768 `canvas.dots { display: none }` — no field at any phone width (fix cycle 1: at 390 the fields sat behind `CLEAR INPUTS.`, the steps' dash and the tree; a 350 px column has no free zone). Density caps: no field lights more than 35 % of any row and the page's dot fields together cover < 3 % of the lower page's area. Count: 5 canvases at 1360–1599 (2 / 1 / 2), 6 at ≥ 1600. **Correction W (2026-09-10):** the two relief coefficients were `.34` and `.24` and the fields did not read at 1× — 19 of 252 cells lit in 03's skyline, 18 of 308 in 02's, 24 of 360 in 04's ground. Raised to `.61` and `.43`; the `column` profile keeps `.22` because a 10-column field is already at 30 % of a row and because `.dots--desk` is the field R-NR-1 measures against a text box. Lit cells 37 / 6 / 37 / 8 / 37 / 8 = 133 (was 83), worst rows 29.5 / 30.0 / 30.6 / 30.0 / 20.0 / 20.0 % against the 35 % cap, area 10 241 px² against the 76 205 px² budget. No canvas box moves, so the placer's keep-clear input is unchanged and every placed and trace count holds at 1440, 1600 and 1920.
3. **Fragments** (`span.frag`, `--text-micro`, line-height 16 px, `--ink-4`, `white-space: pre`, opacity `.16–.28` — under §7.5's `.35`, the lower page is quieter per string and denser per section): placed by `fragments/mount.ts`'s `placeFragments` with a per-section **pool** and the section's rect as the viewport (origins offset by the section's top), the same seeded search, the same travel band, the same 60 px spread and 24 px text clearance, the same hover (§7.5). **Seeds:** 8090 (02), 8091 (03), 8092 (04) — `8088 + the section's ordinal 2 / 3 / 4`. **Opacity:** `.16 + .12 × hash(seed, i, 0, 3)` for string *i* (the fourth hash channel; channels 0–2 are the origin's x, y and phase, as in the hero). Keep-clear per section: every text box in `.grid` (grown 24 px), the panel's box, `pre.tree`, every `.marg` / `.marg-list` / `.stop`, the rail x ± 8, every `canvas.dots` box, the seams (16 px above and below each section border), and — the hero's lesson — each list block (tick-list, marg-list, steps, callouts) grown by its own width to either side. **Same-line clearance (fix cycle 1b):** a fragment whose line band (its box grown 8 px above and below) overlaps a text line's band must start at least 96 px (`--s-24`) right of that line's end or end at least 96 px left of its start — 24 px on a shared line reads as a tail of the line (`attempt 2/3` after `HIGHER QUALITY`, `//` after `scope,`) —, and a trace keeps twice that (192 px) from any text or display line outside a panel on its band, because a dotted run reads as a leader to whatever it ends near; the line's rendered box counts (an uppercase tracked title is wider than its source text). Single glyphs are exempt from the same-line rule — a glyph has no tail — but keep the 24 px box clearance like every fragment. The gutters: at 1360–1599 the 64 px gutters take **single glyphs only** (`.frag--glyph`, one character from `∴ ° × →`, `left: 24px` or `right: 24px`, ≤ 2 per gutter per section, their y seeded within the section — `hash(seed, i, 1, 0)` over the section's height — at least 24 px from either seam and at least 60 px from any other fragment's origin; the hero keeps its gutters silent, the lower page need not); at ≥ 1600 the 264 px gutters also host whole strings (`.frag--gutter`, between 96 px from the viewport edge and 24 px from the content edge; `display: none` below 1600). **Targets at 1440×900: 02 ≥ 10, 03 ≥ 7, 04 ≥ 12 placed** (the comp, which passes its own keep-clear and same-line rules at both tiers — checked box by box: 11 / 7 / 13, of which 1 / 1 / 3 are traces and 2 / 3 / 2 gutter glyphs; at 1920: 13 / 9 / 15, the ≥ 1600 gutters adding `brief -> implementer` and the `T002` trace to 02, `retry(3) -> escalate` and the `47s` trace to 03, `contract blocked` and `fewer blind spots` to 04); 0 below 768 (`.frag { display: none }`). The comp's zones, which are the free bands its geometry leaves: 02 — two rows under the tick-list (392 / 424: `· · · ln 36`, `one file per brief`; `T1 ✓`, `//`, `T2 ▸`), one row under the whisper (`bottom: 20px`: `const brief = compile(spec)`, `fresh context`), `depends_on: [T001]` and `∴` right of the tick-list; 03 — two rows under the serif dash (283 / 315: `first failure`, `attempt 2/3`, `hash ok`; `· · · 3/3`); 04 — the head → breath gap, x 384–525 (`max_budget: 5.00`, `state.json`, `[ 3 / 7 ]`, `°` — three strings and a glyph; a fourth string made the gap read as a column), `lower spend` right of `YOU DESCRIBE`, and two `bottom:`-anchored rows under the creed and the tree (`bottom: 51px` / `19px`: `real software`, `0x2f …`, `· · · 3 seats`; `seed 8088`, `· · · 85 %`, `· · · 5.00 usd`) — anchored to the bottom because 04's text ends 40 px lower at 1920 than at 1440. Beside the breath, the steps and the callouts no fragment is legal: the 96 px rule and the 24 px rule meet.
   **Pool v2** (`features/aura/pool.ts`, three arrays — the strings of the prompt plus the page's own; every string listed, traces included):
   - `POOL_BRIEFS` (02): `const brief = compile(spec)` · `one file per brief` · `brief -> implementer` · `depends_on: [T001]` · `fresh context` · `no memory of the last one` · `in bounds · out of bounds` · `stop and ask` · `contract blocked` · `T1 ✓` · `T2 ▸` · `( 39%, 46% )` · `//` · `∴` · `· · · · · · · · ln 36` · `· · · · · · · · · · · · col 1` · `· · · · · · · · · · T002`
   - `POOL_VALIDATION` (03): `validate(task)` · `typecheck · lint · test` · `while (red) retry()` · `retry(3) -> escalate` · `first failure` · `attempt 2/3` · `412ms` · `evidence saved` · `not its own judge` · `promote(diff)` · `hash ok` · `→` · `×` · `· · · · · · · · · · · · 14:28:16` · `· · · · · · · · 47s` · `· · · · · · · · · · 3/3`
   - `POOL_CONTROL` (04): `max_budget: 5.00` · `pause at 85 %` · `snapshot: pre_task` · `state.json` · `session.jsonl` · `append-only` · `less context` · `more progress` · `lower spend` · `fewer blind spots` · `real software` · `smaller loops` · `higher confidence` · `0x2f 0x62 0x72 0x69 0x65 0x66` · `seed 8088` · `[ 3 / 7 ]` · `°` · `∴` · `worktree` · `· · · · · · · · · · · · 85 %` · `· · · · · · · · 5.00 usd` · `· · · · · · · · · · 3 seats`
   (`0x2f 0x62 0x72 0x69 0x65 0x66` is `/brief` in hex — the hero's trail completed; `( 39%, 46% )` is the brief card's stage centre; `seed 8088` is §7.5's seed. Today's 20-string pool becomes the hero's `POOL_HERO`, unchanged.)
4. **Traces** are the nine pool strings that begin with dots: a run of 8 / 10 / 12 `· ` pairs (99 / 125 / 152 px at 11 px) ending in a coordinate the page owns — the editor's last line (`ln 36`), its caret column (`col 1`), the task id, the transcript's clock, duration and test count, the budget threshold and amount, the seat count. They are the prompt's "light horizontal data traces": placed by the placer like any string, hovering like any string, three per section on the pool, no fourth mechanism. The comp lays them on bands without text (02: under the tick-list at 392; 03: the second row under the serif dash, 315; 04: the two bottom rows) and shows the rest of the pool's traces in the ≥ 1600 gutters (02's `T002`, 03's `47s`); 03's `14:28:16` (211 px) fits no 1440 band and no gutter, so the site's placer is the first to show it.
5. **Rail stops** (`span.stop`, one per section in its `div.aura`, the prompt's "quiet coordinates"): `position: absolute; left: R + 8` (the marg-lists' x), `--text-micro`, line-height 16 px, tracking 0, `--ink-4`, `white-space: nowrap`; a `::before` tick — `position: absolute; right: calc(100% + 5px); top: 8px; width: 6px; height: 1px; background: var(--ink-3)` — that crosses the rail from R − 3 to R + 3 on the box's centre line. Each sits at its artefact's top edge (`top` = the artefact's y − 8): 02 `§02` at 17 (the editor, 25), 03 `T002` at 17 (the terminal, 25), 04 `auth-guard` at 571 / 610 (the tree, 578 / 618). Strings: `§02` · `T002` · `auth-guard`. On `.is-in` the tick flares for 400 ms at 0 ms (§16.6) — the one flare per section. Hidden below 1360 with the rail.
6. **Coordinates and symbols** are strings in the pools above and the stops, not a further mechanism: `( 39%, 46% )`, `0x2f …`, `∴`, `°`, `×`, `→`, `[ 3 / 7 ]`, the traces' tails, `§02`.

**How it differs from today:** one 20-string layer sized to the first viewport, silent below the fold → three per-section layers with their own pools and traces, dot fields the daemons' cells are made of, a rail that ties the three sections to the hero's routes, and a stop where each artefact begins. **What is CSS:** the rail, the spark and its scroll timeline, the marginalia ticks, the stops, every reveal. **What is JS:** the fragment placer (existing), the dot fields' one-time draw plus the shimmer, the reveal observer. Determinism: seeds fixed (8090 / 8091 / 8092 for fragments; 2 / 3 / 5 / 6 / 7 / 8 for the fields), `hash` from `lib/noise`, so two renders of one build are byte-identical.

**v2 build, 2026-09-09 — what was built where this subsection and the code differ.** The design above stands; these lines are the built state.

- **Class names** (D-4): every `.frag`, `.frag--gutter` and `.frag--glyph` above is `.fragment` in the code. The gutter and glyph behaviours are placer decisions per tier, not CSS classes.
- **Pools** (D-5): `src/features/fragments/pool.ts`, not `features/aura/pool.ts`.
- **Opacity channel** (D-8): the placer's existing channel 3, `hash(seed, k, 3, 0)`, scaled to the layer's range. §16.5's `hash(seed, i, 0, 3)` notation names the same channel. A new channel layout would have moved the hero's 19 placements.
- **Canvases are markup** (D-17): `canvas.dots` carry `data-seed / data-cols / data-rows / data-profile` and are positioned by the section sheets; `dots.ts` draws them. Before P5 they are transparent boxes. Rejected: JS-created canvases — the markup is the single source of what sits where.
- **The fragment seeds shipped are 8152 (02), 9400 (03) and 8490 (04)**, not the 8090 / 8091 / 8092 of the paragraph above. The originals were chosen against a placer that did not yet enforce §16.9 item 7; the shipped three were selected by an offline sweep over the real dumped `sectionKeepClear` after the clearance was made correct. The seeds are still fixed and the render is still deterministic; only the numbers moved. 04's seed moved a second time, 8997 -> 8490, when the traces were placed first — see the bullet below.
- **The per-section fragment floors are 6 (02) · 4 (03) · 14 (04)**, not the 10 / 7 / 12 targets above. The comp's counts were hand-placed against a much weaker clearance; against a placer that satisfies §16.9 item 7 the reachable counts are lower. The clearance was NOT weakened to reach the old numbers — the ruling was the opposite: free legal area, keep the clearance. Two changes freed it (the tier margins below and the per-layer growth below) and the rendered counts are 7 / 5 / 16 with zero keep-clear violations of any class. The floors sit one fragment under the rendered counts so a later legal tweak does not break them: 29 / 20 / 26 seeds of 200 clear each floor.
- **The two tiers treat the section margins alike.** Below 1600 the placer no longer reserves the two 64 px edge strips that the ≥ 1600 tier reserves for its gutter strings; the strips are legal area at 1360–1599 as they always were in the comp.
- **The text-clearance growth is per layer**, not one global number: the hero keeps `{ x: 18, y: 0 }` — bit for bit the growth it shipped with — and the three section layers use `{ x: 24, y: 24 }`. A single global growth took the hero from 19 fragments to 15 and reddened `motion.e2e.ts`'s `>= 18` floor. Both axes were made per-layer rather than only the vertical one, so the hero is provably identical to its known-good state.
- **The placer places each layer's traces BEFORE its other strings**, and the same-line rule **skips text lines inside a panel** (team-lead ruling, 2026-09-10). The first is what makes a dotted run reachable at all: every trace is among the last entries of its pool, and the 10–19 strings ahead of it consumed the wide bands a 125–211 px run needs. The second brings `place.ts` into line with this subsection's own words — a trace keeps 192 px "from any text or display line **outside a panel**" — and with `aura.e2e.ts`, which had implemented it correctly all along; the placer was stricter than both. Neither change weakens a clearance. Measured on the rendered page, matching the offline sweep on all nine numbers:

  | | 1440 | 1600 | 1920 |
  |---|---|---|---|
  | `.s02` | 6 fragments · 0 traces | 7 · 1 | 10 · 1 (`· · · ln 36`) |
  | `.s03` | 4 · 0 | 4 · 1 | 6 · 1 (`· · · 14:28:16`) |
  | `.s04` | 14 · 1 (`· · · 85 %`) | 13 · 1 | 16 · 2 |

  At 1440 `.s02` and `.s03` place no trace and cannot: 0 of 200 seeds, and an exhaustive origin scan gives `.s03` ZERO legal origins for any of its three traces even against an EMPTY placement set, while `.s02`'s two placeable ones fall to zero once the 64 px travel band is counted. Their 1440 trace floor is 0 and is recorded as such. The comp's 1 / 1 / 3 at 1440 were hand-placed against a much weaker clearance. §16.5's own remark that "03's `14:28:16` fits no 1440 band and no gutter, so the site's placer is the first to show it" is now literally true — at 1920.
- **The placer's internal spread is 62 px, not 60.** §16.5's number is 60 and `aura.e2e.ts` asserts it on the RENDERED boxes; the placer's guard is on its own float coordinates, so a pair accepted at 60.30 px rendered 59.54 px apart — sub-pixel rounding of a gutter glyph's box. 62 is the margin that makes the specified 60 true in the page. No count, trace count or floor changes at any width. The same exposure exists wherever placer arithmetic is asserted against rendered geometry.
- **`claude‑code` uses a NON-BREAKING HYPHEN (U+2011)** in §16.10's 04 copy, the same device as `mid‑tier`. With an ASCII hyphen it broke across a line at 1200, 1024 and 768, reading as `claude-` over `code`. The glyph is the same width in JetBrains Mono, so no geometry moved. `white-space: nowrap` was rejected: at 768 the callout is 157 px and the value would have overflowed instead of staying on one line. `gpt-5.6 · qwen3` and `swap one mid-run` keep ASCII hyphens — measured at all six widths, neither breaks at one.
- **`CONTROL.seed` is 8490**, not 8997. Placing a 158–185 px trace first costs `.s04` three fragments at 1440, which would have left it at 13 against its floor of 14. A 200-seed sweep found 11 seeds clearing 14 WITH a trace; 8490 is the best of them across all three widths. No floor was lowered and no clearance was weakened — the count was restored by choosing a seed.

### 16.6 Scroll choreography — the run comes down the page

**Concept (one sentence, §7's idea extended):** the brief the hero routed leaves the reviewer's seat as a spark on a rail down the right margin, and each section's artefact paints itself in as the spark reaches it — the editor loads the file, the terminal prints the run, the folder fills — once, on arrival, never on the way back up.

**Mechanism, decided:** reveals are **IntersectionObserver one-shot + CSS** (`features/aura/reveal.ts`: one observer, `rootMargin: '0px 0px -30% 0px'`, `threshold: 0`, adds `.is-in` to the section and unobserves it) — not `animation-timeline: view()`, which is scrubbed by definition and would un-type the transcript on the way up. **The spark alone rides a CSS scroll-driven timeline whose subject is the rail** (fix cycle 1 — with `view()` on the spark itself the subject was the spark's own 5 px box and the ride ran out while the hero was still on screen): `.rail { container-type: size; view-timeline: --rail block }`; `.spark { position: absolute; left: −2px; top: 0; width: 5px; height: 5px; background: var(--white-spark); opacity: .85; display: none }`; `.spark::before` the **wake** — `content: ""; position: absolute; left: 2px; bottom: 100%; width: 1px; height: 20px; background: linear-gradient(to top, var(--ink-3), transparent)` — a 20 px `--ink-3` trail on the rail above the spark, the hero packet's wake in one dimension; `@supports (animation-timeline: view()) { .spark { display: block; animation: ride linear both; animation-timeline: --rail; animation-range: contain 0% contain 100% } }`; `@keyframes ride { to { translate: 0 calc(100cqh − 5px) } }`. The rail is taller than any viewport, so `contain 0%` is the scroll position at which the rail's top meets the viewport's top (the hero → 02 seam at the top edge) and `contain 100%` the one at which the rail's bottom meets the viewport's bottom (the footer rule at the bottom edge); between them the spark's viewport y is `progress × (viewport height − 5)`: it enters at the top edge as the seam arrives there and reaches the bottom edge as the footer rule does — the run comes down the page at the reader's pace. `both` parks it at the rail's top before the ride (visible on the seam as the hero scrolls away) and at the rail's end after it, its bottom on the footer rule. **Without `@supports` the spark is `display: none`** — the comp shows it only because its render engine supports the timeline; do not copy a visible fallback. `prefers-reduced-motion: reduce` → `animation: none; display: none`. No pinning, no scroll-jacking, no layout property animates: opacity, translate and scale only. **Expected spark positions** (real page: nav + hero 1 140 / 1 144, the rail from y 1 140 / 1 144 for 1 764 / 1 857 px, the page 2 992 / 3 089 tall — §16.8; `progress = (scrollY − railTop) / (railHeight − viewportHeight)`, clamped 0–1; viewport y = `railTop + progress × (railHeight − 5) − scrollY`): at 1440×900 the ride spans scroll 1 140 → 2 004 of a 2 092 maximum, so at 25 % scroll (523) the spark waits on the seam at viewport y 617, at 50 % (1 046) at 94, at 75 % (1 569) it is riding at 444, at 100 % (2 092) it is parked at 807 with the rule at 812; at 1920×1080 the ride spans 1 144 → 1 921 of 2 009, so 25 % (502) → 642, 50 % (1 005) → 139, 75 % (1 507) → 502, 100 % (2 009) → 987 with the rule at 992. (The comp, whose hero is a 120 px stub, has the rail at 120 for 1 764 / 1 857 px: at 1920×1080 scrolled to 03's top (595) the spark reads at viewport y 657, at max scroll 986 — `lower-geometry.json` records both.)

**What reveals, in what order (per section, on `.is-in`; every element carries `.reveal` and an `--enter` index; the pre-reveal state `opacity: 0; translate: 0 8px` and the animation are applied only under `html.js` and `@media (prefers-reduced-motion: no-preference)`, in `styles/reveal.css`):**

| section | order (`--enter` × 60 ms, the hero's `enter` keyframes: 480 ms ease-out, `both`) |
|---|---|
| 02 | eyebrow 0 · title 1 · sub 2 · tick-list 3 · **panel box** `figure.panel` 1 (its border and fill paint before anything inside; the box is `.reveal` like the rest — opacity and translate on the figure, its children start hidden by their own rules) · panel bar 2 · **gutter rule** `ol.lines::before` draws down: `animation: draw 432ms linear both; animation-delay: 120ms` (= the bar's 2 × 60) with `@keyframes draw { from { scale: 1 0 } }` and `transform-origin: top` — 432 ms is the rows' total delay, so the rule reaches the bottom as the last row lands · **rows** `li` with `--row: 1…36` at `animation: enter 240ms ease-out both; animation-delay: calc(120ms + var(--row) * 12ms)` (the file loads top to bottom in 0.67 s) · statement 1 4 · caps 1 5 · statement 2 6 · caps 2 7 · marginalia 8 — `.marg` and `.marg-list` use `enter`; their ticks (`.marg-list::before`, `.tick-list::before`, `.callout::before`) use `@keyframes tick { from { scale: 1 0 } }` over 240 ms, `transform-origin: top`, at the block's own delay (the marg-list tick at 480 ms, with its parent) · **stop flare:** `.stop::before` alone runs `@keyframes flare { 0% { background: var(--ink-3) } 15% { background: var(--white-spark) } 100% { background: var(--ink-3) } }` — `animation: flare 400ms linear both` at 0 ms, the moment `.is-in` lands — so the brief arrives at the stop, then the section paints; the marg-list tick does not flare (fix cycle 1b: at 0 ms its parent is still at opacity 0 and the tick itself scales in at 480 ms, so a flare there is invisible) |
| 03 | head as 02 · panel box 1 · bar 2 · rule `draw 2130ms` from 120 ms (21 × 90 + 240) · **rows** `--row: 1…21` at `calc(120ms + var(--row) * 90ms + var(--beat) * 240ms)` with `--beat: 1` on rows 13–21 and `--beat: 0` on the `ol` — the TUI prints a row at a time, whole (no per-character typing), and holds a beat after `FAIL` before `○ retry`; the last row lands at 2.25 s · statements 4, 6 · steps 7 · marginalia 8 · flare as 02 |
| 04 | eyebrow 0 · title 1 · sub 2 · breath lines 2 / 3 / 4 / 5 · lead 6 · callouts 6 / 7 / 8 (tick then the block) · creed 7 · tree rows `span.row` with `--row: 1…9` at `calc(360ms + var(--row) * 30ms)` (the folder fills) · marginalia 8 · flare as 02 |

The dot fields shimmer on desktop: `dots.ts` re-lights **5 % of a field's cells per tick at 4 fps** on `diagram/ticker.ts` (`createTicker(canvas, 4, …)`, so it pauses off-screen and under `document.hidden`; a re-lit cell redraws its hash channels 1 and 2 with the tick as a fourth argument — alpha and glyph change, the profile does not); the round-1 2 % at 2 fps moved two cells per half-second and read as nothing. The fragments hover as §7.5. **Reduced motion:** no pre-reveal state exists (it is scoped to `no-preference`), so every section sits at its final frame without the observer; the spark is `animation: none` + hidden; the fields draw frame 0 and never tick; the fragments are static (§7). **No-JS:** `html.no-js` has no pre-reveal state either — the page is complete without script. **Crawler / first paint:** the hero is untouched; the pre-reveal state applies to sections below the fold only, and the capture harness scrolls through the page before shooting (§14). **Budget:** own code ≤ 16 KB gzipped (raised from 12; the aura feature is ≈ 3–4 KB), total ≤ 90 KB, no library.

**v2 build, 2026-09-09 — the built reveal, and one arithmetic error in the table above.**

- **Pre-reveal state is `opacity: 0` only** (D-9). The 8 px rise comes from the `enter` keyframes' `from`, which shows during the delay under `fill-mode: both`, so the render is the one this subsection specifies. `translate: 0 8px` in the pre-reveal rule was rejected: it shifts every keep-clear box the placer measures.
- **The pre-reveal rule is scoped to `section:not(.is-in)`** (D-10), so the animated element's base opacity is 1 once `.is-in` lands. An implicit `to` of 0 would hold the element invisible under `fill-mode: both`.
- **Infinite animations are cancelled to base, not seeked to time 0.** `currentTime = 0` on an animation driven by a view timeline throws in Chrome, and on an infinite CSS animation it pins frame 0 rather than the resting state. The capture harness cancels instead and skips any animation whose timeline is not `document.timeline`.
- **The page is 3 064 px tall at 1920, not the 3 089 of §16.8.** Measured repeatedly across four capture tags before and after both `hero.css` changes of 2026-09-09. The 1440 figure, 2 992, is right. Recomputing the four 1920 spark positions from a 1 984 px max scroll instead of 2 009 gives roughly `[648, 152, 476, 1012]` against this subsection's `[642, 139, 502, 987]` — deltas of 6 / 13 / 26 / 25 px, every one inside §16.9's ±3 % of the viewport height (±32.4 px at 1080), which is why the spark assertions pass. The numbers above were NOT edited to match the page; the tolerance is doing its job and the discrepancy is recorded instead.

### 16.7 Responsive tiers per section

| width | 02 / 03 | 04 | footer |
|---|---|---|---|
| ≥ 1600 (1920) | the §16.1–16.2 tables' second column: content 1392, the row `330px 517px 1fr` (head 330 · panel 517 · notes 497), rail at 1576, FHD type (`--text-note` 14, `--text-serif-sub` 29, `--text-art` 12 / 14, `--bar-h` 32; the title and every hero token unchanged); `.frag--gutter` and `.dots--gutter` live | the §16.3 table's second column | §16.4 at 1392 |
| 1360–1599 (1440) | the tables' first column: the row `310px 494px 1fr` (head 310 · panel 494 · notes 460); at 1360 the content box is 1232 and the notes 380 — 59 px more than the 321 px measure needs, which is why 1360 is the three-column breakpoint | idem | §16.4 at 1312 |
| 1100–1359 | `.s02 .grid, .s03 .grid` back to `repeat(12, minmax(0, 1fr))`; `.head` cols 1–6 row 1 (the header band: eyebrow, title, serif, tick-list at `margin-top --s-6`); `.panel` cols 1–7 row 2 (`margin-top --s-8`); `.notes` cols 8–12 row 2 (`margin-top --s-8`; the 41ch + 1px measure still fits: 375 px at 1024); `.marg`, `.marg-list`, `.rail`, `.stop`, `.dots--desk` `display: none` | `.head` cols 1–4, `.breath` cols 6–12 (row 1), `.lead` and `.creed` cols 1–5, `.callouts` and `.tree` cols 6–12 | as above |
| 1024–1099 | same as 1100–1359 (the panel needs 494 px; cols 1–7 give 535 at 1024) | same | |
| 768–1023 | `.panel` full width row 2; `.notes` cols 1–8 row 3 | `.breath` full width row 2 (`margin-top --s-8`); `.lead` cols 1–6 row 3; `.callouts` cols 7–12 row 3 as two columns (TOOLS, MODELS; RULES spanning both, `margin-top --s-6`); `.creed` cols 1–6 row 4 (`align-self: start`); `.tree` full width row 5 | one row at 900–1023; §16.4's stack at 768–899 (Correction V) |
| < 768 (390) | §16.1 / §16.2 "390"; no aura at all below 768 (`canvas.dots`, `.frag`, `.stop`, `.rail` off) | §16.3 "390" | §16.4's stack |

The lower page's breakpoints (1360, 1100, 1024, 768) are its own; the hero keeps §11's (1100, 768). `KEEP THE EVIDENCE.` at 28px is 310 px and at 29px 321: below 1360 the head band is ≥ 6 columns, so it never wraps; between 1360 and 1599 the head is exactly 310 px, so the line fits to the pixel, and at ≥ 1600 the 330 px column leaves 9 — the e2e assertion in §16.9 guards it.

### 16.8 Heights, seams, page length, the thumbnail

| section | 1440 | 1920 | 390 | density | shape |
|---|---|---|---|---|---|
| nav + hero | 1 140 (§15.4) | 1 144 | ≈ 2 032 | dense, airy lower-left | glyph field |
| 02 | 557 | 595 | 1 418 | dense | the tallest panel, marginalia |
| 03 | 388 | 404 | 1 074 | dense, short | terminal, the list |
| 04 | 819 | 858 | 1 492 | breath | the serif, three notes, a tree |
| rule + footer | 88 | 88 | 163 | whisper | one row |
| **page** | **2 992** (4 365 in v1) | **3 089** | **≈ 6 179** (7 472 in v1) | | |

Folds: at 1920×1080 the hero ends at 1 072 (its last ink), 02 is 595, 03 is 404 (03, 04's head and the whole breath fit one fold), 04 + the footer are 946 — every section inside a fold; the comp's `lower-1920-s02/-s03/-s04.png` are the proofs (the footer's fold is the 04 capture: one file). At 1440×900 02 is 557 and 04 819 (the 04 fold is clamped at max scroll; 04 + the footer are 907 — 7 px over, ruling 5 asks only for 1920). **Seams:** hero → 02 = the hero's `padding-bottom --s-12` (48) then `.s02`'s full-bleed hairline — the v1 `hr` is gone — with the spark parked on it until the seam reaches the top edge; 02 → 03 = the hairline, with 02's skyline 24 px above it and the rail through it; 03 → 04 = **no rule** — the 03 skyline straddles the border (its bottom 44 px into 04) and the rail runs on, the only two things that cross a seam (measured 2026-09-10 at 1440: the canvas bottom is 44.0 px inside `.s04`, so the straddle is built as specified; the final critique read the field as stopping short of the border and that reading is wrong. What was short was its density, raised by Correction W); 04 → footer = the full-bleed rule with the spark parked on it at max scroll. **The 20 % thumbnail:** glyph field (dense) → the tallest panel (dense) → a shorter panel (dense, a 2 : 3 echo of 02, the reference's own rhythm) → the serif (breath) → one line (whisper). 02 and 03 share one structure by the owner's ruling; their heights differ by 169 px and 04 breaks the pattern, so the thumbnail reads as three silhouettes, not stripes. Hot spots below the hero: the eight blue headings (02), the red hinge answered by green (03), none in 04 — the breath is ink.

**v2 build, 2026-09-09: the page is 3 064 px tall at 1920, not 3 089.** The 1440 figure, 2 992, is right; the 1920 one is 25 px high, measured across four capture tags before and after both `hero.css` changes of that day. Every other row of the table above was met on the built page. The consequence for §16.6's spark positions is recorded in that subsection's build note; the numbers were not edited to match the page.

**v2 build, 2026-09-10: the tiers render as the §16.7 table specifies, measured rather than eyeballed.** At 1360 the 02 row is head 310 · panel 494 (panel x = content left + 334) with all three tops equal, six visible `.marg` and the rail live; at 1200 and 1024 the head band sits above panel + notes with the marginalia, the rail and `.dots--desk` off and 04's breath right of its head; at 768 the panel, the breath and the tree each fill the grid, the notes drop below the panel and the callouts read TOOLS · MODELS over a full-width RULES; at 390 every visible grid child is x 20 · width 350, the tick-list is one 32 px row, the terminal's `.tail` and the panels' second status span are off, the tree's notes are block, the footer is a column, no dot field or fragment is displayed and `scrollWidth` equals `clientWidth`; at 1600 the row steps to head 330 · panel 517 and the gutter dot field lights. `tests/e2e/tiers.e2e.ts` holds all six.

### 16.9 Verification

**Captures per phase (§14):** `<tag>-1440.png`, `-fold.png`, `-390.png`, `-1920.png`, `-1920-fold.png`; for the phase's section the critic adds `-1920-sNN.png` (1920×1080 scrolled to the section's top — `scrollTo(0, section.offsetTop)`, clamped at max scroll; the 04 capture is the footer's) and, for 3.P5, three frames of the 03 transcript at 0.5 / 1.2 / 2.4 s after `.is-in` and four of the spark at 25 / 50 / 75 / 100 % scroll at 1440×900 and 1920×1080.

**Visual diff (ruling 8 — every phase, `visual-diff-<phase>.md` beside the captures).** Targets: `reference-v2.png` for 02, 03 and the footer (expected 1440 = ref × 1.2834 on the viewport; expected 1920 = the reference in the 1392 box: x = 264 + (ref − 53) × 1.3423, sizes and section-relative y × 1.3423); the approved comp — `comps/lower-1920.png`, `lower-1440.png`, `lower-390.png` with `comps/lower-geometry.json` — for 04 and for the aura (the rail, the stops, the fields' boxes, the fragment counts and keep-clear; fragment origins are seeded on the site and hand-placed in the comp, so they are compared by count, zone and clearance, not by pixel). Tolerances: position ±3 % of the viewport (x ±43 / ±58 px, y ±27 / ±32 px at 1440 / 1920), size ±3 % of the expected dimension, copy exact, colour by token. One row per element — `element · expected · measured · delta · verdict` — with a reason column for any row outside tolerance; a row with no reason is a fix brief. The rows are §16.1–16.4's tables plus the comp's `visual-diff-comp.md` rows (59 geometry rows; 49 within tolerance, 10 with recorded reasons: the hero tokens that do not step at 1920, whole-pixel pitch and note size at 1920, 03's six-line statement 2, the footer's type floor and wordmark). The spark rows: its viewport y at 25 / 50 / 75 / 100 % scroll against §16.6's numbers, ±3 % of the viewport height. A phase without its visual-diff file is not done.

**e2e (`tests/e2e/lower.e2e.ts`) — every line mechanically checkable:**
1. Strings present (`textContent` of the section contains, verbatim): 02 — `BEFORE ANY CODE`, `ONE FILE.`, `NOTHING ELSE.`, `BRIEFS`, `CLARITY`, `tasks.md`, `Ln 1, Col 1`, `markdown`, `# TASK BRIEF`, `depends_on: [T001]`, `## Evidence`, `Include test output and typecheck logs in the task result`, `no memory of the last one.`, `scope, file, dependencies, signature`, `CLEAR INPUTS.`, `REVIEW`; 03 — `AFTER EVERY TASK`, `KEEP THE EVIDENCE.`, `splitbrief run`, `task: T002`, `14:28:16`, `Spec → Plan → Briefs → Build → Verify`, `✗ test`, `expected AuthError for expired token`, `retry 2/3`, `PASS`, `3/3 tests passing (412ms)`, `Task complete`, `.splitbrief/sessions/2026-03-08-auth-guard/`, `Correctness is not the implementer's to judge.`, `HIGHER QUALITY`, `YOUR RULES`, `IMPROVE`; 04 — `FOR THE WHOLE RUN`, `WHAT YOU SET.`, `YOU DESCRIBE`, `THE GOAL.`, `SPLITBRIEF HOLDS`, `THE REST.`, `on disk when it stops.`, `CONTRACTS OVER CONTEXT.`, `SMALLER LOOPS. BETTER OUTPUT.`, `claude‑code`, `codex°`, `gpt-5.6 · qwen3`, `swap one mid-run`, `max_retries`, `5.00 usd`, `pre_task · post_task`, `evidence.json`, `snapshots/`, `LIMITS`, `EVIDENCE`; footer — `BUILDS BETTER SOFTWARE.`, `[ docs ]`, `[ github ]`, `PLANS / EXECUTES / REVIEWS`. The whispers are decorative: assert them by DOM presence, never by accessible text — `.s02 .marg-a` text `// contracts over context`, `.s02 .marg-b` `less context` + `more progress.`, `.s03 .marg-a` `// not its own judge`, `.s03 .marg-b` `A SMALLER LOOP` + `A HIGHER BAR` + `REAL PROGRESS`, `.s04 .marg-a` `// your tools, your models, your rules`, `.s04 .marg-b` `smaller loops` + `higher confidence.`, every `.marg` with `aria-hidden="true"`; the stops likewise — `.s02 .stop` `§02`, `.s03 .stop` `T002`, `.s04 .stop` `auth-guard`, inside `.aura[aria-hidden]`. The `h2` of each section has the accessible name `Before any code` / `After every task` / `For the whole run` (the eyebrow span is `aria-hidden`).
2. Counts: `.s02 ol.lines li` = 36 and `.s02 .h` = 8; `.s03 ol.lines li` = 21, `.s03 .mk` = 9, `.s03 .ok` = 6, `.s03 .err` = 2, `.s03 .fail` = 1 and it precedes `.s03 .pass` in DOM order; every `.lines li` has an inline `--row` equal to its number and `.s03 .lines li:nth-child(n+13)` `--beat: 1`; `.s03 ol.steps li` = 5; `.s04 .callout` = 3; `pre.tree .row` = 9 and `pre.tree .note` = 5; `.marg-list` = 3, each with five lines; `.marg` = 6; `.stop` = 3; `.frag` ≥ 10 / 7 / 12 in 02 / 03 / 04 at 1440×900 after `fonts.ready`, of which ≥ 1 per section begins with `· ·` (a trace); `canvas.dots` = 5 at 1440 (2 / 1 / 2) and 6 at 1920, 0 at 390.
3. No horizontal overflow: `document.documentElement.scrollWidth === clientWidth` at 390 and 360 (page.e2e already; keep) and no panel row wider than its `.t` box at 1440 and 1920 (`scrollWidth === clientWidth` and one client rect on every `.lines .t`).
4. Folds at 1920×1080: for `.s02` and `.s03`, after `scrollTo(0, top)`, `getBoundingClientRect().bottom ≤ 1080`; for `.s04`, after `scrollTo(0, min(top, maxScroll))`, `footer.foot` bottom ≤ 1080; at scroll 0 the hero's `.works` dash bottom ≤ 1080.
5. Lines: `.s03 .sub .line:last-child` (`KEEP THE EVIDENCE.`) `scrollWidth ≤ clientWidth` at 1360, 1440, 1600, 1920 and its box height equals one line; `.s04 .breath .line--stanza` one line at 390; every `.notes p` sets the §16.1–16.2 line counts (5 / 3 / 5 / 2 in 02, 4 / 6 in 03 — from `getClientRects()` of a range over the paragraph).
6. Marginalia rows at 1440 (± 1): `.s02 .marg-a` top = `.s02 .notes > p:first-child` top + 4 × 20.8 + 4; `.s02 .marg-list` top = `.s02 .caps` (first) top + 16; `.s02 .marg-b` top = `.s02 .caps` (second) text bottom + 2; `.s03 .marg-a` top = `.s03 .notes > p:first-child` top + 3 × 20.8 + 2.4; `.s03 .marg-list` top = `.s03 .notes > p:nth-child(2)` top + 16; `.s03 .marg-b` top = `.s03 .steps` top + 36; `.marg-a` right edge = the content edge, `.marg-b` right edge = the content edge − 24; `.marg-list` left = rail x + 8 ± 1 where rail x = content right − 80 ± 1 at 1440 and 1920; `.stop` left = rail x + 8 and its tick box spans rail x − 3 … + 3 at the artefact's top edge ± 1.
7. Aura non-intersection: every `.frag` box (± 6 px) intersects no text line box grown 24 px (the `getClientRects()` of every text node under `.grid`), no `.panel`, no `.tree`, no `.marg*`, no `.stop`, no `canvas.dots`, no other `.frag`, and no vertical strip of 16 px around the rail; every multi-character `.frag` keeps §16.5's same-line clearance (96 px, traces 192 px against lines outside a panel) and every pair of `.frag` origins in a section is ≥ 60 px apart — sampled at 0 s and 10 s of animation time (§7.5's test extended to the three layers); every `canvas.dots` intersects no text box; at 390 no `canvas.dots` or `.frag` is displayed. The comp passes this test at 1440 and 1920 (fix cycle 1b).
8. Reduced motion (`test.use({ reducedMotion: 'reduce' })`): `document.getAnimations()` in `.lower` is empty; two full-page captures 4 s apart are pixel-identical; and a reduced-motion full page equals a default full page taken after `settle()`'s scroll-through with every animation seeked to time 0 (`getAnimations().forEach(a => { a.pause(); a.currentTime = 0 })` — the fragments' hover and the fields' shimmer would otherwise differ) pixel for pixel outside the `.spark` box; a cheaper form diffs only the `.grid` boxes.
9. Reveal: with the default project, before scrolling `.s03` has no `.is-in` and its rows have `opacity: 0`; after `scrollTo` to its top and `runFor(3000)` every row's opacity is 1 and the section has `.is-in`; scrolling back to 0 keeps it. The spark: at 1440×900 and 1920×1080 its viewport y at 25 / 50 / 75 / 100 % of max scroll equals §16.6's numbers ± 3 % of the viewport height, and `getComputedStyle(spark).display` is `none` when `CSS.supports('animation-timeline: view()')` is false.
10. Budget: gzipped own JS ≤ 16 KB; total ≤ 90 KB; zero console errors at 1440, 1920 and 390.

**The critic's tell-walk items for the lower page** (on the five captures + the per-section 1920 folds): eye path 04 = `THE GOAL.` → `THE REST.` → `FOR THE WHOLE RUN` → the three heads → the tree, no hesitation; top-3 by size (breath, titles, panels) = top-3 by importance; the callouts are one treatment × 3 — pass only while their lengths differ (3 / 4 / 5 rows) and no fourth block appears; `TOOLS / MODELS / RULES` are built once — the callout heads — and appear elsewhere only as the `--ink-4` whisper; hot spots: exactly the blue headings (02), the red hinge against green (03), nothing in 04; seams as §16.8, the 03 skyline visibly crossing into 04 and the rail continuous through every seam; the crop test on 04's lower-left must lose the ground field between the paragraph and the creed; panel believability (gutter rule continuous, numbers right-aligned, the light in the bar, `Ln 1, Col 1`, no wrapped row at 1440/1920); marginalia on their anchors (a `//` note level with a short last line or in a gap, never beside a 41-character line — 02's fifth line is 31 characters, 03's fourth 31); the whispers 24 px inside the content edge and the `//` notes on it; a stop tick crossing the rail at each artefact's top with its coordinate beside it; `KEEP THE EVIDENCE.` and `SPLITBRIEF HOLDS` on one line at every tier; the tree's `//` column aligned; no fragment reading as a fourth line of any list or a tail of any row (≥ 24 px clear, and beside a list block at least the block's own width); the footer's `°` at the wordmark's cap; the spark parked on the footer rule at max scroll and on the seam before the ride; at 390 the tick-lists as `·` rows, no panel wider than 350, wrapped panel rows hanging under their first line, the tree's notes under their files with no blank row, the footer claim whole under the wordmark, no dot behind any text.

**v2 build, 2026-09-09 — the assertions as built.**

- **Item 2's per-section trace clause is measured, not removed.** After the 2026-09-10 ruling (traces placed first, panel lines exempt from the same-line rule) the floors are: at 1440, `.s04` ≥ 1 trace and `.s02` / `.s03` 0 — the last two cannot hold one, proven by an exhaustive origin scan against an empty placement set; at ≥ 1600 all three carry at least one. `aura.e2e.ts` asserts `.s04` ≥ 1 at 1440 and all three ≥ 1 at 1920, and asserts nothing about `.s02` / `.s03` at 1440, because a test that asserted 0 there would pin a limitation rather than guard a requirement. See §16.5's build note for the table.
- **Item 2's counts are floors of 6 / 4 / 14**, not 10 / 7 / 12 — see §16.5's build note.
- **Item 7's keep-clear text set is every TEXT NODE under `.grid`**, walked with a `TreeWalker` and measured per line with `Range.getClientRects()`. D-18 had proposed element boxes as the stricter form; they are not stricter, they are differently shaped — a Range over a block container returns block-sized rects, which both over-reserve (a fragment 120 px below a paragraph's last line is rejected) and under-reserve (panel rows, tree rows, steps and the tick-list were not covered at all). Per-line rects freed more area than the newly covered rows consumed: the rendered counts ROSE.
- **Item 8's first clause compares `.lower`, not the full page.** Two full-page captures 4 s apart are NOT reliably byte-identical, and every difference is in the hero: over ten fresh-browser runs, four differed, one of them by 10 701 pixels at a maximum channel delta of 21 inside the nav + hero band, the rest by 5 pixels at delta 4 on the two tool labels. The three `.stage canvas` ghost boxes are a recorded nondeterminism of this build. Clipped to the `.lower` box, twelve of twelve runs are byte-identical to the byte. The region was the wrong instrument, not the tolerance. The clip is a page-coordinate `clip` passed to a `fullPage` screenshot, NOT an element screenshot: Playwright scrolls an element into view before capturing it, which fires the reveal observer and makes the same test's `.is-in` count 3 where the un-scrolled page has 0.
- **Item 8's second clause is a LAYOUT equality, not a pixel one.** A reduced-motion page and a settled default page are compared on page height, the three `.grid` boxes, the `.reveal` count and all 303 text-node rects, all of which were measured equal before the assertion was written. The pixel form it replaces compared two million-element buffers and was unwinnable.
- **Buffer comparisons use `expect(a.equals(b)).toBe(true)`, never `toEqual`.** `toEqual` on two 1.1 MB screenshots does not fail — it spins inside the Playwright worker rendering a diff of two million-element structures, where the 30 s test timeout cannot preempt a synchronous loop.
- **"Two capture sets byte-identical" carries a two-part tolerance**: no pixel differs by more than 4, AND at most 0.05 % of pixels differ by more than 1. Measured worst case over ten capture pairs and 120 files is a maximum of 4 and 0.012 %. The residue is vignette-gradient dithering, spread over the whole page — 14.8 % of pixels at exactly one least-significant bit and 0.009 % above it on the narrow viewports — so masking the ghost canvases does not remove it (measured: masked, the over-tolerance counts are unchanged). A single larger number would bound only the amplitude; the two-part rule bounds the extent as well, and anything structural still fails it by orders of magnitude.
- **The dot-field density cap is measured on lit cells** (D-24): count × 77 px² over the six field specs, against 3 % of the lower page's 1440 × 1764. Canvas boxes give 3.2 % and are mostly empty, so they are the wrong denominator. Measured: 83 lit cells, 0.25 % of the area, worst row 30.0 % against the 35 % cap.

**v2 build, 2026-09-10: the transcript frames scroll by document position, instantly.** `tests/e2e/frames.e2e.ts` must reach `.s03` with `scrollTo({ top: el.getBoundingClientRect().top + scrollY, behavior: 'instant' })`, not with `scrollTo(0, el.offsetTop)`. Two independent reasons, both measured: `.lower` is positioned, so it is the offsetParent of all three sections and `.s03`'s `offsetTop` is 557 against a document top of 1697; and `scroll-behavior: smooth` on `html` makes the scroll an animation, which cannot run while the capture test holds the clock paused, leaving `scrollY` at 80. Before this was fixed the three `<tag>-s03-*.png` frames showed the hero. The capture tests assert nothing — they write files — so only opening a PNG can catch a wrong one.

**v2 build, 2026-09-10: the tier assertions.** `tests/e2e/tiers.e2e.ts` pins §16.7 at 1360 / 1200 / 1024 / 768 / 390 / 1600, one test per width. Its 390 test ranges over `.lower .grid > *:visible`, not `.lower .grid > *`: three of the fifteen grid children are the `.marg` and `.marg-list` whispers, which are `display: none` below 1360 and report `x 0 · width 0`. The twelve visible children are each exactly `x 20 · width 350`.

### 16.10 Copy — every string of the lower page, in source order

**Nav / hero steps (§3):** `01` `HERO` · `02` `BRIEFS` · `03` `VALIDATION`.

**02:** aura — `§02` (stop) · fragments from `POOL_BRIEFS` (§16.5) · `02` · `BEFORE ANY CODE` · `ONE FILE.` `ONE BRIEF.` `NOTHING ELSE.` · `BRIEFS` `CONTRACTS` `CONTEXT` `CLARITY` · panel bar `tasks.md` `Ln 1, Col 1` `markdown` · rows 1–36 as §16.1 · `The planner writes one brief per file. The implementer sees only its own brief — not the spec, not the plan, not the other tasks — and runs it in fresh context, with no memory of the last one.` · `ONE FILE.` `ONE BRIEF.` `NOTHING ELSE.` · `A brief that fails its checks never reaches an implementer. splitbrief validates every brief upfront — scope, file, dependencies, signature — and stops at the first domain or contract error.` · `CLEAR INPUTS.` `SAFER OUTPUTS.` · `// contracts over context` · `SPEC` `PLAN` `BRIEFS` `EXECUTE` `REVIEW` · `less context` `more progress.`

**03:** aura — `T002` (stop) · fragments from `POOL_VALIDATION` · `03` · `AFTER EVERY TASK` · `VALIDATE.` `RETRY.` `ESCALATE.` `KEEP THE EVIDENCE.` · panel bar `splitbrief run` `task: T002` `14:28:16` · rows 01–21 as §16.2 · `Correctness is not the implementer's to judge. After every task splitbrief runs typecheck, lint and test in your project and stops at the first failure.` · `A failed task retries with the error attached, up to three times. Then it escalates: a mid‑tier model if configured, a hint from the planner, or a human review. Every run stores logs, results and evidence.` · `01 HIGHER QUALITY` `02 LOWER SPEND` `03 REAL EVIDENCE` `04 YOUR TOOLS` `05 YOUR RULES` · `// not its own judge` · `VALIDATE` `RETRY` `ESCALATE` `EVIDENCE` `IMPROVE` · `A SMALLER LOOP` `A HIGHER BAR` `REAL PROGRESS`

**04:** aura — `auth-guard` (stop) · fragments from `POOL_CONTROL` · `04` · `FOR THE WHOLE RUN` · `NOTHING RUNS` `OUTSIDE` `WHAT YOU SET.` · `YOU DESCRIBE` `THE GOAL.` `SPLITBRIEF HOLDS` `THE REST.` · `You choose the tools, the models and the limits. splitbrief holds them for the whole run: every brief, every retry and every review runs inside the rules you set, and what it did is on disk when it stops.` · `TOOLS` `planner` `claude‑code` `implementer` `opencode` `reviewer` `codex°` · `MODELS` `fable · deepseek` `gpt-5.6 · qwen3` `any lab in any seat` `swap one mid-run` · `RULES` `max_retries` `3` `max_budget` `5.00 usd` `pause_at` `85 %` `escalation` `qwen3 → planner` `snapshots` `pre_task · post_task` · `CONTRACTS OVER CONTEXT.` `SMALLER LOOPS. BETTER OUTPUT.` · the tree as §16.3 · `// your tools, your models, your rules` · `LIMITS` `BUDGET` `RETRIES` `REVIEW` `EVIDENCE` · `smaller loops` `higher confidence.`

**Footer:** `splitbrief°` · `BUILDS BETTER SOFTWARE.` · `[ docs ]` · `[ github ]` · `PLANS / EXECUTES / REVIEWS`

**Aura pools:** §16.5's three lists, verbatim, traces included; gutter glyphs `∴` `°` `×` `→`; stops `§02` `T002` `auth-guard`.

**Removed with v1 (no longer on the page):** `EXAMPLE ROUTES` and its five rows, `MIX THE LABS. / ONE SHARED BRIEF.`, the manifesto heading, paragraph and list, `NOT ITS OWN JUDGE.`, the six rungs, the ladder's statements and fine print, `AFTER THE RUN`, `THE EVIDENCE. / ON DISK. / IN YOUR REPO.`, the record's statement, tree, small print and `$` lines, `BUILT FOR / PEOPLE WHO / ACTUALLY BUILD`, `[ github.com/b4r7x/splitbrief ]`, `SAME TOOLS. / BETTER OUTPUT.` **Removed in fix cycle 1 (2026-09-09):** 04's serif sub `YOUR TOOLS. / YOUR MODELS. / YOUR RULES.`, its whisper `// nothing runs outside them`, its rail list `TOOLS / MODELS / RULES / LIMITS / EVIDENCE`, the MODELS line `fable · deepseek · gpt-5.6`, 02's statement 2 `…validates every task upfront — typecheck, lint, test — …`, and 03's lowercase `a smaller loop / a higher bar / real progress`.
