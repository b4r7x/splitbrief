# Exec Progress — website v2
input: plan.md itinerary | mode: light, sequential | started: 2026-09-09 11:41
baseline: website/ final sweep CLEAN 2026-09-09 (typecheck 0 · lint 59 clean · test 45/45 · e2e 18 passed / 3 shot-skips) — re-run at 3.P1 preflight
references: reference-v2.png · current-1440.png · reference-v1-hero.png · shots: shots/ · design: website/DESIGN.md §16

| Stage | Status | Cycles | Notes |
|---|---|---|---|
| 0 materialize | done | — | 11:41 run dir, prompt.md (prompt + 7 rulings), references copied, ledger written |
| 1 design sheet §16 + comps | done | 1 | design-v2 done 12:22: DESIGN.md §1 v2 tokens (--panel, --red, type/rail tokens; own-JS cap 16 KB gz), §2/§3/§4 steps/§8/§9/§10/§11 FHD tier/§13/§14 (5 captures)/§15 one-liner/§16.1–16.10 (~270 lines); design-notes.md (brief, measured reference table, 3+3 directions, FHD = capped --content-max 1392 at ≥1600 with full-bleed aura, gate+floor, 3-round critique, self 8/10); comps/lower-page.html + lower-{1920,1440,390}.png + 1920 folds s02/s03/s04/foot + lower-geometry.json. Orchestrator looked at lower-1920 / -s02 / -s04 / -390: 02+03 match the reference, 04 = editorial colophon (breath right, 3 callouts, session tree), 390 single column; aura faint in stills. Accepted designer's open items: comp names lower-*, evidence path = product session path, ▶ in --ink-3, new 04 strings (owner: no gates). |
| 1r design critic | done | 2 | critic-design 12:47: 6/10 REVISE, 7 blocking — F1 spark view() subject must be the rail; F2 dot fields behind text at 390 (hide < 768); F3 02/03 fidelity ~20 rows out of ±3 % (panels +20/24 % tall, 41 vs 39 ch, whispers on the rail not the content edge, invented 32 px gap); F4 390 tree blank rows; F5 390 footer wrap + inline footer spec in §16.4; F6 --highlight undefined; F7 ruling 3 — lower page quieter than the hero (raise fragments 10/7/12, rail stops, traces, panel paint, shimmer). 13 carry (F8–F20). Orchestrator ruling on F3 (owner away, per ruling 1 + plan 'reference authoritative for 02/03/footer'): the sheet pulls back to the reference — 41 ch, reference gaps, 11/14 px panel type, whispers at the content edge; product-true copy deviations stay recorded. design-fix-1 (fable, fresh) dispatched 12:48 for F1–F20. design-fix-1 done 13:38: F1–F19 applied to DESIGN.md + comp (rail as view-timeline subject + wake + spark y table; dots hidden < 768; 02/03 row 310/494/1fr, panel type 11/13 (12/14), 41ch+1px measure, whispers at the content edge; span.row tree; footer inlined in §16.4 + wrap; --highlight; fragments 12/7/16 + 9 traces + 3 rail stops + panel paint + gutter draw + shimmer; §16.9 visual-diff block; h2 = title; marg aria-hidden; seeds 8090–8092), F20 kept (owner's call). visual-diff-comp.md: 59 rows, 49 within ±3 %, 10 outside with recorded reasons. Renders overwritten (same names; -foot deleted = -s04). Self 8/10. Re-critique 13:58: 8/10 SHIP, 0 blocking — F1–F20 17 resolved, F17 as ruled, F18 partly (comp-only collisions), F20 open by ruling; 10 out-of-tolerance rows accepted with reasons; ruling 3 met (35 strings + 6 traces + 5 fields + 3 stops + lit spark); gate rows 1–6 pass; feel earned. Carry F21–F24 (comp keep-clear, §16.5 same-line clearance sentence, §16.6 flare timing, .marg dash knock-out) → design-fix-1 applies them 14:00 before the spec (no re-critique; carry items). F25 owner residuals: ▶ ink-3, Build→Verify vs EXECUTE/REVIEW, no closing CTA. design-fix-1 14:05: F21–F24 done (comp passes §16.9 item 7 at 1440/1920 — checked box by box; §16.5 same-line + gutter-glyph rules; only .stop::before flares; .marg text-shadow knock-out); renders + geometry regenerated. STAGE 1 COMPLETE. |
| 2 spec | done | 1 | spec-architect done 15:02: requirements.md (22 REQ, 9 non-goals, 7 constraints, Design, 26 Decisions) · spec.md (executor context, gates G-*, crew/ladder, brief-compile rule, ruling-8 + critic protocols; phases P1a 12 · P1b 6 · P2 3 · P3 3 · P4 3 · P5a 9 · P5b 7 · P6 2 = 45 one-file briefs + 16 fable tasks; coverage + file-map + wc -l checks). 9 recorded deviations from §13/§16 (footer.css in P1; no aura/layer|pool — placer → fragments/place.ts, aura/{dots,keep-clear,reveal}.ts, ticker → lib; one .fragment class; e2e split 7 files; 10 PNG + 2 hero JSON per tag; hero untouched = hero-box JSON + byte-identical crops; pre-reveal opacity:0 scoped :not(.is-in); 24 px anchor hit box; item 8 masks instead of seek 0). |
| 2r spec review + fix (fable, one-shot) | done | 1 | spec-review-fix 15:22: ACCEPT — 20 findings / 20 fixed in place / 0 open (6 blocking: view-timeline currentTime throws → skip non-document timelines in settle/seek + T-511 proof; phone canvas count narrowed to canvas.ghost; motion.css unscoped .steps → new T-121; T-104 selector li > *; re-derived counts; T-403 1920 heights). Spec 694 lines, 62 tasks (46 cursor + 16 fable). · owner 14:20: no opus; ONE fable agent reviews spec.md for completeness and applies the fixes itself in the same pass (no architect ping-pong) |
| 3.P1 markup/footer/nav/shots-1920 | done | 1 | P1b: 14/14 briefs done, Phase 1 exit green (G-build/G-shots p1/G-cap/G-tree, HERO-JSON(p1) empty), .splitbrief/runs/2026-09-09-171455-web-v2-p1b, 21:32 |
| 3.P1c critic | folded | — | owner 15:35: no per-phase Fable critic; cursor review only; one Fable critique at stage 4 |
| 3.P2 section 02 | done | — | sections run (P2+P3+P4 merged, `.splitbrief/runs/2026-09-09-174656-web-v2-sections`): T001 `s02.css` 29/40 and T005 `s02.e2e.ts` 167/200, gates PASS, accepts 9/9 and 5/5, drift none, contract 9/9 rules 0 deviations, `s02.e2e.ts` 6 of 6 tests pass |
| 3.P2c critic | folded | — | owner 15:35: no per-phase Fable critic; cursor review only; one Fable critique at stage 4 |
| 3.P3 section 03 | done | — | same run: T002 `s03.css` 66/70 and T006 `s03.e2e.ts` 184/200, gates PASS, accepts 9/9 and 6/6, drift none, contract 18/18 rules 0 deviations, `s03.e2e.ts` 6 of 6 tests pass |
| 3.P3c critic | folded | — | owner 15:35: no per-phase Fable critic; cursor review only; one Fable critique at stage 4 |
| 3.P4 section 04 + footer | done | — | same run: T-401 split by ruling 17:53 (Correction 3 + §13 amendment for T-606). T003 `s04.css` 133/140, T008 `s04-tiers.css` 100/110, T007 `s04.e2e.ts` 199/200; gates PASS, accepts 9/9, 9/9, 6/6; drift none; contracts 27/27 and 21/21 rules 0 deviations; `s04.e2e.ts` 6 of 6 tests pass incl. the footer row and the 390 stack |
| 3.P4c critic | folded | — | owner 15:35: no per-phase Fable critic; cursor review only; one Fable critique at stage 4 |
| 3.P5 aura + scroll | done | — | P5a: 11/11 done, review pass_with_notes. P5b: COMPLETE AND GREEN. 21 briefs (T001-T008 wiring + F001-F013 fixes), every one done on attempt 1, no retry, no rung spent, no red waived, no count or tolerance loosened. Final: G-type 0, G-lint 0, vitest 55/55, e2e 0 failed / 47 passed / 11 skipped (19.3s); G-build, G-cap, G-tree all PASS; HERO-JSON empty diff vs p0@1440 and vs p1 at both widths; HERO-CROP(p1,p5) byte-identical UNMASKED; REQ-015 5/5 pairs under Correction N; Phase 3 review pass_with_notes with NO CRITICAL FINDINGS (attempt 2, cap reached). Rendered .s02 7 / .s03 5 / .s04 16 / hero 19 with zero keep-clear violations of any class. Ten spec corrections recorded for T-606 (F,G,H,I,J,K,L,M,N,O,P) - the user-visible one is P: no dotted traces at 1440, proven unachievable by 0/3000 seeds plus an exhaustive origin scan, strings kept in the pools for the >=1600 gutters. Residuals R-P5b-10 (mid-width vignette dithering, admitted by Correction N) and R-P5b-11 (frames.e2e.ts view-timeline guard, harmless today). Full P6 handover in orchestrator-notes.md. 23:5x |
| 3.P5c critic | folded | — | owner 15:35: no per-phase Fable critic; cursor review only; one Fable critique at stage 4 |
| 3.P6 tiers + craft | done | — | P6 run `.splitbrief/runs/2026-09-10-000035-web-v2-p6` (orchestrator phase-p6). 4 briefs, 5 attempts, 1 ladder rung (F001 attempt 1, my brief's fault, recorded). T001 `tiers.e2e.ts` 125/200 lines with 6 tests, one per width, all green; T002 `README.md` 11/12 lines; F001 `scroll.e2e.ts` fixes a reduced-motion test that was comparing the HERO; F002 `frames.e2e.ts` fixes the transcript frames, which scrolled to `.s03`'s `offsetTop` (557, measured from the positioned `.lower`) instead of its document top (1697) and did it smoothly under a paused clock, so every `*-s03-*.png` in the P5 and P6 sets showed the hero — found by OPENING the frame, since the capture tests assert nothing (Correction T, R-P6-10). Final gate GREEN: typecheck 0 · lint 0 · vitest 55/55 · e2e 53 passed / 11 skipped / 0 failed. Phase exit: G-build PASS (25.63 kB JS, 10.34 gz), G-shots p6 (10 PNG + 2 JSON + 11 frames), G-cap PASS, G-tree PASS, HERO-JSON(p6) empty vs p0@1440 and vs p1 at both widths, HERO-CROP(p1,p6) [None,None] UNMASKED, REQ-015 5/5 pairs under Correction N (worst case 8 px at delta 1 on fold.png). p5 vs p6 captures: 9 of 10 byte-equal, the tenth differing only in the 6 px spark column at x 1293-1299 (R-P1b-1's recorded intermittency). Lighthouse desktop a11y 96 / perf 100, mobile a11y 96 / perf 94 (R-P6-8, cause measured: the render-blocking Google Fonts stylesheet under throttling; preconnect already in place). CDP a11y tree clean — no `.marg`, `.stop`, `.aura` or rail node, no canvas node, `--ink-3` on `--panel` 4.93. Four new spec corrections for the record: Q (T-601's 390 clause scoped to VISIBLE grid children), R (§16.9 item 8's first clause compares `.lower`, not the full page — measured, the differences were all hero ghost canvases), S (the `handoff/` mirror carries what exists; the twelve `visual-diff-p<N>` / `critique-p<N>` files were never written because every per-phase critic task was skipped by ruling; v1 evidence preserved under `handoff/v1/`), and T (the transcript frames scroll by document position, instantly). Docs done: `DESIGN.md` 13 dated `v2 build` notes, `HANDOFF.md` v2 145 lines, `handoff/` mirror, `RUN/a11y-p6.md`, `RUN/residuals.md` with 10 P6 residuals. T-604/T-605 skipped by team-lead instruction. **Ruling 3 answered by measurement: no dotted trace is placed at ANY width, not only 1440, and Correction P's stated reason is wrong — at 1920 the traces are legal but never reached because they are last in their pools. No area was freed; the only lever is pool order, a §16.5 change, put to the team lead. R-P6-1.** Status cell left `pending` deliberately: the orchestrator was instructed to update only this Notes cell. Phase 3 review `pass_with_notes`, 8 criteria PASS / 5 FAIL, **0 Critical**, cycle 1 of a cap of 2 (attempt 1 killed by SIGTERM at 207 s with no terminal record; same packet re-spawned, no rung). Every FAIL is a ruled skip (T-604, T-605 and the twelve mirror paths they would have written), the mobile-performance residual, or the status cells this seat was told not to touch. The reviewer also caught a FALSE PASS in this run's own record — F001's `grep -c fullPage → 0` accept was written for attempt 1's contract and cannot hold on attempt 2's `fullPage: true, clip`; true value 2, corrected in `validation.md` and struck from the brief. F001's behaviour was never in doubt. **Team-lead ruling 2026-09-10 re-opened the phase and it was the right call:** place each layer's traces BEFORE its strings and exempt panel lines from the same-line rule. Measured first, offline against the real dumped keep-clear, then live — the offline model predicted the browser on all nine numbers exactly. Result: traces now render in ALL THREE sections at 1600 and 1920, and in .s04 at 1440. .s02 and .s03 at 1440 place none and cannot (0 of 200 seeds; exhaustive scan gives .s03 zero legal origins against an empty set), so their trace floor is 0 and the test asserts nothing there. CONTROL seed 8997 -> 8490 to hold the count floor of 14 at 1440 — no floor lowered, no clearance weakened. `claude‑code` takes U+2011 so it stops breaking at its hyphen at 1200/1024/768. A keep-clear red surfaced and was fixed at the cause: the placer accepted a pair at 60.30 px that RENDERED 59.54 px apart, so its internal SPREAD is now 62 to guarantee the specified 60 — zero cost to any count. Corrections Q, R, S, T plus the §16.5/§16.9 amendments and Correction P corrected. 8 briefs, 9 attempts, 1 rung. FINAL: typecheck 0 · lint 0 · vitest 55/55 · e2e 54 passed / 11 skipped / 0 failed; G-build, G-shots(p6 re-captured), G-cap, G-tree PASS; HERO-JSON empty vs p0@1440 and vs p1 at both widths; HERO-CROP(p1,p6) [None,None] UNMASKED — the hero is provably untouched by the placer work. 12 residuals (R-P6-1…12), 2 of them closed by this ruling. |
| 3.P6c critic | folded | — | owner 15:35: no per-phase Fable critic; cursor review only; one Fable critique at stage 4 |
| 3.5 nuke-review | done | — | Independent review of the whole `website/` delta by four read-only `cursor-grok-4.6-high` seats in `--mode plan`, split by dimension (a correctness · b structure/conventions · c CSS/render · d tests), skepticked by the orchestrator against measurement, fixed by `cursor-grok-4.6-xhigh-fast`. Run `.splitbrief/runs/2026-09-10-015754-web-v2-nuke-review`, findings `RUN/nuke-review-findings.md`. **5 fix seats, 5 attempts, 0 rungs, 0 reds waived, no count/cap/tolerance loosened.** 12 candidates → **6 Major fixed, 5 Minor/Info to residuals, 1 REJECTED by measurement.** Majors: F-001 the hero's keep-clear was measured in VIEWPORT space and applied in LAYER-LOCAL space — reproduced, six hero fragments land on the h1 and lede after any resize while scrolled (0 at scroll 0), fixed in `main.ts` and provably a no-op at scroll 0 (HERO-CROP [None,None] unmasked); F-002 `aura.e2e.ts`'s origin-spread assertion read `boundingBox()` mid-hover-translate, not the ORIGIN — true origin minima 70.8/82.4/72.6 at 1440/1600/1920 against a rule of 60, while the old instrument gave 61.76 at 1440 (passing by 1.76 px of luck) and 51.5 at 1920 (would have failed); F-003 that test ran at 1440 only, now at 1440/1600/1920 — verified satisfiable FIRST by running the file's own predicate at all three widths (zero violations of any class at t=0s and t=10s); F-004 §16.9 item 4's hero-fold clause (`.works` bottom ≤ 1080 at 1920) was asserted nowhere despite having regressed once — now asserted, measured 1078.53, 1.47 px of headroom; F-005 nav.css's UNSCOPED phone `.links` rule halved the footer's link gap to 12 px against §16.4's 24 — scoped to `.nav .links`, guarded by a new 390 assertion; **Correction U / F-011** the gate HUNG for ten minutes on `motion.e2e.ts`'s two Buffer `toEqual`, printing 60 of 66 tests green and exiting 0 on a `Terminated: 15` — the exact banned construct §16.9 already names, found by `sample(1)` on a 99.6 %-CPU worker showing `Builtins_StringEqual`; both pairs measured 10/10 stable fresh AND under load first, so no red was hidden, only the RESULT of six tests. Rejected: seat (d)'s Major claiming the spark samples a scroll that never lands under a paused clock — `scrollY` measured EXACT at every sample at both widths (523/1046/1569/2092 and 496/992/1488/1984). FINAL GATE: typecheck 0 · lint 0 · vitest 55/55 · e2e 55 passed / 11 skipped / 0 failed (20.2 s, all 66 reported, from a suite that hung indefinitely before N005). Exit: G-build PASS (25.98 kB JS, 10.44 gz), G-shots p7 (10 PNG + 2 JSON + 11 frames), G-cap PASS, G-tree PASS, HERO-JSON(p7) empty vs p0@1440 and vs p1 at BOTH widths, HERO-CROP(p1,p7) [None,None] UNMASKED, REQ-015 5/5 pairs under Correction N (worst 8 px at delta 1 on one `fold.png`). Drift NONE — the changed set is exactly the six files the five instructions named. p6 vs p7: nine of ten shots byte-identical, the tenth (`390.png`) differing in ONE 89×14 px box, the `[ github ]` link moving to its spec'd gap — the whole visible delta of the stage, and the intended one. Seven residuals R-NR-1…7; R-NR-7 is new and worth the next reader's time: `frames.e2e.ts` captures are not reproducible run to run (proven by two consecutive captures of one build) because it pauses only `.s03`'s animations while section 04's reveal runs on in the same viewport — R-P6-10's blind spot, one line to fix. |
| 4 final critique + fixes | done | 1 | Final render critique (`final-critic-2`, opus — the Fable window had not reset) returned **7/10 REVISE, 2 blocking**, eight fix rows. Team-lead ruling: build F-1, build the half of F-2 that restores what §16.8 already specifies, build the two unambiguous polish rows, send the other five to residuals with the critic's text. Followed exactly; nothing new designed. Run `.splitbrief/runs/2026-09-10-web-v2-final`, pointer `RUN/current-splitbrief-run.final`. **Seven content briefs, F001–F007, every one on attempt 1 — zero ladder rungs, zero reds waived, zero counts, caps or tolerances loosened. The `handoff/` mirror copy (F008 and its repeats) re-ran after each closing append, so its brief count is not a measure of work.** Correction V — `footer.css`'s column stack opened at `max-width: 767px`, one pixel below the 768 tier, so at 768 both calls to action broke between their label and their own closing bracket (`[ docs` / `]`); raised to 899 and verified at 390/700/768/899/900/1024/1440 by text-node rects, one box on one line for both links at every width. Correction W — the two relief dot profiles were sub-perceptual at 1×: `skyline` `.34 → .61`, `ground` `.24 → .43`, `column` untouched (10 cols is already 30 % of a row, and `.dots--desk` is R-NR-1's field). Lit 83 → 133, every worst row under the 35 % cap, area 10 241 of 76 205 px², computed against `lib/noise` BEFORE the spawn; no canvas box moves, so the placer's keep-clear input is byte-identical and every count and trace holds at all three widths. Rendered ink inside the canvases doubles (03 0.237 → 0.510 %, 02 0.152 → 0.325 %, 04 0.198 → 0.317 %). **The critique's stated cause for F-2 is wrong and is recorded as such: the 03 skyline does NOT stop short — measured, exactly 44.0 px of it paint inside `.s04`, as §16.8 specifies. Only its density was short.** Density was chosen over geometry on measurement: an eight-variant sweep against the live placer shows every enlargement into `.s03`'s legal area costs a count (44 cols → 3 at 1440, floor 4) or a trace (36 × 14 → 0 at 1600 and 1920). Correction X — the mono repeat of `ONE FILE. / ONE BRIEF. / NOTHING ELSE.` is `display: none` below 768; at 390 the columns collapse and it read as a duplicated paragraph. That produced the run's only red, `s02.e2e.ts:161` at 94.92 px over its ±3 px pin, and it was **not waived**: the tolerance stands and the pinned constant was re-derived (1513 → 1418.078), with `DESIGN.md` §16.1 and §16.8 carrying the same number in the same pass. The 02 left void is answered by density, not a seed: measured, the 64 px travel band plus the tick-list's 24 px clearance plus the canvas leave NO legal origin there, and growing 02's canvas up costs its ≥ 1600 trace. Every brief proved to be exactly its contract by reversing the edit and re-hashing against `baseline-hashes.txt`. One false accept of this run's own struck with its reason (`dots.ts` is 112 lines, not the brief's 113). One infrastructure red distinguished from a real one — a Chrome LAUNCH timeout from an orphaned Playwright browser (pid 19508, parent 1, up 2 h 32 m) plus this session's `vite dev`; cleared, and the next gate ran in 20.3 s with all 66 tests reported. FINAL GATE GREEN: typecheck 0 · lint 0 (2 inherited warnings) · vitest 12 files / 55 tests · e2e 55 passed / 11 skipped / 0 failed (20.3 s, summary printed). Exit: G-build PASS (25.98 kB JS, 10.44 gz), G-shots `p8` (10 PNG + 2 JSON + 11 frames), G-cap PASS (markup 196/220, file 337/380), G-tree PASS, HERO-JSON(p8) empty vs p0@1440 and vs p1 at BOTH widths, HERO-CROP(p1,p8) [None,None] UNMASKED, REQ-015 **5 of 5 pairs with all 60 file comparisons byte-identical** — Correction N's tolerance was not needed at any width. The p8 captures were opened at 1920, 1440, 768 and 390 and both blocking fixes read as fixed. Five residuals R-F-1…R-F-5 carry the critic's own text for the owner: 03's missing tail element, the three 04 callouts' internal grids, the two unanswered `°` footnote markers, 04's absent hot spot, and the nav dot mark. |
| 4v verify + handoff | pending | — | nuke-verify with `intent: RUN/spec.md` in a fresh session |

### sections run (P2+P3+P4) — CLEAN 19:28
8/8 briefs done on one real attempt each, all inside caps (s02 29/40 · s03 66/70 · s04 133/140 · s04-tiers 100/110 · main.css +4 imports · s02/s03/s04 e2e 167/184/199 of 200); every sheet diffed rule-by-rule vs its After contract, 0 deviations; 18 new e2e tests pass (suite 21 → 39 passed, 9 skipped, 0 failed; vitest 55/55); review pass_with_notes, 34 criteria PASS, 0 Critical, no fix cycle. Deviations ruled: merged main.css brief, skipped per-phase critics, s04 split (§13 amendment for T-606). Residuals: s02.e2e phone bound is ±3 px where T-203 says ±3 % (stricter, brittle — normalise in P6); phase-end gates for P2–P4 still owed at the final Fable pass.

### P1b — CLEAN 21:33
14/14 briefs done (1 ladder rung, T001's alarm). Gates: G-type 0 · G-lint 0 · G-test vitest 55/55, playwright 54/57 (3 failures all p5b's aura/scroll e2e) · G-build 0. Phase 1 exit: G-shots(p1) 10 PNG + 2 hero JSON · HERO-JSON(p1) empty vs p0 · three 1920 rows pass (.works bottom 1070.5 ≤ 1080 fold) · G-cap vs the approved cap table · G-tree zero paths outside website/, nothing staged. No cursor review cycle; 14 per-brief accept records in validation.md. 5 residuals incl. the REQ-015 deferral to P5. Captures p1-*.

### P5 — CLEAN 00:05
P5a 11/11 + P5b 21/21 briefs, all attempt 1 in P5b. typecheck 0 · lint 0 · vitest 55/55 · e2e 0 failed / 47 passed / 11 skipped. G-build, G-cap, G-tree PASS. HERO-JSON empty vs p0 at 1440 and vs p1 at both widths; HERO-CROP(p1,p5) byte-identical unmasked. REQ-015 PASS 5/5 under Correction N (delta ≤ 4 and ≤ 0.05 % over delta 1). Review pass_with_notes, no Critical. Rendered fragments: s02 7 · s03 5 · s04 16 · hero 19, zero keep-clear violations. 10 spec corrections for T-606. Residuals R-P5b-10 (mid-width vignette dithering, admitted by Correction N) and R-P5b-11 (missing view-timeline guard in frames.e2e.ts, harmless today). **Correction P — visual, for the owner's eye: the dotted traces are unachievable at 1440 (0 of 3000 seeds; .s03 has zero legal origins) and the assertion was removed; the strings stay in the pools so the ≥1600 gutters can still show them. P6 to check whether freeing area restores them.**

### P6 — CLEAN 01:55
9 briefs, 10 attempts, 1 rung, zero drift. Gates 54 passed / 11 skipped / 0 failed; every exit gate PASS; hero byte-identical unmasked at both widths; REQ-015 5/5 with all 60 file comparisons byte-identical; review pass_with_notes, no Critical. Traces restored by the pool-order + panel-line ruling: one per section at 1600 and 1920, .s04 also at 1440; .s02/.s03 at 1440 proven impossible (0 of 200 seeds, zero legal origins) and floored at 0. Sub-pixel defect fixed at cause (placer accepted 60.30 px that rendered 59.54; internal spread 60 → 62). `claude‑code` takes U+2011. Docs done: DESIGN 13 build notes + §14 capture-tests-assert-nothing warning, HANDOFF 147 lines, handoff/ mirror with v1 preserved. 12 residuals; the three for the owner: 1440 traces, mobile Lighthouse 94, and the placer-vs-rendered-geometry class (R-P6-12, audit deferred to nuke-review).

### 3.5 nuke-review — CLEAN 03:00
Four grok seats (correctness / structure / CSS-render / tests). 12 candidates → 6 Major fixed by grok, 5 Minor to residuals, 1 rejected by measurement. Majors: hero keep-clear measured in viewport space but applied in layer-local space (6 fragments on the h1/lede after a scrolled resize); the origin-spread assertion read boundingBox mid-hover instead of the origin (passing at 1440 by 1.76 px of luck, violated at 1920); that test ran at 1440 only, now three widths; §16.9 item 4's .works fold clause was asserted nowhere despite regressing once (now 1078.53, 1.47 px headroom); nav.css's unscoped phone rule halved the footer link gap; the gate hung 10 min on two Buffer toEqual and exited 0 on SIGTERM after reporting 60 of 66 tests. Final: typecheck 0 · lint 0 · vitest 55/55 · e2e 55 passed / 11 skipped / 0 failed, all 66 reported. All exit gates PASS, HERO-CROP(p1,p7) clean unmasked, REQ-015 5/5, drift none. Residuals R-NR-1…7.

## Usage-window cuts
- 2026-09-10 03:06 — the final render critique (`final-critic`) hit the Fable limit before it began; the 17:32 window had not reset. Re-dispatched at 03:08 on opus as `final-critic-2`, with a note that the critique is the one place the owner wanted Fable's eye. If the owner wants it re-run on Fable in the morning, the prompt and the capture set are unchanged.
- 2026-09-09 17:32 — phase-p1a (fable) hit the Fable limit mid fix-cycle 1 (after T008 attempt-3 / T009 attempt-2 landed, before the batch gate + review cycle 2). Owner switched the session to Opus 5. Resumed 17:40 as phase-p1a-2 (opus) from the run dir state; all later orchestrators run on opus.
