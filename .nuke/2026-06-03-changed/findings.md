# Findings Ledger
scope: working-tree changes vs HEAD (111 files: 92 M / 2 D / 17 new) | started: 2026-06-03 | status: closed (user-directed final round)
counts: 0 critical / 3 high / 11 medium / 39 low / 22 info

## Confirmed

### F-001 · high · correctness · src/app/keys.ts:83-94, src/features/workflow/screen.tsx:66-97, src/features/workflow/hooks/use-workflow-runner.ts:206-222, src/stores/workflow/actions.ts:19-21
Attach client: Ctrl+C no longer exits and locally cancels the remote-mirrored workflow, freezing the UI. HEAD's Ctrl+C handler had an explicit attach guard: `if (currentScreen === 'workflow' && route.attach) { exit(); return; }` so a single Ctrl+C in an attach client exited immediately. The rewritten handler (keys.ts:83-94) drops that guard entirely. In attach mode `useWorkflowRunner` is created with `enabled: !isAttachedClient` (screen.tsx:88), so its effect returns early (use-workflow-runner.ts:207) and never registers abort/cancel handlers — yet the local `lifecycleStore.phase` is driven by remote events via `onEvent: addEvent` (screen.tsx:95), so `isLivePhase(phase)` is true. First Ctrl+C therefore hits `fireInterrupt(interruptWorkflow)` → `interruptTurn()` → `abortTurn()` returns false (no handler) → `requestCancel()` → `markCancelled()` sets local `cancelled = true`. From then on `addEvent` early-returns when cancelled (actions.ts:21), so the attach client drops ALL further remote events and freezes showing a 'cancelled' state, while the remote workflow keeps running. Worse, the user must press Ctrl+C twice to exit instead of once.
Fix: Restore the attach short-circuit in the Ctrl+C handler: when `route.screen === 'workflow'` and `route.attach` is set, call `exit()` immediately (the attach client is a thin remote viewer with no local turn to interrupt).
round: 1 · skeptic: confirmed (Confirmed end-to-end. HEAD's Ctrl+C handler had an explicit attach short-circuit (`if (currentScreen === 'workflow' && route.attach) { exit(); return; }`) that the rewritten handler drops. In attach mode `useWorkflowRunner` runs with `enabled: !isAttachedClient` so it never registers abort/cancel handlers, yet `lifecycleStore.phase` is driven by remote IPC events via `onEvent: addEvent`, so `isLivePhase(phase)` is true. The first Ctrl+C therefore takes `route.screen==='workflow' && !cancelled && isLivePhase(phase)` -> `fireInterrupt(interruptTurn)` -> `abortTurn()` returns false -> `requestCancel()` -> `markCancelled()` sets local `cancelled=true` (returns 'workflow'), then arms 'exit' (so exit needs a SECOND Ctrl+C). From then on `addEvent` early-returns on `cancelled` (actions.ts:21), dropping all further remote events and freezing the mirror in a 'cancelled' state while the remote workflow keeps running (cancel never crosses IPC; no cancel handler registered). The new ESC handler is also active in attach mode (`isActive: route.screen==='workflow' && ...`) and arms 'interrupt' the same way, compounding the issue. The proposed fix (restore the `route.attach` short-circuit -> `exit()`) is minimal and correct; `route` is in scope. Severity high: breaks the primary exit interaction of an attach client and leaves a silently frozen UI.)

### F-002 · high · errors · src/engine/orchestrator/final-review.ts:142-194, src/engine/orchestrator/run/phases.ts:258-289, src/cli/commands/resume.ts:24-30
Failed final review persists 'final-review' phase that resume can never recover or complete. Goal #2 of the changeset makes final review a blocking gate: when runPlannerReview throws, final-review.ts sets reviewStatus='failed', skips the REVIEW_DONE transition, and returns without calling onComplete (lines 142-194). The last persisted transition is ALL_DONE (line 50), so state.json is left in phase 'final-review'. On resume, resume.ts loads that state verbatim and runs the workflow. runPlanningPhases skips planning (isInterruptedPlanningTurn only matches researching/specifying/planning, not final-review), then runTasksAndReview calls runTaskLoop with all tasks already done and currentTaskIndex past the end, so the loop is a no-op returning status:'complete'. The new guard at phases.ts:260 `taskResult.status !== 'complete' || state.phase !== 'implementing'` is then true (phase is 'final-review', not 'implementing'), so it short-circuits to a summary with completed:false and NEVER reaches runFinalReviewPhase at line 272. There is no other call site of runFinalReviewPhase and nothing transitions 'final-review' back to 'implementing'. End-to-end: a failed planner review becomes a permanent dead-end — every resume re-runs cost prediction, does nothing, and returns 'interrupted', so the session can never reach 'complete'. No test resumes a 'final-review' state. Before this change final review was advisory and always advanced to 'complete', so this regression is introduced by the changeset.
Fix: On resume (or in runTasksAndReview) detect state.phase === 'final-review' and re-enter runFinalReviewPhase instead of routing through the implementing-only task loop, so a previously failed review can be retried to completion.
round: 1 · skeptic: confirmed (A failed planner review now deliberately persists phase 'final-review' (final-review.ts:50 ALL_DONE saved, then the new reviewStatus==='failed' return at lines 164-194 skips REVIEW_DONE/onComplete), and that phase is advertised as resumable (core/phases.ts:51, assertResumableState passes). But on resume the saved 'final-review' state is loaded verbatim; isInterruptedPlanningTurn (phases.ts:46-51) does not match 'final-review' so planning is skipped, runTaskLoop is a no-op returning status:'complete' (loop.ts:49 currentTaskIndex===totalTasks), and the new guard at phases.ts:260 (`status!=='complete' || state.phase!=='implementing'`) short-circuits because phase is 'final-review' — so runFinalReviewPhase (its only caller, phases.ts:272) is never reached and machine.ts:87 lets 'final-review' transition only via REVIEW_DONE, never back to 'implementing'. Every resume re-runs cost prediction, does nothing, and returns 'interrupted'; the session can never reach 'complete'. Before this change a failed review still advanced to 'complete' (verified in git HEAD:final-review.ts), so the persistable dead-end is introduced by this changeset; no test resumes a 'final-review' state (phases.test.ts:953 only tests the forward run leaving phase 'final-review').)

### F-003 · high · correctness · testing/integration/orchestrator/cli-planner-cli-implementer.test.ts:156-158, testing/integration/orchestrator/cli-planner-cli-implementer.test.ts:221-229, testing/integration/orchestrator/full-loop-validation-retry.test.ts:104-106, testing/integration/orchestrator/full-loop-validation-retry.test.ts:576-587
New sandbox-env integration tests fail under /tmp symlink: normalizeMacTmpPath only collapses /private/var, breaking the test gate. Both new tests assert the child's sandbox env equals join(runLog.cwd, SANDBOX_DIR, ...) after passing both sides through normalizeMacTmpPath, which only rewrites /^\/private\/var\//. Trace: createStagedProject (staged-project.ts:50) builds stagedProjectDir from os.tmpdir() = '/tmp/...' and createSandboxEnv (sandbox-env.ts:6-8) derives HOME/TMPDIR/etc from that non-realpath path, so the child receives HOME='/tmp/.../.diptych-sandbox/home'. But the child reports process.cwd() = realpath = '/private/tmp/.../...' (the test's runLog.cwd), so the expected side becomes '/private/tmp/.../home'. Because '/tmp' is a symlink to '/private/tmp' on macOS and the helper only normalizes '/private/var', the two never reconcile. I ran both files: cli-planner-cli-implementer fails at line 221 and full-loop-validation-retry's OpenCode subtest fails at line 576 with Expected '/private/tmp/...home' vs Received '/tmp/...home'; it reproduces with TMPDIR=/tmp and with TMPDIR unset (os.tmpdir() resolves to /tmp here). This deterministically breaks `npm test` and the `test-ci` gate (CLAUDE.md: 'Zero failing gates').
Fix: Broaden normalizeMacTmpPath in both files to strip the macOS '/private' symlink prefix generally (e.g. replace(/^\/private(\/(?:tmp|var)\/)/, '$1') or realpathSync both operands before comparing) so /tmp and /private/tmp reconcile alongside /var and /private/var.
round: 1 · skeptic: confirmed (Both new integration tests deterministically fail in environments where os.tmpdir() is a symlink under /tmp (this sandbox: /tmp/claude-501 -> /private/tmp/claude-501). The child's process.cwd() returns the realpath (/private/tmp/...) while sandboxEnv.HOME/TMPDIR retain the symlink path (/tmp/...), and normalizeMacTmpPath only rewrites /^\/private\/var\//, so /tmp vs /private/tmp never reconcile. I ran both files and they fail at exactly the cited lines (221 and 576: Expected '/private/tmp/...home' vs Received '/tmp/...home'), breaking `npm test` and the test-ci 'Zero failing gates' rule. The fix (realpathSync both operands, or broaden the regex to strip /private for both tmp and var) is a real, minimal improvement, not churn.)

### F-004 · medium · correctness · src/cli/render.ts:29-72, src/lib/terminal/filtered-stdin.ts:224-234
SIGINT termination handler skips fullscreen alt-buffer restore, leaving the terminal in the alternate screen. The new SIGINT/SIGTERM handler (render.ts:62-72) runs `cleanup()` then `process.exit(code)` synchronously. `cleanup` only kills children and calls `filteredStdin.disable()` (which toggles mouse/SGR/bracketed-paste off, filtered-stdin.ts:224-234) — it never writes `\x1b[?1049l`. fullscreen-ink restores the main screen buffer only inside `cleanUpOnExit`, which `await`s `instance.waitUntilExit()` (node_modules/fullscreen-ink/dist/esm/withFullScreen.js:14-17); calling `process.exit()` from the signal handler terminates the process before that async restore can run. So when a real SIGINT arrives during fullscreen (the documented case: a child owns the TTY in cooked mode and Ctrl+C reaches the OS), the process exits with the terminal stuck in the alternate-screen buffer, requiring `reset`. This handler is new in the changeset (HEAD render.ts had no SIGINT handler).
Fix: In the termination handler's cleanup, emit the alt-buffer exit sequence (`process.stdout.write('\x1b[?1049l')`) and restore cursor/raw mode before `process.exit`, or only register the handler in non-fullscreen mode and let Ink's signal-exit cleanup restore fullscreen.
round: 1 · skeptic: confirmed (The new SIGINT/SIGTERM handler (render.ts:29-72, all changeset-new) calls process.exit(code) synchronously after a cleanup that only runs killAllProcesses + filteredStdin.disable(); disable() (filtered-stdin.ts:224-234) writes only the mouse/SGR/bracketed-paste toggles via setTerminalInputModes(false), never the alt-buffer exit. The alt buffer (\x1b[?1049h/l) is owned solely by fullscreen-ink's async cleanUpOnExit, which awaits waitUntilExit() and is NOT a signal-exit handler, so process.exit() terminates before it can write \x1b[?1049l — leaving the terminal stuck in the alternate screen on a signal-during-fullscreen (e.g. external `kill -TERM` while fullscreen, deterministic), requiring `reset`. Ink's signalExit restores raw mode but never touches 1049, confirming the gap. Real, anchored in changeset, not a ledger duplicate, not an intentional convention; emitting \x1b[?1049l in cleanup before exit is a genuine fix.)

### F-005 · medium · performance · src/engine/orchestrator/approval/file-snapshots.ts:76-101, src/engine/orchestrator/approval/file-snapshots.ts:41-55, src/engine/orchestrator/approval/staged-project.ts:24-31, src/engine/orchestrator/approval/staged-project.ts:48-69, src/engine/orchestrator/escalation/step.ts:47
Staged no-git gate re-reads full content of every tracked file on each gate (and at capture), forced by newly excluding .git from staged copies. The change adds '.git' to STAGED_COPY_EXCLUDE (staged-project.ts:24-31), so a staged project never has git metadata. getChangedFilesSinceSnapshot therefore always takes the new getChangedFilesSinceBaselineContents branch (file-snapshots.ts:103-111) for staged dirs, which calls collectTrackedFiles (full recursive readdir + a git check-ignore subprocess) and then readFile-loads the FULL content of EVERY tracked file to string-compare against baselineFileContents (file-snapshots.ts:84-94). The baseline itself was produced by captureProjectFileContents, which also full-reads every tracked file (file-snapshots.ts:41-55). Before this change the staged copy kept .git and used the fast git-status path. In runRetryStep, createStagedProject runs once per retry/escalation attempt (step.ts:47) — so a failing task with maxRetries plus hint/full tiers performs roughly 2 full-repo content scans per attempt (capture + gate), i.e. O(repo bytes) re-read many times. The sibling module change-detection.ts (same changeset, captureFileHashes at change-detection.ts:14-28) solves the identical no-git-baseline problem with streaming SHA hashes that hold only a 64-char digest per file, proving the cheaper pattern is available in-repo.
Fix: Have the staged no-git baseline store per-file hashes (reuse hashFile/captureFileHashes from change-detection.ts) instead of full file contents, and diff by hash so unchanged files are never fully re-read into memory on each gate.
round: 1 · skeptic: confirmed (Real regression: the change added '.git' to STAGED_COPY_EXCLUDE (staged-project.ts:29), so a staged project has no git metadata and getChangedFilesSinceSnapshot always takes getChangedFilesSinceBaselineContents (file-snapshots.ts:107-111), which readFile-loads the FULL content of every tracked file. The baseline itself (captureProjectFileContents, file-snapshots.ts:41-55) also full-reads every tracked file and retains the entire repo's content in baselineFileContents. createStagedProject runs once per retry attempt (step.ts:47), so each attempt does 2 full-repo content scans plus repo-sized memory retention; HEAD kept .git and used the fast git-status path. baselineFileContents is consumed only by the detector, so the sibling change-detection.ts hash pattern (hashFile/captureFileHashes) is a correct, cheaper drop-in.)

### F-006 · medium · errors · src/engine/orchestrator/approval/gate-and-promote.ts:138-152, src/engine/orchestrator/approval/staged-project.ts:78-105, src/engine/orchestrator/approval/file-snapshots.ts:152-166, src/engine/orchestrator/escalation/step.ts:85-103
Staged-promotion throw escapes gate-and-promote uncaught, skipping cleanup and crashing the retry/task flow. The changeset adds path-confinement asserts to the promote path: assertPathConfined in promoteStagedChanges' file loop (staged-project.ts:79-82) and assertWritablePathConfined in writeCurrentFileContent (file-snapshots.ts:157,164). promoteStagedChanges is called in gate-and-promote.ts:140 OUTSIDE the only try/catch (that try, lines 71-85, wraps just getChangedFilesSinceSnapshot and is itself only swallowed when catchChangedFilesError is set). When a target file's parent is a symlink escaping the project (exactly the case the new staged-project.test.ts:213-237 'does not promote through a symlinked target parent' proves rejects with /unsafe path/), or any fs write error occurs, the throw propagates out of gateAndPromoteChangedFiles before cleanup?.() at line 152 runs. Neither caller wraps the call: escalation/step.ts:85 has no try/catch around it (its only catch, lines 64-67, surrounds invokeRetry), and step.ts's local `staged` is unreachable to any caller, so the os.tmpdir() staged dir leaks. Pre-change, writeCurrentFileContent had no asserts and never threw on a symlinked target (it silently wrote through it), so this new throw path has no error contract — a defensive security check becomes an uncaught workflow crash plus temp-dir leak instead of a clean promote-conflict/error outcome.
Fix: Wrap the promoteStagedChanges call in gate-and-promote.ts (lines 139-150) in try/catch that runs cleanup?.() and returns a 'promote-conflict' or 'error' outcome (matching how path-escape/write failures should surface), so the new path-confinement and fs-write throws are handled rather than escaping uncaught and leaking the staged temp directory.
round: 1 · skeptic: confirmed (Verified: the changeset adds throwing path-confinement asserts to the promote path (assertPathConfined in promoteStagedChanges staged-project.ts:79-82, assertWritablePathConfined in writeCurrentFileContent file-snapshots.ts:157,164). promoteStagedChanges is awaited at gate-and-promote.ts:140 OUTSIDE the only try/catch (lines 71-85, which wraps only getChangedFilesSinceSnapshot). Both callers — escalation/step.ts:85-103 (its catch only wraps invokeRetry, lines 49-67) and apply-changed-files.ts:32-61 — leave this call unwrapped, so the throw skips cleanup?.() (gate-and-promote.ts:152) and leaks the os.tmpdir() staged directory, converting a clean per-task promote-conflict/error outcome into an uncaught workflow failure. The cited test (staged-project.test.ts:213-229) proves the symlinked-target-parent case rejects with /unsafe path/. Pre-change writeCurrentFileContent never threw, so this is a new error contract.)

### F-007 · medium · correctness+security · src/engine/orchestrator/escalation/run-escalation-tier.ts:203-212, src/engine/orchestrator/escalation/run-escalation-tier.ts:236-262, src/engine/planners/escalation.ts:28-58, src/engine/planners/cli.ts:95-119, src/engine/planners/cli.ts:163
Tier-1 planner hint escalation runs unsandboxed: caller omits sandboxEnv/fileIgnoreProjectDir, CLI planners spawn and write to the real project with the real environment. The changeset threads a best-effort sandbox (staged dir + redirected HOME/XDG/npm/pip/cargo env) through every implementer retry path and the tier-2 full planner escalation. escalateHint (escalation.ts:48-54) was given sandboxEnv/fileIgnoreProjectDir parameters for the same purpose, but its only caller, runHintTier in run-escalation-tier.ts:203-212, passes only {task,error,projectDir:ctx.projectDir,callbacks,languageContext} and omits both optional params. Because EscalateOptions marks them optional (planners/types.ts:145-146), this compiles. For a CLI-kind planner (codex/opencode/aider used as planner), capabilities spread ONE_SHOT_API_CAPS so supportsHintEscalation is true (cli.ts:163) and hintSuccessMode is 'files' (cli.ts:158); escalateHint then captures a change-detection baseline on the REAL ctx.projectDir (escalation.ts:43-47, fileIgnoreProjectDir undefined) and invokeEscalate spawns the tool via spawnAndCollect with cwd:ctx.projectDir and env:undefined (cli.ts:98-99), i.e. the real process.env with no HOME/XDG/cache redirection. The tool writes files directly into the real project before any staging/promotion gate runs, defeating the sandbox the rest of the change builds. (The default claude-code planner is unaffected: CONVERSATIONAL_CAPS sets supportsHintEscalation:false so escalateHint returns early at escalation.ts:33-35.)
Fix: In runHintTier, stage the project (or reuse the staged project) and pass sandboxEnv and fileIgnoreProjectDir into ctx.planner.escalateHint exactly as runFullTier/runRetryStep do, or run hint escalation through the same staged runRetryStep machinery so CLI-planner hint writes land in the sandbox and are gated before promotion.
round: 1 · skeptic: confirmed (runHintTier calls ctx.planner.escalateHint directly (run-escalation-tier.ts:203-212) passing only task/error/projectDir(=real ctx.projectDir)/callbacks/languageContext, omitting the now-optional sandboxEnv and fileIgnoreProjectDir (types.ts:145-146). For a CLI-kind planner (ONE_SHOT_API_CAPS → supportsHintEscalation:true, hintSuccessMode:'files', cli.ts:158/163), escalateHint then spawns the tool with cwd:ctx.projectDir and env:undefined (real process.env) (cli.ts:99,156-157), and captures the baseline against the real projectDir with fileIgnoreProjectDir undefined (escalation.ts:43-47) — so hint-tier writes land in the real project with the real env before any staging/promotion gate, unlike runFullTier which receives sandboxEnv/fileIgnoreProjectDir through runRetryStep (step.ts:61-62). Default claude-code (CONVERSATIONAL_CAPS, supportsHintEscalation:false) is unaffected.)

### F-008 · medium · correctness · src/engine/orchestrator/run/phases.ts:258-270, src/engine/orchestrator/final-review.ts:50, src/engine/orchestrator/final-review.ts:164-194, src/core/phases.ts:41-52, src/stores/navigation/session-select.ts:10-39
Resuming a failed-final-review session dead-ends forever (never re-runs review). The completion-gate change (final-review.ts:164-194) now leaves the workflow persisted in phase 'final-review' (no REVIEW_DONE) when the planner review fails, saved as status 'interrupted'. 'final-review' is in RESUMABLE_PHASES (core/phases.ts:51) and is reported live by isSessionLive, so the TUI session list (session-select.ts:25 isResumable→true) lets the user resume it. On resume the flow runs init → runPlanningPhases (skipped, not interrupted planning) → runTasksAndReview. runTaskLoop returns status:'complete' (all tasks done) but state.phase stays 'final-review', so the NEW guard at phases.ts:260 (`state.phase !== 'implementing'`) short-circuits to a summary with completed:false and never calls runFinalReviewPhase. runFinalReviewPhase is the only caller of the review and unconditionally dispatches ALL_DONE (final-review.ts:50), which is illegal from 'final-review' (machine.ts phaseActions['final-review']=['REVIEW_DONE']) and would throw — the guard was added precisely to avoid that throw, but it provides no path to re-enter review. Result: every resume re-loads 'final-review', runs an empty task loop, hits the guard, and returns interrupted again — the session is resumable but can never be advanced or completed. At HEAD this was unreachable because a failed review still transitioned REVIEW_DONE→complete; the changeset both creates the stable resumable 'final-review' state and blocks re-entry. No integration test covers resume-from-final-review.
Fix: In runTasksAndReview (or earlier in the resume path) detect state.phase === 'final-review' and call runFinalReviewPhase via a branch that does NOT re-dispatch ALL_DONE (review the already-final-review state directly), so a resumed failed-review session can retry the review and reach 'complete'.
round: 1 · skeptic: confirmed (The new completion-gate early-return (final-review.ts:164-194) leaves a failed review persisted in phase 'final-review' (verified by test final-review.test.ts:271). 'final-review' is resumable, so the TUI offers resume. On resume, all tasks are done so runTaskLoop falls through to status 'complete' with phase still 'final-review' (loop.ts:204), and the new guard at phases.ts:260 (state.phase !== 'implementing') short-circuits to completed:false, never calling runFinalReviewPhase. runFinalReviewPhase is the only review caller and unconditionally dispatches ALL_DONE (final-review.ts:50), which is illegal from 'final-review' (machine.ts:87) and throws (machine.ts:197-198). At HEAD a failed review still transitioned REVIEW_DONE->complete (git diff shows the early-return is new), so the changeset both creates the stable resumable state and blocks re-entry; no test covers resume-from-final-review. The session is resumable but can never reach 'complete'.)

### F-009 · medium · correctness+structure+errors+architecture · src/engine/orchestrator/run/phases.ts:288-289, src/engine/orchestrator/final-review.ts:196-228, src/core/state/persistence.ts:21-36, src/engine/orchestrator/final-review.ts:31-46, src/engine/orchestrator/final-review.ts:193-228
final-review completion status laundered through disk instead of returned. Intent #2 makes final review a completion gate: on planner-review failure `runFinalReviewPhase` leaves the workflow in `final-review` (no `REVIEW_DONE` transition, final-review.ts:193 returns before line 196), and only on success transitions to `complete` and calls `onComplete`. The function holds the authoritative final `state` in its local var but its signature returns only `Summary` (final-review.ts:46). To recover whether review succeeded, the caller `runTasksAndReview` now re-reads state from disk: `const finalState = loadState(...); return { summary, completed: ... && finalState?.phase === 'complete' }` (phases.ts:288-289). The in-memory `state` the caller still holds is stale after the call. The success/failure signal is round-tripped through the filesystem (and depends on `transitionAndSave`/`saveState` having synchronously persisted, state-ops.ts:47) rather than flowing through the function contract — a leaky boundary and hidden temporal coupling introduced by this changeset.
Fix: Return the final `WorkflowState` (or an explicit `{ status: 'complete' | 'final-review-failed' }`) from `runFinalReviewPhase` and have `runTasksAndReview` derive `completed` from that value instead of calling `loadState` to re-read the phase from disk.
round: 1 · skeptic: confirmed (runFinalReviewPhase holds the authoritative final state locally and transitions to 'complete' only on success (final-review.ts:196 REVIEW_DONE) but returns only Summary (final-review.ts:46). The caller re-reads the just-written state from disk via loadState to recover the completion signal: `const finalState = loadState(...); return { summary, completed: ... && finalState?.phase === 'complete' }` (phases.ts:288-289). This round-trips a value the function already held through the filesystem and leaves the caller's in-memory `state` stale — a leaky boundary/temporal coupling introduced by this changeset; returning the final WorkflowState (or an explicit status) is a clean fix. No functional defect (saveState/loadState are synchronous), hence structure-level medium.)

### F-010 · medium · correctness · src/engine/runners/sandbox-env.ts:5-33, src/engine/orchestrator/task/run-implementation.ts:43-78, src/engine/implementers/base.ts:204-208, src/engine/implementers/cli.ts:32-58
Sandbox HOME/XDG redirect breaks credential & config lookup for direct CLI implementers (claude-code, codex, npm, etc.). createSandboxEnv returns process.env with HOME, XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_CACHE_HOME, npm_config_cache, CARGO_HOME, PIP_CACHE_DIR all repointed at empty freshly-mkdir'd dirs under the staged copy's .diptych-sandbox. run-implementation.ts:43-44 sets usesStaging = (writesFiles === 'direct') for EVERY direct-writing implementer, and base.ts:306 makes writesFiles === 'direct' equivalent to extractsCode === false — which is true for ALL cli/agent/agent-sdk/command implementers. The sandboxEnv is threaded through base.ts:207 → cli.ts:32/agent-sdk.ts:37/command-invoke.ts:52 into the spawned subprocess. Before this changeset these subprocesses inherited the real process.env (real HOME); now a staged claude-code/codex/opencode/aider/copilot subprocess sees an empty HOME and XDG_CONFIG_HOME, so OAuth tokens in ~/.claude/.credentials.json, ~/.codex/auth.json, ~/.config/* and private-registry tokens in ~/.npmrc are no longer found. Tools that did not export ANTHROPIC_API_KEY (i.e. logged in via `claude login`) will silently fail to authenticate inside staged runs, which is the default path for direct implementers.
Fix: Either limit the HOME/XDG redirect to tools that authenticate via env (keep real HOME for known CLI tools), or seed the sandbox HOME/XDG_CONFIG_HOME with copies of the credential/config files the configured tool needs (e.g. ~/.claude, ~/.codex, ~/.npmrc) so authenticated CLI implementers keep working under staging.
round: 1 · skeptic: confirmed (Genuine changeset-introduced regression. HEAD's staged-project.ts had no env redirect (verified git show HEAD); the new createSandboxEnv repoints HOME, XDG_CONFIG/DATA/CACHE_HOME at empty freshly-mkdir'd dirs and threads it into every staged direct CLI/agent/command/agent-sdk subprocess (run-implementation.ts:77, cli.ts:42, command-invoke.ts:52, agent-sdk.ts:37, plus all escalation/retry paths). Staged direct implementers are already the default path for direct writers (usesStaging = writesFiles==='direct'). For tools authenticating via OAuth/login rather than env var (claude-code -> ~/.claude/.credentials.json on Linux, codex -> ~/.codex/auth.json, npm -> ~/.npmrc tokens) the credential files under HOME are now unreachable, causing silent auth failure that worked before. Env-var auth (ANTHROPIC_API_KEY etc., diptych's documented path) is preserved via ...process.env, so impact is bounded — hence medium, not high. The added tests only exercise a fake opencode on PATH, never real credential lookup, so the break is untested.)

### F-011 · low · tests · src/app/keys.ts:81-133, src/app/keys.test.tsx:134-262
No test covers the new ESC prompt-pending gate (approval/cost prompt disables ESC arming). keys.ts:81 adds promptPending = approvalPromptStore/costApprovalStore status === 'pending' and keys.ts:133 folds !promptPending into the ESC useInput isActive gate, so an ESC press is silently ignored (no arm, no interrupt, no cancel) while an approval or cost-approval prompt is on screen. This is changed behavior: the gate condition expanded vs HEAD. The ESC ladder describe block in keys.test.tsx (134-262) covers live-phase, question-mode, split-sequence, window-expiry, home-screen and overlay gating, but never sets approvalPromptStore/costApprovalStore to 'pending'; grep confirms neither keys.test.tsx nor testing/integration/ui/esc-through-filtered-stdin.test.tsx ever references those two stores. A regression that drops the promptPending term (re-enabling ESC interrupt during an approval prompt) would pass the whole suite.
Fix: Add one keys.test.tsx (or integration) case that sets approvalPromptStore (and a second for costApprovalStore) status to 'pending' on the workflow screen, presses ESC, and asserts abortStore.armed stays 'none' and no interrupt/cancel fires.
round: 1 · skeptic: confirmed (keys.ts:81 and keys.ts:133 add a new `!promptPending` term to the ESC `isActive` gate (new behavior in this changeset, intent item 8), but no test sets approvalPromptStore/costApprovalStore to 'pending' — grep confirms neither keys.test.tsx nor esc-through-filtered-stdin.test.tsx references those stores, so dropping the `!promptPending` term (re-enabling ESC interrupt during an approval/cost prompt) would pass the entire suite. The store exposes `_approvalPromptInternal.set`/`__testReset`, so the proposed coverage test is actionable.)

### F-012 · low · structure+errors+stack · src/cli/options.ts:8-10, src/core/readiness/collect.ts:87-92, src/core/config/runtime/build-runner.ts:72-79, src/cli/options.ts:43, src/cli/options.ts:68, src/core/config/runtime/overrides.ts:37, src/core/config/runtime/overrides.ts:161-198, src/core/config/runtime/build-runner.ts:61-80, src/cli/options.ts:66-68, src/core/config/runtime/build-runner.ts:65, src/core/config/runtime/build-runner.ts:75
New numeric CLI options parse with Number() and silently drop garbage input. The new parseNumberOption (src/cli/options.ts:8-10) returns Number(value) for --planner-context-length / --implementer-context-length, so a non-numeric argument yields NaN with no error to the user. Tracing the value: workflowOptsToCLIOverrides sets contextLength to the NaN, applyCLIOverrides forwards it into buildRunnerFromOverrides where NaN !== undefined keeps it in opts, resolveGenerationParams does `opts.contextLength ?? ex?.contextLength` (build-runner.ts:65) and `??` does not catch NaN, then spreadGenParams `gen.contextLength && {...}` (build-runner.ts:75) drops the falsy NaN. Net effect: a typo like `--planner-context-length abc` is accepted, the override is silently ignored, AND any previously-configured contextLength is dropped (NaN replaces it before the && guard discards both). This follows the pre-existing house convention (the older --budget flag uses parseFloat with the same NaN behavior, src/cli/options.ts:77), so it is consistent style rather than a regression, but it is an unvalidated numeric boundary on a brand-new flag.
Fix: Have parseNumberOption validate via Number.isFinite(n) (or commander's InvalidArgumentError) and reject non-numeric input instead of producing NaN, so a malformed --*-context-length fails fast at the CLI boundary rather than being silently swallowed.
round: 1 · skeptic: confirmed (The new --planner-context-length / --implementer-context-length flags parse via the new parseNumberOption = Number(value) (options.ts:8-10) with no validation. The main CLI path (collect.ts:88-89 and build-overrides.ts:10) feeds workflowOptsToCLIOverrides output straight into applyCLIOverrides WITHOUT CLIOverridesSchema.parse, so NaN is never rejected. The trace holds: buildRunnerFromOverrides keeps NaN (overrides.ts:191, NaN !== undefined), resolveGenerationParams returns NaN via ?? (build-runner.ts:65 — ?? does not catch NaN, so a previously-configured contextLength is replaced by NaN), then spreadGenParams drops the falsy NaN (build-runner.ts:75). Net: a typo silently drops the override AND any existing configured contextLength. Unlike --budget, which validates with Number.isFinite at overrides.ts:349, the new context-length flags have no such guard. Anchored in changeset; fix (Number.isFinite check) improves it.)

### F-013 · low · simplicity+slop+conventions · src/core/config/runtime/overrides.ts:200-203, src/core/config/accessors/implementer-profiles.ts:40-43
Duplicated stripImplementerProfileMetadata re-implements existing private stripProfileMetadata. The new `stripImplementerProfileMetadata` in overrides.ts:200-203 is logic-identical to the pre-existing private `stripProfileMetadata` in implementer-profiles.ts:40-43: same `const { label: _label, costTier: _costTier, capabilities: _capabilities, ...rest } = profile` destructure followed by `ImplementerConfigSchema.parse(rest)`. overrides.ts already imports `pickDefaultProfileName` from `../accessors/implementer-profiles.js`, so it could trivially reuse an exported strip helper instead of copying it. Both files live in src/core/config/, so the quality bar's rule ('Before duplicating a helper, move it down to the layer all consumers can import rather than copy') applies directly. This is the 2nd occurrence of this exact strip-and-parse helper, introduced by the changeset.
Fix: Export `stripProfileMetadata` from implementer-profiles.ts (or rename it) and import it in overrides.ts, deleting the duplicate `stripImplementerProfileMetadata`.
round: 1 · skeptic: confirmed (New stripImplementerProfileMetadata (overrides.ts:200-203) is logic-identical to the pre-existing private stripProfileMetadata (implementer-profiles.ts:40-43) — same metadata destructure + ImplementerConfigSchema.parse(rest); only the rest-var name differs. Both live in src/core/config/ and overrides.ts already imports from accessors/implementer-profiles.js, so the quality bar's 'move the helper down rather than copy' rule applies. Anchored in the changeset (the new copy was added), not a convention, not in the ledger.)

### F-014 · low · simplicity+dry · src/core/config/runtime/overrides.ts:271-316, src/core/config/runtime/overrides.ts:274-287, src/core/config/runtime/overrides.ts:293-313, src/core/config/runtime/overrides.ts:29-38, src/core/config/runtime/overrides.ts:90-99
applyCLIOverrides re-spreads each RunnerOverrides field conditionally instead of passing the already-shaped object. RunnerOverrideSchema (overrides.ts:29-38) infers exactly { tool?, model?, command?, apiBase?, apiKey?, args?, outputFormat?, contextLength? }, all `| undefined` under exactOptionalPropertyTypes (tsconfig exactOptionalPropertyTypes:true), which is structurally identical to the RunnerOverrides interface (overrides.ts:90-99). So `overrides.planner` is already a valid RunnerOverrides. Yet the planner block (lines 274-287) reconstructs an identical object via eight `...(overrides.planner.X !== undefined && { X })` spreads, and applyRunnerOverrides already no-ops on all-undefined input (lines 162-173). The change extended this re-spread from 3 fields to 8 fields across two blocks. The planner block could be `applyRunnerOverrides('planner', overrides.planner, next)`; the implementer block is identical except contextLength falls back to the top-level overrides.contextLength (lines 309-312), the only field needing real merge logic.
Fix: Pass `overrides.planner` directly to applyRunnerOverrides('planner', …); for the implementer, pass `{ ...overrides.implementer, ...(merged contextLength) }`, deleting the ~30 lines of conditional field re-spreading.
round: 1 · skeptic: confirmed (The planner block (overrides.ts:274-287) reconstructs an object structurally identical to overrides.planner via 8 conditional spreads, then passes it to applyRunnerOverrides which already no-ops on all-undefined input; buildRunnerFromOverrides treats present-but-undefined keys identically to absent ones, so overrides.planner could be passed directly. The implementer block is the same except the top-level contextLength fallback (309-312) is the only real merge. Tests assert resulting config, not the strip mechanism, so the simplification preserves behavior.)

### F-015 · low · types · src/core/config/runtime/overrides.ts:29-38, src/core/config/runtime/overrides.ts:90-99
Duplicated runner-override shape: RunnerOverrides interface vs RunnerOverrideSchema extended in lockstep. `RunnerOverrideSchema` (Zod, lines 29-38) and the `RunnerOverrides` TS interface (lines 90-99) define the identical 8-field optional shape (tool, model, command, apiBase, apiKey, args, outputFormat, contextLength). They are structurally equivalent: `RunnerOverrideSchema.outputFormat` is `OutputFormatSchema.optional()` (= `OutputFormat | undefined`) and `RunnerOverrides.outputFormat` is `BuildRunnerOpts['outputFormat'] | undefined` (also `OutputFormat | undefined`); contextLength is `number | undefined` in both. This changeset extended BOTH definitions in lockstep with the same five new fields (apiBase/apiKey/args/outputFormat plus moved contextLength), doubling the manual-sync surface from 4 fields to 8 and creating a divergence hazard (a future field added to one but not the other compiles silently). The schema is the single source the `z.infer<CLIOverrides>` and parse path already use; the parallel interface is redundant.
Fix: Replace the standalone `RunnerOverrides` interface with `type RunnerOverrides = z.infer<typeof RunnerOverrideSchema>` (deriving the type from the schema) so the override shape is single-sourced and cannot drift.
round: 1 · skeptic: confirmed (RunnerOverrideSchema (29-38) and the RunnerOverrides interface (90-99) are structurally equivalent 8-field shapes — outputFormat is OutputFormat|undefined on both (OutputFormatSchema.optional() vs BuildRunnerOpts['outputFormat']|undefined, where BuildRunnerOpts['outputFormat']=OutputFormat). The changeset extended both in lockstep, doubling the manual-sync surface; deriving the type from the schema single-sources the shape, matching the bar's single-source rule. Real, anchored, not in ledger.)

### F-016 · low · correctness+structure+conventions · src/core/keybindings/registry.ts:21-32, src/features/help/overlay.tsx:49-56, src/app.tsx:136
Duplicate keybinding key 'Esc Esc' produces duplicate React keys in the help overlay on the workflow screen. The changeset added two SHORTCUTS entries with the identical `key: 'Esc Esc'` (id `interrupt` lines 21-26 and id `cancel` lines 27-32), both scoped to `screens: ['workflow']`. `getShortcutsForScreen('workflow')` (registry.ts:52-54) returns both. The help overlay renders each shortcut as `<Box key={s.key}>` (overlay.tsx:50), so on the workflow screen React receives two siblings with the same key `Esc Esc`. The overlay is reachable on the workflow screen: app.tsx:136 renders `<HelpOverlay currentScreen={screen}>` and workflow-chrome.tsx:99 passes `currentScreen="workflow"`, while Ctrl+/ opens the help overlay regardless of screen. This triggers React's 'two children with the same key' warning and unstable reconciliation of those two rows. Before this change every registry key was unique, so the change introduces the collision.
Fix: Give the two `Esc Esc` rows distinct React keys (e.g. `key={s.id}` in overlay.tsx) or merge the `interrupt`/`cancel` entries into a single registry row since they share the same key and screen.
round: 1 · skeptic: confirmed (The changeset adds two new SHORTCUTS entries with the identical key 'Esc Esc' (ids 'interrupt' and 'cancel'), both scoped to screens:['workflow']. getShortcutsForScreen('workflow') returns both, and the unchanged help overlay renders each as <Box key={s.key}>, so opening Help on the workflow screen (reachable via Ctrl+/ regardless of screen) emits two siblings with the same React key 'Esc Esc' — React's duplicate-key warning. HEAD had no 'Esc Esc' entry, so the collision is introduced by this change. Severity lowered to low: the rows are a static, never-reordered map, so real impact is the console warning, not visible misrendering. Fix (key={s.id}) is correct since id is unique.)

### F-017 · low · slop+dead-code · src/core/types/config-options.ts:18, src/core/types/config-options.ts:27, src/core/config/runtime/overrides.ts:71, src/core/config/runtime/overrides.ts:76, src/core/config/runtime/overrides.ts:66
Dead WorkflowOpts fields plannerApiKey/implementerApiKey: declared and read but never produced. This changeset adds `plannerApiKey?` (config-options.ts:18) and `implementerApiKey?` (config-options.ts:27) to WorkflowOpts, and `workflowOptsToCLIOverrides` reads them as the left operand of `opts.plannerApiKey ?? opts.plannerApiKeyEnv` (overrides.ts:66) and `opts.implementerApiKey ?? opts.implementerApiKeyEnv` (overrides.ts:76). WorkflowOpts is populated solely by commander `.action((..., opts) => ...)` from registered CLI flags, but the only key flags added in options.ts are `--planner-api-key-env`/`--implementer-api-key-env` (options.ts:34,52) which camelCase to `plannerApiKeyEnv`/`implementerApiKeyEnv` — there is no `--planner-api-key`/`--implementer-api-key` flag and no RPC/programmatic setter (grep across src/ and testing/ finds only the declaration and the `??` read; no producer). So both fields are permanently undefined and the `??` always evaluates to the right operand; start.test.ts:348/358 confirms the env path drives the value. The fields and their left-operand reads are dead.
Fix: Delete the `plannerApiKey` and `implementerApiKey` fields from WorkflowOpts and simplify the two `workflowOptsToCLIOverrides` reads to `apiKey: opts.plannerApiKeyEnv` and `apiKey: opts.implementerApiKeyEnv`.
round: 1 · skeptic: confirmed (The fields plannerApiKey/implementerApiKey are declared (config-options.ts:18,27) and read only as the left operand of `??` (overrides.ts:66,76) but have no producer anywhere: a grep across src/ and testing/ for these names (excluding the Env variant) returns ONLY the declarations and the two reads; the only camelCase CLI flags are --planner-api-key-env/--implementer-api-key-env (options.ts:34,52 → plannerApiKeyEnv/implementerApiKeyEnv), and WorkflowOpts is populated solely by commander .action() callbacks from registered flags. So both fields are permanently undefined, the `??` always falls through to the env operand (start.test.ts:348/358 assert apiKey: 'env:PLANNER_KEY' / 'env:OPENROUTER_API_KEY'), and per context.md 'delete dead code (no future use)' + 'no speculative features' this dead code should be removed.)

### F-018 · low · types · src/engine/agent-sdk-backend.ts:191-194, src/engine/implementers/base.ts:96-99, src/engine/implementers/command-invoke.ts:21-24, src/engine/change-detection.ts:55-74
detectChanges callable signature duplicated inline across three files instead of a named ChangeDetector type. The callable shape `(projectDir: string, before: ChangeDetectorBaseline) => Promise<{ changed: boolean; output: string }>` is hand-spelled in three separate type declarations: AgentSdkBackend.detectChanges (agent-sdk-backend.ts:191-194, in this chunk), ImplementerBaseConfig.detectChanges (base.ts:96-99), and CommandBasedImplementerOpts.detectChanges (command-invoke.ts:21-24). `change-detection.ts` already owns the `ChangeDetectorBaseline` type and `createChangeDetector` returns exactly this callable, but exports no named type for it. This changeset had to edit all three copies in lockstep (git HEAD shows each was `before: string[]` and each was changed to `before: ChangeDetectorBaseline`), which is the concrete maintenance hazard the duplicated-type rule guards against; the inline `{ changed: boolean; output: string }` return shape is likewise re-spelled at each site.
Fix: Export a single named `ChangeDetector` type alias from change-detection.ts (e.g. `export type ChangeDetector = (projectDir: string, before: ChangeDetectorBaseline) => Promise<{ changed: boolean; output: string }>`) and reference it in agent-sdk-backend.ts, base.ts, and command-invoke.ts instead of re-spelling the signature.
round: 1 · skeptic: confirmed (The callable shape (projectDir: string, before: ChangeDetectorBaseline) => Promise<{changed: boolean; output: string}> is re-spelled inline at three interface members while change-detection.ts already owns the ChangeDetectorBaseline input type and the canonical createChangeDetector implementation that produces exactly this callable; the changeset edited all three copies in lockstep (before: string[] -> before: ChangeDetectorBaseline), demonstrating the maintenance hazard, and the same engine layer already establishes the idiomatic fix via named callable aliases (EventSink, QueueHandler, AutoSnapshotFn, RendererFunction). Exporting a single ChangeDetector type alias is a genuine DRY improvement aligned with the project bar, not churn; impact is minor since the shapes are currently identical, so low severity.)

### F-019 · low · tests · src/engine/change-detection.test.ts:8-40, src/engine/change-detection.ts:30-39
file-hashes change detection lacks tests for new-file and deletion cases. The new file-hashes code path (`captureChangeDetectorBaseline` → `hasHashChanges`) is the success/failure signal for direct/sandbox implementers when the staged dir has no `.git`. Its only test (change-detection.test.ts) covers exactly two behaviors: (a) ignored additions (`dist/`, `*.log`) do NOT count as a change, and (b) modifying an existing tracked file (`src/app.ts`) DOES count. The two genuinely-new branches of `hasHashChanges` — a key present only in `after` (a new, non-ignored file → must report `changed:true`) and a key present only in `before` (a deleted tracked file → `changed:true`) — are never exercised here. The git-status path's new-file case is covered in base.test.ts:232-280, but that uses the git baseline, not the file-hashes baseline, so the new code is untested for those branches. A regression dropping new/deleted keys from the union would silently make a sandbox implementer report "exited without changing any files" despite real edits, with no failing test.
Fix: Add two cases to change-detection.test.ts file-hashes suite: writing a new non-ignored file (e.g. `src/added.ts`) must yield `{changed:true}`, and deleting a baseline-tracked file (e.g. `src/app.ts`) must yield `{changed:true}`.
round: 1 · skeptic: confirmed (The new file-hashes path's two genuinely-new branches — a key present only in `after` (new non-ignored file) and only in `before` (deleted tracked file) — are exercised by no test. change-detection.test.ts only covers ignored additions (no change) and modifying an existing tracked file (change), and base.test.ts:232-280 runs against a git repo (createTestGitRepo at base.test.ts:16-17), so it takes the git-status branch in base.ts:172, never file-hashes. Since the sandbox no longer copies .git, a new file written by a direct/sandbox implementer is the primary success signal and is untested; a regression dropping new/deleted keys from the union would silently report 'exited without changing any files'. The proposed two test cases assert observable behavior and close a real coverage hole without churn.)

### F-020 · low · performance · src/engine/change-detection.ts:14-28, src/engine/change-detection.ts:55-74, src/engine/snapshots/files.ts:12-20, src/engine/implementers/base.ts:170-262, src/engine/implementers/base.ts:170-175, src/engine/implementers/base.ts:251-252
Unbounded concurrent read-stream fan-out in per-task file-hash change detection (EMFILE risk → silently corrupts change detection). The changeset rewires the implementer base (CHUNK-C) so that for a no-git staged sandbox it captures and re-checks a file-hash baseline instead of a cheap `git status`. base.ts:172 calls captureChangeDetectorBaseline (before invoke) and base.ts:252 calls detectChanges (after invoke); in the no-git path both route into change-detection.ts:captureFileHashes, which does `Promise.all(files.map(hashFile))` over EVERY tracked file with no concurrency cap. hashFile (snapshots/files.ts:12) opens a fresh createReadStream per file, so a real project with thousands of source files opens thousands of file descriptors simultaneously, risking EMFILE. hashFile swallows the stream error and resolves `null` (files.ts:16), so an FD-exhaustion error during the before- or after-pass is recorded as a `null` hash, which can flip the changed/unchanged comparison in hasHashChanges and make a task that wrote files be reported as 'exited without changing any files' (or vice-versa). This is the changeset's intended new sandbox path (staged projects exclude .git), and it runs once before and once after every implementer invocation, per task and per retry.
Fix: Bound the read-stream fan-out in captureFileHashes with a fixed concurrency limit (e.g. batch the files.map over hashFile through a small pool) so descriptor count stays constant regardless of project size.
round: 1 · skeptic: confirmed (captureFileHashes does Promise.all(files.map(hashFile)) with no concurrency cap over the entire staged project (a full cp of projectDir, staged-project.ts:52), and each hashFile opens a fresh createReadStream (files.ts:15). HEAD's change detection used a single git status subprocess, so this fan-out is genuinely new, runs once before (base.ts:172) and once after (base.ts:252) every invoke/retry, and hashFile swallows stream errors to null (files.ts:16) which under FD exhaustion can flip the changed/unchanged comparison in hasHashChanges. Downgraded to low: EMFILE only triggers on very large projects, the no-git staged path is best-effort, and the corruption flip needs FD exhaustion plus coincidental null collision; a bounded pool would be a real non-churn fix.)

### F-021 · low · correctness+simplicity+slop+dead-code+conventions+stack · src/engine/change-detection.ts:2-3, src/engine/change-detection.ts:1-4
Duplicate import statement from the same module in change-detection.ts. Lines 2-3 import two symbols from the same module with two separate plain value-import statements: `import { getCurrentChangedFiles } from '../lib/git.js';` then `import { isGitRepo } from '../lib/git.js';`. Both `isGitRepo` (git.ts:55) and `getCurrentChangedFiles` (git.ts:96) are value exports of `src/lib/git.ts`, and both lines are plain (non-`type`) imports, so there is no type/value-import reason to keep them separate. The diff confirms line 3 (`isGitRepo`) was newly added by this changeset rather than appended to the existing line 2 import — a classic AI-generated artifact of bolting on a new import line instead of editing the existing one.
Fix: Merge the two imports into a single statement: `import { getCurrentChangedFiles, isGitRepo } from '../lib/git.js';`.
round: 1 · skeptic: confirmed (Lines 2-3 import getCurrentChangedFiles and isGitRepo from '../lib/git.js' as two separate value-import statements; the diff confirms line 3 was newly added by this changeset rather than merged into line 2. Every other engine file (e.g. final-review.ts:9 imports two symbols on one line) consolidates multi-symbol git.js imports, so this is a genuine AI slop artifact with a trivial, non-churn fix.)

### F-022 · low · tests · src/engine/implementers/base.ts:170-174, src/engine/implementers/base.ts:204-208, src/engine/implementers/agent-sdk.ts:31-38, src/engine/implementers/command-invoke.ts:39-55
Changed sandboxEnv threading into agent-sdk/api/agent implementers has no direct test in CHUNK-C. The genuinely new behavior in this chunk is `opts.sandboxEnv` threading: base.ts:207 passes `sandboxEnv` into `invoke`, agent-sdk.ts:37 forwards it as the SDK `env`, command-invoke.ts:52 forwards it to the spawned runner, and base.ts:170-174 now uses `captureChangeDetectorBaseline(projectDir, { ignoreProjectDir: opts.fileIgnoreProjectDir })`. Grepping all three CHUNK-C test files for `sandboxEnv|fileIgnoreProjectDir|HOME|TMPDIR|env:` returns nothing, and the agent-sdk 'threads apiKey' test only exercises the `env=undefined, apiKey set` branch (it never sets sandboxEnv, so the changed `...(env ?? process.env)` branch in agent-sdk-backend.ts:251 is never hit). Only the `cli` implementer's env passthrough is proven end-to-end, and that is in CHUNK-J (cli-planner-cli-implementer.test.ts:141,221-227), not here; the agent-sdk and command-based (agent) env paths are unverified.
Fix: Add a CHUNK-C test that sets `sandboxEnv` and asserts it reaches the SDK `options.env` (agent-sdk) and the spawned subprocess env (agent via command-invoke), covering the `...(env ?? process.env)` branch in agent-sdk-backend.ts:251.
round: 1 · skeptic: confirmed (The genuinely-new sandboxEnv threading into the agent-sdk backend (options.env) and the agent/command-based subprocess env has no direct test in CHUNK-C: grepping the three CHUNK-C test files for sandboxEnv/fileIgnoreProjectDir/HOME/TMPDIR returns nothing, and the agent-sdk 'threads apiKey' test only sets apiKey so the new `...(env ?? process.env)` branch is never hit. step.test.ts:130-148 asserts sandboxEnv only at the orchestrator boundary via a mock invokeRetry, and the cli implementer (a distinct spawnAndCollect/runClaudeOneShot path) is the only one proven end-to-end in CHUNK-J. This leaves a coverage gap on security-relevant env-isolation behavior.)

### F-023 · low · types · src/engine/implementers/base.ts:96-99, src/engine/implementers/command-invoke.ts:20-25, src/engine/agent-sdk-backend.ts:191-194
detectChanges function/result shape duplicated inline across 3 sites instead of a named ChangeDetector contract. The change-detector signature `(projectDir: string, before: ChangeDetectorBaseline) => Promise<{ changed: boolean; output: string }>` is spelled out inline in three production sites, all structurally identical to what `createChangeDetector` in `src/engine/change-detection.ts` actually returns. That module already exports the named `ChangeDetectorBaseline` type and the `createChangeDetector` factory, but not a named type for the detector function or its `{ changed: boolean; output: string }` result. This changeset modified two of the three copies (base.ts:96-99 and command-invoke.ts:20-25, changing `before: string[]` -> `before: ChangeDetectorBaseline`) and left the result shape and overall function type re-spelled rather than referencing a single source. End-to-end: `createChangeDetector(label)` (change-detection.ts:55-74) produces the function; consumers in cli.ts/agent.ts/agent-sdk.ts assign it into the `detectChanges?` slot typed by each of the three inline copies — so the producer's true type exists once but is hand-mirrored three times.
Fix: Export a single named type from `src/engine/change-detection.ts` (e.g. `export type ChangeDetector = ReturnType<typeof createChangeDetector>` or an explicit function type alias) and have `ImplementerBaseConfig.detectChanges`, `CommandBasedImplementerOpts.detectChanges`, and `AgentSdkBackend.detectChanges` reference it instead of re-spelling the signature and `{ changed: boolean; output: string }` result.
round: 1 · skeptic: confirmed (The change-detector function signature plus its `{ changed: boolean; output: string }` result is spelled inline at three production sites, and this changeset edited all three `before:` params (string[] -> ChangeDetectorBaseline), so the duplication is anchored in changed lines. `createChangeDetector` is the single producer of that exact shape; exporting `type ChangeDetector = ReturnType<typeof createChangeDetector>` would single-source it per the 'DRY at 3rd occurrence / never re-spell' convention — a genuine improvement, not churn.)

### F-024 · low · types · src/engine/orchestrator/approval/file-snapshots.ts:41-55, src/engine/orchestrator/approval/file-snapshots.ts:76-101, src/engine/snapshots/files.ts:8-10
Inline `{ ignoreProjectDir?: string | undefined }` shape re-spelled instead of reusing the named `CollectTrackedFilesOptions` contract introduced in the same changeset. This changeset adds a named type `CollectTrackedFilesOptions = { ignoreProjectDir?: string | undefined }` in `snapshots/files.ts:8-10` and threads it into `collectTrackedFiles(projectDir, opts)`. The two new CHUNK-E functions `captureProjectFileContents` (file-snapshots.ts:41-55) and `getChangedFilesSinceBaselineContents` (file-snapshots.ts:76-101) both declare the identical shape inline as `opts: { ignoreProjectDir?: string | undefined } = {}` and forward `opts` verbatim into `collectTrackedFiles` (lines 45 and 81). Traced end-to-end: the inline param type is structurally the same contract as the named `CollectTrackedFilesOptions`, and `file-snapshots.ts:12` already imports from `../../snapshots/files.js`, so the named type is in reach. The single-source closed-shape/named-contract rule is mildly violated; drift risk if the option set grows. Typecheck passes (`tsc --noEmit` exit 0), so this is structural, not a compile error.
Fix: Import `CollectTrackedFilesOptions` from `../../snapshots/files.js` and annotate the `opts` parameters of `captureProjectFileContents` and `getChangedFilesSinceBaselineContents` with it instead of re-spelling the inline object shape.
round: 1 · skeptic: confirmed (Verified: the changeset adds and exports the named type CollectTrackedFilesOptions (files.ts:8-10) and threads it into collectTrackedFiles, but the two new functions captureProjectFileContents (file-snapshots.ts:43) and getChangedFilesSinceBaselineContents (file-snapshots.ts:79) re-spell the identical inline shape { ignoreProjectDir?: string | undefined }. file-snapshots.ts:12 already imports from ../../snapshots/files.js and files.ts has no import cycle back, so the named type is in reach; this violates the single-source closed-shape rule. Fix is a one-line import, not churn.)

### F-025 · low · errors · src/engine/orchestrator/approval/staged-project.ts:39-46
shouldCopyToStagedProject silently drops project files when lstat fails. The changeset replaces the original synchronous, never-throwing cp filter (HEAD: `(source) => !isStagedCopyExcluded(basename(source))`) with shouldCopyToStagedProject, whose try/catch returns false on any lstat failure (line 44). A transient lstat error (race with a concurrent delete, permission glitch) is indistinguishable from a deliberate symlink exclusion: the entry is silently omitted from the staged copy, so the implementer/retry runs against an incomplete project tree with no warning. The empty catch swallows a real IO failure as if it were an intended skip.
Fix: In shouldCopyToStagedProject only skip on a confirmed symlink; let unexpected lstat errors propagate (or log) rather than silently returning false, so a missing source file is not mistaken for an exclusion.
round: 1 · skeptic: confirmed (Verified: the changeset replaces the HEAD synchronous never-throwing cp filter (source => !isStagedCopyExcluded(basename(source))) with async shouldCopyToStagedProject whose catch returns false on ANY lstat failure (staged-project.ts:43-45), making a transient lstat error (concurrent delete / permission glitch) indistinguishable from the intended symlink exclusion. The file is then silently omitted from the staged copy and the retry/implementer runs against an incomplete tree with no warning. Only the symlink case is intended per changeset goal 3; swallowing other lstat errors is incidental. Low because the trigger is a rare race during cp recursion.)

### F-026 · low · correctness · src/engine/orchestrator/escalation/run-escalation-tier.ts:297-313, src/engine/change-detection.ts:41-53, src/engine/orchestrator/approval/file-snapshots.ts:37-39
Staged escalation change-detection uses upward git discovery (isGitRepo) instead of local .git check, breaking file-hash detection when TMPDIR is inside a git repo. Tier-2 escalation (runFullTier) calls runRetryStep, which creates a staged project under os.tmpdir() and invokes ctx.planner.escalateFull with projectDir = staged.projectDir and fileIgnoreProjectDir = ctx.projectDir. For file-mode planners (escalateFullMode:'files' in planners/agent.ts, hintSuccessMode:'files' in cli.ts/command-invoke.ts), escalateFull/escalateHint call captureChangeDetectorBaseline(stagedDir,{ignoreProjectDir}), which decides via isGitRepo(stagedDir) = git checkIsRepo(). I verified `git rev-parse --is-inside-work-tree` returns true in a subdirectory of any ancestor git repo (upward discovery). The staged copy intentionally excludes its own .git (STAGED_COPY_EXCLUDE in staged-project.ts:24-31), so the file-hash path is intended; but if os.tmpdir() (or TMPDIR) lives inside a git repo, isGitRepo returns true and the git-status path runs against the unrelated ancestor repo, ignoring ignoreProjectDir and producing wrong/empty change detection — so escalateFull can report false success or false failure. The sibling change-detector in file-snapshots.ts correctly uses hasGitMetadata = existsSync(stagedDir/.git) (local only), proving the two new baseline mechanisms disagree on what 'is a git repo' means for the staged dir.
Fix: Make captureChangeDetectorBaseline use a local-only .git existence check (existsSync(join(projectDir,'.git'))) matching file-snapshots.hasGitMetadata, instead of git's upward-discovering checkIsRepo, so staged copies always take the file-hash baseline path.
round: 1 · skeptic: confirmed (New code captureChangeDetectorBaseline (change-detection.ts:45) branches on isGitRepo = git.checkIsRepo() which does upward git discovery, while the sibling new mechanism file-snapshots.hasGitMetadata (line 37-39) uses local existsSync(.git). Staged dirs created under tmpdir intentionally exclude .git (staged-project.ts:29), so if TMPDIR is inside an ancestor git repo, isGitRepo returns true and the git-status path runs against the unrelated ancestor, ignoring ignoreProjectDir and producing wrong change detection for file-mode escalateFull/escalateHint. The two new baseline mechanisms disagree on what counts as a git repo for the staged dir. Conditional trigger keeps it low, but the divergence is real and inside the changeset.)

### F-027 · low · structure+simplicity+dry · src/engine/orchestrator/final-review.ts:164-228, src/engine/orchestrator/final-review.ts:164-194, src/engine/orchestrator/final-review.ts:196-228, src/engine/orchestrator/final-review.ts:169-193, src/engine/orchestrator/final-review.ts:200-224, src/engine/orchestrator/final-review.ts:79-86
final-review.ts duplicates summary-build + writeReviewPacket block across failed/success branches. The changeset added a new failed-review branch (lines 164-194) that, after the early `return`, runs in parallel with the pre-existing success branch (lines 196-228). Both branches build the identical `summaryOpts` object (169-176 vs 200-207), call `buildSummary(summaryOpts)` to form `preSummary` (177 vs 209), then run a byte-identical `writeReviewPacket({ projectDir, sessionId, summary: preSummary, state, finalReviewStatus: reviewStatus })` inside an identical try/catch with the same `publishWarningFromError('Review packet generation failed', ...)` (178-192 vs 210-224), and finally call `buildSummary(summaryOpts)` again to return. The only real divergence is the success path's extra `transitionAndSave(REVIEW_DONE)`, `workflow_complete` publish, and `callbacks.onComplete`. The change grew a second copy of an existing block rather than computing the review packet / summary once before branching only on the completion side-effects. End-to-end: both `reviewStatus==='failed'` and `'written'` paths flow through this duplicated packet/summary logic, verified against final-review.test.ts which exercises both (status 'failed' at line 273-277, 'written' at 133-141).
Fix: Hoist the shared `summaryOpts` construction, `preSummary` build, and `writeReviewPacket` try/catch above the `reviewStatus === 'failed'` check so it runs once; keep only the success-specific `transitionAndSave`/`workflow_complete`/`onComplete` side-effects inside the branch.
round: 1 · skeptic: confirmed (The changeset added a failed-review branch (final-review.ts:164-194) that duplicates the pre-existing success branch's summaryOpts construction (169-176 vs 200-207), buildSummary→preSummary (177 vs 209), and the writeReviewPacket try/catch with identical publishWarningFromError text (178-192 vs 210-224); only transitionAndSave(REVIEW_DONE)/workflow_complete/onComplete diverge. Real in-function duplication. Kept low: it is only the 2nd occurrence (codebase convention is 'DRY at the 3rd occurrence'), and the candidate's literal 'hoist above the failed check' fix is subtly wrong because the success path intentionally builds the packet/summary with post-REVIEW_DONE state — a correct dedup must preserve that ordering.)

### F-028 · low · tests · src/engine/orchestrator/recovery/builders/task.ts:58-63, src/engine/orchestrator/recovery/recovery.test.ts:139-148
Changed retry-exhausted recommendedAction (override → retry-same-worker) is unasserted. buildRetryExhaustedRecoveryIssue's recommendation preference list changed from `['route-bigger-worker','planner-split-rebase','skip-current-task']` to `['route-bigger-worker','retry-same-worker','pause-run','skip-current-task']` (task.ts:58-63). This introduces a new branch: when route-bigger is unavailable but `allowRetryOverride: true`, chooseRecommended now returns `retry-same-worker`. The only test covering the override case (recovery.test.ts:139-148) asserts `override.availableActions).toContain('retry-same-worker')` but never asserts `override.recommendedAction`, so the newly-recommended `retry-same-worker` value goes unverified. The route-bigger-present case is asserted (line 127 → 'route-bigger-worker'), but the changed override fallback path is not.
Fix: Add `expect(override.recommendedAction).toBe('retry-same-worker')` to the override sub-case in recovery.test.ts:139-148 to lock in the changed recommendation.
round: 1 · skeptic: confirmed (Real coverage gap: the changeset rewrote the retry-exhausted preference list to ['route-bigger-worker','retry-same-worker','pause-run','skip-current-task'] (task.ts:59-64). In the override sub-case (no routeBiggerProfile, allowRetryOverride:true), chooseRecommended now returns 'retry-same-worker', but recovery.test.ts:139-148 only asserts override.availableActions.toContain('retry-same-worker') and never asserts override.recommendedAction, leaving the changed recommendation unverified. Adding the assertion locks in real changed behavior.)

### F-029 · low · simplicity+slop+dead-code+conventions · src/engine/orchestrator/user-edit/conflicts.ts:76-84
Duplicate identical branches in actionsFor after regenerate-rebase removal. The changeset removed 'regenerate-rebase' from the 'future-task-stale-input' branch of actionsFor (git diff line: `return ['continue-unrelated', 'pause', 'abort-workflow'];`). That branch now returns a literal byte-identical to the 'unrelated' branch above it (lines 77-79 vs 80-82), so the two separate `if` blocks no longer serve any distinguishing purpose. Before the change the branches differed (the future-stale branch included 'regenerate-rebase'); the removal collapsed them into duplicates without merging. The kind distinction still matters for `safeToContinue` at line 98, but not for the action list, so keeping two arms is dead structure. Biome's recommended rules do not catch identical branch bodies (different conditions), so the lint gate passes and this slips through.
Fix: Collapse the two arms into one guard: `if (kind === 'unrelated' || kind === 'future-task-stale-input') return ['continue-unrelated', 'pause', 'abort-workflow'];`.
round: 1 · skeptic: confirmed (The changeset removed 'regenerate-rebase' from the future-task-stale-input arm, leaving conflicts.ts:80-82 a byte-identical literal to the unrelated arm at lines 77-79. safeToContinue (line 98) is computed independently, so the two actionsFor arms are now redundant dead structure introduced by this change; collapsing them is a safe, genuine simplification.)

### F-030 · low · tests · src/engine/providers/client.ts:84-89, src/engine/providers/registry.test.ts:67-96, src/engine/providers/errors.ts:51-55
Missing test for the new invalidApiKeyEnvReference error path (empty env: reference). The changeset adds apiKeyEnvReference() (client.ts:84-89) which throws providerError.invalidApiKeyEnvReference() when an apiKey override is exactly 'env:' or 'env:' followed by only whitespace (empty variable name after the prefix). This throw path is reachable from user config/CLI: src/core/config/runtime/overrides.ts:66 sources the override from opts.plannerApiKeyEnv (a CLI flag), so a user passing --planner-api-key-env env: would trip it. The new 'apiKey env references' describe block (registry.test.ts:67-96) covers the happy path (resolves env var), the missing-env-var throw (apiKeyEnvMissing), and the exfiltration interaction, but a repo-wide grep for invalidApiKeyEnvReference / 'env:' literal in any .test.ts returns nothing — this distinct new error branch and its 'expected env:VARIABLE_NAME' contract message are entirely uncovered. Other changed behavior in CHUNK-D is well covered: provider apiKey() resolution is asserted via observable getProvider(...).apiKey(), and the planner escalation sandboxEnv/fileIgnoreProjectDir switch from getCurrentChangedFiles to captureChangeDetectorBaseline is verified at the change-detection layer (change-detection.test.ts).
Fix: Add a test to the 'apiKey env references' block asserting getProvider('openrouter', { apiKey: 'env:' }) (and a whitespace-only variant like 'env:   ') throws matching /env:VARIABLE_NAME/.
round: 1 · skeptic: confirmed (apiKeyEnvReference (client.ts:84-89) throws providerError.invalidApiKeyEnvReference() when apiKey is exactly 'env:' or 'env:' + whitespace. This is reachable from user config (YAML apiKey: 'env:') and from the new CLI flag: parseEnvRefOption('env:') returns 'env:' (options.ts:13), which flows via overrides.ts:66 (apiKey: opts.plannerApiKeyEnv) into getProvider. The new 'apiKey env references' test block (registry.test.ts:67-96) covers happy path, missing-var, and exfiltration but not this empty-reference throw; a repo-wide grep for invalidApiKeyEnvReference / the empty 'env:' literal across all .test.ts returns nothing. New behavior with a distinct error branch left uncovered.)

### F-031 · low · simplicity · src/engine/providers/client.ts:99-105, src/engine/providers/client.ts:164-167
resolveProviderOverrides rebuilds whole overrides object to read back one field. resolveProviderOverrides is a single-use wrapper (only called at line 164) that takes the overrides object, resolves the apiKey via resolveApiKeyOverride, then reconstructs the entire object with a conditional spread `{ ...overrides, ...(apiKey !== undefined && { apiKey }) }`. But its sole consumer createMetadataProvider reads ONLY resolvedOverrides?.apiKey (line 167); apiBase is read separately from the original overrides?.apiBase at line 163, and no other field of resolvedOverrides is touched. The object reconstruction and the extra function layer therefore produce nothing the apiKey string alone wouldn't. The conditional spread is also a no-op for correctness: when apiKey is undefined the literal-key path already passed through overrides.apiKey === undefined, so spreading vs not spreading is identical.
Fix: Delete resolveProviderOverrides and inline `const resolvedApiKey = resolveApiKeyOverride(overrides?.apiKey);` in createMetadataProvider, then use `resolvedApiKey ?? process.env[opts.envKeyName] ?? opts.apiKeyDefault ?? ''` in the apiKey closure.
round: 1 · skeptic: confirmed (resolveProviderOverrides (client.ts:99-105) is a module-private single-use wrapper that resolves apiKey then rebuilds the entire overrides object via spread, but its only consumer createMetadataProvider reads exactly one field, resolvedOverrides?.apiKey (line 167); apiBase is read separately from the original overrides at line 163 and no other resolved field is touched. The object reconstruction and extra function layer are pure indirection over a single resolved string; inlining resolveApiKeyOverride(overrides?.apiKey) is behavior-preserving and removes a layer. Anchored in the changeset (these lines are newly added in the diff) and not below any convention threshold.)

### F-032 · low · types · src/features/workflow/handlers.ts:47, src/app/keys.ts:32, src/app/keys.ts:58, src/app/keys.test.tsx:14, testing/integration/ui/esc-through-filtered-stdin.test.tsx:38
Interrupt-result union `'turn' | 'workflow' | 'none'` re-spelled as an inline shape in 5 places instead of one named type. The changeset introduces `interruptTurn(): 'turn' | 'workflow' | 'none'` in handlers.ts:47 (the canonical contract: 'turn' = abort handler fired, 'workflow' = cancel fired, 'none' = nothing stoppable). The same literal union is then re-typed inline by the consumer in app/keys.ts at line 32 (`interruptWorkflow?: (() => 'turn' | 'workflow' | 'none')`) and line 58 (`fireInterrupt(interruptWorkflow: () => 'turn' | 'workflow' | 'none')`), and again inline in the e2e harness at esc-through-filtered-stdin.test.tsx:38. The keys test even invents its own local alias `type InterruptResult = 'turn' | 'workflow' | 'none'` (keys.test.tsx:14), confirming a named contract is warranted and is being re-spelled rather than imported. This is the charter's 'ad-hoc inline shape where a named contract should exist / duplicated type definition' and the quality-bar's single-source-closed-set rule (define a union once, derive everywhere). No runtime impact since all five spellings are structurally identical, but they can silently diverge.
Fix: Export a single named type (e.g. `export type InterruptResult = 'turn' | 'workflow' | 'none'`) next to `interruptTurn` in src/features/workflow/handlers.ts and import it in app/keys.ts, keys.test.tsx, and the e2e harness, deleting the inline literal unions and the local `InterruptResult` alias.
round: 1 · skeptic: confirmed (The literal union 'turn'|'workflow'|'none' is re-spelled inline in five changeset sites — handlers.ts:47 (return), keys.ts:32 (option), keys.ts:58 (fireInterrupt param), esc-through-filtered-stdin.test.tsx:38 (harness prop) — and the keys test even invents a local 'type InterruptResult' alias (keys.test.tsx:14), confirming a named contract is warranted. This violates the quality bar's single-source closed-set rule (define a union once, derive everywhere). Exporting one named type and importing it removes silent-divergence risk.)

### F-033 · low · simplicity+slop+dead-code+architecture · src/features/workflow/recovery-prompt.ts:30-37, src/features/workflow/recovery-prompt.ts:66-72, src/features/workflow/recovery-prompt.ts:8-15, src/features/workflow/recovery-prompt.ts:123-128, src/features/workflow/recovery-prompt.ts:17-42
Stale 'ask planner' aliases and format branch left reachable-looking after planner-split-rebase removed from ACTION_ORDER. This change removed 'planner-split-rebase' from ACTION_ORDER (lines 8-15). getRecoveryPromptActions filters ACTION_ORDER (line 52) and findAliasedAction iterates only ACTION_ORDER (lines 123-128), so the 6-element ACTION_ALIASES['planner-split-rebase'] list ('p','planner','split','rebase','split rebase','planner-split-rebase') at lines 30-37 is now unreachable - confirmed by the updated test asserting parseRecoveryActionAnswer('p',...) falls through to 'pause-run'. The 'ask planner to ...' branch in formatRecoveryActionText (lines 66-72) is likewise unreachable since formatRecoveryActionChoice is only ever called with actions from getRecoveryPromptActions or toRecoveryAction (which maps regenerate-rebase to pause-run, never planner-split-rebase). The ACTION_KEYS entry and the switch case are type-required by Record<RecoveryAction,...>/assertNever, but the non-empty alias array is not, so it reads as live routing that no longer fires - misleading dead code.
Fix: Empty the ACTION_ALIASES['planner-split-rebase'] array (keep the key for the Record type) so the dead 'p'/'split'/'rebase' routing is not presented as live.
round: 1 · skeptic: confirmed (The changeset removed 'planner-split-rebase' from ACTION_ORDER (recovery-prompt.ts:8-15). The only consumer of ACTION_ALIASES is findAliasedAction (line 125), which iterates ACTION_ORDER, so ACTION_ALIASES['planner-split-rebase'] (lines 30-37) is never read — the 6-element array is dead data that misleadingly reads as live 'p'/'split'/'rebase' routing that now silently falls through to pause-run. Emptying the array (keeping the key for the Record type) is a genuine, changeset-anchored cleanup.)

### F-034 · low · simplicity · src/features/workflow/user-edit-conflict-prompt.ts:31-33, src/features/workflow/user-edit-conflict-prompt.ts:46-48
Dead defensive filter visibleActions() strips a value the same changeset stopped producing. The same changeset removed 'regenerate-rebase' from DESTRUCTIVE_CONFLICT_ACTIONS and from every actionsFor(...) result in src/engine/orchestrator/user-edit/conflicts.ts:12-13,80-82, so no live code path can place 'regenerate-rebase' in a UserEditConflict.availableActions. The conflict object handed to formatUserEditConflictPrompt is always freshly classified at runtime (classifyUserEditConflict / approval-conflict path; it is never deserialized from a saved state - traced through use-prompt-callbacks.ts:72-75, use-ipc-prompt-dispatcher.ts:34-41, detection.ts, escalation/approval-conflict.ts). The new visibleActions() helper added at lines 31-33 filters out 'regenerate-rebase', but it can never actually remove anything, so it is dead defensive YAGNI code introduced alongside the removal it guards against.
Fix: Delete visibleActions() and map conflict.availableActions directly at line 46; the enum member only needs to remain for the assertNever switch in toRecoveryAction, not a runtime filter.
round: 1 · skeptic: confirmed (The same changeset removed 'regenerate-rebase' from DESTRUCTIVE_CONFLICT_ACTIONS and the future-task-stale-input branch (conflicts.ts:12-13,80-82), so no live producer emits it in availableActions. UserEditConflict has no Zod schema and is never persisted/deserialized — it only reaches formatUserEditConflictPrompt freshly classified in-memory via onUserEditConflict. Thus visibleActions() (lines 31-33) can never filter anything; it is dead defensive code and inlining the map at line 46 is a real simplification.)

### F-035 · low · correctness+structure+errors · testing/helpers/filtered-stdin-harness.tsx:55-90, testing/helpers/filtered-stdin-harness.tsx:56-77, testing/integration/ui/esc-through-filtered-stdin.test.tsx:1-339, testing/helpers/filtered-stdin-harness.tsx:57-64, testing/helpers/filtered-stdin-harness.tsx:85-89, src/lib/terminal/filtered-stdin.ts:159-163, src/lib/terminal/filtered-stdin.ts:212, testing/helpers/filtered-stdin-harness.tsx:56-64
Harness stdout mute is installed after the code it claims to suppress, so terminal-mode escape sequences leak into test output. In renderThroughFilteredStdin, createFilteredStdin(source) is called on line 57 — which runs setTerminalInputModes(true) (filtered-stdin.ts:212 -> 159-163) and writes [?1000h[?1006h[?2004h to the REAL process.stdout — BEFORE the mute (process.stdout.write override) is installed on line 64. Symmetrically, unmount() calls filtered.disable() on line 87 which writes the disable sequences AFTER the mute was already restored on line 76. The comment on lines 62-65 states the mute exists specifically to suppress 'the filter writes mouse/paste enable sequences to the real stdout during setup', but those exact sequences escape it. Running the new integration test confirms the leak: `npx vitest run testing/integration/ui/esc-through-filtered-stdin.test.tsx` prints repeated [?1000h[?1006h[?2004h[?1000l[?1006l[?2004l garbage to the terminal. The leak is cosmetic only (disable sequences cancel the enable sequences, leaving terminal state clean), but the guard does not deliver its documented purpose.
Fix: Install the process.stdout.write mute before calling createFilteredStdin (and keep it installed across disable()), or have the harness pass through a no-op write to the filter setup, so the mouse/paste enable and disable sequences are actually suppressed as the comment claims.
round: 1 · skeptic: confirmed (Empirically verified: createFilteredStdin(source) on line 57 runs setTerminalInputModes(true) (filtered-stdin.ts:212->159-163) writing [?1000h/1006h/2004h to the REAL process.stdout BEFORE the mute is installed on line 64; symmetrically disable() on line 87 runs after realWrite is restored on line 76. Running the test prints the enable/disable sequences to the terminal, so the mute's documented purpose (comment lines 62-63) is genuinely not delivered. Cosmetic only (tests pass), but the guard is a no-op for the sequences it claims to suppress.)

### F-036 · low · errors · testing/helpers/filtered-stdin-harness.tsx:61-77, testing/helpers/filtered-stdin-harness.tsx:60-91
Test harness leaves process.stdout.write permanently muted if render() throws. renderThroughFilteredStdin replaces process.stdout.write with a no-op stub at line 64, calls Ink's render(element, ...) at line 66-74, then restores the real write at line 76. The restore is not in a try/finally. If render() throws (e.g. a component error in the element under test, or an Ink init failure), the function propagates the exception with process.stdout.write still pointing at the no-op stub. Because process.stdout is a process-global shared across the whole Vitest worker, every subsequent test in that worker would then silently lose stdout output and any assertion that reads real terminal frames could misbehave with no diagnostic. The failure mode hides the real error class (a render failure) behind cascading mystery failures in unrelated tests.
Fix: Wrap the render call in try/finally so process.stdout.write = realWrite always runs, restoring stdout even when render() throws.
round: 1 · skeptic: confirmed (Verified: process.stdout.write is replaced with a no-op stub on line 64, render() is called on lines 66-74, and the restore on line 76 is not in a try/finally. If render() throws (component/Ink init failure), the exception propagates with process.stdout.write still muted; since process.stdout is a worker-global, every later test in that Vitest worker silently loses stdout. The try/finally fix is correct and improves robustness.)

### F-037 · low · tests · testing/integration/orchestrator/abort-mid-task.test.ts:60-70, testing/integration/orchestrator/abort-mid-task.test.ts:92
abort-mid-task asserts a tautology (mock's own abortStore.arm side-effect), not engine abort behavior. The implementer mock itself calls abortStore.arm('exit') (line 67), then line 92 asserts expect(abortStore.get().armed).not.toBe('none'). I confirmed src/engine/ never imports abortStore (grep returned nothing) and runTaskLoop (src/engine/orchestrator/task/loop.ts:50,166) only reads wctx.signal?.aborted — the engine never reads the UI abort store. So this assertion only verifies that the mock executed its own arm() call; it proves nothing about how the orchestrator handles abort. The change touched this exact line (HEAD diff renamed markPending/pending -> arm/armed), making it the natural point to drop the tautology. The real abort behavior is covered by the AbortSignal-driven assertions (T002 never started, no task_completed, currentTaskIndex < tasks.length).
Fix: Delete the abortStore assertion or replace it with a check that exercises real engine behavior; the signal-driven assertions already prove the abort path.
round: 1 · skeptic: confirmed (Line 92 asserts `abortStore.get().armed).not.toBe('none')` after the implementer mock itself called `abortStore.arm('exit')` at line 67 — a tautology, since the engine never reads abortStore (grep of src/engine returned nothing) and runTaskLoop only reads `wctx.signal?.aborted` (loop.ts:50,166). The assertion only verifies the mock ran its own arm(), proving nothing about orchestrator abort behavior, which is already covered by the signal-driven assertions (lines 93-96). The change touched exactly this line (diff: markPending/pending → arm/armed), so it is anchored; deleting the tautology improves the test.)

### F-038 · low · simplicity+slop · testing/integration/orchestrator/full-loop-validation-retry.test.ts:81-102, testing/integration/orchestrator/full-loop-validation-retry.test.ts:572-574, testing/integration/orchestrator/full-loop-validation-retry.test.ts:70-102
Over-engineered isFakeOpencodeRun type guard for a JSON file the test itself just wrote. `full-loop-validation-retry.test.ts` adds a 21-line `isFakeOpencodeRun` `value is` type guard (lines 81-102) that exhaustively checks `cwd`, `args`, and four `env` keys, then applies it twice — `expect(isFakeOpencodeRun(runLog)).toBe(true)` and `if (!isFakeOpencodeRun(runLog)) throw new Error(...)` (lines 572-574) — to parse a `opencode-run.json` the fake binary in this same test wrote in a fully controlled fixture. It is the only test file in the suite that hand-rolls a `value is` guard (grep confirms). The sibling new test in the same changeset, `cli-planner-cli-implementer.test.ts:215-219`, asserts the identical fake-opencode run-log shape with a plain `JSON.parse(...) as { cwd; args; env }` cast. The guard buys no real safety here (data is self-produced), duplicates the shape declaration, and the redundant expect-plus-throw double-check obscures a one-line read.
Fix: Replace the guard and its double application with the same simple typed cast used in cli-planner-cli-implementer.test.ts (`JSON.parse(...) as FakeOpencodeRun`), or read the fields directly; delete `isFakeOpencodeRun`.
round: 1 · skeptic: confirmed (The 21-line `isFakeOpencodeRun` `value is` guard (lines 81-102) exhaustively validates a JSON file the test's own fake binary wrote in a fully controlled fixture, then applies it twice (expect at 573 + throw at 574) — buying no real safety and obscuring a one-line read. It is the only `value is` guard in testing/ (grep confirmed), and the sibling new file in the same changeset uses a plain `as` cast for the identical contract; TESTING.md:14 explicitly discourages re-validating already-typed/self-produced data. The simple-cast fix genuinely simplifies.)

### F-039 · low · simplicity+slop · testing/integration/ui/esc-through-filtered-stdin.test.tsx:1, testing/integration/ui/esc-through-filtered-stdin.test.tsx:91-92, testing/integration/ui/esc-through-filtered-stdin.test.tsx:330-339, testing/integration/ui/esc-through-filtered-stdin.test.tsx:136-137
Hand-rolled vi_noop counter + manual abort counters reinvent vi.fn() with a false 'render timing' justification. The new ESC test deliberately omits the `vi` import (line 1 imports only `afterEach, beforeEach, describe, expect, it`) and instead invents a `vi_noop()` closure-based call counter (lines 330-339) plus repeated manual `let aborted = 0; ... aborted += 1` counters (e.g. lines 91-92, 114, 133, 261). The justification comment at line 330 claims this avoids 'pulling vitest's vi into the render tree timing.' That claim is directly contradicted by the sibling test for the very same `useAppKeys` hook and same `exit`/`interruptWorkflow` callbacks, `src/app/keys.test.tsx`, which imports `vi`, passes `vi.fn()`/`vi.fn<...>()` as those callbacks, asserts via `toHaveBeenCalledTimes`, and renders through Ink with no timing problem. Both tests drive Ink's real `render`; the only difference is the input bridge, which has no bearing on whether `vi.fn()` records calls. The bespoke counter and unfounded comment add cleverness that obscures intent.
Fix: Import `vi`, pass `vi.fn()` for `exit` and the abort handler, and assert with `toHaveBeenCalledTimes`; delete the `vi_noop` helper and the manual counter variables and the misleading comment.
round: 1 · skeptic: confirmed (The new test (untracked, read in full) deliberately omits the vi import (line 1) and hand-rolls a vi_noop call counter (lines 330-339) plus manual `let aborted = 0; aborted += 1` counters; the justification comment 'to avoid pulling vitest's vi into the render tree timing' is false — vi.fn() is a call recorder that never touches timers, and vitest.config.ts sets no mockReset/restoreMocks, so vi.fn() works freely with real timers (as many testing/integration tests do). The sibling test keys.test.tsx uses vi.fn()/toHaveBeenCalledTimes for the very same useAppKeys/exit/interrupt callbacks. The bespoke helper and fabricated comment are removable slop.)

### F-040 · low · tests · testing/integration/ui/esc-through-filtered-stdin.test.tsx:26-31, testing/integration/ui/esc-through-filtered-stdin.test.tsx:84-99, src/features/workflow/components/feedback-row.tsx:8-10
ESC pipeline test couples to verbatim UI hint copy, partly duplicating armed-state assertions. The test hard-codes HINT_INTERRUPT='Esc again to interrupt', HINT_CANCEL='Esc again to cancel workflow', HINT_EXIT='Ctrl+C again to exit' (lines 26-28) and asserts lastFrame().toContain(<verbatim>) repeatedly. These strings are duplicated from feedback-row.tsx:8-10; a copy edit (e.g. adding 'Press ') breaks the test with no behavior change. Several of these frame assertions sit next to abortStore.get().armed checks that already pin the same state transition, so the verbatim-copy assertions are partly redundant. The quality bar explicitly flags coupling to verbatim copy in frames.
Fix: Assert the rendered hint by importing the hint constant from feedback-row instead of re-spelling the literal, or rely on the armed-state assertions and keep only one render-integration check that the FeedbackRow shows a hint.
round: 1 · skeptic: confirmed (The test re-spells verbatim production copy as local constants (lines 26-28: 'Esc again to interrupt' etc.) that duplicate the module-private ARMED_MESSAGES in feedback-row.tsx:7-11 (both new in the changeset, exact-string match) and asserts lastFrame().toContain(<literal>). A copy edit breaks the test with no behavior change — exactly the 'coupling to verbatim copy in frames' anti-pattern the quality bar flags. Exporting ARMED_MESSAGES and importing it (or deriving the constant) de-couples the assertion. The redundancy sub-claim is weaker — the frame check legitimately proves the hint renders in this e2e pipeline test — so the fix should de-couple, not delete, the frame assertion.)

### F-041 · low · conventions · testing/integration/ui/esc-through-filtered-stdin.test.tsx:3-5
New ESC integration test uses relative ../../helpers imports instead of the repo-wide #testing/helpers alias. Across testing/integration/ the #testing/helpers alias is used 103 times vs only 4 relative ../../helpers imports, and 3 of those 4 are this new file (lines 3-5: filtered-stdin-harness.js, ink.js, stores.js). Its own sibling in the same folder, testing/integration/ui/workflow-events-display.test.tsx:2-3, imports the identical helpers (ink.js, stores.js) via #testing/helpers/. Both resolve and typecheck (the #testing/* -> ./testing/* mapping is defined in package.json:51 and tsconfig.test.json:9), so this is purely a naming/import-style inconsistency across similar modules, not a functional defect.
Fix: Change the three relative helper imports in esc-through-filtered-stdin.test.tsx to the #testing/helpers/ alias to match every other testing/integration/ test.
round: 1 · skeptic: confirmed (New changeset file uses relative '../../helpers/*' imports for filtered-stdin-harness.js, ink.js, stores.js while 103 of 107 integration imports (and its own sibling workflow-events-display.test.tsx:2-3 importing the identical ink/stores helpers) use the '#testing/helpers/*' alias defined at package.json:51 and tsconfig.test.json:9. It is real, anchored in new changeset lines, not a sanctioned convention, and the alias swap is a surgical consistency improvement, not churn; severity stays low since no gate/invariant enforces it.)

### F-042 · info · tests · src/app/keys.test.tsx:161-161, src/app/keys.test.tsx:181-181, src/app/keys.test.tsx:221-221
ESC tests hardcode the 35ms debounce delay, coupling them to an internal constant. Three ESC tests call vi.advanceTimersByTime(35) to flush the deferred arm. The 35 is DEFAULT_DELAY_MS in escape-debounce.ts:1, not an exported/imported value, so the tests duplicate that magic number. If DEFAULT_DELAY_MS is later tuned (e.g. to disambiguate split sequences over slower links), advancing exactly 35ms may no longer flush the timer and these three tests churn even though the observable ladder is unchanged.
Fix: Advance by a value comfortably above the debounce (e.g. 100) or import DEFAULT_DELAY_MS from escape-debounce.ts so the wait tracks the real delay instead of a copied literal.
round: 1 · skeptic: confirmed (Verified: keys.test.tsx lines 161, 181, 221 call vi.advanceTimersByTime(35), duplicating the module-private DEFAULT_DELAY_MS = 35 from escape-debounce.ts:1 (not exported, so the literal is copied). Both the test changes and escape-debounce.ts are in the working-tree changeset. The coupling is real: raising the delay would break these three asserts even though the observed ESC ladder is unchanged, and advancing comfortably above the debounce (or importing the constant) is a genuine, non-churn decoupling. Correctly an info-level test-brittleness item.)

### F-043 · info · errors · src/core/config/runtime/overrides.ts:175-198, src/core/config/runtime/build-runner.ts:137-155
--implementer-api-key-env / --planner-api-key-env silently dropped for CLI runner kinds. When the resolved runner kind is `cli` (e.g. `--implementer claude-code --implementer-api-key-env FOO`), buildRunnerFromOverrides sets baseOpts to existingToOpts/cli and spreads `apiKey: 'env:FOO'` into opts (overrides.ts:187-193), but buildCliConfig (build-runner.ts:137-155) never reads opts.apiKey, so the override vanishes with no warning. The user-supplied --implementer-api-key-env flag is accepted on the command line and then discarded silently. It is harmless for CLI tools (which have no apiKey), but a user combining a CLI tool with an api-key-env flag gets no feedback that the flag was ignored.
Fix: Either warn (warnStderr) when apiBase/apiKey overrides are supplied but the resolved runner kind cannot use them, or document that these flags only apply to api/agent-sdk runners.
round: 1 · skeptic: confirmed (For a cli-kind runner, buildRunnerFromOverrides spreads apiKey into opts (overrides.ts:187-188, new) but buildCliConfig (build-runner.ts:137-155) never reads opts.apiKey, so --implementer-api-key-env/--planner-api-key-env is silently dropped. Help text for --implementer-api-key-env lacks the 'for API providers' qualifier, so a user combining it with a CLI tool gets no feedback. Harmless (CLI tools have no apiKey) but an info-level UX/feedback gap; the entire api-key-env surface is new in this changeset.)

### F-044 · info · conventions · src/core/keybindings/registry.ts:12-17, src/app/keys.ts:83-94
Ctrl+C shortcut description claims 'Interrupt, then exit' on all screens but interrupt only happens on the workflow screen. The `exit` shortcut description was changed to 'Interrupt, then exit (press again)' for `screens: ALL_SCREENS` (registry.ts:12-17). But the Ctrl+C handler (keys.ts:83-94) only calls `fireInterrupt` when `route.screen === 'workflow' && !cancelled && isLivePhase(phase)`; on home/summary screens it just arms exit with no interrupt (the injected `interruptWorkflow` returns 'none' off-workflow). The keys.test.tsx 'Ctrl+C on the home screen arms exit (no interrupt)' test confirms no interrupt fires off-workflow. The single ALL_SCREENS description therefore overstates behavior on non-workflow screens.
Fix: Either scope an 'Interrupt, then exit' description to the workflow screen and a plain 'Exit (press again)' to other screens, or reword the ALL_SCREENS description to not imply interrupt everywhere.
round: 1 · skeptic: confirmed (The changeset changed the 'exit' description from 'Exit (double-press)' to 'Interrupt, then exit (press again)' for screens:ALL_SCREENS, but the Ctrl+C handler only calls fireInterrupt when route.screen==='workflow' && !cancelled && isLivePhase(phase); on home/summary it just arms exit with no interrupt, and even on the workflow screen no interrupt fires off a live phase. keys.test.tsx confirms interruptWorkflow is not called on the home screen. The single ALL_SCREENS string overstates behavior off-workflow. Factually real, anchored in the changed line, not a duplicate, not an intended convention; rewording is a genuine minor accuracy improvement, not churn. Info severity: help-text imprecision only.)

### F-045 · info · stack · src/engine/orchestrator/approval/file-snapshots.ts:157-165
Duplicate TOCTOU re-check assertWritablePathConfined lacks the explanatory comment its fs.ts sibling carries. writeCurrentFileContent now calls assertWritablePathConfined(file, projectDir) twice with identical args — once before rm/mkdir (line 157) and once after mkdir(dirname(path), { recursive: true }) (line 164). This mirrors the sanctioned TOCTOU pattern in src/lib/fs.ts:112-119 (writeConfinedSecureFileAsync), but that sibling documents the second call with a 'Re-check after mkdir: creating the parent may have materialized a symlink target' comment, while file-snapshots.ts has none. Without the comment the second identical call reads as accidental duplication rather than a deliberate post-mkdir symlink re-check. The convention allows why-comments on non-obvious logic; this non-obvious re-check is missing one.
Fix: Add a short why-comment above the second assertWritablePathConfined call mirroring fs.ts (re-check after mkdir to catch a newly materialized symlink parent).
round: 1 · skeptic: confirmed (Factually real and anchored in the changeset: writeCurrentFileContent calls assertWritablePathConfined twice (before rm/mkdir and after mkdir) with no explanatory comment, whereas its sibling writeConfinedSecureFileAsync in lib/fs.ts documents the identical post-mkdir re-check. The second call reads as accidental duplication without a why-comment; adding the one-liner the sibling already carries is parity-with-fs.ts, not churn, and the convention permits why-comments on non-obvious logic.)

### F-046 · info · tests · src/engine/orchestrator/escalation/step.test.ts:130-178
step.test.ts re-asserts the full sandbox-env directory layout, duplicating createSandboxEnv's contract and coupling the propagation test to internal dir naming. The test 'passes the staged sandbox env to retry invocation' exists to prove runRetryStep propagates the staged project's sandboxEnv to invokeRetry. But lines 140-144 assert the exact layout `args.sandboxEnv.HOME === join(args.projectDir, SANDBOX_DIR, 'home')`, `.TMPDIR === .../tmp`, `.npm_config_cache === .../npm-cache`. That exact mapping is the contract of createSandboxEnv (src/engine/runners/sandbox-env.ts:6-32) and is already asserted as a unit in staged-project.test.ts:37-47, plus end-to-end in full-loop-validation-retry.test.ts:577-586 and cli-planner-cli-implementer.test.ts:222-228. Re-spelling the directory naming here is redundant and means any rename in createSandboxEnv churns four test files; the propagation contract is fully captured by the existsSync checks at 145-146 (the env points at real, created dirs) plus a non-undefined assertion.
Fix: Replace the three `.toBe(join(args.projectDir, SANDBOX_DIR, ...))` layout assertions (lines 140-144) with a check that args.sandboxEnv is defined and equals staged sandbox semantics already covered by existsSync, leaving the canonical layout assertions to staged-project.test.ts.
round: 1 · skeptic: confirmed (Real: step.test.ts:140-144 re-spells createSandboxEnv's directory layout (HOME/TMPDIR/npm_config_cache = join(...SANDBOX_DIR,...)) which is already the asserted unit contract in staged-project.test.ts:42-45 and re-asserted in cli-planner-cli-implementer.test.ts:221-228. The propagation contract (staged env, not original project) is already proven by args.projectDir!==projectDir plus the existsSync checks at 145-146, so the layout assertions are redundant coupling to internal dir naming across 4 files. Lowered to info: the path assertions are mild and partly defensible, not a behavior bug.)

### F-047 · info · tests · src/engine/orchestrator/final-review.test.ts:192-195, src/engine/orchestrator/final-review.test.ts:221-222
Non-discriminating, redundant '## Task Briefs' assertions in final-review fallback test. The new test 'reviews the task brief packet, falling back to current state tasks when tasks.md is missing' asserts `reviewPrompts[0]).toContain('## Task Briefs')` in BOTH the persisted-tasks.md branch (line 192) and the fallback branch (line 221). buildFinalReviewPrompt (src/engine/spec/prompts/review.ts:36-40) emits the `Task Briefs` section heading unconditionally regardless of body content, so this assertion is trivially true in both branches and does not discriminate the fallback path — only the marker-string assertions (lines 193-195 persisted vs 222 state-derived) actually exercise the `readSpecFileOrEmpty(TASKS_FILE) || formatTasks(state.tasks)` fallback at final-review.ts:104-105. The heading text is already covered by the dedicated review.test.ts:12 (`expect(result).toContain('## Task Briefs')`), so these assertions redundantly re-test the prompt builder's static heading and couple final-review tests to prompt-formatting glyphs that will churn if the heading is renamed.
Fix: Drop the two `toContain('## Task Briefs')` assertions from final-review.test.ts and keep only the discriminating marker-string assertions (persisted vs state-derived), leaving heading coverage to review.test.ts.
round: 1 · skeptic: confirmed (Real: buildFinalReviewPrompt at review.ts:36-40 emits the 'Task Briefs' heading unconditionally ({ heading: 'Task Briefs', body: opts.taskBriefs }) regardless of body, so toContain('## Task Briefs') at final-review.test.ts:192 and :221 is trivially true in both branches and does not discriminate the readSpecFileOrEmpty||formatTasks fallback. The discriminating signal is the marker strings (lines 193-194 vs 222); the heading is already covered by review.test.ts:12.)

### F-048 · info · architecture · src/engine/orchestrator/recovery/builders/recovery-actions.ts:36-41, src/features/workflow/user-edit-conflict-prompt.ts:14-29
Duplicated cross-layer UserEditConflictAction to RecoveryAction mapping (engine + features). The same `UserEditConflictAction -> RecoveryAction` mapping is implemented twice in two different layers, and both copies were edited in this changeset (the `regenerate-rebase` line changed in each as part of the planner-split-rebase removal, intent #7). `mapUserEditAction` in engine (recovery-actions.ts:36) maps continue-unrelated->continue, regenerate-rebase->pause-run, pause->pause-run; `toRecoveryAction` in features (user-edit-conflict-prompt.ts:14) encodes the identical 5-case mapping. They must stay in lockstep but neither imports the other (engine cannot import features by the zero-engine->React rule, and features did not reuse the engine helper). A future edit to one mapping silently desyncs the UI from the engine recommendation.
Fix: Move the single canonical mapping into core/ (e.g. a `userEditActionToRecoveryAction` in core/schemas/recovery or core/recovery), then have both the engine builder and the features prompt import it so the union mapping has one source of truth.
round: 1 · skeptic: confirmed (The same UserEditConflictAction->RecoveryAction mapping is genuinely duplicated across two layers (engine mapUserEditAction and features toRecoveryAction), and intent #7 forced an identical edit (regenerate-rebase -> pause-run) in BOTH this changeset, proving real lockstep coupling and a desync hazard; a canonical core/ helper would be importable by both. Downgraded to info because it is only the 2nd occurrence, below the project's stated 'DRY at the 3rd occurrence' threshold, and the two functions also differ structurally (guard-chain w/ passthrough vs exhaustive switch w/ assertNever).)

### F-049 · info · structure · src/engine/orchestrator/run/phases.ts:60-66
runPlanningPhases checks isInterruptedPlanningTurn on both state and savedState (redundant dual guard). `shouldRunPlanning` is computed from `isInterruptedPlanningTurn(savedState)` (line 61) while the CONTINUE_TURN transition is guarded by `isInterruptedPlanningTurn(state)` (line 63), evaluating the same predicate against two variables. Tracing run.ts:109-119 and init.ts:201-202, on a resume `init.state` is assigned the same object as `savedState` (`state = savedState`), and on a fresh run `savedState` is undefined while `state` is a freshly START-ed state with `awaitingContinue:false`; so the two checks can never disagree. The split is provably correct but invites a future reader to assume the two can diverge and reason about a non-existent case.
Fix: Compute the predicate once into a local (e.g. `const interrupted = isInterruptedPlanningTurn(state)`) and reuse it for both the transition guard and the shouldRunPlanning term, dropping the savedState variant.
round: 1 · skeptic: confirmed (Verified: shouldRunPlanning uses isInterruptedPlanningTurn(savedState) (phases.ts:61) while the CONTINUE_TURN guard uses isInterruptedPlanningTurn(state) (phases.ts:63). On resume init.ts:202 sets state = savedState (same reference); on a fresh run savedState is undefined and state is freshly START-ed with awaitingContinue defaulting to false (workflow.ts:44), so the two evaluations can never disagree. The dual guard is provably correct but evaluates the same predicate against two variables; computing it once is a clean DRY improvement, not churn.)

### F-050 · info · tests · src/engine/orchestrator/task/loop-recovery.test.ts:165-168, src/engine/orchestrator/task/routing.ts:42-52
context-overflow error test couples to verbatim routingBlockMessage copy. The test asserts `events.find(error).message` via `expect.stringContaining('Pause and revise the plan')`, which is a verbatim substring of the new copy in `routingBlockMessage` (routing.ts:48). I traced the path: `runTaskLoop` -> context-overflow guard -> `routingBlockMessage(decision)` -> emitted as an `error` event the test reads off the bus. The assertion was edited in this changeset solely to follow the reworded string ('Ask the planner to split the task' -> 'Pause and revise the plan'). It does verify the new guidance reached the bus, but it will churn on any future copy edit even when behavior is unchanged. This is the only verbatim-copy coupling in the chunk; the rest of the assertions key on structured fields (availableActions, recommendedAction, reason).
Fix: Assert the structured contract instead of the prose — e.g. check the error event's `reason`/recovery fields, or drop the substring and rely on the already-present `recovery_prompted` assertion (availableActions/recommendedAction) which uniquely identifies the changed behavior.
round: 1 · skeptic: confirmed (Real: loop-recovery.test.ts:167 asserts stringContaining('Pause and revise the plan'), a verbatim substring of the reworded routingBlockMessage copy at routing.ts:48, and was edited in this changeset solely to follow the rename. It couples the test to prose that will churn on any future copy edit; the structured recovery_prompted assertion (169-177) is a different event and does not cover the error-bus message, so it could assert error-event presence structurally instead of the exact prose.)

### F-051 · info · dead-code · src/engine/snapshots/files.ts:6-9
Unnecessarily exported type CollectTrackedFilesOptions in snapshots/files.ts. The change adds `export type CollectTrackedFilesOptions = { ignoreProjectDir?: string | undefined }` but it is consumed only within its own file at files.ts:50 (the `collectTrackedFiles` opts param). A repo-wide grep for `CollectTrackedFilesOptions` returns no importer outside files.ts, and the parallel new callers that accept the same option (`change-detection.ts:16,43`, `file-snapshots.ts:43,79`) each re-spell an inline `{ ignoreProjectDir?: string | undefined }` rather than importing this type. Knip does not flag it (same-file use counts as used), so no gate fails, but the `export` keyword adds dead public surface with no consumer.
Fix: Drop the `export` so it is a file-local type, or have the inline-typed callers import it to justify the export.
round: 1 · skeptic: confirmed (The changeset adds `export type CollectTrackedFilesOptions` (files.ts:8-10) but a repo-wide grep shows it is referenced only inside files.ts itself (lines 8, 50); knip's `ignoreExportsUsedInFile: true` plus knip not being part of `test-ci` means no gate flags it, so the `export` keyword is dead public surface. Three parallel new sites that accept the same option re-spell the inline `{ ignoreProjectDir?: string | undefined }` (change-detection.ts:16,43; file-snapshots.ts:43,79) instead of importing it, so the export is neither used nor justified; dropping `export` removes the dead surface per the 'delete dead exports' bar.)

### F-052 · info · simplicity · src/lib/terminal/escape-debounce.ts:1-15, src/app/keys.ts:131
scheduleEscapeAction delayMs is speculative config with no production caller. scheduleEscapeAction(action, delayMs = DEFAULT_DELAY_MS) exposes a per-call override of the 35ms debounce, but the only production caller (src/app/keys.ts:131 `scheduleEscapeAction(() => abortStore.arm(target))`) always uses the default. I grepped every call site: the sole override is escape-debounce.test.ts:35, and the test that exercises it ('honors a custom delay') exists only because the parameter exists — circular justification with no real consumer. The delay is a single hardcoded UX constant; the parameter is speculative config (YAGNI) on a brand-new module.
Fix: Drop the delayMs parameter and use the DEFAULT_DELAY_MS constant directly inside scheduleEscapeAction; delete the now-moot custom-delay test.
round: 1 · skeptic: confirmed (scheduleEscapeAction exposes a delayMs override that no production caller uses (keys.ts:131 always takes the default), and the only override is the 'honors a custom delay' test that exists solely because the parameter exists — speculative config (YAGNI) on a brand-new module, contrary to the project's 'no speculative features' rule. Lowered to info: impact is one moot parameter plus one self-justifying test.)

### F-053 · info · tests · src/lib/terminal/filtered-stdin.test.ts:391-398
Misleading test name claims the filter "folds" a split arrow key when it only preserves bytes. The test 'still folds a real arrow key (ESC then [A) into one escape sequence' and its comment imply FilteredStdin reassembles/folds the split sequence. Tracing splitMouseChunk: the first chunk '\x1b' hits the `lastEsc === 0` branch (filtered-stdin.ts:176) and is flushed immediately; the second chunk '[A' is then written separately. The filter emits two separate writes and folds nothing — the actual folding happens downstream in Ink's input parser plus the escape-debounce in src/app/keys.ts. The assertion itself (delivered bytes === [0x1b,0x5b,0x41], i.e. non-mouse bytes pass through in order) is correct and is the right observable contract; only the name/comment mischaracterize the layer's responsibility, which can mislead a future reader into thinking the filter buffers split escape sequences.
Fix: Rename the test to reflect the real contract (e.g. 'preserves a split escape sequence byte-for-byte without swallowing the leading ESC') and drop the 'folds into one sequence' wording from the inline comment.
round: 1 · skeptic: confirmed (The test name 'still folds a real arrow key (ESC then [A) into one escape sequence' and its comment imply the filter reassembles a split escape sequence, but tracing splitMouseChunk the first chunk '\x1b' hits the bare-ESC branch (lastEsc===0) and is flushed immediately, and '[A' is written separately — the filter folds nothing, it only preserves bytes in order. The assertion is correct; the name/comment mislead about the layer's responsibility. Anchored in the new test file.)

### F-054 · info · conventions · testing/helpers/filtered-stdin-harness.tsx:16-27
Lone runtime class CaptureStdout in test helper breaks the codebase's zero-class norm. The new harness declares `class CaptureStdout extends EventEmitter`. A repo-wide grep (`class ` across `testing/` and `src/` non-test) shows this is the only `class` declaration anywhere in the `testing/` tree, and the established repo pattern for fake stdout/stdin in test helpers is plain module-scoped factory functions over `ink-testing-library` (see testing/helpers/ink.ts) or `Object.assign`-decorated `PassThrough`/`EventEmitter` instances (see makeFakeStdin in filtered-stdin.test.ts:73-83 and makeSourceStdin in this same file at lines 29-45). The convention (context.md: zero runtime classes; test fixtures may use classes only when class behavior is under test) does not strictly gate `testing/` — the invariants script (scripts/check-invariants.ts:66-68) scopes the class check to `src/` excluding tests — so this trips no gate, but `CaptureStdout` is infrastructure scaffolding (a frame-capturing stdout stub), not behavior under test, making it an unnecessary stylistic deviation from the surrounding zero-class code.
Fix: Replace `class CaptureStdout extends EventEmitter` with the harness's own established pattern: build a stdout stub via `Object.assign(new EventEmitter(), { isTTY: true, columns: 100, frames: [], write, lastFrame })`, mirroring makeSourceStdin in the same file.
round: 1 · skeptic: confirmed (Verified: class CaptureStdout (line 16) is the only class declaration in the entire testing/ tree, and it deviates from the file's own established pattern (makeSourceStdin at lines 29-45 uses Object.assign over a PassThrough). The class is infrastructure scaffolding, not behavior-under-test, so it conflicts with the zero-class spirit. Kept at the lowest severity (info): the invariants gate (check-invariants.ts id 8) scopes only to src/ excluding tests, so testing/ is not gated and the convention text formally governs 'production source' only.)

### F-055 · info · simplicity+dead-code · testing/helpers/filtered-stdin-harness.tsx:48-91, testing/integration/ui/esc-through-filtered-stdin.test.tsx, testing/helpers/filtered-stdin-harness.tsx:47-53, testing/helpers/filtered-stdin-harness.tsx:80
Unused `frames` field exposed on new FilteredStdinHarness helper. The new `FilteredStdinHarness` interface declares `frames: readonly string[]` (line 49) and `renderThroughFilteredStdin` returns `frames: stdout.frames` (line 80), backed by the `CaptureStdout.frames` array. I traced every consumer: the only importer of `renderThroughFilteredStdin` is `testing/integration/ui/esc-through-filtered-stdin.test.tsx`, which reads only `lastFrame()`, `pressBytes`, `unmount`, and `filtered` — never `.frames`. A repo-wide grep confirms zero readers of `harness.frames`. The `frames` array exists solely so `write` can push into it (line 22), but nothing consumes it, making the exposed field and its backing array dead surface introduced by this new file.
Fix: Drop `frames` from the `FilteredStdinHarness` interface and the returned object, and remove the now-unused `frames` array from `CaptureStdout` (keep the `last`/`lastFrame` path).
round: 1 · skeptic: confirmed (Verified dead surface: the FilteredStdinHarness.frames field (line 49) and the returned frames: stdout.frames (line 80), backed by CaptureStdout.frames (line 19, pushed at 22), are never read. Grep of the sole consumer (esc-through-filtered-stdin.test.tsx) shows only lastFrame/pressBytes/unmount/filtered used, zero reads of .frames. Lowered to info: it mirrors the established renderFeature helper (ink.ts:6) which also exposes frames per ink-testing-library, so the dead surface is a convention echo rather than a clear defect.)

### F-056 · info · tests · testing/integration/orchestrator/abort-mid-task.test.ts:26-29, testing/integration/orchestrator/abort-mid-task.test.ts:67, src/stores/workflow/abort.ts:15-30
abort-mid-task leaks abortStore's 2s arm timer because resetAllStores().reset() does not clear it. abortStore.arm('exit') schedules a module-level setTimeout(WINDOW_MS=2000) (abort.ts:18). The test's afterEach (lines 26-29) only runs cleanupTempDir and never calls abortStore.clear(); only the next beforeEach's resetAllStores() runs. resetAllStores() calls abortStore.reset(), but reset comes from storeBase (create-store.ts:61) and only sets state to initial — it does not touch armTimer. So the 2000ms timer survives reset and fires later. In this single-test file Vitest isolation means no observable cross-test damage today, but the cleanup pattern relies on a reset that cannot fully clean up; the sibling esc-through-filtered-stdin.test.tsx correctly calls abortStore.clear() in afterEach.
Fix: Add abortStore.clear() to the afterEach of abort-mid-task.test.ts (matching the ESC test) so the armed timer is cancelled.
round: 1 · skeptic: confirmed (abortStore.arm('exit') schedules a non-unref'd module-level 2000ms setTimeout (abort.ts:18). The test's afterEach (lines 27-29) only calls cleanupTempDir and never abortStore.clear(); resetAllStores()→abortStore.reset()→storeBase→createStore.reset = set(getInitial()) (create-store.ts:61) which does NOT touch armTimer, so the timer survives and fires later. The sibling esc-through-filtered-stdin.test.tsx correctly calls abortStore.clear() in afterEach (line 67). No observable cross-test damage under Vitest isolation today, but a real cleanup gap; the one-line clear() fix matches the sibling pattern.)

### F-057 · info · types · testing/integration/ui/esc-through-filtered-stdin.test.tsx:33-39
Interrupt-result union `'turn' | 'workflow' | 'none'` re-spelled inline in new ESC test instead of a single named type. The new test's `InstantWorkflowApp` prop types `interruptWorkflow?: () => 'turn' | 'workflow' | 'none'` (line 38), adding a fifth literal copy of this closed union. The same changeset's CHUNK-A test src/app/keys.test.tsx:14 already extracted `type InterruptResult = 'turn' | 'workflow' | 'none'`, while handlers.ts:47 (`interruptTurn` return), keys.ts:32 and keys.ts:58 also re-spell it raw. The union is a single closed set that has no exported name, so the new test had nothing to import and copied the literals — the project's single-source closed-set convention is violated and a future edit to the set must be mirrored in five places.
Fix: Export a named alias (e.g. `InterruptResult`) from `src/features/workflow/handlers.ts` as the return type of `interruptTurn`, and import it in the ESC test and `keys.ts` instead of re-spelling the literal union.
round: 1 · skeptic: confirmed (The closed union `'turn' | 'workflow' | 'none'` is re-spelled raw at handlers.ts:47 (the producer, interruptTurn's return), keys.ts:32 and keys.ts:58, and esc test:38 — all new changeset lines — with no exported named alias; keys.test.tsx:14 extracts a local InterruptResult but does not export it. This violates the single-source closed-sets convention (context.md line 86: 'unions/enums defined once... never re-spell'). Exporting an InterruptResult alias from handlers.ts and importing it removes the duplication.)

### F-058 · medium · miss-hunter · docs/SLASH-COMMANDS-REFERENCE.md:361-365, docs/SLASH-COMMANDS-REFERENCE.md:415, docs/SLASH-COMMANDS-REFERENCE.md:383, src/app/keys.ts:71-140, src/stores/workflow/abort.ts:3-30, src/core/keybindings/registry.ts:12-32, src/features/workflow/keyboard.ts
SLASH-COMMANDS-REFERENCE.md describes the deleted Ctrl+C/Esc abort behavior (dead constant, removed store field, stale message and registry label, missing new Esc Esc shortcuts). The changeset (intent #8) rewrote Ctrl+C/Esc handling but docs/SLASH-COMMANDS-REFERENCE.md (NOT in the changeset) still documents the old behavior and now references code that was deleted. Line 362 cites `DOUBLE_PRESS_WINDOW_MS` — a constant removed from keys.ts (grep of src/ finds it nowhere). Line 365 says the first Ctrl+C "marks the abort store as pending" and shows the message "Aborting… Ctrl+C again to exit": abortStore no longer has a `pending` field (now `armed: ArmedKind`, abort.ts:3-6) and that exact `feedbackStore.setMessage('Aborting… Ctrl+C again to exit')` call was deleted (git HEAD:keys.ts:69), replaced by ARMED_MESSAGES.exit = 'Ctrl+C again to exit' (feedback-row.tsx:8). Line 415's registry cross-reference shows `exit | Ctrl+C | Exit (double-press)` but the changeset changed that registry description to 'Interrupt, then exit (press again)' (registry.ts:12-17), and the table omits the two NEW registry shortcuts `interrupt`/`cancel` (both `Esc Esc`, registry.ts:21-32). Line 383 points Esc-navigate-home to `keyboard.ts:21` (the `handleWorkflowEscape` function), which the changeset deleted and folded into app/keys.ts:113-116. Every one of these doc claims now describes code that was changed or removed by this changeset.
Fix: Update docs/SLASH-COMMANDS-REFERENCE.md to the new model: replace the `DOUBLE_PRESS_WINDOW_MS`/`pending`/'Aborting…' wording with the `abortStore.arm('exit')`/`armed` flow and the 'Ctrl+C again to exit' message, correct the registry description to 'Interrupt, then exit (press again)', add the new `Esc Esc` interrupt/cancel rows, and fix the now-dangling `keyboard.ts:21` source pointer.
round: 2 · skeptic: confirmed (docs/SLASH-COMMANDS-REFERENCE.md was accurate at HEAD but the working-tree change made every cited reference stale: it removed DOUBLE_PRESS_WINDOW_MS (gone from src/), replaced the abort store's pending:boolean with armed:ArmedKind (abort.ts:3-6) and moved the 'Aborting…' message into feedback-row.tsx:8 ARMED_MESSAGES, rewrote the registry 'exit' description to 'Interrupt, then exit (press again)' and added two new Esc Esc interrupt/cancel rows, and deleted handleWorkflowEscape (the old keyboard.ts:21 the doc still points to), folding Esc-navigate-home into app/keys.ts:113-116. This squarely fits the 'docs the change made stale' anchoring carve-out and is the inverse of the rejected R-037/R-038, which were pre-existing staleness in untouched lines; not a duplicate (zero ledger hits for this file or its tokens). Minor wording imprecision (file keyboard.ts still exists) does not undermine the finding since the cited function was in fact deleted.)

### F-059 · medium · fresh-1 · src/cli/headless.ts:22-42, src/cli/headless.ts:112-117, src/engine/orchestrator/run/run.ts:150-151, src/engine/orchestrator/run/phases.ts:285-290, src/engine/orchestrator/final-review.ts:164-194
Failed final-review completion gate exits headless/CI with code 0 (success) instead of signaling failure. Intent #2 makes final review a blocking completion gate: on planner-review failure runFinalReviewPhase leaves the phase at 'final-review', emits no workflow_complete, and does not call onComplete (final-review.ts:164-194). runTasksAndReview then returns completed:false because loadState's phase !== 'complete' (phases.ts:289), so run.ts sets sessionStatus='interrupted' (run.ts:151) but the run promise still RESOLVES normally with a summary — no exception is thrown. In headless mode runHeadless awaits runWorkflow and then only fails (non-zero) via emitRecoveryAndFailIfPending when state.pendingRecovery is set (headless.ts:22-42); a failed final review sets finalReviewStatus:'failed' in the review packet but never creates a pendingRecovery issue, and onComplete is the no-op `() => undefined` (headless.ts:112). End-to-end: `diptych start --json` over a feature whose final review FAILS the new gate returns from runHeadless with no throw, so the CLI exits 0. At HEAD final review was advisory and always transitioned to 'complete', so exit 0 was correct; the changeset turns the gate on internally but never surfaces the failure as a non-zero exit, so a CI pipeline keying on exit code treats a failed-review/incomplete workflow as success. No test covers headless exit behavior on a failed final review.
Fix: In runHeadless (or the headless caller) detect a non-complete terminal phase (state.phase === 'final-review' / finalReviewStatus 'failed') after runWorkflow returns and exit non-zero with a diagnostic, mirroring emitRecoveryAndFailIfPending, so the completion gate is observable to headless/CI consumers.
round: 2 · skeptic: confirmed (At HEAD a failed final review still hit transitionAndSave(REVIEW_DONE) -> phase 'complete', so headless exit 0 was correct. The changeset's new completion gate (final-review.ts:164-194) returns early on reviewStatus==='failed' without REVIEW_DONE/workflow_complete/onComplete and without setting pendingRecovery, leaving phase 'final-review'; phases.ts:289 returns completed:false and run.ts:151 sets sessionStatus='interrupted', but runWorkflow still RESOLVES (no throw). runHeadless (headless.ts:116) then calls emitRecoveryAndFailIfPending, which only throws on state.pendingRecovery — never set by a failed review — so the CLI exits 0 (false success) on an incomplete/failed-review run, with no test covering it. This is a distinct observable consequence (CI/headless exit-code contract) not covered by F-002/F-008 (which address resume dead-ends and session status), directly caused by the changeset's final-review change, and the proposed surface-the-failure fix genuinely improves observability rather than churning.)

### F-060 · medium · quality:CONFIG · src/engine/implementers/agent-sdk.ts:16, src/engine/planners/agent-sdk.ts:20-24, src/engine/agent-sdk-backend.ts:228-255, src/engine/providers/client.ts:82-105, src/core/config/runtime/build-runner.ts:202-214, src/cli/options.ts:13, src/core/config/runtime/overrides.ts:66-76
env:NAME apiKey references are unresolved on the agent-sdk path (planner + implementer), injecting the literal 'env:VAR' as ANTHROPIC_API_KEY and deferring an auth failure. This changeset introduces the env:NAME apiKey convention (intent #4) plus the new --planner-api-key-env / --implementer-api-key-env flags (parseEnvRefOption, options.ts:13 produces 'env:VAR'), but it wires env: resolution ONLY into the OpenAI-compatible API provider boundary: resolveApiKeyOverride/apiKeyEnvReference live in client.ts (84-105) and are reached only via getProvider → createMetadataProvider for kind 'api'. For kind 'agent-sdk', the flow is: overrides set apiKey='env:VAR' (overrides.ts:66/76), buildAgentSdkConfig stores it verbatim (build-runner.ts:209), createAgentSdkImplementer reads config.implementer.apiKey raw (agent-sdk.ts:16) and createAgentSdkPlanner receives config.planner.apiKey raw (factory.ts:55 → planners/agent-sdk.ts:24), and agent-sdk-backend.ts:253 then sets options.env.ANTHROPIC_API_KEY = apiKey = the literal string 'env:VAR'. The SDK therefore authenticates with a malformed key. Worse, isAgentSdkAvailable(apiKey) returns true because the literal 'env:VAR' is truthy (agent-sdk-backend.ts:73), so the readiness check passes and the failure is deferred to the live SDK call. At HEAD there was no env: resolution anywhere (git show HEAD:client.ts has none), so the changeset made env: a documented, CLI-exposed feature while leaving agent-sdk silently broken; the api path (api.ts:38-40 → getProvider) resolves it correctly, proving the inconsistency.
Fix: Resolve env: apiKey references for the agent-sdk path too — call the existing apiKeyEnvReference/resolveApiKeyOverride helper (export it from client.ts) when reading config.{implementer,planner}.apiKey in createAgentSdkImplementer/createAgentSdkPlanner (or centralize resolution in build-runner so every kind stores a resolved key), so 'env:VAR' becomes the real secret before it is injected as ANTHROPIC_API_KEY.
round: 2 · skeptic: confirmed (The changeset introduces the env:NAME apiKey convention and the new --planner/--implementer-api-key-env flags (intent #4) but wires env: resolution ONLY into the API provider boundary (resolveApiKeyOverride is reached solely via getProvider->createMetadataProvider for kind 'api', client.ts:91-105/164-167; the api implementer resolves correctly at api.ts:38-41). For kind agent-sdk, the override apiKey:'env:VAR' is stored verbatim by buildAgentSdkConfig (build-runner.ts:209), read raw by createAgentSdkImplementer (agent-sdk.ts:16) and createAgentSdkPlanner via factory.ts:55, and the backend injects the literal as ANTHROPIC_API_KEY (agent-sdk-backend.ts:251-253, a line this changeset edited). isAgentSdkAvailable('env:VAR') returns true because the literal is truthy (agent-sdk-backend.ts:73), so readiness passes and a malformed-key auth failure is deferred to the live SDK call. agent-sdk legitimately accepts apiKey (runner-fields.ts:48 apiKey optional, usesApiKey:true), --planner explicitly lists agent-sdk (options.ts:28), and the env-key flag help is generic, so the broken combination is user-reachable. Not a duplicate of F-043 (cli-kind, usesApiKey:false, key dropped not injected, harmless) nor F-017/R-017/R-030 (dead fields / DRY of the env: literal); not intentional. Fix (resolve env: before injecting) genuinely improves the code.)

### F-061 · medium · behavior:RESUME · src/engine/orchestrator/final-review.ts:164-194, src/features/workflow/screen.tsx:74-75, src/features/workflow/hooks/use-workflow-runner.ts:157-187, src/core/phases.ts:54-61
Final-review failure leaves the interactive TUI stuck on the workflow screen with no terminal navigation. The new completion-gate branch in runFinalReviewPhase (final-review.ts:164-194) returns a summary WITHOUT calling callbacks.onComplete and without emitting workflow_complete; the persisted phase stays 'final-review' (set by ALL_DONE at line 50). In the TUI, the ONLY navigation off the workflow screen is screen.tsx:75 `onComplete = () => routerStore.navigate({ to: 'summary', ... })`. I traced the live run: use-workflow-runner.ts awaits runWorkflow (line 157), which returns normally on a failed review (no throw, not aborted); the post-run block (lines 183-187) sees not-aborted and no pendingRecovery, so startWorkflow exits cleanly. Because onComplete is never invoked, the router never leaves the workflow screen. lifecycleStore.phase remains 'final-review', which is a LIVE_PHASE (phases.ts:54-61), so PipelineBar renders stage 4 and the screen looks like an in-progress run. The user is left on a frozen-looking workflow screen with no summary, no home navigation, and (per app/keys.ts:90, isLivePhase('final-review')===true) ESC/Ctrl+C first arms 'interrupt' on a turn that already returned, requiring a second press to do anything. An error event is logged only when the planner review throws (line 144), but that merely appends to the event log and never changes phase or navigates. Before this changeset a failed review still transitioned to 'complete' and called onComplete, so the TUI always reached the summary screen; the gate change introduces this live-session dead-end (distinct from the disk-resume dead-end in F-002/F-008).
Fix: On a failed final-review gate, drive the live TUI to a terminal state: have runFinalReviewPhase emit a dedicated terminal signal (or have the runner detect the non-complete return) and navigate the workflow screen to the summary/home screen (or render an explicit 'final review failed' terminal panel) so the user is not stranded on a live-looking workflow screen.
round: 2 · skeptic: confirmed (The new failed-review branch returns at final-review.ts:193 without calling callbacks.onComplete and without emitting workflow_complete (verified). At HEAD even a failed review (reviewStatus='failed' from the catch) still fell through to transitionAndSave(REVIEW_DONE) + workflow_complete + callbacks.onComplete, so the live TUI always reached the summary. onComplete is the ONLY navigation off the workflow screen (screen.tsx:74-75 -> navigate to summary). In a live run use-workflow-runner awaits runWorkflow (line 157), which returns normally on a failed review (no throw, not aborted); the post-run block returns at line 187 (no pendingRecovery), so onComplete never fires and the router never leaves the workflow screen. lifecycleStore.phase stays 'final-review', a LIVE_PHASE (phases.ts:60), so the screen looks in-progress and Ctrl+C/ESC (keys.ts:90 isLivePhase true) arm interrupt on an already-returned turn. Distinct from F-002/F-008 (disk-resume dead-ends).)

### F-062 · low · hygiene:scope+miss-hunter · docs/CLI-REFERENCE.md:65-101, docs/CLI-REFERENCE.md:469-487, src/cli/options.ts:32-69, docs/CLI-REFERENCE.md:64-101, docs/CONFIGURATION.md:930-954, src/cli/options.ts:29-69
CLI-REFERENCE.md start/resume flag docs omit all 10 new planner/implementer override flags. The changeset adds 10 new CLI flags via addWorkflowOptions (options.ts:32-69): --planner-api-base, --planner-api-key-env, --planner-args, --planner-output-format, --planner-context-length, and the 5 --implementer-* equivalents. addWorkflowOptions backs start, resume, continue, and last, and the values flow end-to-end through workflowOptsToCLIOverrides → applyCLIOverrides into runner config (overrides.ts:59-99,161-198), so they are real functional flags. But the CLI-REFERENCE.md `diptych start` Usage synopsis (lines 65-72) and Options table (lines 79-101) document every prior planner/implementer flag (--planner-model, --planner-command, etc.) yet list none of the 10 new ones; a repo-wide grep for these flag names across docs/ returns zero hits. The resume section (lines 469-487) defers to start for the shared flag list, so it inherits the same gap. The reference therefore advertises an incomplete flag surface for the canonical entry-point command.
Fix: Add rows for the 10 new --planner-*/--implementer-* flags (api-base, api-key-env, args, output-format, context-length) to the start Options table and synopsis in CLI-REFERENCE.md, noting api-base/api-key-env apply to api/agent-sdk runners and args/output-format/context-length per runner kind.
round: 2 · skeptic: confirmed (The changeset adds 10 functional planner/implementer override flags in src/cli/options.ts:32-69 (confirmed new via git diff HEAD), they flow end-to-end through workflowOptsToCLIOverrides (overrides.ts:65-69,75-79) into runner config, yet none appear in docs/CLI-REFERENCE.md (Options table 79-101, Usage synopsis 64-72) or anywhere in docs/ (repo-wide grep = zero hits). The canonical start/resume command-reference doc was COMPLETE at HEAD and is now incomplete solely because of this change, so the staleness is newly introduced by the changeset (the 'docs the change made stale' category), distinguishing it from rejected R-037/R-038 which cited pre-existing untouched staleness. No ledger entry (F-### or R-###) covers the CLI-REFERENCE.md flag gap; the fix (add the 10 flag rows) genuinely completes a new user-facing surface and is not churn.)

### F-063 · low · quality:SHELL · docs/TESTING.md:343-349, src/stores/workflow/abort.ts:32-36, src/stores/create-store.ts:66-71
TESTING.md 'Good' example calls non-existent abortStore.set(). The changeset edited the TESTING.md sub-store reset example, changing `abortStore.set({ pending: true })` to `abortStore.set({ armed: 'exit' })` (TESTING.md:345). But `abortStore` is `{ ...storeBase(store), arm, clear }` (abort.ts:32-36) and `storeBase` exposes only `use/get/subscribe/reset` (create-store.ts:66-71) — there is no `set` method. A repo-wide grep confirms `abortStore.set` is used nowhere in src/ or testing/. The sibling test the same changeset updated (actions.test.ts:111) correctly seeds the store via `abortStore.arm('exit')`, so the doc's 'Good' example contradicts the real, working API it is meant to illustrate — a reader copying it writes code that does not compile or run.
Fix: Replace `abortStore.set({ armed: 'exit' })` in the TESTING.md 'Good' example with `abortStore.arm('exit')`, matching the actual store API and the updated actions.test.ts.
round: 2 · skeptic: confirmed (The changeset edited TESTING.md:345 to `abortStore.set({ armed: 'exit' })`, but `abortStore` (abort.ts:32-36 = `{ ...storeBase(store), arm, clear }`) has no `set` method because `storeBase` (create-store.ts:66-71) exposes only use/get/subscribe/reset. A repo-wide grep confirms `abortStore.set` exists nowhere; every real test seeds via `abortStore.arm(...)`, so the 'Good' example demonstrates a non-existent API that would not compile/run. Not present in the ledger (existing abortStore entries cover a test tautology, a timer-leak, ESC coupling, and config YAGNI — none address this doc API mismatch), anchored to a changed line, and the arm('exit') fix is correct.)

### F-064 · low · sec:stdin-tui · src/app/keys.ts:110-116, src/app/keys.ts:117-131, src/lib/terminal/escape-debounce.ts:5-15
Cancelled-state ESC navigates home immediately, bypassing the split-escape-sequence debounce, so an arrow key over a slow link spuriously leaves the workflow screen. The new cancelled-navigate branch (keys.ts:110-116) fires `routerStore.navigate({ to: 'home' })` on the first ESC byte whenever `lifecycleStore.cancelled` is true and no arm timer is pending. Unlike the arm path below it (lines 117-131), which defers via scheduleEscapeAction so a split escape sequence (`\x1b` then `[A`) can cancel it, this branch acts instantly. After cancellation `escapeArmTarget()` returns null (it requires `!cancelled`), so no arm timer is ever scheduled, meaning `isEscapeActionPending()` is always false on the cancelled screen and `!isEscapeActionPending()` is always true. End-to-end: the workflow is cancelled (markCancelled sets cancelled=true and does NOT navigate away — actions.ts:60-77 — so the user stays on the workflow screen viewing the cancelled state), the user presses an arrow/nav key to scroll, the slow link splits `\x1b[A` into `\x1b` + `[A`, the bare `\x1b` is parsed by Ink as key.escape and the navigate-home branch fires before the trailing `[A` arrives. The whole escape-debounce module (escape-debounce.ts:5-8 comment) exists to prevent exactly this misfire on split sequences, but the cancelled-navigate path forgot to use it. keys.test.tsx's split-sequence test (line 193) only covers the arm path, never the cancelled-navigate path, so the regression is untested.
Fix: Defer the cancelled-state navigate-home through scheduleEscapeAction (and let the non-ESC-byte handler at keys.ts:138-140 cancel it) so a split escape sequence cancels the navigation before it fires, matching the arm path.
round: 2 · skeptic: confirmed (Verified: the new cancelled-navigate branch (keys.ts:113-116) fires routerStore.navigate({to:'home'}) on the first bare ESC byte without the scheduleEscapeAction debounce the arm path uses; since escapeArmTarget() returns null when cancelled (keys.ts:51-56), no arm timer is ever scheduled so isEscapeActionPending() is always false on the cancelled screen and the branch acts instantly. The split-sequence test (keys.test.tsx:193) confirms a slow-link arrow key delivers '\x1b' and '[A' as separate useInput events, and arrow keys scroll the workflow screen (keyboard.ts:55-91), so on the cancelled screen the bare '\x1b' would navigate home before '[A' arrives. New in the changeset (HEAD had no ESC workflow handler), not a ledger duplicate, not intentional; deferring the cancelled-navigate through scheduleEscapeAction (matching the arm path) is a sound fix, not churn.)

### F-065 · low · fresh-3 · src/app/keys.ts:83-94, src/features/workflow/components/feedback-row.tsx:7-25, src/features/workflow/components/workflow-chrome.tsx:92
Ctrl+C on home/summary/non-workflow screens silently arms exit with no on-screen hint (single-press exit regressed to a hidden two-press). The rewritten Ctrl+C handler (keys.ts:83-94) ends with an unconditional `abortStore.arm('exit')` on every screen; only when `armed === 'exit'` already (the second press) does it call `exit()`. At HEAD the off-workflow branch was `} else { exit(); }`, so a single Ctrl+C exited immediately. The armed-state hint is rendered only by `FeedbackRow` (feedback-row.tsx:18, `ARMED_MESSAGES[armed]`), and `FeedbackRow` is mounted solely inside `workflow-chrome.tsx:92` (the workflow screen). I grepped the home and summary screens (`src/features/home`, `src/features/summary`) and the global `layout.tsx`/`app.tsx`: none read `abortStore`/`armed`. End-to-end: on the home or summary screen a user presses Ctrl+C once, the store arms 'exit' invisibly, the screen shows no change, and the app does not quit; the user must press Ctrl+C a second time within the 2000ms `WINDOW_MS` (abort.ts) to exit, with no feedback that a second press is required. Test keys.test.tsx:114 pins this two-press behavior as intended, but the missing feedback off-workflow makes the app appear frozen on the first press.
Fix: Render the armed-exit hint on non-workflow screens too (e.g. a shared status line driven by `abortStore.armed`), or keep single-press exit on home/summary by special-casing `route.screen !== 'workflow'` in the Ctrl+C handler so off-workflow Ctrl+C exits immediately as before.
round: 2 · skeptic: confirmed (Verified that off-workflow (home/summary) the first Ctrl+C silently arms 'exit' (keys.ts:93) and only a second press within 2000ms exits (keys.ts:85-88; abort.ts:12-22), while the armed-exit hint 'Ctrl+C again to exit' (feedback-row.tsx:8,18) renders only inside WorkflowScreen's chrome — no home/summary/layout component reads abortStore.armed (grep across src/features/home, src/features/summary, src/app.tsx, src/layout.tsx returns nothing). HEAD exited immediately off-workflow on a single press, so the hidden two-press is newly introduced by the changeset. The two-press itself is intentional and tested (keys.test.tsx:114), so the candidate's 'regressed exit' framing is partly overstated and its fix-option-b (restore single-press) would contradict tested intent; the real, report-worthy substance is the missing on-screen feedback off-workflow (fix-option-a). Not an exact duplicate of F-044 (registry description) — that is the help text, this is the runtime hint. Low severity: UX feedback gap, no crash or data loss; user can still exit with a second press.)

### F-066 · low · fresh-3 · src/engine/orchestrator/approval/file-snapshots.ts:76-101, src/engine/orchestrator/approval/staged-project.test.ts:62-88
Staged-promotion baseline change detector untested for the file-deletion case (deleted tracked file -> promote as removal). The new no-git staged change detector `getChangedFilesSinceBaselineContents` (file-snapshots.ts:76-101) reports a file as changed when it is present in the baseline but absent from the current tracked set (the loop at lines 96-98 `for (const file of Object.keys(baselineFileContents)) if (!currentFileSet.has(file)) changedFiles.add(file)`); promotion then writes that file as content `null`, i.e. `rm` (writeCurrentFileContent file-snapshots.ts:159-161). This is reachable: a staged direct-implementer task that deletes a tracked file must surface the deletion through `getChangedFilesSinceSnapshot`'s new baseline-contents branch (line 76-81). The only staged-baseline tests (staged-project.test.ts:62-88) cover ignored additions (no change) and modifying an existing file (change); the new-file case is covered only indirectly by resume-planning-direct-implementer.test.ts (it creates a file), and I found no test anywhere (grep for rmSync/unlinkSync in the staged tests returned nothing) that deletes a baseline file in the staged dir and asserts the deletion is detected and promoted. A regression dropping the baseline-only-keys loop would silently fail to propagate deletions with no failing test. This is distinct from F-019, which covers the sibling `change-detection.ts` `hasHashChanges` function used by the implementer base, not this promotion-path function.
Fix: Add a staged-project.test.ts case that deletes a baseline-tracked file in the staged dir, asserts `getChangedFilesSinceSnapshot` includes it, and (via promoteStagedChanges) asserts the file is removed from the real project.
round: 2 · skeptic: confirmed (Genuine, non-duplicate test-coverage gap anchored in new changeset code. getChangedFilesSinceBaselineContents (file-snapshots.ts:76-101) is new; its deletion branch (loop at lines 96-98 adding baseline-only keys -> promote writes null -> rm at 159-161) is reachable for a staged direct implementer that deletes a tracked file (wired via staged-project.ts:57-64, promoted via promoteStagedChanges reading null content at staged-project.ts:102). staged-project.test.ts contains no rmSync/unlinkSync and its only getChangedFilesSinceSnapshot assertions (lines 76, 79) cover ignored-additions and modification, never deletion; the resume integration test only writes a file (resume-planning-direct-implementer.test.ts:84), so the baseline-only-keys loop is untested and a regression dropping it would silently fail to propagate deletions. Distinct from F-019 (which covers change-detection.ts hasHashChanges, a boolean hash-based detector for the implementer base) and from F-024 (a types/inline-shape issue on the same lines). Severity raised from the candidate's 'info' to 'low' to match the ledger's grading of the analogous deletion-branch test gap F-019 (low), since this branch governs whether real file deletions reach the user's project.)

### F-067 · low · behavior:SANDBOX+sec:spawn-fs · src/engine/orchestrator/approval/staged-project.ts:48-68, src/engine/orchestrator/approval/staged-project.ts:57, src/engine/orchestrator/approval/staged-project.ts:60, src/engine/runners/sandbox-env.ts:15-19, src/engine/orchestrator/task/run-implementation.ts:44, src/engine/orchestrator/escalation/step.ts:47, src/engine/orchestrator/approval/staged-project.ts:48-69
createStagedProject leaks the temp staging dir when the new baseline-capture or sandbox-env mkdir steps throw. createStagedProject creates stagedRoot via mkdtemp (line 50) but only hands the caller the cleanup() closure (lines 65-67) on the SUCCESSFUL return at line 61. The changeset inserts two NEW throwing operations between the cp and that return: captureProjectFileContents (line 57, which readFile-loads every tracked file and can throw on IO/encoding errors) and createSandboxEnv (line 60, which fires 8 mkdir calls — sandbox-env.ts:15-19 — that can throw on ENOSPC/EACCES/quota). At HEAD (git show HEAD:staged-project.ts) the function returned immediately after cp, so cp was the only throw point; the change widens the leak window with two new failure points. If either throws, createStagedProject rejects before returning the StagedProject, so both callers — run-implementation.ts:44 (`const staged = usesStaging ? await createStagedProject(projectDir) : undefined`, no surrounding try/catch; the try begins at line 49) and escalation/step.ts:47 (`const staged = await createStagedProject(...)`, try begins at line 49) — never receive cleanup, and stagedRoot (a full recursive copy of the project under os.tmpdir()) leaks permanently. This is distinct from F-006/R-008 (which concern promoteStagedChanges throwing in the gate path, byte-identical to HEAD) and F-005 (full-content re-reads): it is the leak of stagedRoot from inside createStagedProject itself, caused by changeset-added pre-return work.
Fix: Wrap the cp/captureProjectFileContents/createSandboxEnv body of createStagedProject in a try/catch that rmSync's stagedRoot (recursive, force) before re-throwing, so a failure in baseline capture or sandbox mkdir cleans up the temp staging directory instead of leaking it.
round: 2 · skeptic: confirmed (At HEAD createStagedProject returned immediately after cp (only throw point before handing cleanup to the caller). The changeset inserts two new throwing pre-return steps — captureProjectFileContents (full readFile of every tracked file) and createSandboxEnv (8 mkdir calls) — between cp and the return that builds cleanup. If either throws, the function rejects before returning StagedProject, so neither caller receives cleanup and the full recursive tmpdir copy (stagedRoot) leaks. Both callers acquire staged OUTSIDE their try blocks (run-implementation.ts:44 before the try at line 49; step.ts:47 before the try at line 49), so no catch runs cleanup. Distinct from F-006/R-008 (which concern promoteStagedChanges / gate-path throws, byte-identical to HEAD) and F-005. Genuine fix; widened-leak-window is changeset-introduced.)

### F-068 · low · behavior:ESCALATION · src/engine/orchestrator/escalation/run-escalation-tier.ts:297-313, src/engine/orchestrator/escalation/escalation.test.ts:287-340, testing/integration/orchestrator/escalation-cascade.test.ts:66, testing/integration/orchestrator/retry-then-escalate.test.ts:116
Full-tier escalateFull sandbox-env wiring is untested across all test levels. The changeset wires the staged sandbox into the tier-2 planner escalation: runFullTier's invokeRetry now destructures sandboxEnv/fileIgnoreProjectDir from RetryInvokeArgs and forwards them into ctx.planner.escalateFull({ ..., sandboxEnv, fileIgnoreProjectDir }) (run-escalation-tier.ts:297-313). For a CLI-kind planner this is the ONLY planner path that actually runs sandboxed: invokeEscalate forwards sandboxEnv into the spawned subprocess env (planners/cli.ts:99,156-157), so dropping that forwarding would silently un-sandbox tier-2 escalation (the inverse of the F-007 hint-tier hole). I traced every test: escalation.test.ts mocks escalateFull as a vi.fn() and asserts only outcome (completed/method, lines 337-339), never the args; the escalation-cascade and retry-then-escalate integration tests likewise mock escalateFull and never assert sandboxEnv/fileIgnoreProjectDir; and step.test.ts:130-178 only proves the IMPLEMENTER retry path receives sandboxEnv (a different invokeRetry callback than the escalateFull one). A grep across testing/ and the escalation tests for escalateFull/escalateHint combined with sandbox/fileIgnore/objectContaining/toHaveBeenCalledWith returns nothing, so the security-relevant tier-2 planner sandbox wiring has zero coverage. This is distinct from the ledger's F-022 (agent-sdk/api/agent implementer threading) and F-046 (step.test.ts dir-layout assertions), neither of which touches the escalateFull planner path.
Fix: Add a test (extend escalation.test.ts's tier-2 case) that passes a spy escalateFull and asserts it was called with sandboxEnv pointing at the staged SANDBOX_DIR and fileIgnoreProjectDir equal to the real project dir, locking in the tier-2 planner sandbox wiring.
round: 2 · skeptic: confirmed (The tier-2 invokeRetry in runFullTier destructures sandboxEnv/fileIgnoreProjectDir and forwards them into ctx.planner.escalateFull (run-escalation-tier.ts:297-313), all changeset-new per git diff HEAD. For a CLI-kind planner this is the only planner path that actually runs sandboxed: escalateFull -> invokeEscalate -> invoke -> runOnce -> spawnAndCollect({ env: sandboxEnv }) (cli.ts:99,156-157), so dropping the forwarding would silently un-sandbox tier-2 escalation (the inverse of F-007). No test asserts these args: escalation.test.ts mocks escalateFull as vi.fn() and asserts only outcome (lines 337-339); escalation-cascade and retry-then-escalate mock escalateFull and never assert sandboxEnv/fileIgnoreProjectDir; faux/planner.ts escalateFull destructures only {error}. A grep for escalateFull args combined with sandbox/objectContaining/toHaveBeenCalledWith returns nothing. Distinct from F-022 (implementer threading) and F-046 (step.test.ts implementer-path layout).)

### F-069 · low · sec:stdin-tui · src/lib/terminal/filtered-stdin.ts:165-184, src/lib/terminal/filtered-stdin.ts:59-73, src/lib/terminal/filtered-stdin.ts:194-210
Split paste-start/end marker is broken when its leading ESC arrives as a standalone chunk (bare-ESC short-circuit flushes it before the paste layer can hold it). The changeset adds a bare-ESC short-circuit to splitMouseChunk (lines 176-178) that flushes a lone trailing ESC immediately when `tail === '' && (lastEsc === 0 || raw[lastEsc-1] === '')`. This collides with the new bracketed-paste layer. Trace with a paste-start marker `[200~` split so the ESC arrives alone: dataHandler(chunk1='') -> splitMouseChunk returns {processable:'', partial:''} (bare-ESC branch, lastEsc===0), so the ESC is written out; stripPasteMarkers sees only '' and prefixLengthHeldBack returns 0 (it deliberately never holds a lone ESC, lines 64-66). Then dataHandler(chunk2='[200~text') -> stripPasteMarkers('[200~text') finds no PASTE_START (the ESC is gone), so it emits the literal '[200~text' to Ink and pasteActive stays false. The marker bytes leak into the editor and bracketed-paste is never detected. The end-marker case is worse: a `[201~` split the same way leaves pasteActive stuck `true`, which makes the keys.ts ESC guard (`getActiveFilteredStdin()?.isPasteActive()`) suppress every subsequent Escape keypress. The existing paste-split test (filtered-stdin.test.ts:502) only covers a mid-chunk ESC (`before[20`) that the paste layer holds, never the standalone-ESC split that the bare-ESC short-circuit flushes; the lone-ESC tests (lines 369-389) deliberately deliver `` alone, proving the authors treat standalone-ESC delivery as a real chunking scenario.
Fix: In splitMouseChunk, only flush a standalone trailing ESC when it cannot be the head of a paste marker — withhold a lone trailing ESC (return it as `partial`) like the pre-paste mouse.ts did, or have stripPasteMarkers hold a trailing lone `` as `partial` so a split paste marker reassembles before the marker scan.
round: 2 · skeptic: confirmed (Factually real and anchored: the changeset added the bare-ESC short-circuit (splitMouseChunk:176-178) that flushes a lone trailing \x1b, whereas HEAD's mouse.ts withheld it as partial (verified via git show HEAD:src/lib/terminal/mouse.ts — that branch did not exist and PARTIAL_SGR_MOUSE_RE held the lone ESC). splitMouseChunk runs BEFORE stripPasteMarkers in dataHandler (filtered-stdin.ts:196 then :204), so when a paste-start \x1b[200~ has its leading ESC delivered as a standalone chunk, the ESC is flushed, [200~ leaks literally, and pasteActive never sets; the symmetric \x1b[201~ split leaves pasteActive stuck true, suppressing every subsequent ESC via keys.ts:100. Not a duplicate (R-034 rejected the unrelated unbounded-buffer claim); existing tests cover only mid-chunk paste splits, never the standalone-ESC split. Low severity: needs a specific OS split point and Ctrl+C remains a recovery path; the fix (withhold a lone trailing ESC) is a genuine improvement.)

### F-070 · info · hygiene:scope · docs/STORES-AND-UI.md:68, src/stores/workflow/abort.ts:1-36, docs/STORES.md:178
STORES-AND-UI.md abortStore description still describes the removed pending-signal shape. The changeset rewrites abortStore from `{ pending: boolean }` / markPending() to `{ armed: ArmedKind }` (ArmedKind = 'none'|'exit'|'interrupt'|'cancel') / arm(kind) (abort.ts). The companion doc STORES.md was correctly updated in this changeset (the diff shows `| abortStore | ... | { armed: ArmedKind } | arm(kind) (2s auto-clear), clear() |`). But the sibling living doc STORES-AND-UI.md:68 still summarizes the store as 'abortStore -- pending abort signal', echoing the old binary `pending` field name and missing the new multi-state armed model (interrupt/cancel/exit). The two store docs now disagree about the same store after the change touched only one of them.
Fix: Update STORES-AND-UI.md:68 to describe abortStore as the armed-abort indicator (`armed`: none/interrupt/cancel/exit) matching the updated STORES.md, dropping the 'pending' wording.
round: 2 · skeptic: confirmed (STORES-AND-UI.md:68 still reads '- **abortStore** -- pending abort signal.', which accurately described the store at HEAD ({ pending: boolean } / markPending in abort.ts). The changeset rewrote abort.ts to { armed: ArmedKind } / arm(kind) and updated the sibling living doc STORES.md (diff line 9->10) but left STORES-AND-UI.md stale; both docs are authoritative store references linked from docs/README.md and CLAUDE.md, so the change introduced the drift. Not in the ledger (existing abortStore entries are code/test only), not a convention, fix is a one-line doc update — no churn.)

### F-071 · info · behavior:SHELL+quality:SHELL · docs/STORES.md:55, docs/STORES.md:178, src/stores/workflow/abort.ts:3-9
STORES.md file-tree comment still says abort store is a "pending flag" after the pending→armed rename. The changeset reworked the abort store from a `{ pending: boolean }` flag with `markPending()` to a `{ armed: ArmedKind }` state machine with `arm(kind)` (abort.ts:3-9). The author updated the abortStore row in the STORES.md store table (line 178: now `{ armed: ArmedKind }` / `arm(kind)` — confirmed in the diff), but left the file-tree comment three sections earlier at line 55 reading `abort.ts  # Abort pending flag + auto-clear timer`. The store no longer has any "pending flag"; the same edited doc now describes the store two contradictory ways. The rename made the line-55 comment stale and only one of the two occurrences in the file was updated.
Fix: Update docs/STORES.md:55 to describe the armed-kind abort state (e.g. `abort.ts  # Armed abort intent (ArmedKind) + auto-clear timer`) so the file-tree comment matches the table row already corrected at line 178.
round: 2 · skeptic: confirmed (The changeset rewrote abort.ts from { pending: boolean }/markPending() to { armed: ArmedKind }/arm(kind) and updated the abortStore table row in STORES.md (diff at line 178: pending->armed, markPending->arm), but left the file-tree comment in the SAME edited doc at line 55 reading 'Abort pending flag + auto-clear timer' — now internally contradictory with the corrected row 123 lines down. Not in the ledger (grep of findings.md for STORES.md/file-tree/pending-flag returned nothing; existing abort entries F-055/F-056/R-016 concern tests and source). The one-line doc fix removes a real same-file contradiction with no churn. Severity lowered to info: cosmetic doc-comment staleness, no runtime impact, matching how the ledger graded comparable doc/test-hygiene items.)

### F-072 · info · quality:SANDBOX · src/engine/agent-sdk-backend.ts:247-255
Stale comment on agent-sdk options.env no longer matches the sandbox-env branch the changeset added. The comment at lines 247-249 ('Scope ANTHROPIC_API_KEY to this SDK call ... Omit `env` entirely when no override is set so the SDK inherits process.env as usual') was written for the HEAD code which was `if (apiKey) options.env = { ...process.env, ANTHROPIC_API_KEY: apiKey };` (verified via git show HEAD:agent-sdk-backend.ts:245-248). This changeset rewrote the documented code to `if (env || apiKey) { options.env = { ...(env ?? process.env), ...(apiKey ? { ANTHROPIC_API_KEY: apiKey } : {}) }; }`, adding the new sandbox `env` parameter (threaded from opts.sandboxEnv via agent-sdk.ts:37). The comment was not updated, so it now only describes the apiKey purpose and its claim that env is omitted 'when no override is set' is misleading: when the sandbox `env` is supplied (the default staged direct-implementer path) but no apiKey override exists, options.env is set to the sandbox env, not omitted. The comment no longer documents the load-bearing sandbox-isolation purpose of this block.
Fix: Update the comment to state that options.env is also set to scope the sandbox HOME/XDG/cache redirect (opts.env) for the SDK subprocess, and is only omitted when neither a sandbox env nor an apiKey override is present.
round: 2 · skeptic: confirmed (The comment at agent-sdk-backend.ts:247-249 was written for HEAD's `if (apiKey) options.env = {...process.env, ANTHROPIC_API_KEY: apiKey}` (verified via git show HEAD) and describes only the apiKey-scoping purpose, but this changeset rewrote the block to `if (env || apiKey)` adding the new sandbox `env` parameter threaded from agent-sdk.ts:37 (`env: opts.sandboxEnv`) via base.ts:207 and run-implementation.ts:77 (staged.sandboxEnv, the default direct-implementer path). The comment now fails to document the load-bearing sandbox HOME/XDG-redirect purpose of the env block; it is anchored in the change, not a duplicate of F-018/F-023 (which cover the type-signature dup at lines 191-194), not an intentional convention, and the one-line fix matches the changeset's own why-comment pattern.)

### F-073 · info · quality:CONFIG+behavior:IMPL+quality:IMPL+fresh-2 · src/engine/claude-invoke.ts:144, src/engine/claude-invoke.ts:150, src/engine/claude-invoke.ts:165, src/engine/planners/claude-code.ts:39-49, src/engine/planners/claude-code.ts:81-88, src/engine/claude-invoke.ts:150-151
Dead `env` parameter on runClaudePlannerStream — wired into spawn but no producer ever sets it. This changeset added `env?: NodeJS.ProcessEnv` to `ClaudePlannerStreamOpts` (claude-invoke.ts:144) and wired `env: opts.env` into the `spawnWithStdin` call (claude-invoke.ts:165), in lockstep with the same `env` addition to `runClaudeOneShot` (claude-invoke.ts:182,195). But the sandbox env is only ever needed on the staged ESCALATION path, which uses `runClaudeOneShot`, not `runClaudePlannerStream`. I traced both production callers of `runClaudePlannerStream`: `invokeWithSessionFallback` (claude-code.ts:39, the PLAN path) and `injectUserTurn` (claude-code.ts:81) — neither passes `env`, and `invokePlan` never threads `sandboxEnv` because planning always runs on the real project dir, never sandboxed. A grep of `src/` and the (unchanged) `claude-invoke.test.ts` confirms no caller or test ever supplies `env` to `runClaudePlannerStream`. By contrast `runClaudeOneShot.env` is live (cli.ts:36 implementer and claude-code.ts:65 escalate both pass it). So `runClaudePlannerStream.env` is permanently `undefined` and `env: opts.env` always spawns with `env: undefined` (inherit process.env) — dead speculative surface added by the changeset. Knip gate [18] does not catch it because it flags dead exports/files, not dead function parameters.
Fix: Delete the `env?` field from `ClaudePlannerStreamOpts` and remove the `env: opts.env` line from the `spawnWithStdin` call in `runClaudePlannerStream`, leaving the live `env` param only on `runClaudeOneShot`.
round: 2 · skeptic: confirmed (The changeset adds env? to ClaudePlannerStreamOpts (claude-invoke.ts:144) and wires env: opts.env into the spawn (line 165), but no caller ever supplies it: invokeWithSessionFallback (claude-code.ts:39-49) and injectUserTurn (claude-code.ts:81-88) never pass env, the base plan loop (base.ts:148-162) never threads sandboxEnv into invokePlan, and the test file only manipulates process.env['PATH']. Sandbox env is architecturally scoped to the escalation path (runClaudeOneShot, live via invokeEscalate -> env: sandboxEnv at claude-code.ts:72), so the planner-stream env is permanently undefined dead surface. This is NOT a duplicate of R-451 (that defense rested on escalateHint/escalateFull sharing one EscalateOptions interface; here ClaudePlannerStreamOpts and ClaudeOneShotOpts are separate interfaces, so the candidate's surgical fix removes only the dead field and leaves the live one-shot env untouched, creating no asymmetry). Matches the bar's 'delete dead exports (no future use)'; info because env: undefined already inherits process.env so there is zero behavioral impact.)

### F-074 · info · quality:ESCALATION · src/engine/orchestrator/recovery/builders/task.ts:59-64, src/engine/orchestrator/recovery/builders/task.ts:202-217, src/engine/orchestrator/recovery/builders/recovery-actions.ts:18-26
Unreachable trailing preference elements in new chooseRecommended() calls (retry-exhausted, dependency-blocked). The changeset replaced literal recommendedAction values (formerly 'planner-split-rebase') with chooseRecommended(actions, preferences) calls whose preference lists contain a trailing element that can never be selected. chooseRecommended (recovery-actions.ts:22-25) returns the first preference present in actions. In buildRetryExhaustedRecoveryIssue the action set always contains 'pause-run' (it is an unconditional literal in the orderedActions input at task.ts:56), and the preference list is ['route-bigger-worker','retry-same-worker','pause-run','skip-current-task'] — so 'pause-run' (3rd) always matches first and the 4th preference 'skip-current-task' is unreachable. Same pattern in buildDependencyBlockedRecoveryIssue: actions always contain 'pause-run' (task.ts:202) but the new preference list ['pause-run','skip-current-task'] (task.ts:217) makes 'skip-current-task' unreachable. I traced orderedActions/ACTION_ORDER: 'pause-run' is in ACTION_ORDER (recovery-actions.ts:9) and is requested unconditionally in both builders, confirming it is always present and the trailing preference is dead. These preference lists are new in this changeset (HEAD used literal recommendedAction). The dependency-blocked recommendation is exercised by loop-recovery.test.ts:159/175 (recommendedAction 'pause-run'), confirming the first preference always wins.
Fix: Drop the unreachable trailing 'skip-current-task' from the two new chooseRecommended preference lists in task.ts (retry-exhausted line 63 and dependency-blocked line 217), since 'pause-run' is always present and selected first.
round: 2 · skeptic: confirmed (chooseRecommended returns the first preference present in actions. In buildDependencyBlockedRecoveryIssue the new (changeset-introduced; HEAD used a literal recommendedAction:'planner-split-rebase') preference list ['pause-run','skip-current-task'] (task.ts:217) makes 'skip-current-task' unreachable because 'pause-run' is unconditionally in actions (task.ts:202) — verified by recovery.test.ts:369 asserting recommendedAction==='pause-run'. In buildRetryExhaustedRecoveryIssue the rewritten list ['route-bigger-worker','retry-same-worker','pause-run','skip-current-task'] (task.ts:59-64) likewise leaves the trailing 'skip-current-task' unreachable since 'pause-run' is always present (task.ts:56). Both preference lists are changed lines this changeset; the dependency-blocked dead entry is genuinely new. Distinct from F-028 (a missing test assertion, not dead code). Minor dead-code nit; dropping the trailing 'skip-current-task' is a clean simplification per the 'delete dead code' convention.)

### F-075 · info · behavior:INPUT · src/features/workflow/handlers.test.ts:26-32, src/features/workflow/handlers.ts:40-51
interruptTurn 'workflow' test never asserts the registered cancel handler actually fired. The new test "falls back to 'workflow' (cancel) when no abort handler exists" creates `const cancel = vi.fn()`, registers it via `setCancelHandler(cancel)`, then asserts only `interruptTurn()` returns 'workflow' and `lifecycleStore.cancelled` is true. It never asserts `cancel` was invoked. But the load-bearing side effect of `requestCancel()` is the `handlers.cancel?.()` call (handlers.ts:43), which in production is `() => { inputMode.resetMode(); controller.abort(); }` (use-workflow-runner.ts:99-102) — the only thing that actually aborts the workflow's AbortController. A regression that dropped the `handlers.cancel?.()` call would still set `cancelled` and return 'workflow', so this new test would pass while the workflow never actually aborted. The spy is registered for exactly this assertion yet the assertion is missing.
Fix: Add `expect(cancel).toHaveBeenCalledTimes(1)` to the 'workflow' case so the cancel-handler invocation (the controller.abort path) is pinned.
round: 2 · skeptic: confirmed (The new test (handlers.test.ts:26-32) registers `const cancel = vi.fn()` via setCancelHandler but asserts only the 'workflow' return value and lifecycleStore.cancelled — both of which are satisfied by markCancelled() alone (handlers.ts:40-44 returns true / sets cancelled before handlers.cancel?.() at line 43 runs). The cancel handler's invocation, the load-bearing side effect (in production `() => { inputMode.resetMode(); controller.abort(); }` at use-workflow-runner.ts:99-102 — the only thing that aborts the workflow's AbortController), is never asserted, so a regression dropping `handlers.cancel?.()` would still pass. Here the call count IS the contract, so adding `expect(cancel).toHaveBeenCalledTimes(1)` is a genuine behavior-pinning improvement aligned with the bar (cf. the ledger's own F-053 recommending vi.fn()+toHaveBeenCalledTimes for the analogous abort path), not churn. Distinct from ledger entries about handlers.test.ts (F-032/F-057/R-019 concern union-type re-spelling, not the missing spy assertion) and other info test gaps (F-053 names filtered-stdin.test.ts, F-056 names abort-mid-task.test.ts).)

## Rejected — do not re-report, do not re-judge

### R-001 · stack+security · src/engine/runners/sandbox-env.ts:20-32, src/engine/orchestrator/approval/staged-project.ts:60, src/engine/implementers/cli.ts:32-58, src/engine/implementers/agent-sdk.ts:37, src/engine/runners/sandbox-env.ts:20-33, src/engine/orchestrator/approval/staged-project.ts:60-63, src/engine/implementers/cli.ts:32, src/engine/implementers/command-invoke.ts:52, src/engine/orchestrator/approval/staged-project.ts:60-64, src/engine/implementers/cli.ts:32-55
Claimed "Sandbox env spreads full process.env to spawned implementer/planner subprocesses, exposing all secrets"; refuted - Not a worsening (the candidate itself concedes this): before the change every implementer/planner subprocess was spawned with env undefined, so Node already inherited the full process.env including all secrets (verified git show HEAD:cli.ts / command-invoke.ts pass no env; spawn.ts:47-52 spawns with the given env). The new createSandboxEnv only spreads the same full env plus HOME/cache overrides, so no additional secret reaches the child. Secret isolation is explicitly out of scope per the changeset spec ('best effort, not a hard OS sandbox'; 'network side effects are not prevented'; redirect scoped to keeping tool STATE out of the real user env, lines 86/89/90-92/102-103). The proposed allowlist fix would strip the very provider key/OAuth creds the implementer needs (see F-010) — harmful churn.
round: 1

### R-002 · architecture · src/engine/runners/sandbox-env.ts:1-33, src/engine/orchestrator/approval/staged-project.ts:7
Claimed "sandbox-env.ts placed in engine/runners but consumed only by orchestrator/approval"; refuted - Factually accurate (sole importer is orchestrator/approval/staged-project.ts:7; runners/ otherwise holds factory/command-based/trust/errors/types) but it is a subjective cohesion nit, not a defect. There is no layering violation (orchestrator importing engine/runners is a valid downward/lateral edge), no duplication, no barrel. The file builds the environment that runner subprocesses execute in, so living near the runner subsystem is a defensible design choice. The cited LAYERS rule is about moving DUPLICATED helpers down, which does not apply to a single-consumer file. Moving it is pure churn with no correctness or cohesion benefit.
round: 1

### R-003 · security · src/engine/runners/sandbox-env.ts:20-32
Claimed "Sandbox env redirection is incomplete: XDG_STATE_HOME (and Windows USERPROFILE/APPDATA) inherited from the real environment override the redirected HOME default"; refuted - The changeset spec explicitly enumerates the intended redirect set as 'HOME, TMP/TMPDIR/TEMP, XDG cache/config/data, npm, pip, and cargo' (lines 90-92, 211-214) — XDG_STATE_HOME is deliberately not in that scoped enumeration, so its omission matches changeset intent rather than being an oversight. The HOME redirect already captures the default case (when XDG_STATE_HOME is unset, tools compute $HOME/.local/state which now lands inside the sandbox); the alleged escape only triggers when the operator has explicitly exported an absolute XDG_STATE_HOME, an uncommon configuration. The Windows USERPROFILE/APPDATA portion exceeds the changeset's Unix-scoped intent. Borderline-real but below the bar and within declared scope — uncertain-after-research, reject.
round: 1

### R-004 · performance · src/engine/change-detection.ts:14-28, src/engine/snapshots/files.ts:48-61, src/lib/git.ts:106-124
Claimed "Extra `git check-ignore` subprocess spawned twice per direct-implementer turn in the file-hash detector"; refuted - checkIgnoredPaths spawns exactly one bounded git check-ignore --stdin subprocess per collectTrackedFiles call (git.ts:108-123, batched stdin), so the cost is two trivial subprocess spawns per turn, dwarfed by the LLM implementer invocation that the candidate itself concedes is negligible. The proposed cross-pass cache adds state/complexity for no meaningful gain, contradicting the project's 'no premature abstraction / no speculative configurability' convention — this is churn, not improvement.
round: 1

### R-005 · dry · src/engine/change-detection.ts:14-39, src/engine/orchestrator/approval/file-snapshots.ts:39-99
Claimed "captureFileHashes and captureProjectFileContents are parallel tracked-file -> Record snapshot routines"; refuted - The two cited routines sit on opposite sides of a layer boundary (engine/change-detection.ts vs engine/orchestrator/approval/file-snapshots.ts) and differ in value function (hashFile(absPath) vs readCurrentFileContent(projectDir,file)), path signature, and the uniqueProjectFiles filter that only captureProjectFileContents applies. Extracting a snapshotTrackedFiles wrapper for ~6-line map-to-record skeletons that differ in mapping directly contradicts the project's explicit '3 similar lines > unnecessary helper / no premature abstractions' convention — net churn.
round: 1

### R-006 · structure+slop · src/engine/orchestrator/approval/file-snapshots.ts:135-138, src/engine/orchestrator/approval/file-snapshots.ts:29-35
Claimed "Redundant SANDBOX_DIR filter on the final result list — all inputs are already filtered by uniqueProjectFiles"; refuted - The added SANDBOX_DIR filter at line 137 is indeed dead for the git path (all three Set inputs derive from uniqueProjectFiles which already strips SANDBOX_DIR at line 33), but removing it is churn that conflicts with the pre-existing parallel DIPTYCH_DIR filter at line 136 it deliberately mirrors, and it sits on a security-sensitive promotion result list where a redundant belt-and-suspenders filter is defensible. Near-zero signal; the only honest 'fix' would also have to touch the pre-existing line 136 (out of scope) to avoid asymmetry.
round: 1

### R-007 · simplicity+slop · src/engine/orchestrator/approval/file-snapshots.ts:152-166
Claimed "Duplicated assertWritablePathConfined call in writeCurrentFileContent — second invocation is provably redundant"; refuted - The second assertWritablePathConfined (line 164, after mkdir, before writeFile) is NOT provably redundant: the first check (line 157) can only realpath-resolve up to the nearest *pre-existing* ancestor, whereas after mkdir materializes the intermediate directories the second check resolves the full chain and catches a symlink escape planted concurrently between the two calls. writeCurrentFileContent is the promoteStagedChanges write path (staged-project.ts:99-103), whose threat model is exactly the agent/subprocess mutating the target — a textbook TOCTOU re-check immediately before the write. The cited symlink test throws at line 157 and never exercises the second check, so it does not prove redundancy. Removing it weakens the guard.
round: 1

### R-008 · structure · src/engine/orchestrator/escalation/step.ts:85-105, src/engine/orchestrator/approval/gate-and-promote.ts:71-85
Claimed "Staged temp-dir leaks when changed-file detection throws in the escalation retry path"; refuted - The gateAndPromoteChangedFiles call in step.ts:85-103 (no catchChangedFilesError, no try/finally, cleanup: staged.cleanup) is byte-identical at HEAD — git show HEAD confirms the same call; the working-tree diff to step.ts only adds sandboxEnv/fileIgnoreProjectDir to the invokeRetry call. The leak-on-throw path is pre-existing untouched code, and the candidate itself concedes 'the change does not worsen this'. Per the scope rule, pre-existing issues in unchanged lines that the change does not worsen are out of scope.
round: 1

### R-009 · structure · src/engine/orchestrator/escalation/step.ts:47-62, src/engine/orchestrator/approval/staged-project.ts:48-68, src/engine/orchestrator/approval/file-snapshots.ts:41-55
Claimed "Full project copy + sandbox env + full-content baseline built unconditionally for non-direct (api/planner) escalation tiers"; refuted - The core premise is factually wrong: the intermediate 'api' tier uses an extracts-code implementer (base.ts:306 writesFiles:'extracted-code') whose retry applies extracted code INTO staged.projectDir and relies on gate-and-promote staging; file-mode planners (escalateFullMode/hintSuccessMode 'files') write files directly into staged.projectDir and detect changes there (escalation.ts:74-85). So the staging, sandbox env, and baseline are load-bearing for these tiers, not unused work. The proposed fix gating on capabilities.writesFiles === 'direct' would specifically exclude the api/extracted-code and file-mode-planner paths, breaking their isolation and promotion — churn that introduces bugs, not an improvement.
round: 1

### R-010 · errors · src/engine/orchestrator/approval/file-snapshots.ts:76-101, src/engine/snapshots/files.ts:48-61
Claimed "Staged baseline change-detection now relies on a git check-ignore failure swallow that would promote build artifacts"; refuted - Core premise is materially wrong. The candidate assumes checkIgnoredPaths runs gitignore evaluation against the staged copy (which has no .git) and depends on a sandbox env where git may be unavailable. In reality createStagedProject stores ignoreProjectDir: projectDir (staged-project.ts:64) and getChangedFilesSinceSnapshot forwards it (file-snapshots.ts:108-110), so collectTrackedFiles -> checkIgnoredPaths runs git check-ignore in the ORIGINAL project dir which has working .git, via spawn with the orchestrator's own env (lib/git.ts:109, cwd: dir, no sandbox env). The swallow in checkIgnoredPaths is pre-existing and unchanged (no diff). The only residual trigger is git check-ignore failing on a machine where the project's git otherwise works — essentially never — and the proposed fix (block promotion on check-ignore failure) risks breaking legitimate promotion. Uncertain/flawed premise = reject.
round: 1

### R-011 · performance · src/engine/orchestrator/approval/file-snapshots.ts:46-53, src/engine/orchestrator/approval/file-snapshots.ts:85-94
Claimed "Unbounded concurrent readFile fan-out over all tracked files in staged capture and baseline diff (EMFILE risk on large repos)"; refuted - Unbounded Promise.all readFile fan-out is the established, uniform pattern across the entire file (pre-existing getChangedFilesSnapshot at 57-74 and captureCurrentFileContents at 168-181 already fan out the same way) plus diff.ts and staged-project.ts; no concurrency-limiting utility (p-limit/chunk/mapLimit) exists anywhere in src/. The new functions conform to convention over a file set that excludes node_modules/.git and respects .gitignore, so the EMFILE scenario is speculative and capping just these two sites would be churn against the local pattern, which the project conventions explicitly discourage (no speculative/defensive over-engineering).
round: 1

### R-012 · stack · src/engine/orchestrator/approval/staged-project.ts:39-56
Claimed "verbatimSymlinks: true is dead config after symlink-rejecting copy filter"; refuted - verbatimSymlinks:true is not dead config: per Node docs it skips path resolution for symlinks generally, and combined with the lstat filter it is intentional defense-in-depth. In the TOCTOU window between the filter's lstat (line 42) and cp's actual entry handling, verbatimSymlinks:true ensures any symlink that slips past the filter is copied verbatim rather than dereferenced (which could pull content from outside the project into staging) - precisely the threat this changeset hardens against. Removing it weakens hardening, so the proposed fix is counterproductive.
round: 1

### R-013 · dry · src/engine/orchestrator/run/phases.ts:46-51, src/core/phases.ts:54-65
Claimed "isInterruptedPlanningTurn inline-spells the planner-active phase set instead of deriving from core/phases.ts"; refuted - The triple {researching, specifying, planning} is NOT a closed set defined in core/phases.ts - it does not equal LIVE_PHASES (which also includes implementing/escalating/final-review) nor PLANNER_COST_PHASES nor any other named set. It is a single occurrence and the candidate's own fix admits a new predicate would have to be created. The single-source-closed-sets rule targets re-spelling of an existing core set, not minting a new abstraction for one call site; creating isPlanningTurnPhase here is premature abstraction the project explicitly discourages.
round: 1

### R-014 · dry · src/engine/orchestrator/escalation/run-escalation-tier.ts:108-138, src/engine/orchestrator/escalation/run-escalation-tier.ts:231-262, src/engine/orchestrator/escalation/run-escalation-tier.ts:294-318, src/engine/orchestrator/escalation/local-retries.ts:50-83
Claimed "Escalation tier invokeRetry callbacks repeat the sandboxEnv/fileIgnoreProjectDir destructure-and-forward shape"; refuted - Self-defeating: the three invokeRetry callbacks wrap different runner methods (intermediateImplementer.retry, implementer.retry, planner.escalateFull) with divergent argument shapes and non-uniform destructures (runFullTier omits attempts). The candidate itself states the bodies 'are not identical', extraction is 'low-value', and its own fix concludes 'otherwise leave as-is'. This is inherent typed-callback plumbing, not duplicated logic; extracting would couple unrelated methods and is not an improvement.
round: 1

### R-015 · security · src/engine/orchestrator/approval/staged-project.ts:83-105, src/engine/orchestrator/approval/gate-and-promote.ts:87-145
Claimed "Non-atomic conflict-check vs write in promoteStagedChanges leaves a lost-update window"; refuted - The non-atomic conflict-check loop (lines 83-91) and the separate write loop (lines 98-105) in promoteStagedChanges are byte-identical between HEAD and the working tree — git show HEAD confirms the exact same check-then-write structure. The changeset's only edit to this function is the added assertPathConfined loop (orthogonal path-confinement hardening); the lost-update race is entirely pre-existing in untouched lines and is neither introduced nor worsened by the change, so it is out of scope per agent rule 4.
round: 1

### R-016 · correctness+stack · src/stores/workflow/abort.ts:13-36, testing/helpers/stores.ts:32, testing/integration/orchestrator/abort-mid-task.test.ts:62-92
Claimed "abortStore.reset() leaves the 2s armTimer live, leaking a deferred state mutation across resetAllStores() boundaries"; refuted - The factual claims are real and anchored in the changeset (abort.ts was fully rewritten; reset comes from storeBase = set(getInitial()) and does not clear armTimer, only clear() does), but the asserted impact is false. Vitest runs each test file in an isolated module registry by default (no isolate:false / pool override in vitest.config.ts), so the module-scoped armTimer in abort-mid-task.test.ts cannot leak into any other file (actions.test.ts, keys.test.tsx, abort.test.ts are separate module instances); and abort-mid-task.test.ts contains a single it block, so there is no 'later real-timer test' in the same file to corrupt. Production never calls abortStore.reset() — resetWorkflow() uses abortStore.clear() (actions.ts:81), which already clears the timer. The proposed reset-override is defensive churn guarding a leak that cannot manifest. (NOTE: F-056 confirms the narrower test-hygiene gap — afterEach lacks abortStore.clear() — as info.)
round: 1

### R-017 · dry · src/cli/options.ts:11-13, src/engine/providers/client.ts:82-89
Claimed "env: apiKey-reference convention duplicated across CLI option parser and provider client"; refuted - The 'env:' literal has only TWO production occurrences (options.ts:13 and client.ts:82 API_KEY_ENV_PREFIX) — below the project's own stated 'DRY at the 3rd occurrence' rule (context.md line 88). The two sites also do opposite things (CLI prepends the prefix to normalize input; client strips it to resolve), and deduplicating would require src/cli/ to import a deep src/engine/providers/client.ts internal — a cross-layer dependency (cli sits outside the utils→lib→core→engine→features chain) that is worse than two 5-char literals. Intentional/acceptable per conventions; fix is churn.
round: 1

### R-018 · architecture · src/cli/options.ts:32-69, src/core/types/config-options.ts:14-31, src/core/config/runtime/overrides.ts:29-38, src/core/config/runtime/overrides.ts:90-99, src/core/config/runtime/overrides.ts:59-88, src/core/config/runtime/overrides.ts:269-316
Claimed "Runner-override field set re-spelled across six parallel surfaces"; refuted - The parallel-surface pattern (RunnerOverrideSchema + RunnerOverrides interface + WorkflowOpts + workflowOptsToCLIOverrides mapping + inline applyCLIOverrides spreads) is PRE-EXISTING: git show HEAD confirms the planner/implementer branches already hand-re-spread tool/model/command before this change; the changeset only extends it consistently with the new fields, which does not worsen the architecture (agent rule 4). The most actionable sub-claim (pass overrides.planner straight through) only works for the planner branch — the implementer branch genuinely needs custom logic to merge the legacy top-level overrides.contextLength (overrides.ts:309-312). The schema/interface/CLI triad serve distinct purposes (Zod validation boundary vs internal type vs flat CLI shape); 'deriving from one field list' for ~8 fields is over-engineering, not a clear improvement.
round: 1

### R-019 · simplicity · src/features/workflow/handlers.ts:47-51, src/app/keys.ts:58-63
Claimed "interruptTurn returns a three-valued contract no production caller consumes"; refuted - Returning the discriminated result 'turn'|'workflow'|'none' is a defensible, self-documenting contract whose return value is the function's observable behavior (asserted directly in handlers.test.ts). Collapsing it to void would force tests to reach into store internals (lifecycleStore.cancelled) and reduce testability — that is churn, not improvement. The stronger, mutually-exclusive reading (treat the union as a real centralizable contract) is confirmed as F-032/F-057.
round: 1

### R-020 · tests · src/features/workflow/user-edit-conflict-prompt.test.ts:9-45, src/features/workflow/user-edit-conflict-prompt.ts:14-33
Claimed "user-edit-conflict prompt test no longer exercises the new regenerate-rebase suppression/remap path"; refuted - The premise is false: UserEditConflict is never a saved state (no schema, never serialized to disk), so visibleActions() and the regenerate-rebase->pause-run remap do NOT degrade 'legacy saved states' reachable through this prompt. Legacy saved states persist as RecoveryIssue (pendingRecovery) and render through formatRecoveryPrompt — which is exactly why recovery-prompt.test.ts carries the legacy fixture. The asymmetry the candidate flags is correct and intentional; adding a regenerate-rebase fixture here would test an unreachable path (and visibleActions is itself dead per F-034), so the proposed test is misguided rather than missing coverage.
round: 1

### R-021 · conventions · src/features/workflow/user-edit-conflict-prompt.ts:14-29, src/features/workflow/user-edit-conflict-prompt.ts:31-48
Claimed "Unreachable 'regenerate-rebase' branch in toRecoveryAction now returns a placeholder value"; refuted - The 'regenerate-rebase' case is correctly retained for assertNever exhaustiveness over the Zod-derived UserEditConflictAction union, which intentionally keeps 'regenerate-rebase' (enums.ts:133) for legacy saved states per changeset intent #7. Its mapped value 'pause-run' is NOT a dead placeholder: the sibling legacy handler mapUserEditAction (recovery-actions.ts:38, also in this changeset) maps the same 'regenerate-rebase' to the identical 'pause-run', so the value is the deliberate, correct, consistent legacy mapping. Both proposed fixes are pure churn — adding a decorative comment (against the 'no decorative comments' convention) or changing a correct value.
round: 1

### R-022 · slop · testing/integration/ui/esc-through-filtered-stdin.test.tsx:60-62
Claimed "Fabricated explanatory comment: instant mode does not run under 'researching' phase"; refuted - The comment is substantively accurate, not hallucinated. run/init.ts:217 dispatches `{ type: 'START' }` for every fresh (non-resumed) run including instant mode; START transitions idle→researching (machine.ts:202-203), which IS a live phase (phases.ts:54-55); then runPlanningPhases→runInstantPlanning runs the planner while state.phase==='researching', and only after the planner returns does START_INSTANT flip to implementing (instant.ts:128-131). The candidate's refutation cites the wrong path (planning/init.ts, which indeed does not exist, but the comment just says 'init.ts' and run/init.ts matches) and misreads run.ts:171 (a summary-building createInitialState fallback, not the dispatch path).
round: 1

### R-023 · errors · testing/integration/ui/esc-through-filtered-stdin.test.tsx:65-71
Claimed "esc-through-filtered-stdin afterEach does not clear the active filtered-stdin global on early test failure"; refuted - Real omission but zero observable consequence, so the fix is defensive churn. makeSourceStdin() creates a fresh PassThrough per harness, so a leaked dataHandler sits on a dead source that is never written to again; the activeFilteredStdin global is overwritten by the next renderThroughFilteredStdin (setActiveFilteredStdin) before any code reads it. The candidate concedes the leak is benign. Adding cleanup for a leak that cannot manifest runs against the project's explicit no-defensive-over-engineering stance.
round: 1

### R-024 · tests · testing/integration/ui/esc-through-filtered-stdin.test.tsx:30-31, testing/integration/ui/esc-through-filtered-stdin.test.tsx:73-110, testing/helpers/ink.ts:23-32
Claimed "ESC debounce/pipeline test relies on real timers and fixed 80ms waits for a 35ms debounce"; refuted - Factually accurate (tick uses real setTimeout in ink.ts:23-32; AFTER_PRESS_MS=80 over a 35ms debounce) but this is a deliberate, already-documented trade-off: fake timers cannot fake the real PassThrough->filter->Ink data-event source, which the quality bar itself acknowledges for stream-bound tests, and the rationale is already inline at lines 29-31. The 80ms margin is >2x the 35ms defer, the proposed fix is conditional ('if flakes appear') and largely 'document (already partially present)', so it is non-actionable churn, not a defect introduced by the change. Also ink.ts is unchanged vs HEAD.
round: 1

### R-025 · structure · testing/integration/orchestrator/cli-planner-cli-implementer.test.ts:131-158, testing/integration/orchestrator/full-loop-validation-retry.test.ts:104-142, testing/integration/orchestrator/full-loop-validation-retry.test.ts:570-587
Claimed "Duplicated fake-CLI-binary scaffolding across the two new orchestrator integration tests"; refuted - Only 2 occurrences, below the project's explicit 'DRY at the 3rd occurrence' bar (context.md:88); the candidate itself concedes this is 'a restructuring opportunity rather than a hard violation.' The two scaffolds also genuinely differ (planner jsonl vs implementer json, 3 vs 4 env keys, different markers/prompts) — only `normalizeMacTmpPath` is identical. Extracting a shared faux/cli-binary helper now is premature abstraction (CLAUDE.md 'No premature abstractions'), i.e. churn, not improvement.
round: 1

### R-026 · types · testing/integration/orchestrator/cli-planner-cli-implementer.test.ts:208-219, testing/integration/orchestrator/full-loop-validation-retry.test.ts:70-102
Claimed "Fake-opencode run-log JSON parsed with raw inline `as {...}` casts instead of the type guard a sibling new test already defines"; refuted - The proposed fix is to ADD a type guard (mirroring `isFakeOpencodeRun`) to cli-planner-cli-implementer.test.ts and lift a shared run-log type — i.e. it propagates the exact over-engineering that F-038 (correctly) wants removed, and directly contradicts it. The inline `as {...}` cast on self-produced test data is the simpler, idiomatic choice; converting it to guard-based validation is churn and the shared-type lift overlaps the premature-abstraction problem. No genuine improvement.
round: 1

### R-027 · tests · testing/integration/orchestrator/cli-planner-cli-implementer.test.ts:220-237, testing/integration/orchestrator/full-loop-validation-retry.test.ts:575-593
Claimed "Duplicated OpenCode staged-sandbox-env assertions across two integration files"; refuted - The two cited blocks are not identical (full-loop also asserts XDG_CACHE_HOME->cache and uses a typed guard) and each lives inside an integration test with a distinct end-to-end purpose; the sandbox-env block is the only way to verify isolation is actually applied to the real spawned subprocess in that specific flow (the fake binary logs its observed process.env). This is intentional test-pyramid defense-in-depth, not redundancy: unit tests (staged-project.test.ts, step.test.ts) verify construction/pass-through, integration tests verify the contract survives the real spawn boundary. The proposed extract-a-helper/drop-one fix would couple two independent integration tests and reduce their self-containment — churn, not improvement — and the repo's DRY rule targets the 3rd occurrence of production logic, not behavior assertions in separate end-to-end tests.
round: 1

### R-028 · types+hygiene · scripts/check-invariants.ts:140, CLAUDE.md:88, src/lib/terminal/filtered-stdin.ts:156
Claimed "Invariant gate #17 allowlist references deleted file `src/lib/terminal/mouse.ts`, diverging from updated CLAUDE.md"; refuted - Not anchored in the changeset: scripts/check-invariants.ts is unchanged vs HEAD (git diff HEAD empty; line 140 last touched 2026-05-30 commit 7f936ec2, not the working tree). The mouse.ts deletion does not worsen or functionally interact with the gate #17 allowlist token, because mouse.ts never contained a non-null `!` assertion (verified) so the `lib/terminal/mouse` exclusion was already a dead no-op before and after deletion; the candidate itself concedes it is 'not a functional gate break' and 'already dead'. The proposed fix edits a file outside the working-tree changeset and produces no behavioral improvement.
round: 1

### R-029 · simplicity · src/app/keys.ts:28-32, src/app/keys.ts:58-63
Claimed "interruptWorkflow return type 'turn'|'workflow'|'none' is threaded through keys.ts but never read in production"; refuted - The union return type is not speculative: it is the real, fully-consumed return type of `interruptTurn` (handlers.ts:47-51), which is the exact function wired in at app.tsx:58 (`interruptWorkflow: interruptTurn`). Typing the prop as `() => void` would make the boundary contract LESS faithful to the function actually passed and erase a meaningful discriminant, so the proposed change is churn that degrades type accuracy rather than an improvement.
round: 1

### R-030 · types · src/engine/providers/client.ts:82, src/cli/options.ts:12-14
Claimed "'env:' apiKey prefix token duplicated as magic string (CLI) and named constant (engine) with no single source"; refuted - The 'env:' token appears in exactly two changed sites (cli/options.ts:13 inline, client.ts:82 named const) — the candidate itself states this is the 2nd occurrence, below the DRY trigger. context.md conventions explicitly say 'DRY at the 3rd occurrence', so extracting at the 2nd is exactly the premature-abstraction churn the convention forbids. No layering issue either (CLI already imports from engine/providers). Intentional per the stated DRY-at-3rd convention.
round: 1

### R-031 · security · src/engine/providers/client.ts:62-76, src/engine/providers/registry.ts:54-73
Claimed "Provider apiBase accepts arbitrary http hosts (no SSRF/loopback/metadata guard) on the config override path"; refuted - validateProviderBaseURL (client.ts:62-76) is pre-existing and unchanged in the working-tree diff — only the apiKey-env block and line 167 changed. The permissive host policy (allowing localhost/RFC1918/metadata IPs) is intentional and required: Ollama/LM Studio legitimately need http+localhost endpoints, and the apiBase config capability predates this change; the new CLI flags merely expose the same existing, intentional capability. The candidate itself concludes no fix is warranted and the new exfiltration guard already blocks key leakage. Not an actionable defect introduced or worsened by the change.
round: 1

### R-032 · slop · src/lib/terminal/filtered-stdin.ts:3-11, src/lib/terminal/filtered-stdin.ts:38-53
Claimed "MouseEvent carries four dead/redundant fields (button, shift, meta, ctrl) no production code reads"; refuted - The MouseEvent dead fields (button/shift/meta/ctrl) and parseMouseEvents were moved verbatim from the deleted mouse.ts (confirmed via git show HEAD:src/lib/terminal/mouse.ts — identical interface and function). A file rename/move does not worsen the pre-existing dead surface, so it is out of scope per agent rule 4; the candidate itself concedes this.
round: 1

### R-033 · errors · src/lib/terminal/filtered-stdin.ts:194-210
Claimed "Chunked UTF-8 decode in filtered stdin can corrupt multi-byte chars split across data chunks"; refuted - The per-chunk chunk.toString('utf8') decode is byte-identical to HEAD's mouse.ts dataHandler (pre-existing). The 'paste widens exposure' argument fails: bracketed-paste support only strips marker bytes — pasted multi-byte content already flowed through the exact same decoder before the change (mouse mode was already active), and enabling \x1b[?2004h does not alter OS chunking or the decode call. No genuine worsening, so not anchored in the changeset.
round: 1

### R-034 · performance · src/lib/terminal/filtered-stdin.ts:165-184, src/lib/terminal/filtered-stdin.ts:194-210
Claimed "Unbounded `partial` buffer in filtered-stdin bridge under a never-terminating SGR mouse sequence"; refuted - The unbounded partial growth is real (verified: 6633+ chars under adversarial '\x1b[<' + endless digits/semicolons with no terminator), but PARTIAL_SGR_MOUSE_RE and the withholding branch are byte-identical to HEAD's deleted mouse.ts; the changeset only ADDS a bare-ESC short-circuit that reduces withholding, so it does not introduce or worsen the unbounded path. Pre-existing and not anchored as worsened; bracketed-paste does not touch the mouse partial path. Trigger is degenerate (any non-digit byte immediately flushes).
round: 1

### R-035 · tests · src/engine/implementers/agent-sdk.test.ts:90-132, src/engine/implementers/agent.test.ts:100-122, src/engine/implementers/api.test.ts:69-88, src/engine/implementers/api.test.ts:290-319
Claimed "New abort/signal tests in CHUNK-C assert pre-existing behavior, not the changeset's changed lines"; refuted - The 5 abort tests are new additions in this changeset and pass; adding regression tests that pin abort behavior is good practice, not a defect, and 'asserts pre-existing production code' is not itself a code-quality problem. The proposed fix (drop the tests) is churn that reduces coverage. The looser OpenAI `toBeInstanceOf(AbortSignal)` assertion is justified because that path routes through the OpenAI SDK (api.ts:50-61) which may wrap the signal, unlike the direct-fetch Anthropic path where `toBe(controller.signal)` is used.
round: 1

### R-036 · performance · src/engine/implementers/base.ts:170-175, src/engine/implementers/base.ts:251-252, src/engine/change-detection.ts:14-28, src/engine/change-detection.ts:55-67, src/engine/snapshots/files.ts:48-61
Claimed "Per-task double full-tree hash + git check-ignore subprocess on the no-git staged path"; refuted - The double full-tree hash + git check-ignore on the no-git staged path is real and new, but it is the intentional file-hash change-detection mechanism for staged projects with no .git (changeset intent #3), and the two passes are intrinsic to before/after change detection. The proposed fix (reuse the once-captured staging baseline as the single before-state for all invokes) would break per-invoke change-detection semantics: a retry/escalation invoke that itself changes nothing would falsely report 'changed' because a prior invoke already diverged from the staging baseline. That is a correctness regression, not an improvement, so the finding resolves to churn. (NOTE: the EMFILE/uncapped fan-out aspect of this code is separately confirmed as F-020.)
round: 1

### R-037 · errors · docs/FEATURES.md:142-148
Claimed "FEATURES.md final-review section still claims it always fires workflow_complete, contradicting the new failure gate"; refuted - The cited FEATURES.md lines 142-148 (final-review section, incl. "Fires workflow_complete" at 146 and "No opt-out" at 148) are NOT part of the working-tree changeset: `git diff HEAD -- docs/FEATURES.md` shows the only modified line is 367 (the planner-split-rebase recovery-action row), and `git blame` dates lines 146/148 to commit e55831ed on 2026-04-27, untouched by this change. The changeset's FEATURES.md edit (recovery actions) neither touches nor interacts with the final-review section, so this is a pre-existing doc-vs-code staleness in untouched lines, which is out of scope per the audit's rule that the file's M status does not bring unedited paragraphs into scope.
round: 1

### R-038 · errors · docs/WORKFLOW.md:104-105
Claimed "WORKFLOW.md transition table omits the final-review failure self-hold introduced by the changeset"; refuted - Not anchored in the changeset: the cited WORKFLOW.md:104-105 transition rows are untouched (last modified 2026-05-04 commit 482815d9; `git diff HEAD` shows the only WORKFLOW.md change is the planner-split-rebase recovery-actions row near line 313). The failure path is the absence of a transition (early return, no event dispatched), which transition tables do not enumerate, and the held final-review state is already documented as a resumable phase (line 273), so nothing is hidden or worsened by the changeset.
round: 1

### R-039 · tests · src/engine/snapshots/checkpoint-summary.test.ts:254-276
Claimed "checkpoint-summary test pins exact derived-constant array, redundant with adjacent toContain"; refuted - `CHECKPOINT_RESTORE_SAFETY.excludedPaths` is the user-visible restore-safety contract rendered in the UI, so asserting the exact closed set `['.git/','.diptych/','.diptych-sandbox/','node_modules/','.trees/']` (line 260) is appropriate behavior locking, not a brittle mirror; adding `.diptych-sandbox/` is precisely how the changeset proves the sandbox-hardening intent reached the metadata, and any future change to the set is a user-visible contract change that should force a conscious test update. The claimed redundancy with line 275 is false: line 275 asserts the separate `text.excludedPaths` rendered string field, a different surface from the structured array. The proposed fix would weaken the assertion and is churn.
round: 1

### R-040 · dead-code · src/engine/planners/escalation.ts:43-56, src/engine/orchestrator/escalation/run-escalation-tier.ts:203-212
Claimed "escalateHint sandbox plumbing is orphaned — opts.sandboxEnv / opts.fileIgnoreProjectDir are always undefined in the hint path"; refuted - Factually accurate that opts.sandboxEnv/opts.fileIgnoreProjectDir are always undefined for escalateHint's sole production caller (run-escalation-tier.ts:203), but this is symmetric pass-through of a shared EscalateOptions interface that both escalateHint and escalateFull implement against the same invokeEscalate config: the fields are optional, undefined is the default so forwarding is behaviorally inert and zero-cost (not 'dead' — the values are passed to real functions and the type is live via escalateFull), and the forwarding would become live the moment any caller supplies them (plausible given hintSuccessMode:'files' lets hints write files). Both proposed fixes are net-negative churn: removing the forwarding creates a confusing asymmetry between two functions taking identical options, and wiring values in is speculative since the hint planner is intentionally run un-staged against the real ctx.projectDir. (NOTE: F-007 confirms the SECURITY/correctness consequence of this same omission for CLI-kind planners; this R-entry only rejects the dead-code framing.)
round: 1

### R-041 · conventions · testing/e2e/scenarios/real-cli-planner-implementer.smoke.test.ts:1-93
Claimed "New e2e smoke test is the only scenario file using a .smoke.test.ts infix, diverging from its 6 siblings"; refuted - The facts are accurate (sole .smoke.test.ts repo-wide; glob testing/e2e/scenarios/**/*.test.ts at vitest.e2e.config.ts:5 matches by trailing .test.ts so .smoke has no collection effect), but there is no enforced naming convention banning infixes and the .smoke segment carries a meaningful distinction: this file shells out to REAL CLI binaries and is env-gated/skipped by default (it.skip, lines 13-14), unlike the 6 deterministic fake-driven sibling scenarios. Renaming to match siblings would erase that signal for surface uniformity with zero functional benefit, which is churn rather than a genuine improvement (fails criteria 5).
round: 1

### R-042 · hygiene:scope · docs/ARCHITECTURE.md:347, src/stores/workflow/abort.ts:1-36
Claimed "ARCHITECTURE.md cites abortStore.markPending, a method renamed away by the changeset"; refuted - docs/ARCHITECTURE.md is NOT in the working-tree diff (git diff HEAD --stat and git status --porcelain both empty), so the finding is not anchored in the changeset. Moreover line 347 is deliberate historical narrative describing the pre-EventBus 'earlier design' of ad-hoc side-effects — the same sentence also names feedbackStore.setError, which still exists with 10+ live call sites in src/, proving these are intentionally old-design names, not stale current-API references. The rename of markPending->arm does not make this passage stale; renaming it to 'arm' would falsify the history (the old design used markPending). Fix is accuracy-damaging churn.
round: 2

### R-043 · hygiene:scope · docs/CONFIGURATION.md:124, docs/CONFIGURATION.md:897, src/engine/providers/client.ts:82-105
Claimed "CONFIGURATION.md / API-KEYS.md do not document the new apiKey: env:NAME resolution form"; refuted - Code facts are real (client.ts:82-105 adds env:NAME resolution, reachable via registry.ts:91-94), but docs/CONFIGURATION.md and docs/API-KEYS.md are entirely outside the working-tree changeset (no M status, empty `git diff HEAD`, absent from CHUNK-J's doc list) and the cited lines are not contradicted by the change — they merely omit a new capability (the candidate itself concedes 'does not actively contradict... but it omits'). This is a pre-existing doc-completeness gap in untouched files, failing anchoring criterion #4, and matches the established rejection precedent R-037/R-038 (doc-staleness in lines not part of the changeset is out of scope; a touched doc's M status — let alone an untouched file — does not pull unedited/omitted paragraphs into scope).
round: 2

### R-044 · hygiene:scope · docs/CONFIGURATION.md:376-385, src/engine/orchestrator/validation.ts:39-66, src/engine/orchestrator/validation.ts:186-209
Claimed "CONFIGURATION.md validation resolution section omits the new configured-missing-command hard-fail"; refuted - Code facts are real (validation.ts:186-209 adds the configured-missing-command hard-fail, confirmed in `git diff HEAD`), but docs/CONFIGURATION.md is entirely untouched by the changeset (no M status, empty diff) and lines 376-385 are not contradicted by the change — the 'graceful skip' wording still correctly describes layers 2-4, and the candidate concedes the doc 'does not actively contradict the new behavior' but omits the new layer-1 failure mode. This is a documentation-completeness gap in an untouched file, not breakage the change made stale, failing anchoring criterion #4 and matching the rejection precedent R-037/R-038.
round: 2

### R-045 · quality:SHELL · scripts/check-invariants.ts:140, CLAUDE.md:88
Claimed "Invariant #17 allowlist still references deleted lib/terminal/mouse.ts"; refuted - Exact duplicate of already-rejected ledger entry R-028 (same files scripts/check-invariants.ts:140 + CLAUDE.md:88, same claim, same harmless-orphan reasoning). Also independently fails anchoring: git diff HEAD -- scripts/check-invariants.ts is empty, so line 140's `lib/terminal/mouse` allowlist token is pre-existing untouched code; mouse.ts never held a non-null `!` assertion so the token was a dead no-op before and after deletion (no worsening), and the candidate itself concedes it is harmless to the gate, so the fix edits an out-of-changeset file for no behavioral improvement (churn).
round: 2

### R-046 · behavior:INPUT · src/cli/render.test.ts:1-38, src/cli/render.ts:62-72
Claimed "render.test.ts does not exercise the SIGINT cleanup body that restores terminal input modes"; refuted - Factually real (render.test.ts:1-38 only unit-tests the extracted createTerminationHandler with mock cleanup/exit; the actual cleanup closure at render.ts:63-68 — killAllProcesses, setActiveFilteredStdin(undefined), filteredStdin?.disable(), rawKeyTap removal — is never driven by a test). But the proposed fix is churn against the project's explicit no-defensive/no-speculative-test stance, mirroring rejected R-023/R-024 and capped-info F-056/F-057: the closure is trivial glue delegating to already-tested units, and the author deliberately split out createTerminationHandler to make the genuinely testable contract (ordering, run-once, exit codes) unit-testable. Worse, the real behavioral defect in this very closure (incomplete terminal restore — never writes \x1b[?1049l) is already confirmed as F-004, so a test asserting the current 'disable() is called' behavior would lock in the buggy incomplete cleanup rather than catch a meaningful regression.
round: 2

### R-047 · quality:RECOVERY+dry:scope+miss-hunter · src/engine/orchestrator/recovery/builders/recovery-actions.ts:4-11, src/features/workflow/recovery-prompt.ts:8-15, src/core/schemas/enums.ts:106-114
Claimed "RecoveryAction ordering re-spelled in two layers (engine + feature ACTION_ORDER), both hand-edited in lockstep, instead of derived from the canonical RECOVERY_ACTIONS enum"; refuted - The two ACTION_ORDER arrays are pre-existing: at HEAD both engine (recovery-actions.ts) and feature (recovery-prompt.ts) already spelled all 7 elements identically (exact copies of RECOVERY_ACTIONS), so the cross-layer duplication is not introduced by the changeset, which only removed one element consistently from both — matching the already-accepted R-018 reasoning that consistent extension of a pre-existing parallel surface is not a worsening. There are exactly 2 production ACTION_ORDER copies (below the project's stated 'DRY at the 3rd occurrence' trigger). The proposed 'derive from RECOVERY_ACTIONS' fix couples a UX display-ordering concern to a schema-enum concern and re-spells 'planner-split-rebase' inside the filter anyway, so it is churn rather than a clear improvement. Not in the ledger but rejected on criteria 4/5.
round: 2

### R-048 · sec:cli-config · src/engine/providers/client.ts:82-105, src/engine/providers/client.ts:159-167, src/engine/implementers/agent-sdk.ts:16-23, src/engine/runners/factory.ts:51-58, src/engine/agent-sdk-backend.ts:250-255, src/cli/options.ts:12-14, src/core/config/runtime/overrides.ts:66-76, src/core/config/runtime/build-runner.ts:202-214
Claimed "agent-sdk planner/implementer sends literal `env:NAME` as the Anthropic API key (env-ref resolution only added at the API-provider boundary)"; refuted - The literal-send on the agent-sdk path is PRE-EXISTING, unchanged behavior: agent-sdk.ts:16, factory.ts and build-runner.ts have ZERO diff vs HEAD, and at HEAD client.ts had no env: resolution at all (verified: `git show HEAD:client.ts | grep -c env:` == 0), so `apiKey: env:NAME` was sent literally on EVERY path before this change — the changeset did not introduce or worsen the agent-sdk path (out of scope per agent rule 4). The 'fix scoped to the API provider boundary' is the changeset's explicit, documented intent (spec line 226), so leaving agent-sdk unresolved is intentional, not a regression (rule 3 = yes). The only genuinely-new sliver — the new --planner/implementer-api-key-env flags being able to route env:NAME into an agent-sdk runner — is the same flag-routing class already captured by confirmed F-043; the candidate misframes pre-existing behavior as 're-opening a fixed issue.'
round: 2

### R-049 · behavior:INPUT · src/lib/terminal/filtered-stdin.ts:75-110, src/lib/terminal/filtered-stdin.ts:204-205, src/lib/terminal/filtered-stdin.ts:223, src/app/keys.ts:99-100
Claimed "Lost bracketed-paste end marker permanently disables ESC interrupt/cancel (no pasteActive timeout/reset)"; refuted - The stuck-pasteActive mechanism is real, but the candidate's trigger ('dropped connection or terminal that emits start but never close') is speculation about external terminal behavior, not a code-traced defect — bracketed-paste terminals reliably send PASTE_END atomically. The realistic, code-traced way the end marker is actually lost is candidate 21's standalone-ESC split (the new bare-ESC short-circuit flushes a lone \x1b[201~ leading ESC), so candidate 5 is a symptom of that root cause, not an independent finding. Its proposed fix (inactivity timer / reset-on-lone-ESC) is churn that papers over the symptom and risks breaking legitimate pastes containing a lone ESC byte, versus the correct fix of withholding the lone trailing ESC (covered by id 21). Confirming id 21 already captures the anchored defect; this would double-count via a speculative trigger.
round: 2

### R-050 · fresh-1 · src/lib/terminal/filtered-stdin.ts:75-110, src/lib/terminal/filtered-stdin.ts:194-210, src/app/keys.ts:96-100, src/lib/terminal/filtered-stdin.test.ts:522-535, testing/integration/ui/esc-through-filtered-stdin.test.tsx:285-310
Claimed "isPasteActive() guard is defeated when a complete bracketed paste with an embedded ESC arrives in a single chunk"; refuted - The core claim ('a spurious interrupt/cancel arm fires') is factually wrong. A complete single-chunk paste with an embedded raw ESC is written to the PassThrough as ONE filtered.write of the cleaned body (filtered-stdin.ts:208). Ink reads it as a single data event and calls parseKeypress(body) exactly once (use-input.js:42-43); Ink does not split a multi-char chunk into per-byte keypresses (quality-bar.md:31, use-input doc). parseKeypress sets key.name='escape' ONLY when the whole sequence equals '\x1b' or '\x1b\x1b' (parse-keypress.js:437), so a multi-byte body like 'abc\x1bdef' yields key.escape===false and the keys.ts ESC handler returns at 'if (!key.escape) return;' (keys.ts:98). The premise that Ink 'fires the ESC handler for the embedded \x1b while isPasteActive() returns false' does not hold; no arm fires. The pasteActive-flips-before-write timing observation is true but inert. Fails factual reality (criterion 1).
round: 2

### R-051 · sec:scope · src/cli/options.ts:12-14, src/cli/options.ts:33-37, src/cli/options.ts:51-55, src/core/config/runtime/overrides.ts:66, src/core/config/runtime/overrides.ts:76, src/engine/runners/factory.ts:53-55, src/engine/implementers/agent-sdk.ts:16-23, src/engine/planners/agent-sdk.ts:20-26, src/engine/agent-sdk-backend.ts:228-255
Claimed "Agent-SDK runner uses unresolved `env:NAME` apiKey as the literal ANTHROPIC_API_KEY (new --*-api-key-env flags bypass the env-ref boundary)"; refuted - skeptic unavailable after retry, rejected by protocol (uncertain = reject)
round: 2

### R-052 · fresh-2 · src/cli/options.ts:38-46, src/cli/options.ts:60-72, src/core/config/runtime/overrides.ts:269-316, src/core/readiness/collect.ts:86-103, src/core/config/runtime/build-runner.ts:105-111
Claimed "New --*-output-format / --*-args flags skip friendly CLI validation, surfacing a raw ZodError instead of a clear message"; refuted - skeptic unavailable after retry, rejected by protocol (uncertain = reject)
round: 2
